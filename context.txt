VCSC PROJECT CONTEXT
====================

Purpose
-------
This is the durable handoff file for continuing the project after a chat
restart. When asked to "look at context.txt and figure out what to do next":

1. Read this file first.
2. Inspect the current tree and verify that it agrees with this record.
3. Continue with the first unfinished item in "Immediate TODO" unless the user
   gives newer instructions.
4. Update this file after every meaningful change or decision.
5. Keep top-level remove.txt accurate. It contains one repository-relative path
   per intentionally deleted file, suitable for:

       rm `cat remove.txt`

Project goal
------------
Reduce the existing general-purpose N 6502 toolchain to a small C-like language
and toolchain aimed specifically at the Atari 2600/VCS. Compatibility with the
old general-purpose language is not a goal. Features are being removed in
controlled, testable vertical slices.

Current source lineage
----------------------
Initial archive inspected:

    vcsc.20260716_140727.tar.gz

Input archive for this revision:

    vcsc.20260716_153441.tar.gz

The archive has no wrapper directory. COPYING, Makefile, compiler/, test/, and
other top-level entries are directly at archive root. Preserve that layout.

Settled final-language decisions
--------------------------------
* Target: Atari 2600/VCS using the 6507 CPU.
* All multibyte values are little-endian.
* The only user-visible basic integer types will be:

      int8_t
      uint8_t
      int16_t
      uint16_t

* No float or double.
* No int32_t, uint32_t, or wider integers.
* void remains a special non-value type.
* Pointers are special 16-bit little-endian values.
* Functions retain normal source-level parameters and lexical scope.
* The final ABI has no software argument/local stack.
* Parameters and automatic locals use statically allocated storage.
* A function has one activation record and is intentionally non-reentrant.
* Direct recursion, mutual recursion, callback re-entry, and any other path that
  activates the same function twice must be rejected.
* JSR/RTS still use the 6502 hardware stack for return addresses.
* Function returns:

      void:            no value
      8-bit integer:   A; X unspecified
      16-bit integer:  A low byte, X high byte
      pointer:         A low byte, X high byte

* No function may return a value larger than two bytes.
* Struct, union, and array returns are forbidden even when the aggregate is one
  or two bytes. Use an explicit result pointer.

Completed vertical slices
-------------------------
1. One- and two-byte little-endian integer and pointer returns use A:X.
2. Every fixed parameter of a directly named function is callee-owned symbol
   storage rather than software-stack storage.
3. Function return types are now restricted to void, supported 8/16-bit
   little-endian integers, and 16-bit pointers. The old caller-owned return-slot
   ABI has been deleted.

Current return ABI
------------------
Relevant code:

    compiler/compile_function.c
        return_type_is_void()
        return_type_is_supported()
        return_type_uses_ax()
        function_uses_ax_return()
        validate_function_return_type()

    compiler/compile_call.c
        direct and indirect call result handling

    compiler/abi_meta.c
        return_void / return_ax metadata

Every function declaration and definition is validated. The compiler rejects:

* aggregate and array returns;
* floating-point returns;
* integers larger than two bytes;
* two-byte big-endian integer returns;
* any other noninteger, nonpointer value return.

Pointers to aggregate or otherwise unsupported pointee types are legal because
the returned value is still a 16-bit pointer.

There is no compiler `return_value` metadata mode and no caller-owned return
storage. A caller that consumes a result may still copy A:X to caller-private
software-stack scratch because the old expression engine expects memory-backed
values. That scratch is not part of the function ABI.

Inside a non-void function, `$$` is the source-level return object. It is
currently a hidden callee-local scratch object. The epilogue loads it into A:X.
This scratch should become fixed-address storage when automatic locals are
converted.

Current direct-parameter ABI
----------------------------
Every fixed parameter of a directly named function has a callee-owned symbol.
At a direct call, the caller:

1. reserves one reusable temporary area large enough for the largest argument;
2. evaluates arguments left-to-right;
3. converts each argument in that scratch area;
4. copies it to the corresponding callee parameter symbol;
5. executes JSR;
6. consumes A:X and releases scratch.

The temporary evaluation area still uses the software stack. Parameters do not.
Parameter symbols may be ordinary absolute storage or zero-page storage.

Call-graph and function-pointer rules
-------------------------------------
A parameterized direct function may not be active twice. Existing compiler and
linker call-graph checks reject same-unit and cross-object cycles.

A plain function pointer contains only a code address and cannot identify the
callee-owned parameter symbols. Therefore:

* taking the address of a parameterized function is rejected;
* parameterized indirect calls are unsupported;
* zero-parameter function pointers still work temporarily;
* function pointers are not yet promised in the final minimal language.

Atari 2600 hardware constraints
-------------------------------
* The VCS has 128 physical bytes of RIOT RAM at $80-$FF.
* $180-$1FF is a mirror of the same physical RAM, not separate stack RAM.
* Static storage and JSR/RTS/PHA/PHP stack use therefore compete for one pool.
* Every outstanding JSR consumes two physical RAM bytes until RTS.
* The 6507 has no usable IRQ or NMI inputs, so asynchronous interrupt re-entry
  is not the normal problem; program-controlled re-entry still is.
* Visible kernels have only 76 CPU cycles per scanline. Calls in kernels will
  eventually require an explicit policy or automatic inlining.

Known VCS configuration defect — next task
------------------------------------------
File:

    libraries/vcs/vcs_4k.cfg

It currently models these as independent memories:

    RAM:      $0080-$00FF
    CPUSTACK: $0180-$01FF

They are mirrors of one physical 128-byte RIOT RAM block. This double-counts RAM
and can hide variable/return-stack collisions. The next slice is to model one
physical pool and reserve a conservative upper portion for hardware-stack use.

Old software-stack machinery still present
------------------------------------------
_nl_sp/_nl_fp and _pushN/_popN are still used for:

* automatic locals, including the temporary `$$` return object;
* expression temporaries and spill slots;
* direct-call argument-evaluation scratch;
* caller-private scratch used to consume A:X results;
* variadic argument blobs and metadata;
* aggregate copy machinery and old runtime helpers.

Do not remove the software stack blindly. Automatic locals and retained
expression scratch need replacements first.

Test policy
-----------
When a feature is removed:

1. delete positive tests whose purpose is to prove that feature works;
2. keep or add negative tests proving the compiler rejects it cleanly;
3. rewrite tests whose real subject remains useful to use legal types or
   out-parameters;
4. list every intentionally deleted test path in remove.txt.

In this return-restriction slice:

* 7 obsolete compile/codegen positive tests were deleted;
* 28 obsolete E2E positive tests were deleted;
* 6 focused return-contract tests were added;
* useful wide/float/endian arithmetic tests were converted to void functions
  with global/out-value results, because this slice rejects those types only as
  function returns; their complete removal is a later TODO.

Files changed in this revision
------------------------------
Modified:

    compiler/ABI.txt
    compiler/README.md
    compiler/abi_meta.c
    compiler/compile_call.c
    compiler/compile_expr_slot.c
    compiler/compile_function.c
    compiler/compile_function.h
    compiler/compile_lvalue.c
    compiler/compile_stmt.c
    compiler/compile_toplevel.c
    context.txt
    remove.txt
    test/abi_metadata_exactops_codegen_test.n
    test/big_endian_helper_codegen_test.n
    test/big_endian_mul_low_half_codegen_test.n
    test/big_endian_promotion_codegen_test.n
    test/big_endian_unary_minus_codegen_test.n
    test/bitfield_sign_extend_helper_codegen_test.n
    test/compound_promotion_codegen_test.n
    test/compound_test.n
    test/e2e_abi_builtin_int_mismatch_fail.n
    test/e2e_abi_builtin_int_mismatch_fail__def.n
    test/e2e_abi_exactops_function_mismatch_fail.n
    test/e2e_abi_exactops_function_mismatch_fail__def.n
    test/e2e_abi_exactops_global_mismatch_fail.n
    test/e2e_archive_function_pointer.n
    test/e2e_big_endian_integer_helpers_verify.n
    test/e2e_mixed_endian_compound_verify.n
    test/e2e_mixed_endian_target_typed_verify.n
    test/exactops_missing_operator_error_test.n
    test/exactops_mixed_left_add_error_test.n
    test/exactops_mixed_shift_error_test.n
    test/exactops_visible_operator_codegen_test.n
    test/float_add_codegen_test.n
    test/float_builtin_be_codegen_test.n
    test/float_builtin_codegen_test.n
    test/float_div_codegen_test.n
    test/float_mul_codegen_test.n
    test/float_sub_codegen_test.n
    test/global_expr_test.n
    test/if_test.n
    test/implicit_signed_machine_helpers_codegen_test.n
    test/int_helper_big_endian_codegen_test.n
    test/int_helper_codegen_test.n
    test/integer_promotion_codegen_test.n
    test/labeled_control_test.n
    test/loop_bool_test.n
    test/mixed_endian_compound_codegen_test.n
    test/mixed_endian_target_typed_codegen_test.n
    test/nonexactops_same_type_helper_fallback_codegen_test.n
    test/operator_promotion_overload_test.n
    test/ordinary_mixed_signed_add_error_test.n
    test/ordinary_overload_ambiguous_test.n
    test/ordinary_overload_best_viable_test.n
    test/ordinary_overload_exact_match_test.n
    test/ordinary_overload_function_pointer_test.n
    test/pointer_inc_expr_codegen_test.n
    test/ptrarith_oddint_test.n
    test/return_slot_decl_error_test.n
    test/return_slot_global_error_test.n
    test/return_slot_param_decl_error_test.n
    test/return_slot_void_error_test.n
    test/shift_wide_copy_codegen_test.n
    test/shortcut_endian_cast_codegen_test.n
    test/simple.n
    test/string_escape_check.n
    test/string_literal_codegen_test.n
    test/subscript_mixed_endian_index_error_test.n
    test/switch_range_codegen_test.n
    test/switch_range_compact_codegen_test.n
    test/switch_range_swapped_warning_test.n
    test/switch_test.n
    test/visible_same_type_operator_beats_helper_codegen_test.n
    test/weak_builtin_operator_codegen_test.n

Added:

    test/pointer_to_aggregate_return_codegen_test.n
    test/unsupported_aggregate_return_error_test.n
    test/unsupported_array_return_error_test.n
    test/unsupported_big_endian_return_error_test.n
    test/unsupported_float_return_error_test.n
    test/unsupported_wide_return_error_test.n

Removed intentionally (also listed in remove.txt):

    test/array_return_codegen_test.n
    test/e2e_call_argument_order_indirect3_verify.n
    test/e2e_call_argument_order_indirect4_verify.n
    test/e2e_call_argument_order_indirect_verify.n
    test/e2e_function_pointer_smoke.n
    test/e2e_generated_float_archive_add_verify.n
    test/e2e_generated_float_archive_compare_verify.n
    test/e2e_generated_float_archive_custom_be_mul_verify.n
    test/e2e_generated_float_archive_div_verify.n
    test/e2e_generated_float_archive_exactops_verify.n
    test/e2e_generated_float_archive_wide_verify.n
    test/e2e_generated_float_custom_be_mul_verify.n
    test/e2e_generated_float_custom_be_verify.n
    test/e2e_generated_float_custom_le_mul_verify.n
    test/e2e_generated_float_custom_le_verify.n
    test/e2e_generated_float_ieee64_verify.n
    test/e2e_generated_float_ieee_mul_verify.n
    test/e2e_generated_float_ieee_verify.n
    test/e2e_return_slot_verify.n
    test/e2e_sbrk_smoke.n
    test/e2e_shortcut_endian_cast_verify.n
    test/e2e_sprintf_integer_verify.n
    test/e2e_stdarg_mixed_width_verify.n
    test/e2e_stdarg_zero_variadic_args_verify.n
    test/e2e_variadic_blob_verify.n
    test/e2e_variadic_indirect_blob_verify.n
    test/e2e_variadic_zero_blob_verify.n
    test/e2e_variadic_zero_indirect_blob_verify.n
    test/e2e_vsprintf_integer_verify.n
    test/float_gen_codegen_test.n
    test/generated_float_archive_codegen_test.n
    test/generated_float_archive_exactops_codegen_test.n
    test/generated_float_archive_gf10_source_test.n
    test/generated_float_archive_split_core_test.n
    test/return_slot_struct_member_codegen_test.n

Build environment
-----------------
The host lacked bison. Supplied Debian packages were unpacked without root into:

    /mnt/data/vcsc_toolroot

Required environment:

    export PATH=/mnt/data/vcsc_toolroot/usr/bin:$PATH
    export LD_LIBRARY_PATH=/mnt/data/vcsc_toolroot/usr/lib/x86_64-linux-gnu:${LD_LIBRARY_PATH:-}
    export BISON_PKGDATADIR=/mnt/data/vcsc_toolroot/usr/share/bison

BISON_PKGDATADIR is required because the privately unpacked bison otherwise
looks under the host /usr/share/bison.

Build and test results
----------------------
Clean tools build:

    make clean
    make -j2 tools

Result: passed with the project's warning-as-error settings.

Compile suite after curating obsolete tests:

    279 passed, 0 failed, 279 total

End-to-end suite after curating obsolete tests:

    118 passed, 0 failed, 118 total

The E2E suite was run in alphabetic chunks because one monolithic invocation
exceeded the execution wrapper timeout. All chunks passed; together they cover
all 118 remaining E2E tests.

Immediate TODO
--------------
The first unfinished item is the default next action.

[x] 1. Return retained scalar/pointer values in A:X.

[x] 2. Make direct fixed parameters callee-owned symbols and reject call cycles.

[x] 3. Enforce legal return types and delete the caller-owned return-slot ABI.

[ ] 4. Fix libraries/vcs/vcs_4k.cfg so mirrored RIOT RAM is not modeled as two
       independent memories. Initially reserve a conservative fixed amount for
       the hardware return stack if exact call-depth sizing is not ready.

[ ] 5. Replace stack-backed automatic locals with fixed-address per-function
       storage. Start with distinct frames; defer frame overlay optimization.

[ ] 6. Inventory every retained _pushN/_popN use and classify it as automatic
       local allocation, expression temporary, caller argument scratch,
       aggregate support, variadic support, or obsolete runtime support.

[ ] 7. Replace expression temporary storage for the retained 8/16-bit language,
       probably with per-function static scratch plus register-first lowering.

[ ] 8. Remove _nl_sp/_nl_fp and the software argument/local stack after no
       retained compiler path depends on it.

[ ] 9. Enforce the final basic-type set: int8_t, uint8_t, int16_t, uint16_t.
       Remove floats, arbitrary endian, and 32/64-bit support and tests in
       controlled groups.

[ ] 10. Produce and verify an unbanked 4K VCS cartridge using the static-frame
        ABI and the real 128-byte shared RAM model.

[ ] 11. Delete unsupported tools/runtime/language features one group at a time,
        recording all removed paths in remove.txt and keeping retained tests
        green.

Open implementation questions
-----------------------------
* Whether static frames will later be overlaid using whole-program call/liveness
  analysis. Start with distinct storage.
* Exact fixed or calculated hardware-stack reserve in RIOT RAM.
* Expression scratch allocation after the software stack is removed.
* Whether separate compilation remains throughout simplification.
* Kernel-function syntax and cycle checking/inlining policy.
* Final fate of zero-argument function pointers.

Change log
----------
2026-07-16, initial context
* Documented the static-frame VCS goal and mirrored-RAM problem.
* Added top-level context.txt and remove.txt policy.

2026-07-16, A:X return slice
* Changed one- and two-byte scalar/pointer returns to A:X.
* Made `$$` a callee-local return scratch object.
* Added return_ax metadata and simulator coverage.
* Temporarily retained legacy oversized/aggregate return storage.

2026-07-16, default direct-parameter symbols
* Made direct fixed parameters symbol-backed by default.
* Removed fixed direct parameters from software-stack call frames.
* Expanded recursion/cycle validation.
* Rejected parameterized function pointers; retained zero-argument pointers.

2026-07-16, legal-return enforcement
* Recorded and enforced the two-byte maximum and aggregate-return prohibition.
* Added declaration/definition validation and indirect-call validation.
* Deleted the caller-owned return-slot call-frame path.
* Replaced `return_value` ABI metadata with only return_void or return_ax.
* Renamed user diagnostics from return "slot" to return "object".
* Added negative tests for aggregate, array, float, wide, and big-endian returns.
* Added a positive pointer-to-aggregate A:X test.
* Deleted 35 obsolete positive tests; all paths are in remove.txt.
* Curated the retained suites to 279/279 compile and 118/118 E2E.

End-of-session checklist
------------------------
Before returning another modified archive:

[ ] Update this file and the TODO ordering.
[ ] Record every changed, added, and intentionally removed source/test file.
[ ] Keep remove.txt synchronized with intentional removals.
[ ] Record exact build/test totals and any known failures.
[ ] Run make clean and remove test-runner scratch before packaging.
[ ] Repack with the same archive-root layout.
