 __   __ ___  ___   ___
 \ \ / // __|/ __| / __|
  \ V /| (__ \__ \| (__
   \_/  \___||___/ \___|

VCSC PROJECT CONTEXT
====================

Purpose
-------
This is the durable handoff file for continuing the project after a chat
restart. When asked to "look at context.txt and figure out what to do next":

1. Read this file first.
2. Inspect the current tree and verify that it agrees with this record.
3. Continue with the first unfinished item in "Immediate TODO" unless the user
   gives newer instructions.
4. Update this file after every meaningful change or decision.
5. Keep top-level remove.txt accurate. It contains one repository-relative path
   per intentionally deleted file, suitable for:

       rm `cat remove.txt`

Project goal
------------
Maintain VCSC as a brutally pared-down Atari VCS C-like compiler and companion
toolchain aimed specifically at the Atari 2600/VCS. Compatibility with the old
general-purpose N language or n65 tool names is not a goal. Features are changed
in controlled, testable vertical slices.

Current source lineage
----------------------
Initial archive inspected:

    vcsc.20260716_140727.tar.gz

Input archive for this revision:

    vcsc.20260719_161954.tar.gz

The archive has no wrapper directory. COPYING, Makefile, compiler/, test/, and
other top-level entries are directly at archive root. Preserve that layout.

Settled final-language decisions
--------------------------------
* Target: Atari 2600/VCS using the 6507 CPU.
* All multibyte values are little-endian.
* The stock machine interface exposes eight ordinary binary integer types and
  four unsigned packed-decimal integer types:

      int8_t      uint8_t
      int16_t     uint16_t
      int24_t     uint24_t
      int32_t     uint32_t
      bcd8_t      bcd16_t
      bcd24_t     bcd32_t

  The BCD types store two decimal digits per byte in little-endian digit-pair
  order and have numeric ranges 0..99, 0..9999, 0..999999, and 0..99999999.

* `int`, `char`, and `bool` are ordinary identifiers rather than built-in or
  reserved type names. Programs may introduce them as transparent typedef
  aliases, for example `typedef int16_t int;`.
* String literals are NUL-terminated `uint8_t` arrays stored in cartridge ROM.
  They decay to `uint8_t *` in pointer initializers; writing through such a
  pointer is invalid even though `const` is not yet enforced by the type system.
* No float or double.
* The stock VCS headers expose canonical signed and unsigned 8-, 16-, 24-,
  and 32-bit ordinary types. The compiler core handles user-declared ordinary
  signed or unsigned little-endian integer widths from one through four bytes.
  `bcd32_t` is the largest currently exposed packed-decimal type.
* The stock runtime provides no heap, `sbrk`, `malloc`, or `free`.
* The vendored batari Basic kernel tree is retained as source/reference
  material for possible conversion to VCSC; it is intentionally not dead-code
  pruning fodder merely because it is not yet wired into the build.
* void remains a special non-value type.
* Pointers are special 16-bit little-endian values.
* Subtracting compatible pointers yields `int16_t` and a C-style element count.
  Differences outside the representable `int16_t` range are undefined.
* Functions retain normal source-level parameters and lexical scope.
* The final ABI has no software argument/local stack.
* Parameters and automatic locals use statically allocated storage.
* A function has one activation record and is intentionally non-reentrant.
* Direct recursion, mutual recursion, callback re-entry, and any other path that
  activates the same function twice must be rejected.
* Each source-level function name has exactly one signature. Matching prototypes
  and a later definition are legal; ordinary named-function overloading is not.
* JSR/RTS still use the 6502 hardware stack for return addresses.
* Function returns use exact-sized callee-owned zero-page memory:

      void:                         no return object
      8/16/24/32-bit binary integer: function$__return, 1, 2, 3, or 4 bytes
      8/16/24/32-bit packed BCD:    function$__return, 1, 2, 3, or 4 bytes
      pointer:                      function$__return, 2 bytes

  The callee writes `$$`, whose linker-visible symbol is
  `function$__return`, and every value-returning epilogue is plain `RTS`. The
  caller copies directly from the callee object. A, X, and Y are caller-
  clobbered and carry no ordinary language return value.

* All four stock packed-BCD types may be returned. Ordinary binary memory
  returns and stock type names support widths from one through four bytes.
* Struct, union, and array returns remain forbidden even when small. Use an
  explicit result pointer.

Completed vertical slices
-------------------------
1. Value-returning functions own exact-sized zero-page `function$__return`
   objects. Callers consume those objects directly; registers are not part of
   the ordinary return ABI.
2. Every fixed parameter of a directly named function is callee-owned symbol
   storage rather than software-stack storage.
3. Function return types are restricted to void, currently supported binary
   integers, packed-BCD integers through `bcd24_t`, and 16-bit pointers.
   Aggregates and arrays remain forbidden.
4. The VCS linker stopped modeling mirrored $0180-$01FF stack addresses as
   separate physical memory. The first implementation used a fixed $E0-$FF
   reserve; completed slice 24 supersedes that partition with call-graph sizing.
5. Every named automatic local and the hidden `$$` return object now has
   function-qualified fixed-address storage. Automatic initializers still run
   whenever control reaches the declaration on each invocation.
6. Every remaining compiler `_pushN` allocation site has been inventoried and
   classified in top-level software_stack_inventory.txt. A regression test
   detects unrecorded additions, removals, or count changes.
7. Ordinary direct calls evaluate arguments through fixed compiler-generated
   call-site scratch rather than a software-stack call frame. Results are copied
   directly from callee-owned return objects, so return-only scratch is gone.
8. Variadic functions and callable types have been removed. The lexer rejects
   `...`; variadic call lowering, hidden metadata, `stdarg.vcsc`, `sprintf.vcsc`, and
   the legacy direct-call software-stack fallback are gone.
9. Function pointers and indirect calls have been removed. Function-pointer
   declarators and function names used as values are rejected; `_callptr0` and
   the final software-stack call frame in `compile_call.c` are gone.
10. General truth-value evaluation and discarded non-call expression results
    now use fixed per-site BSS scratch instead of `_pushN`/`_popN`.
11. Simple assignments into fixed symbols now evaluate and convert through
    fixed per-site `__n65_assigntmp_N` BSS scratch instead of
    `_pushN`/`_popN`.
12. Two-operand comparisons and the main built-in binary operator families now
    use fixed per-site BSS scratch instead of `_pushN`/`_popN`: pointer
    subtraction, scalar/pointer addition and subtraction, shifts, bitwise
    operations, multiplication, division, and remainder.
13. Cast conversion, function-local/global absolute-reference conversion, and
    built-in increment/decrement now use fixed per-site BSS scratch. Fixed
    symbol-to-lvalue helpers cover ordinary, indirect, and bit-field lvalues.
14. Built-in compound assignment, switch-expression comparison, simple
    indirect/absolute/bit-field assignment, runtime local and return-object
    initialization, and local absolute-reference initialization now use fixed
    BSS scratch. `compile_stmt.c` no longer emits `_pushN` or `_popN`.
15. Pointer/index lvalue evaluation, bit-field helper pointer preservation,
    braced assignments, and runtime global initializers now use fixed BSS
    scratch. `compile_lvalue.c` and `compile_init.c` no longer emit `_pushN` or
    `_popN`.
16. Operator overloading, `$exactops`, weak built-in operator dispatch, the
    weak-operator generator/runtime members, and generated exact-operator float
    fixtures have been removed. Compiler source emits no `_pushN` or `_popN`.
    Ordinary named-function overloading was retained temporarily and removed in
    completed slice 31.
17. Floating-point support has been removed from the language, compiler, driver,
    build, runtime, machine definitions, and positive test suite. Floating-point
    literals and `$float`/`$float:*` type flags remain only as direct rejection
    paths.
18. Big-endian and mixed-endian target support has been removed. All target
    integers, pointers, constants, initializers, conversions, comparisons, and
    runtime helpers are little-endian. `$endian:big`, `($big)`, and `($little)`
    remain only as direct rejection paths.
19. [Superseded by slice 39.] Ordinary binary target integer widths were
    temporarily restricted to exactly one or two bytes while inherited wide
    support was pruned. Slice 39 rebuilt and tested width-generic one- through
    four-byte scalar machinery without yet adding the canonical public names.
20. The stock language and machine interface now use only int8_t, uint8_t,
    int16_t, and uint16_t as built-in integer names. Comparisons and logical
    expressions produce an ordinary uint8_t; there is no special boolean type.
21. Simple transparent typedef aliases are supported. `int`, `char`, and `bool`
    are ordinary identifiers and may be introduced as aliases for canonical
    types. String literals used through pointers are emitted in RODATA/ROM.
22. The linker can emit flat `.bin` files, and examples/01_solid_color/solid_color.vcsc builds
    and verifies as a complete unbanked 4096-byte VCS cartridge with reset code
    at $F000, valid vectors, the static-frame ABI, and the real shared RIOT RAM
    map.
23. Inline assembly resolves source-level absolute `ref` names according to the
    opcode's operand access: read instructions use the read address, stores use
    the write address, and read-modify-write instructions require identical
    non-`none` read/write addresses. Illegal side use and split-address RMW are
    compile-time errors. The solid-color kernel now uses TIA register names.
24. The VCS linker configuration declares all 128 physical RIOT RAM bytes and
    marks the region `callstack = callgraph`. After archive selection and
    cross-object cycle validation, vcsc-ld computes the longest linked call path,
    reserves two top-of-RAM bytes per function level, and reports/exports the
    resulting hardware-stack range. This covers JSR return addresses only;
    inline assembly and separately assembled routine internals remain
    unaccounted future work.
25. Common VCS byte operations now bypass permanent expression scratch.
    Discarded uint8_t increment/decrement, unsigned byte comparisons, constant
    byte stores, and byte-to-hardware-register stores lower directly. A simple
    uint8_t runtime index into a power-of-two-sized array element is scaled
    inline through compiler-owned arg0:arg1 and added to ptr0, with no
    `__vcsc_indextmp_N` or generic multiply helper. The Ode player uses the
    natural `music[music_index].field` form, allocates zero BSS scratch, and
    retains only its two persistent state bytes.
26. All remaining compiler-generated temporary BSS is pooled by function and
    nesting depth as `__vcsc_scratch_N`. Sequential expressions in one function
    reuse a depth slot; nested expressions receive deeper slots; caller and
    callee functions retain distinct physical storage. Leases remain live
    through final copy-out, are released LIFO, and are checked at translation-
    unit completion. Binary expressions whose two operands are fields of the
    same runtime-indexed aggregate element now compute the common address once
    and load both fields by offsets when strict side-effect-free conditions hold.
27. Heap/`sbrk` support has been removed. The `_sbrk` and `__init_sbrk`
    archive members, `_vcsc_sbrk` zero-page pointer, compiler name-mangling
    exception, and allocator documentation are gone. `sbrk` is now an ordinary
    user identifier, and the runtime workspace shrank from 22 to 20 bytes.
28. Explicit runtime `/`, `%`, `/=`, and `%=` operations whose divisor is a
    compile-time positive power of two greater than one emit a performance
    warning. Code generation and C-compatible signed division/remainder
    semantics remain unchanged; literal-only expressions still fold silently.
29. The residual target software stack has been removed. Division/remainder
    use a private four-byte BSS workspace owned by the archive-selected `_divNle`
    member; programs without `/` or `%` link neither the helper nor its workspace.
    `_vcsc_sp`, `_pushN`, `_popN`, `sp2ptr*`, `ARGSTACK`, startup stack-base
    initialization, and the stale per-function frame prologue are gone. Slice
    38 later removed the remaining runtime frame pointer as well.
30. [Superseded by slice 37.] Scalar/pointer return epilogues temporarily used
    direct zero-page loads from the hidden `$$` object. Slice 37 removed those
    loads and made the callee-owned object itself the ABI result. The object
    writer fix for one-byte relocatable operands remains required.
31. Ordinary named-function overloading has been removed. Each source name now
    identifies one declaration/definition signature and one unmangled assembler
    symbol. Matching prototypes remain legal; a second signature is rejected.
    Direct calls retain fixed-parameter count checks, safe integer widening,
    null-pointer literal conversion, typed-object-pointer to `void*` conversion,
    and exact-lvalue enforcement for `ref` parameters. The thousand-line
    overload resolver was replaced by a compact direct-function registry.
32. Generic interrupt-entry support has been removed. The optional `nint`
    archive, compiled `_handle_nmi`/`_handle_irq` defaults, and separate
    `vcsc-rt0-noint.s` member are gone. The stock startup archive member retains
    only weak `__nmi` and `__irqbrk` RTI vector fillers because every 6502 image
    still requires vector addresses. The VCS 6507 has no connected hardware IRQ
    or NMI inputs, and the reduced runtime exposes no compiled interrupt-handler
    ABI.
33. The installed generic 6502 machine definition and the two unwired
    generic linker-layout files have been removed. `test/machine_6502.vcsc`
    remains only as an isolated compiler-test fixture. Native VCS bindings,
    `vcs_4k.cfg`, and both retained batari Basic kernel families are guarded
    by a regression test.
34. The linker's implicit generic machine map has been removed. Direct
    `vcsc-ld` use now requires an explicit script/config, while `vcsc` discovers
    and supplies `vcs_4k.cfg` from the source tree or installed `share/vcs`
    directory when the user does not pass `-T`. Generic end-to-end tests use
    the explicit test-only `test/generic_6502.cfg` layout.
35. Added unsigned packed-decimal `bcd8_t`, `bcd16_t`, and `bcd24_t`.
    `$bcd` is valid only with `$integer:unsigned` on one-, two-, or three-byte
    little-endian scalar declarations. Literal values are range-checked and
    encoded numerically into packed digit pairs, so `42`, `0x2a`, `052`, and
    `0b101010` all store as `$42`. Addition and subtraction use tightly scoped
    `SED`/`CLD` around the actual `ADC`/`SBC` chain; assignment, BCD-width
    conversion, increment/decrement, comparisons, truth tests, fixed parameters,
    and switch are supported. Unsupported arithmetic and runtime BCD/binary
    conversion are rejected. ABI metadata distinguishes BCD from same-sized
    binary integers. `bcd24_t` is returnable through the memory-return ABI.
36. Valued prefix/postfix increment and decrement expressions are no longer
    treated as ordinary reference lvalues by shortcut classifiers. This
    preserves required side effects in compact byte comparisons, direct stores
    to absolute refs, and optimized one-byte array indexes. Ref parameters now
    correctly reject `value++`/`value--` arguments because those expressions are
    values, not lvalues.
37. Function results now remain in each callee's exact-sized hidden zero-page
    `function$__return` object. Non-static definitions export that object;
    separately compiled callers import it when consuming the result. Callers
    copy directly from the return symbol. Value epilogues are plain `RTS`, ABI
    metadata uses `return_memory`, and all four BCD widths return within and
    across translation units.
38. The inherited runtime frame pointer has been removed. Scratch leases now
    select fixed `__vcsc_scratch_N` symbols entirely at compile time, generated
    code addresses those symbols directly, and ordinary calls no longer push or
    restore scratch-base state. `_vcsc_fp`, `fp2ptr*`, startup initialization,
    and the two-byte-per-depth preservation allowance are gone. The stock
    runtime zero-page workspace is 16 bytes, and the linked hardware-stack
    reserve is two bytes per source call depth, plus one fixed two-byte
    startup cursor allowance when runtime initializer functions are linked.

39. Ordinary binary scalar lowering is width-generic from one through four bytes.
    User-declared signed and unsigned little-endian integer types of those widths
    now work through literals, initializers, assignment, extension/truncation,
    parameters, memory returns, arithmetic, comparisons, shifts, bitwise and
    compound operations, increment/decrement, and constant folding. The generic
    multiplication helper already accepted arbitrary widths; division/remainder's
    private dividend workspace was expanded from two to four bytes.
40. Added first-class stock `int24_t` and `uint24_t` declarations. Their
    three-byte little-endian values use the width-generic operator, parameter,
    and memory-return paths. Same-signed widths widen automatically; mixed
    signed/unsigned runtime operations retain the explicit-cast rule. Literal
    encoding preserves VCSC's established width-sized bit-pattern behavior, and
    negative runtime literals now encode correctly instead of reaching the
    unsigned text parser.
41. Added first-class stock `int32_t` and `uint32_t` declarations. Four-byte
    literals, parameters, arithmetic, comparisons, shifts, multiplication,
    division/remainder, and callee-owned memory returns use the already proven
    width-generic paths. Boundary regressions cover exact little-endian layout,
    signed and unsigned limits, fifth-byte rejection, widening, mixed-signedness
    rejection, and cross-object returns.
42. Added first-class stock `bcd32_t` for eight packed decimal digits. Four-byte
    BCD literals encode numeric values as little-endian digit pairs; arithmetic,
    comparisons, widening/truncation, parameters, and memory returns use the
    existing width-generic BCD paths. The legal range is 0..99999999.

Side quest: examples/02_ode_to_joy keeps its 32-step, 128-byte score as a const
ROM array and implements both `music_tick()` and `music_apply_current()` in the
C-like source. The player now uses the natural direct indexed form
`music[music_index].field`; the optimized uint8_t/power-of-two array path emits
no BSS expression scratch and no multiply helper. Only `music_index` and
`music_counter` persist in RAM. The source player runs inside a TIM64T-governed
30-line overscan budget. Two final WSYNCs produce Stella-reported 262-line NTSC
frames; the raw interval between successive VSYNC assertions is 263 whole
scanlines. The execution regression uses that same Stella-equivalent accounting
through ordinary, note-change, silent-gap, and score-wrap paths. The temporary
companion assembly player remains removed.

Current return ABI
------------------
Relevant code:

    compiler/compile_function.c
        return_type_is_void()
        return_type_is_supported()
        return_type_has_value()
        function_has_return_object()
        function_return_symbol_name()
        validate_function_return_type()

    compiler/compile_call.c
        direct-call argument/result handling

    compiler/abi_meta.c
        return_void / return_memory metadata

Every function declaration and definition is validated. The compiler currently
accepts void, one-through-four-byte ordinary binary integers, one/two/three-byte
packed BCD integers, and 16-bit pointers. It rejects aggregates, arrays, and
other noninteger/nonpointer returns. The stock interface names ordinary widths
through 32 bits.

Inside a non-void function, `$$` is the source-level return object. It is an
exact-sized hidden callee-owned zero-page symbol named `function$__return`.
`return expr;` writes it, and the common epilogue is plain `RTS` for every
current width. Non-static definitions `.zpexport` the symbol. A caller in another
object `.zpimport`s it only when the result is consumed.

After `JSR`, the caller copies directly from `function$__return` into the
enclosing destination. Ordinary generated calls do not push or restore compiler
state. A, X,
and Y are ordinary caller-clobbered registers, not result registers. Ignored
return values cause no copy. There is no `return_value` metadata mode, no
caller-owned return slot, and no register-result scratch lease.

Current direct-parameter ABI
----------------------------
Every fixed parameter of a directly named function has a callee-owned symbol.
At each ordinary direct call, the compiler may lease the caller function's
current scratch-depth slot. The lease selects a fixed `__vcsc_scratch_N` symbol
at compile time. The caller:

1. evaluates each argument left-to-right through directly addressed scratch
   when conversion or temporary storage is required;
2. copies each converted value to the corresponding callee parameter symbol;
3. executes JSR with no compiler-state pushes;
4. when the result is used, copies directly from the callee-owned
   `function$__return` symbol into the enclosing destination.

Scratch is pooled per function and nesting depth. Sequential uses in one
non-reentrant function reuse the same slot, nested uses receive deeper slots,
and different functions receive distinct physical symbols so caller values
remain live during a callee. Each slot is emitted once at the maximum size seen
for that depth. LIFO release and an end-of-compilation zero-depth check reject
unsafe lifetime overlap. Parameter symbols may be ordinary absolute storage or
zero-page storage.

Call-graph and call-target rules
--------------------------------
No function body may be active twice because its parameters, automatic locals,
and return object share one fixed activation record. Compiler and linker
call-graph checks reject same-unit and cross-object cycles, including cycles
through zero-parameter functions.

Every call target must be a directly named function. Function-pointer
declarators are rejected, as are bare function names or `&function` used as
values. This keeps every call edge visible to cycle analysis and eliminates the
indirect-call ABI and `_callptr0` runtime trampoline.

The same linker-visible function/edge metadata now drives longest-path stack
sizing. The linker counts nodes on the longest acyclic path after all selected
objects and archive members are known. External assembly functions reached by a
source call appear as one graph node, but calls and pushes inside their assembly
bodies are not visible yet.

Atari 2600 hardware constraints
-------------------------------
* The VCS has 128 physical bytes of RIOT RAM at $80-$FF.
* $180-$1FF is a mirror of the same physical RAM, not separate stack RAM.
* Static storage and JSR/RTS/PHA/PHP stack use therefore compete for one pool.
* Every outstanding JSR consumes two physical RAM bytes until RTS.
* The 6507 has no usable IRQ or NMI inputs, so asynchronous interrupt re-entry
  is not the normal problem; program-controlled re-entry still is.
* Visible kernels have only 76 CPU cycles per scanline. Calls in kernels will
  eventually require an explicit policy or automatic inlining.

Current VCS RAM model
---------------------
File:

    libraries/vcs/vcs_4k.cfg

The linker models one physical RIOT RAM pool rather than independent RAM and
CPUSTACK regions. `vcs_4k.cfg` declares the full block:

    $80-$FF   128 physical bytes, with `callstack = callgraph`

Before layout, vcsc-ld shrinks that region from the top. For a maximum linked
source call depth D, the base reserve is `2 * D` bytes: two bytes for each
active JSR return address, including the assembly startup call into a source
root. If the linked image contains one or more runtime initializer functions,
vcsc-ld adds one fixed two-byte allowance because startup preserves its
init-table cursor while calling them. Ordinary generated calls push no
parameter, return, or scratch-base state.

For `01_solid_color`, D=2, so ordinary allocation receives $80-$FB (124 bytes)
and $FC-$FF is reserved. For the fully source-level Ode player, D=3, so ordinary
allocation receives $80-$F9 (122 bytes) and $FA-$FF is reserved. The bytes are
accessed through their page-1 mirrors by the 6502 stack pointer.

The map reports a `CALL STACK` block and the linker exports:

    __call_stack_depth
    __call_stack_size
    __call_stack_start
    __call_stack_top

Known limitation: inline assembly can issue PHA/PHP/JSR, and separately assembled
routines can contain hidden calls or pushes. Those are deliberately not included
in the first implementation and must be accounted for later.

Tests verify dynamic map sizing, a four-level path, overflow enforcement, and
that the configuration declares the full RIOT block without a separate
CPUSTACK region.

Software-stack status
---------------------
Top-level software_stack_inventory.txt is the authoritative checked inventory.
Compiler source contains zero `_pushN` allocation sites and zero `_popN` cleanup
paths, and the runtime implementation has now been removed completely:

* `_vcsc_sp` is not allocated, exported, or initialized;
* `_pushN`, `_popN`, and every `sp2ptr*` helper are absent from source and the
  runtime archive;
* linker configurations no longer declare an `ARGSTACK` segment;
* compiled functions have no software-stack entry prologue;
* retained division/remainder uses a private four-byte BSS object in `_divNle`,
  selected from the runtime archive only when `/` or `%` is linked.

Pooled fixed scratch is selected by a compile-time lease stack and addressed
directly through linker-resolved `__vcsc_scratch_N` symbols. `_vcsc_fp`, its
startup/runtime storage, the `fp2ptr*` helpers, and call-site save/restore code
are gone. The generic regression scans compiler/runtime source, generated code,
and the rebuilt archive so the removed machinery cannot quietly return.

Test policy
-----------
When a feature is removed:

1. delete positive tests whose purpose is to prove that feature works;
2. keep or add negative tests proving the compiler rejects it cleanly;
3. rewrite tests whose real subject remains useful to use legal types or
   out-parameters;
4. list every intentionally deleted path in remove.txt.

For the big/mixed-endian purge:

* target multibyte values are unconditionally little-endian;
* the compiler retains only direct rejection paths for `$endian:big`, `($big)`,
  and `($little)`;
* all big-endian and mixed-endian compiler branches and 37 big-endian nlib
  procedures were removed;
* 24 obsolete positive endian tests were intentionally removed and three direct
  rejection tests were added;
* all removed paths are in top-level remove.txt.

For the wide-binary-integer purge:

* ordinary binary target integer declarations may be one through four bytes;
* `$bcd` declarations are the deliberate unsigned one-, two-, and three-byte
  packed-decimal exception;
* target pointers must be two bytes;
* integer literals may carry up to 32 bits when a typed ordinary destination or
  operation supplies a matching width; the stock untyped default remains int16_t;
* positive 24/32/64-bit arithmetic, promotion, extension, shift, helper, and
  return tests were removed or rewritten to legal widths;
* PetiteFFS was removed because its public model requires a 32-bit DWORD;
* dead add/subtract/increment runtime assembly files were removed;
* host-side `long long`, `uint32_t`, and `uint64_t` used inside the compiler,
  assembler, and MD5 implementation are not target-language types and remain.

Files changed in this revision
------------------------------
Hard-renamed tool implementations and build products:

    driver/n65cc.c                -> driver/vcsc.c
    compiler/n65c.c               -> compiler/vcsc_cc1.c
    assembler/n65asm.c            -> assembler/vcsc_as.c
    archiver/n65ar.c              -> archiver/vcsc_ar.c
    linker/n65ld*.c/h             -> linker/vcsc_ld*.c/h
    n65cc/n65c/n65asm/n65ld/...   -> vcsc/vcsc-cc1/vcsc-as/vcsc-ld/...

Hard-renamed runtime and installed layout:

    libraries/nlib/               -> libraries/runtime/
    nlib.a65                      -> libvcsc.a65
    nlib.inc                      -> vcsc-runtime.inc
    nrt0.s                        -> vcsc-rt0.s
    nlib_zeropage.s               -> vcsc-zeropage.s
    /opt/n                        -> /opt/vcsc

All 347 language source, include, example, and test files formerly ending in
`.n` now end in `.vcsc`. Their 347 old paths, plus 27 other renamed tool/runtime
paths, are recorded individually in `remove.txt`.

Current source/build/documentation files were updated throughout the repository,
including top-level and component Makefiles, driver/tool diagnostics, parser/test
source discovery, ABI/runtime symbol names, archive reader/writer magic, install
rules, examples, Doxygen configuration, and all maintained documentation.

Tests added:

    test/vcsc_branding.pl
    test/vcsc_branding.test

The branding regression verifies the hard-cut paths and suffixes, the renamed
archive signature, the documentation banners, and continued presence of both
batari Basic kernel families. The vendored batari kernel source itself was not
removed or translated. `remove.txt` now contains 861 unique cumulative paths.

Build environment
-----------------
The host lacked bison. Supplied Debian packages were unpacked without root into:

    /mnt/data/vcsc_local_tools

Required environment:

    export PATH=/mnt/data/vcsc_local_tools/usr/bin:$PATH
    export LD_LIBRARY_PATH=/mnt/data/vcsc_local_tools/usr/lib/x86_64-linux-gnu:${LD_LIBRARY_PATH:-}
    export BISON_PKGDATADIR=/mnt/data/vcsc_local_tools/usr/share/bison

BISON_PKGDATADIR is required because the privately unpacked bison otherwise
looks under the host /usr/share/bison.

Build and test results
----------------------
Clean warning-as-error tools build:

    make tools -j2

Result: passed.

Compile suite:

    266 passed, 0 failed, 266 total

End-to-end/generic suite:

    117 passed, 0 failed, 117 total

Staged install check:

    passed

Both examples still build directly as raw 4096-byte cartridges without an
explicit `-T`. The staged installed toolchain also compiles the VCS header smoke
program using `int24_t`, `uint24_t`, `int32_t`, and `uint32_t` and produces
an exact 4096-byte cartridge.
Bare `vcsc-ld` invocation without a script still fails with a focused diagnostic,
while retained generic e2e tests use `test/generic_6502.cfg`.

Immediate TODO
--------------
The first unfinished item is the default next action.

[x] 1. Initially return retained scalar/pointer values in A:X. Task 18a
       now supersedes this with callee-owned memory returns.

[x] 2. Make direct fixed parameters callee-owned symbols and reject call cycles.

[x] 3. Enforce legal return types and delete the caller-owned return-slot ABI.

[x] 4. Fix libraries/vcs/vcs_4k.cfg so mirrored RIOT RAM is not modeled as two
       independent memories. The initial fixed reserve was later replaced by
       call-graph-derived hardware-stack sizing.

[x] 5. Replace stack-backed automatic locals with fixed-address per-function
       storage. Start with distinct frames; defer frame overlay optimization.

[x] 6. Inventory every retained _pushN/_popN use and classify it as automatic
       local allocation, expression temporary, caller argument scratch,
       aggregate-only support, or obsolete runtime support.

[x] 7. Replace ordinary direct-call argument/result scratch with fixed call-site
       storage. Leave the indirect-call path isolated on the old stack
       temporarily.

[x] 8. Remove variadic functions, stdarg machinery, and the now-isolated legacy
       direct-call software-stack fallback.

[x] 9. Remove function pointers and the indirect-call software-stack ABI.

[x] 10. Replace retained expression, statement, lvalue, and initializer
        temporary storage for the 8/16-bit language with fixed BSS scratch.
        Slice 15e later pooled that scratch by function and nesting depth.

[x] 11. Remove operator overloading, `$exactops`, and weak built-in operator
        dispatch. Compiler source now emits zero `_pushN`/`_popN` calls. Keep
        pooled scratch temporarily used `_vcsc_fp`; task 18b later removed that
        final runtime indirection after `_vcsc_sp` and the stack helpers were gone.

[x] 12. Enforce the final basic-type set: int8_t, uint8_t, int16_t, uint16_t
        in controlled groups.

        [x] 12a. Remove floating-point types, literals, compiler/runtime code,
                  driver linkage, and positive tests.
        [x] 12b. Remove big-endian and mixed-endian type/expression support.
        [x] 12c. Remove 24/32/64-bit integer types, helpers, and positive
                  tests; enforce one- and two-byte target integers only.
        [x] 12d. Replace transitional compiler-required `bool`, `char`, and
                  `int` names with uint8_t, int8_t, and int16_t so the stock
                  machine interface exposes only the canonical four integer
                  names.

[x] 13. Produce and verify an unbanked 4K VCS cartridge using the static-frame
        ABI and the real 128-byte shared RAM model. The example emits a raw
        4096-byte .bin and has an automated map/vector/size regression test.

[x] 14. Resolve source-level absolute `ref` names in inline assembly using
        opcode-aware access semantics. Reads require a read address, writes
        require a write address, and read-modify-write instructions require
        identical non-`none` read/write addresses. Reject all mismatches rather
        than warning or falling back.

[x] Side quest: add a second complete 4K example that plays a repeating
    frame-driven classical melody from a const ROM array of volume, frequency,
    control, and timing structs, plus reusable NTSC TIA sound aliases. The
    corrected player runs inside a fixed TIM64T overscan budget, uses two final
    WSYNCs, and has an execution regression proving Stella-reported 262-line
    frames rather than the former 261-line output.

[x] 15. Finish the general compiler-efficiency work exposed by Ode to Joy
        before continuing broad functional reduction. The source-level
        direct-index player, compact byte paths, lifetime-pooled scratch, and
        repeated-address reuse now pass the RAM and fixed-overscan gates.

        Acceptance criteria:

        [x] 15a. Express `music_tick()` in `ode_to_joy.vcsc`; retain the const ROM
                  array of `MusicStep` structs and remove the companion player
                  assembly once the source version passes.
        [x] 15b. Fit the complete cartridge in the VCS RAM map without relying
                  on accidental spare bytes or moving ordinary game logic into
                  assembly. Whole-program call-graph sizing replaced the fixed
                  32-byte hardware-stack partition.
        [x] 15c. Keep every execution path--ordinary counter tick, note change,
                  silent gap, and end-of-score wrap--inside the fixed 30-line
                  TIM64T overscan budget while preserving stable Stella-reported
                  262-line NTSC frames in the execution regression.
        [x] 15d. Make natural direct `music[index].field` practical: inline-scale
                  a simple uint8_t index for power-of-two element sizes without
                  an indextmp or multiply helper; emit compact ordinary uint8_t
                  increment/comparison/constant-store paths; restore the Ode
                  source to direct indexing; retain zero BSS scratch and stable
                  262-line timing. A high-byte regression covers index 64 into
                  four-byte elements.
        [x] 15e. Pool general compiler scratch by function and nesting depth,
                  retaining leases through final copy-out and verifying LIFO
                  lifetime safety. Reuse one computed element address when two
                  binary operands are fields of the same runtime-indexed
                  aggregate element under strict side-effect-free conditions.
                  Unsupported index forms use generic pooled scratch.

[x] 16. Delete unsupported tools/runtime/language features one group at a time,
        recording all removed paths in remove.txt and keeping retained tests
        green.

        [x] 16a. Remove heap/sbrk support and its initialization/runtime tests.
        [x] 16b. Remove residual `_vcsc_sp`, `_pushN`, `_popN`, and `sp2ptr*`
                  runtime machinery after giving retained division a private
                  fixed workspace. Task 18b later removed `_vcsc_fp` after
                  pooled scratch became directly addressed static storage.
        [x] 16c. Fix retained compiler correctness defects inherited from N
                  before deleting more language machinery.

                  [x] 16c1. Correct pointer-pointer subtraction typing and
                            lowering. Compatible pointers now produce `int16_t`
                            element counts. Byte elements skip division; larger
                            elements use sign-preserving division by object size.
                            Incompatible pointed-to types are rejected. Tests
                            cover positive and negative byte and three-byte
                            element differences plus result width.
                  [x] 16c2. Preserve pointer declarators and array-to-pointer
                            decay through assignment scratch. Array detection
                            now uses the original expression declarator before
                            lvalue adjustment, and the direct lvalue-copy fast
                            path cannot intercept pointer-targeted decay.
                            Fixed scratch retains the destination declarator.
                            Tests cover local, global, file-static, function-
                            static, and indirect pointer destinations with one-
                            and two-byte element arrays.
        [x] 16d. Remove ordinary named-function overloading. Each function name
                  now has one signature and one unmangled assembler symbol;
                  matching declarations plus one definition remain supported.
        [x] 16e. Prune unused generic libraries, machine targets, tools, and
                  grammar surfaces in dependency-ordered groups.

                  [x] 16e1. Remove the generic interrupt-entry library and
                            handler ABI. Keep only mandatory weak RTI vector
                            fillers in the stock VCS startup member.
                  [x] 16e2. Preserve the vendored batari Basic kernel
                            reference tree for possible conversion/adaptation.
                            It remains intentionally unwired for now, but tests
                            guard both standard and multisprite kernel families.
                  [x] 16e3. Remove the generic installed `machine_6502.vcsc` target
                            and the unwired `libraries/runtime/n.cfg` and
                            `linker/default.cfg` layouts. Keep the test-local
                            generic machine fixture and all native VCS material.
                  [x] 16e4. Eliminated the linker's implicit generic non-VCS
                            memory-map fallback. The stock driver discovers and
                            uses `vcs_4k.cfg` by default when no `-T` is supplied;
                            direct linker use without a script fails clearly.
                            Generic end-to-end tests use an explicit test-only
                            layout.

        [x] 16f. Add hardware-backed unsigned packed-BCD types `bcd8_t`,
                  `bcd16_t`, and `bcd24_t`, including numeric literal packing,
                  decimal-mode add/subtract, restrictions, ABI identity,
                  documentation, and focused/full regressions. This is a new
                  Atari-specific capability requested after pruning, not a
                  restoration of generic wide binary integers.
        [x] 16g. Fix valued `++`/`--` side effects outside ordinary
                  assignment lowering. Central lvalue classification now keeps
                  increment/decrement expressions out of direct compare,
                  absolute-ref copy, byte-index, and `ref`-argument shortcuts.

[x] 17. Rename the project and toolchain to `vcsc` as a hard cut. The public
        driver is `vcsc`; companion tools are `vcsc-cc1`, `vcsc-as`, `vcsc-ld`,
        `vcsc-ar`, and `vcsc-sim`. Source files use `.vcsc`; the runtime lives in
        `libraries/runtime` and installs `libvcsc.a65` plus `vcsc-runtime.inc`.
        Generated/runtime symbols use VCSC prefixes, the archive magic is
        `VCSCAR\x01`, and no compatibility wrappers preserve obsolete commands.
        Every maintained documentation file starts with the canonical lowercase
        `vcsc` Standard-font FIGlet banner. Historical changelog entries retain
        old spellings where needed to describe what actually happened.

[x] 18. Generalize scalar widths now that function returns are memory-backed.

        [x] 18a. Replace the A:X scalar return convention with exact-sized
                  callee-owned `function$__return` objects. Export/import the
                  hidden zero-page object across translation units, make every
                  value epilogue plain `RTS`, consume results directly from
                  memory, and allow `bcd24_t` returns.
        [x] 18b. Remove the inherited runtime frame pointer. Make scratch
                  activation a compile-time symbol stack, address
                  `__vcsc_scratch_N` directly, remove `_vcsc_fp` and `fp2ptr*`,
                  eliminate call-site save/restore pushes, and size the hardware
                  call stack at two bytes per linked call depth, plus the
                  fixed startup cursor allowance required by runtime initializers.
        [x] 18c. Generalize ordinary binary scalar handling from hard-coded
                  one/two-byte assumptions to widths one through four. Audit
                  literals, extension/truncation, assignment, parameters,
                  arithmetic, comparisons, shifts, compound assignment,
                  increment/decrement, constant folding, and helper interfaces.
        [x] 18d. Add first-class `int24_t` and `uint24_t`, retaining VCSC's
                  existing rule that mixed signed/unsigned runtime operations
                  require an explicit cast rather than C's usual conversions.
        [x] 18e. Add first-class `int32_t` and `uint32_t` after the width-generic
                  paths are proven. Keep pointers 16-bit.
        [x] 18f. Add unsigned packed-decimal `bcd32_t` for eight digits once
                  four-byte scalar storage and copying are established.

[ ] 19. Inventory the retained batari Basic standard and multisprite kernels for
        conversion to VCSC. Identify required language, assembler, linker, and
        runtime capabilities; separate mechanical syntax conversion from real
        kernel-model incompatibilities; then propose an incremental conversion
        plan before modifying the vendored kernel source.

Inline assembly absolute-ref rules
----------------------------------
Inline assembly remains single-line and mostly opaque, but source-level
absolute `ref` identifiers in opcode operands are resolved by the compiler.
Official 6502 loads, compares, and arithmetic reads use the ref read address;
stores use the write address; ASL/LSR/ROL/ROR/INC/DEC and recognized unofficial
read-modify-write opcodes require both addresses to exist and normalize to the
same assembler expression.

The compiler rejects:

* a read instruction using a write-only `@[none/write]` ref;
* a store using a read-only `@[read/none]` ref;
* any read-modify-write instruction using a missing or differing side;
* immediate/control-address use of a ref without one identical read/write
  address;
* a ref operand under an opcode whose access behavior is unknown.

Assembler-local `@labels` are never treated as source identifiers. Inline asm
still does not track register or flag clobbers.

Open implementation questions
-----------------------------
The batari Basic tree is intentionally retained for later conversion work.
Functional reduction task 16 and the hard VCSC rename in task 17 are complete.
Tasks 18a through 18f are complete. The next queued action is task 19: inventory
the retained batari Basic kernels and propose a conversion plan before changing
vendored kernel source.

* Whether static frames will later be overlaid using whole-program call/liveness
  analysis. Start with distinct storage.
* Extend call-graph hardware-stack sizing to account for PHA/PHP/JSR hidden in
  inline assembly and for stack use inside separately assembled routines.
* Whether common computed-address reuse should later extend across statement
  boundaries. The current implementation deliberately reuses only two operands
  inside one binary expression, where identical index trees and intervening
  clobbers can be ruled out mechanically.
* Whether to add explicit runtime conversion operations between packed BCD and
  ordinary binary integers. Current casts deliberately reject representation
  changes; compile-time constants convert numerically.
* Whether later demand justifies BCD multiplication/division helpers. The
  current hardware-backed feature deliberately supports only add/subtract and
  operations that preserve valid packed digits.
* Whether separate compilation remains throughout simplification.
* Kernel-function syntax and cycle checking/inlining policy.

Change log
----------
Entries are oldest first. Append each new revision at the bottom. Timestamps use
America/Los_Angeles local time and the format `YYYY-MM-DD HH:MM:SS PDT`.
Backfilled timestamps are the `context.txt` modification times stored in the
corresponding released archives, so they mark revision completion closely and
reproducibly rather than the exact instant of each individual source edit.

2026-07-16 14:18:32 PDT, initial context
* Documented the static-frame VCS goal and mirrored-RAM problem.
* Added top-level context.txt and remove.txt policy.

2026-07-16 14:53:31 PDT, A:X return slice
* Changed one- and two-byte scalar/pointer returns to A:X.
* Made `$$` a callee-local return scratch object.
* Added return_ax metadata and simulator coverage.
* Temporarily retained legacy oversized/aggregate return storage.


2026-07-16 15:28:56 PDT, default direct-parameter symbols
* Made direct fixed parameters symbol-backed by default.
* Removed fixed direct parameters from software-stack call frames.
* Expanded recursion/cycle validation.
* Rejected parameterized function pointers; retained zero-argument pointers.


2026-07-16 15:58:23 PDT, legal-return enforcement
* Recorded and enforced the two-byte maximum and aggregate-return prohibition.
* Added declaration/definition validation and indirect-call validation.
* Deleted the caller-owned return-slot call-frame path.
* Replaced `return_value` ABI metadata with only return_void or return_ax.
* Renamed user diagnostics from return "slot" to return "object".
* Added negative tests for aggregate, array, float, wide, and big-endian returns.
* Added a positive pointer-to-aggregate A:X test.
* Deleted 35 obsolete positive tests; all paths are in remove.txt.
* Curated the retained suites to 279/279 compile and 118/118 E2E.


2026-07-16 16:23:03 PDT, physical VCS RAM partition
* Removed the fictitious independent CPUSTACK region from vcs_4k.cfg.
* Limited linker-allocatable RIOT RAM to $80-$DF.
* Reserved $E0-$FF, mirrored at $01E0-$01FF, for the 6502 hardware stack.
* Mapped transitional ARGSTACK to the real RAM pool.
* Added map, overflow, and configuration-regression tests.
* No files were removed.


2026-07-16 17:21:54 PDT, fixed-address automatic locals
* Converted named automatic locals and the hidden `$$` object to function-
  qualified fixed-address symbols.
* Kept automatic initialization semantics: initialization executes each time
  control reaches the declaration.
* Expanded cycle rejection to every function body because every function now
  owns fixed activation storage.
* Retained balanced software-stack scratch only for expression evaluation,
  conversions, and other transitional compiler machinery.
* Fixed array-to-pointer decay when assignment evaluation uses temporary
  scratch; the previous transitional lowering copied the first array byte.
* Added focused code-generation and repeated-call execution tests.
* No files were removed.


2026-07-16 17:37:15 PDT, software-stack inventory
* Counted and classified all 30 compiler `_pushN` allocation sites and their 78
  normal/error `_popN` emissions.
* Confirmed that no ordinary named automatic local or `$$` allocation remains
  on the software stack.
* Added top-level software_stack_inventory.txt with per-file/per-purpose
  disposition and an ordered removal plan.
* Added a generic regression test that rejects unclassified stack-emission
  changes.
* Selected direct-call argument/result scratch as the next implementation slice.
* No files were removed.


2026-07-16 18:02:02 PDT, fixed direct-call scratch
* Replaced ordinary nonvariadic direct-call `_pushN`/`_popN` call frames with
  private fixed BSS symbols named `__n65_calltmp_N`.
* Temporarily redirects `fp` to call-site scratch while evaluating arguments,
  then copies converted values to callee-owned parameter symbols.
* Captures A:X through the same scratch only when old expression machinery needs
  a memory-backed result.
* Added scratch high-water tracking so nested expression temporaries determine
  the correct fixed allocation size.
* Kept variadic and indirect calls on the isolated legacy stack paths.
* Added a code-generation test forbidding `_pushN`/`_popN` in a simple direct
  call and updated affected assembly expectations.
* Passed 281/281 compile tests and 123/123 end-to-end/generic tests.
* No files were removed.


2026-07-16 18:22:34 PDT, variadic removal
* Removed `...` from the parser grammar and made the lexer reject it directly.
* Deleted variadic direct and indirect call lowering, hidden variadic locals,
  builtin `va_start`/`va_arg`/`va_end`, and variadic ABI metadata.
* Deleted `libraries/nlib/stdarg.n` and `libraries/nlib/sprintf.n`.
* Deleted 13 obsolete variadic/stdarg tests and added two rejection tests.
* Deleted the obsolete direct-call stack fallback and persistent function-frame
  `_pushN`/`_popN` emissions.
* Reduced the checked compiler stack inventory from 30/78 to 28/75.
* Passed 269/269 compile tests and 122/122 end-to-end/generic tests.


2026-07-16 19:15:02 PDT, function-pointer removal
* Rejected all function-pointer declarators while retaining parser recognition
  for a direct diagnostic.
* Rejected bare function names and `&function` used as data values, including
  attempts to store code addresses in ordinary data pointers.
* Deleted indirect-call lowering, function-designator overload matching,
  function-pointer ABI fingerprinting, and the `_callptr0` runtime trampoline.
* Removed four obsolete positive tests and added three focused rejection tests.
* Reduced the checked compiler stack inventory from 28/75 to 27/73; no
  software-stack call frame remains in `compile_call.c`.


2026-07-16 19:45:13 PDT, fixed truth/discard expression scratch
* Replaced general truth-value `_pushN`/`_popN` scratch with fixed per-site BSS
  symbols named `__n65_truthtmp_N`.
* Replaced discarded non-call expression-result stack scratch with fixed per-site
  BSS symbols named `__n65_discardtmp_N`.
* Temporarily redirects `fp` to each symbol, tracks nested scratch high-water,
  restores `fp` through the 6502 hardware stack, and reads truth bytes directly
  from fixed storage.
* Added focused code-generation tests and updated affected assembly expectations.
* Reduced the checked compiler stack inventory from 27/73 to 25/69.
* Passed 273/273 compile tests and 120/120 end-to-end/generic tests.
* No files were removed.


2026-07-16 20:14:46 PDT, fixed simple-assignment scratch
* Replaced the fixed-symbol simple-assignment conversion `_pushN`/`_popN`
  buffer with private per-site BSS symbols named `__n65_assigntmp_N`.
* Made assignment scratch explicitly destination-typed so widening, narrowing,
  signedness, and endian conversion happen before the final symbol copy.
* Added code-generation and runtime tests for an 8-bit-to-16-bit assignment.
* Moved four legacy wide-shift dump buffers from $0300 to unused $E000 because
  the new fixed BSS scratch legitimately expands beyond their old hard-coded
  buffer address; their computed output bytes remain unchanged.
* Reduced the checked compiler stack inventory from 25/69 to 24/67.
* Passed 274/274 compile tests and 121/121 end-to-end/generic tests.
* No files were removed.


2026-07-16 20:43:14 PDT, fixed comparison and binary-expression scratch
* Replaced the two-operand comparison `_pushN`/`_popN` buffer with fixed per-site
  BSS symbols named `__n65_comparetmp_N`.
* Replaced stack scratch for pointer subtraction, scalar/pointer addition and
  subtraction, shifts, bitwise operations, multiplication, division, and
  remainder with `__n65_ptrdifftmp_N`, `__n65_addtmp_N`,
  `__n65_shifttmp_N`, and `__n65_binarytmp_N`.
* Added fixed-symbol-to-fixed-symbol conversion support for copied expression
  results while preserving the caller frame.
* Added focused code-generation and execution tests and updated two exact-
  assembly expectations.
* Reduced the checked compiler stack inventory from 24/67 to 19/52.
* Passed 276/276 compile tests and 122/122 end-to-end/generic tests.
* No files were removed.


2026-07-16 21:23:40 PDT, fixed cast/absolute-reference/increment scratch
* Replaced cast-conversion software-stack scratch with fixed per-site BSS
  symbols named `__n65_casttmp_N`.
* Replaced function-local and global absolute-reference conversion scratch with
  `__n65_abslocaltmp_N` and `__n65_absglobaltmp_N`.
* Replaced built-in pre/post increment/decrement scratch with
  `__n65_incdectmp_N`.
* Added fixed symbol-to-lvalue and bit-field-lvalue-to-symbol helpers so built-in
  increment/decrement covers ordinary, indirect, absolute, and bit-field
  lvalues without `_pushN`/`_popN`.
* Added focused code-generation and execution tests.
* Reduced the checked compiler stack inventory from 19/52 to 15/41.
* Passed 277/277 compile tests and 123/123 end-to-end/generic tests.
* Recorded the pre-existing assignment-expression `post++` fast-path defect for
  a later independent fix; standalone built-in increment/decrement is correct.
* No files were removed.


2026-07-16 22:11:56 PDT, compound/switch/initializer fixed-scratch batch
* Replaced built-in compound-assignment scratch with `__n65_compoundtmp_N`.
* Replaced switch expression/case scratch with reusable `__n65_switchtmp_N`; fp
  is restored before every generated branch and before case bodies.
* Replaced simple indirect/absolute/bit-field assignment scratch with
  `__n65_lvalueassigntmp_N`.
* Replaced runtime local and return-object initialization with `__n65_inittmp_N`.
* Replaced local absolute-reference initialization with `__n65_absinittmp_N`.
* `compile_stmt.c` no longer emits `_pushN` or `_popN`.
* Software-stack inventory fell from 15/41 to 10/22.
* Moved the two obsolete 9-16-byte shift-test dump buffers from $E000 to $1800;
  fixed-scratch code growth had reached $E000, while $1800 remains unused RAM in
  their generic test map.
* No files were removed.


2026-07-16 22:58:49 PDT, lvalue/braced/global-init fixed-scratch batch
* Replaced nonconstant pointer/index lvalue scratch with per-site
  `__n65_indextmp_N` BSS storage.
* Removed unnecessary software-stack pointer saves from bit-field read/write
  helpers; preparing `ptr1` does not clobber the lvalue address in `ptr0`.
* Replaced braced-assignment scratch with `__n65_bracedtmp_N`.
* Replaced the synthesized runtime-global-initializer software frame with
  `__n65_globalinittmp_N`, preserving and restoring the caller `fp` through the
  6502 hardware stack.
* `compile_lvalue.c` and `compile_init.c` no longer emit `_pushN` or `_popN`.
* Software-stack inventory fell from 10/22 to 5/13; every survivor is an
  obsolete operator-overload/weak-operator path.
* No files were removed.


2026-07-16 23:48:16 PDT, operator-overload and final compiler-stack-emitter removal
* Removed user-defined operator declarations, lookup, mangling, dispatch, `$exactops`,
  and weak built-in operator fallback while retaining ordinary function overloading.
* The lexer keeps explicit rejection rules for `operator...` and `$exactops`.
* Removed the final five compiler `_pushN` sites and thirteen `_popN` emissions; the
  checked compiler inventory is now 0/0.
* Removed the weak-operator generator/runtime members, custom-float exact-operator
  generator, generated fixture plumbing, and 167 obsolete source/test/fixture files.
* Localized the transitional half/float/double subtraction sign flip inside each
  float subtraction object, removing its dependency on the deleted weak unary-negation
  symbol without reviving the generic weak-operator runtime.
* Added focused rejection tests for operator overloading and `$exactops`.
* All intentionally deleted paths are listed cumulatively in remove.txt.


2026-07-17 00:21:13 PDT, floating-point purge
* Removed floating-point literal AST/constant handling, type-style handling,
  code generation, initializer packing, comparison/arithmetic lowering, and
  compiler float implementation files.
* The lexer rejects floating-point literals and type declarations reject
  `$float`/`$float:*` with direct diagnostics.
* Removed the complete `libraries/float` runtime and its build/install wiring.
* Removed automatic float-runtime discovery/linking from `n65cc` and the test
  harness.
* Removed float declarations from the generic, VCS, and test machine definitions.
* Removed the dead 2 MB `libraries/nlib/genasm.pl` float bundle generator.
* Deleted obsolete positive float tests and rewrote broad fixtures whose actual
  purpose remains useful.
* Added focused rejection tests for floating-point types and literals.
* Removed 152 files in this revision; remove.txt now contains 373 cumulative paths.
* Passed 241/241 compile tests and 101/101 end-to-end/generic tests.


2026-07-17 08:40:47 PDT, big/mixed-endian purge
* Removed target-endian selection from type promotion, conversion, expression
  lowering, lvalue addressing, initializers, return handling, overload ranking,
  and ABI fingerprints.
* Target integers, pointers, constants, and runtime values are now
  unconditionally little-endian.
* Removed 37 big-endian procedures from the nlib assembly templates; the rebuilt
  archive has no big-endian helper members.
* `$endian:big`, `($big)`, and `($little)` remain only as direct rejection
  diagnostics. `$endian:little` remains accepted temporarily by the general type
  declaration syntax but carries no selectable ABI state.
* Removed 24 obsolete positive endian tests and added three direct rejection
  tests. remove.txt now contains 397 cumulative paths.
* Passed 224/224 compile tests and 97/97 end-to-end/generic tests; final clean
  E2E validation ran as 49-case and 48-case chunks.


2026-07-17 09:10:37 PDT, 24/32/64-bit target-integer purge
* Enforced exactly one- or two-byte target integer declarations and two-byte
  pointers; untyped literals larger than 16 bits are rejected.
* Added canonical int8_t, uint8_t, int16_t, and uint16_t definitions to the
  generic, VCS, and test machine files.
* Removed wide add/subtract/increment lowering and deleted their now-dead nlib
  assembly sources.
* Removed PetiteFFS because it requires a 32-bit DWORD model.
* Removed 20 obsolete positive wide-integer tests, rewrote useful broad tests to
  legal widths, and added four direct rejection tests.
* Fixed pointer-pointer subtraction to produce the signed `int16_t` difference type
  rather than incorrectly inheriting the pointee type.
* remove.txt now contains 447 cumulative paths.
* Passed 217/217 compile tests and 88/88 end-to-end/generic tests.


2026-07-17 10:09:27 PDT, canonical four-type interface
* Replaced target-language uses of transitional `bool`, `char`, and `int` with
  `uint8_t`, `int8_t`, and `int16_t`. The stock generic, VCS, and test machine
  definitions now expose only int8_t, uint8_t, int16_t, and uint16_t.
* Removed the compiler's special boolean-type requirement and semantics.
  Comparisons and logical expressions now produce an ordinary uint8_t.
* Added direct rejection tests for legacy bool, char, and int declarations and
  canonical-type ABI/code-generation coverage.
* Renamed retained tests whose filenames encoded legacy type names and listed
  every old pathname in remove.txt. remove.txt now has 458 cumulative paths.
* Passed 217/217 compile tests and 88/88 end-to-end/generic tests.
* Deferred the project/toolchain rename to `vcsc` until after functional
  reduction; recorded the tagline "a brutally pared-down Atari VCS C-like
  compiler" as the final task-list item.


2026-07-17 10:43:22 PDT, typedef aliases, ROM strings, and first 4K cartridge
* Added simple transparent `typedef ExistingType Alias;` declarations. `int`,
  `char`, and `bool` are ordinary identifiers and may be aliases rather than
  built-ins.
* Moved pointer-backed string literals from DATA/RAM to RODATA/ROM and covered
  `uint8_t *string := "Hello, World!\n"` end to end.
* Added flat `.bin` output to n65ld; internal holes are filled with $FF.
* Corrected vcs.n/riot.n so source-level memory declarations expose only
  $80-$DF and do not double-count the page-1 stack mirror.
* Added examples/01_solid_color/solid_color.n, a complete 262-scanline unbanked cartridge
  using a static-frame function call during initialization.
* Added an automated cartridge test for exact 4096-byte size, reset prefix,
  vectors, map bounds, and function symbols.
* Removed three obsolete permanent-rejection tests for int/char/bool and listed
  them in remove.txt; remove.txt now has 461 cumulative paths.
* Passed 215/215 compile tests and 90/90 end-to-end/generic tests.


2026-07-17 10:56:32 PDT, changelog timestamp backfill
* Added exact local timestamps to every existing changelog entry.
* Standardized changelog ordering as newest first and timestamp format as
  `YYYY-MM-DD HH:MM:SS PDT`.
* Backfilled times from the `context.txt` modification timestamp stored in each
  corresponding released tarball; these record release-completion time, not the
  instant each individual edit was made.
* No source, test, build, or removal-list changes.


2026-07-17 11:36:20 PDT, opcode-aware inline-asm refs
* Inserted completed task 14 before functional reduction and renumbered the
  later functional-reduction and project-rename tasks to 15 and 16.
* Inline assembly now resolves absolute `ref` source names according to opcode
  operand access: reads use the read address, stores use the write address, and
  read-modify-write instructions require identical non-`none` addresses.
* Rejected read-from-`none`, write-to-`none`, split-address read-modify-write,
  noncanonical address uses, and ref operands under unknown opcode semantics.
  There are no warning/fallback footguns.
* Kept assembler-local `@labels` distinct from source ref identifiers.
* Updated examples/01_solid_color/solid_color.n to use VSYNC, VBLANK, and WSYNC
  names in its inline kernel instead of raw TIA addresses; updated the cartridge
  regression and VCS documentation for the user-renamed example directory.
* Added five focused compile/code-generation tests.
* Reversed the changelog to chronological order; future entries append at the
  bottom.
* Passed 220/220 compile tests and 90/90 end-to-end/generic tests; the cartridge
  remains exactly 4096 bytes.
* No files were removed; remove.txt remains at 461 cumulative paths.


2026-07-17 12:16:30 PDT, NTSC sound aliases and Ode to Joy cartridge
* Added `libraries/vcs/sound_ntsc.n` with named TIA control, NTSC lead-voice
  frequency, volume, and frame-duration aliases.
* Added `examples/02_ode_to_joy`, a complete unbanked 4K cartridge that repeats
  the opening phrase of Beethoven's Ode to Joy on TIA audio channel 0.
* Stored the 16-step score as a 64-byte const ROM array of `MusicStep` structs
  containing volume, frequency, control, and timing values.
* Added `music_tick()`, called immediately before the frame-loop jump, to count
  frames, advance or wrap the score index, and apply AUDV0/AUDF0/AUDC0 values.
* Used symbolic VSYNC, VBLANK, and WSYNC names in the inline kernel so the new
  example also exercises opcode-aware inline-assembly ref resolution.
* Added a cartridge regression for exact ROM size, vectors, RAM map, score
  bytes/RODATA placement, player symbols, call placement, and channel writes.
* Passed 220/220 compile tests and 91/91 end-to-end/generic tests; both examples
  build as exact 4096-byte cartridges.
* No files were removed; remove.txt remains at 461 cumulative paths.


2026-07-17 13:19:16 PDT, stable NTSC timer budget for Ode to Joy
* Diagnosed the 290-line `PAL*` output: the original `music_tick()` ran outside
  the 262 explicit WSYNC lines, so its variable execution time lengthened every
  frame.
* Expanded the ROM score to 32 four-byte steps (128 bytes), inserting a
  two-frame `MUSIC_TIME_GAP` after every musical/rest step so repeated notes are
  articulated without another RAM-hungry indexed expression.
* Retained the C-like const array of `MusicStep` structs but moved the
  cycle-critical player into `examples/02_ode_to_joy/music_player.s`; the
  compiler's current general indexed-struct lowering cannot fit inside a
  30-scanline overscan budget.
* Started TIM64T at 35 on entry to overscan, ran `music_tick()` while it counted
  down, waited for INTIM to reach zero, and used a final WSYNC to align the next
  VSYNC to a scanline boundary.
* Added a minimal VCS timing harness around the repository's 6502 core. It
  verifies VSYNC spacing, WSYNC stalls, RIOT timer completion, and both silent
  and sounding channel-0 updates across ordinary, note-change, gap, and wrap
  paths.
* Verified 1497 consecutive complete frames at exactly 262 scanlines in a
  1500-VSYNC run with no timer overrun; both examples remain exact 4096-byte
  cartridges.
* Reduced the Ode RAM high-water mark from $D8 to $A1 by avoiding permanent
  fixed scratch for the cycle-critical indexed-struct player.
* Passed 220/220 compile tests and 91/91 end-to-end/generic tests.
* No files were removed; remove.txt remains at 461 cumulative paths.


2026-07-17 13:27:51 PDT, source-level music player acceptance gate
* Recorded the companion assembly music player as a temporary diagnostic
  workaround rather than an acceptable endpoint for the compiler.
* Inserted a new task 15 before broad functional reduction: compile the complete
  Ode to Joy `music_tick()` from the C-like source while retaining the const ROM
  array of structs, fitting the VCS RAM map, and meeting the fixed 30-line
  overscan deadline on ordinary, transition, gap, and wrap paths.
* Made efficient scratch lifetime/overlay, indexed array-of-struct lowering,
  repeated-address reuse, and compact 8-bit increment/comparison generation
  explicit compiler acceptance requirements rather than program-specific
  special cases.
* Renumbered broad functional reduction to task 16 and the final `vcsc` rename
  to task 17.
* Documentation only; no source, tests, build files, or remove.txt paths changed.


2026-07-17 13:53:58 PDT, call-graph stack sizing and source-level music player
* Replaced the fixed 32-byte VCS hardware-stack partition with linker-side
  whole-program call-graph sizing. A `callstack = callgraph` MEMORY region is
  shortened from the top by four bytes per maximum function level: two return
  bytes plus the compiler's known two-byte fp-preservation allowance.
* Added map output and generated symbols for call-stack depth, size, start, and
  top; added a four-level depth regression and updated VCS overflow/map tests.
* Documented that inline-assembly pushes/calls and stack use hidden inside
  separately assembled routines are not yet included.
* Replaced the Ode companion assembly player with source-level
  `music_tick()`/`music_apply_current()`. The player retains a pointer to the
  current ROM `MusicStep`, advances or wraps it once per expired step, and
  updates TIA channel 0 from ordinary C-like code.
* Verified the source player fits: call depth 3 reserves $F4-$FF, ordinary
  allocation ends at $B8, and the cartridge remains 4096 bytes.
* Verified 1497 complete consecutive frames at exactly 262 scanlines across
  normal, transition, gap, and wrap paths with no timer underflow.
* Passed 220/220 compile tests and 92/92 end-to-end tests.
* Removed `examples/02_ode_to_joy/music_player.s`; remove.txt now contains 462
  unique cumulative paths.


2026-07-17 14:10:25 PDT, correct Ode 261-line frame off-by-one
* Confirmed in Stella that the timer-based Ode kernel produced 261 displayed
  scanlines despite the old harness reporting 262.
* Added a second final `sta WSYNC` after the TIM64T/INTIM wait, restoring the
  emulator-visible frame to 262 lines without moving `music_tick()` outside the
  fixed overscan budget.
* Corrected the timing harness to distinguish raw VSYNC-assertion spacing from
  Stella's displayed scanline count: a valid 262-line frame has a 263-whole-line
  assertion interval in this kernel.
* Added a negative calibration check during validation: the prior one-WSYNC ROM
  is rejected as 261 Stella-displayed lines, while the corrected ROM passes
  1497 complete frames at 262.
* Updated the Ode cartridge regression and README for the required two-WSYNC
  tail and corrected timing terminology.
* Passed 220/220 compile tests and 92/92 end-to-end tests; both examples remain
  exact 4096-byte cartridges.
* No files were added or removed; remove.txt remains at 462 cumulative paths.


2026-07-17 15:02:00 PDT, direct indexed-struct byte lowering
* Restored the Ode player to natural `music[music_index].field` source rather
  than carrying a persistent pointer to the current score element.
* Added a fast lvalue-index path for simple unsigned one-byte indices and
  power-of-two element sizes. It scales through compiler-owned `arg0:arg1`,
  adds the full 16-bit offset to `ptr0`, and avoids `__n65_indextmp_N` and the
  generic multiplication helper.
* Added direct scratch-free lowering for discarded one-byte increment/decrement,
  unsigned byte comparisons against constants or byte lvalues, constant byte
  stores, and byte lvalue stores to absolute hardware refs.
* The Ode cartridge now has zero BSS scratch and only two persistent player
  bytes at $96-$97; its direct-index code remains inside the fixed overscan
  budget and passes 1497 consecutive Stella-reported 262-line frames.
* Added focused assembly regression coverage plus an execution test using index
  64 into four-byte structs, proving the scaled offset correctly crosses a
  256-byte page.
* Updated three older code-generation tests whose expected scratch/address
  sequences were intentionally superseded by direct byte stores and inc/dec.
* Passed 221/221 compile tests and 93/93 end-to-end tests; both examples remain
  exact 4096-byte cartridges.
* No files were removed; remove.txt remains at 462 unique cumulative paths.


2026-07-17 15:31:37 PDT, lifetime-pooled scratch and common indexed address reuse
* Replaced the per-site temporary-symbol families with depth-indexed
  `__n65_scratch_N` pools scoped to each function/translation-unit context.
  Sequential expressions reuse a slot, nested expressions receive deeper slots,
  and caller/callee functions retain separate physical storage.
* Added explicit acquire/activate/deactivate/release leases, LIFO validation, and
  an end-of-compilation check that rejects unreleased scratch. Each pooled slot
  is emitted once at its maximum observed size.
* Kept scratch leases live until final copy-out. Execution testing caught and
  fixed an early-release bug where recomputing a computed destination address
  could overwrite the pending RHS value; the same lifetime rule was applied to
  cast, operator, braced, compound, and increment/decrement copy-out paths.
* Added safe common-address lowering for two binary operands that are direct
  fields of the same runtime-indexed aggregate element. Identical index trees
  compute ptr0 once and load both fields by offsets; differing indices and
  ambiguous pointer/bitfield/ref/conversion cases retain independent lowering.
* Added layout, address-count, and execution regressions covering sequential
  reuse, nested separation, caller/callee isolation, computed assignments,
  braced and compound stores, increment/decrement, and shared indexed fields.
* Passed 223/223 compile tests and 94/94 end-to-end tests; both examples remain
  exact 4096-byte cartridges, and Ode passes 1497 consecutive Stella-reported
  262-line frames.
* No files were removed; remove.txt remains at 462 unique cumulative paths.

2026-07-17 15:53:35 PDT, remove heap and sbrk runtime support
* Removed `libraries/nlib/asm/sbrk.asm`, including `_sbrk` and its archive-
  selected `__init_sbrk` initializer.
* Removed the `_nl_sbrk` zero-page object and its import/alias glue, shrinking
  the stock runtime workspace from 22 to 20 bytes and reclaiming two VCS RAM
  bytes.
* Removed the compiler's special external-`sbrk` to `_sbrk` symbol mapping and
  the obsolete assembler-name reservation. `sbrk` is now an ordinary user
  function name; added a code-generation regression proving that behavior.
* Updated runtime, compiler, linker, limitation, and software-stack inventory
  documentation to state that the stock runtime has no heap or allocator.
* Updated the Ode cartridge map regression for the two-byte zero-page shift;
  it still allocates zero BSS scratch and only two bytes of persistent state.
* Passed 224/224 compile tests and 94/94 end-to-end/generic tests.
* Added one test and removed one runtime source; remove.txt now contains 463
  unique cumulative paths.


2026-07-17 16:32:32 PDT, warn on runtime division by powers of two
* Added performance warnings for explicit runtime `/`, `%`, `/=`, and `%=`
  operations with compile-time positive power-of-two divisors greater than one.
* Division and remainder code generation and signed semantics are unchanged; the
  warnings suggest explicit shifts or masks only when their different signed
  behavior is acceptable or intended.
* Literal-only constant expressions continue to fold without warnings. Divisor
  one and non-power-of-two constants also remain silent.
* Added focused positive and negative diagnostic regressions.
* Passed 226/226 compile tests and 94/94 end-to-end/generic tests.
* No files were removed; remove.txt remains at 463 unique cumulative paths.


2026-07-18 15:31:03 PDT, remove residual software stack
* Gave `_divNle` a private two-byte BSS dividend workspace. The archive member
  and workspace are linked only when division or remainder is used.
* Removed `_nl_sp`, `_pushN`, `_popN`, every `sp2ptr*` helper, the obsolete
  `ARGSTACK` linker segments, startup `sp` initialization, and the stale
  per-function `fp := sp` prologue.
* Kept `_nl_fp` only as the temporary base used by lifetime-pooled fixed
  scratch. Stock runtime zero-page usage fell from 20 bytes to 18 bytes.
* Added regressions proving ordinary function entry is stack-free, division
  workspace is archive-local and optional, and removed runtime symbols cannot
  reappear in source or archive members.
* Updated the Ode map expectations after reclaiming two zero-page bytes; the
  player still has zero BSS scratch and exactly two persistent state bytes.
* Passed 227/227 compile tests and 95/95 end-to-end/generic tests.
* Added three test files and removed `libraries/nlib/asm/sp2ptr.asm`; remove.txt
  now contains 464 unique cumulative paths.


2026-07-18 16:01:27 PDT, shorten scalar return epilogues for VCS zero page
* Moved hidden one- and two-byte `$$` return objects to `ZEROPAGE` and changed
  the common epilogue to direct loads: `LDA zp; RTS` for 8-bit values and
  `LDA zp; LDX zp+1; RTS` for 16-bit integers and pointers.
* Removed the old indexed epilogue's `LDY` and `TAX` instructions. Including
  RTS, the 8-bit path fell from 6 bytes/12 cycles to 3 bytes/9 cycles, and the
  16-bit path fell from 12 bytes/20 cycles to 5 bytes/12 cycles.
* Fixed the assembler object writer so a plain relocatable expression used by a
  one-byte operand is emitted as a low-byte relocation rather than an invalid
  word relocation.
* Added linked VCS-byte regression coverage for the exact `A5 zp 60` and
  `A5 zp A6 zp+1 60` encodings; updated source-code generation expectations.
* Passed 227/227 compile tests and 96/96 end-to-end/generic tests; both examples
  remain exact 4096-byte cartridges.
* Added two test files and removed none; remove.txt remains at 464 unique
  cumulative paths.


2026-07-18 16:51:21 PDT, record inherited N correctness bugs as required TODOs
* Added task 16c ahead of further functional deletion for two retained compiler
  defects exposed during VCSC reduction.
* Recorded pointer-pointer subtraction's required `int16_t` result type and
  explicit multi-byte-element semantic regressions.
* Recorded the array-to-pointer decay/scratch bug, including direct,
  scratch-routed, and execution acceptance tests proving that pointer
  assignment stores the array address rather than its first byte.
* Renumbered named-function overloading review to 16d and generic pruning to
  16e; the final project rename remains task 17.
* Documentation only; no source, test, build, or remove.txt changes.

2026-07-18 18:10:34 PDT, signed element-count pointer subtraction
* Centralized pointer-difference result selection on the signed pointer-width
  type, which is `int16_t` in VCSC, and rejected incompatible pointed-to types.
* Changed lowering to subtract addresses using the pointer representation and
  return a C-style element count. Byte elements require no scaling; larger
  elements preserve the sign while dividing by their object size.
* Fixed the inherited divider-buffer alias where the remainder output cleared
  the divisor before pointer-distance scaling.
* Added focused compile and simulator regressions for result width, incompatible
  types, and positive/negative distances for one- and three-byte objects.
* Passed 228/228 compile tests and 97/97 end-to-end/generic tests, including both
  exact 4096-byte cartridge regressions.
* No files were removed; remove.txt remains at 464 unique cumulative paths.


2026-07-18 19:13:34 PDT, preserve array decay through assignment temporaries
* Changed pointer-targeted array detection to use the source expression's
  original array declarator before lvalue adjustment.
* Prevented the direct same-type lvalue-copy fast path from copying array bytes
  into pointer destinations; fixed scratch continues to retain the destination
  pointer declarator.
* Added direct and indirect code-generation tests plus simulator coverage for
  local, global, file-static, function-static, and indirect pointer targets with
  one- and two-byte element arrays:
  `test/array_decay_assignment_codegen_test.n`,
  `test/array_decay_indirect_assignment_codegen_test.n`, and
  `test/e2e_array_decay_assignment_temp_verify.n`.
* Passed 230/230 compile tests and 98/98 end-to-end/generic tests, including both
  exact 4096-byte cartridge regressions.
* No files were removed; remove.txt remains at 464 unique cumulative paths.


2026-07-18 21:30:19 PDT, remove ordinary named-function overloading
* Replaced the 996-line ordinary overload resolver with a 410-line direct
  function registry. Each source name now has one signature and one unmangled
  assembler symbol; matching prototypes and a later definition remain legal.
* Preserved direct-call validation for argument count, exact `ref` lvalues,
  safe integer widening, null pointer literals, object-pointer conversion to
  `void*`, and rejection of the reverse conversion without an explicit cast.
* Added direct rejection coverage for a second function signature and argument-
  count mismatch. Reworked useful null-pointer and pointer-helper execution
  tests to use distinct function names, and removed pure overload-selection
  tests.
* Removed overload-signature mangling from the Unicode identifier test while
  retaining Unicode symbol escaping, assembler/linker acceptance, diagnostics,
  and simulator execution.
* Renamed the assembler `op_namespace.s` fixture to
  `extended_symbol_namespace.s` and removed stale operator-overload wording
  while preserving coverage for linker-visible `?@...` symbols versus local
  `@...` labels.
* Passed 227/227 compile tests and 97/97 end-to-end/generic tests, including both
  exact 4096-byte cartridge regressions.
* Added seven source/test files and removed or renamed eleven old paths;
  remove.txt now contains 475 unique cumulative paths.


2026-07-18 23:13:21 PDT, remove generic interrupt runtime
* Removed the optional `libraries/nint` archive, source, build/install plumbing,
  and documentation. The VCS 6507 has no connected hardware IRQ or NMI inputs,
  and the reduced runtime no longer advertises a compiled interrupt-handler ABI.
* Removed `libraries/nlib/asm/handler.asm` and the separate `nrt0_noint.s`
  archive member. Folded weak `__nmi` and `__irqbrk` RTI vector fillers into
  `nrt0.s`, which is already selected by `__reset`.
* Added a regression proving that the old library and archive members are gone
  while the mandatory vector targets remain.
* Passed 227/227 compile tests, 98/98 end-to-end/generic tests, both exact
  4096-byte cartridge regressions, and the staged install check.
* Added two test files and removed nine paths; remove.txt now contains 484
  unique cumulative paths.


2026-07-18 23:42:47 PDT, remove installed generic target bundle
* Recorded the user decision to retain the complete vendored batari Basic
  standard and multisprite kernel trees for possible later conversion. The
  pruning regression now treats those files as required project material.
* Removed `libraries/nlib/machine_6502.n`; the similarly named file under
  `test/` remains an isolated compiler-test fixture rather than an installed
  production target.
* Removed the unwired generic linker layouts `libraries/nlib/n.cfg` and
  `linker/default.cfg`. Native `libraries/vcs/vcs.n` and `vcs_4k.cfg` remain.
* Removed stale install/uninstall and documentation references to the generic
  target bundle, and clarified that platform definitions are selected
  explicitly.
* Added `generic_target_bundle_pruned` coverage proving the removed files stay
  gone while the VCS target, test fixture, and both batari kernel families stay.
* Passed 227/227 compile tests and 99/99 end-to-end/generic tests, including both
  exact 4096-byte cartridge regressions.
* Added two test files and removed three target/layout files; remove.txt now
  contains 487 unique cumulative paths.


2026-07-19 00:00:00 PDT, make VCS 4K layout the driver default
* Removed `n65ld`'s compiled-in generic memory map. Direct linker invocation
  now requires `-T FILE`, `--script=FILE`, or a positional `.cfg` and emits a
  focused diagnostic when no script is supplied.
* Extended `n65cc` path discovery to locate `libraries/vcs/vcs_4k.cfg` in the
  source tree or `share/vcs/vcs_4k.cfg` beside an installed toolchain. The
  driver always passes an explicit script, using the user-supplied `-T` when
  present and the bundled VCS 4K layout otherwise.
* Added `test/generic_6502.cfg` as an explicit test-only layout for retained
  generic compiler/linker/simulator regressions. The production toolchain no
  longer preserves a generic target by accident.
* Added regressions for the driver's default VCS script and the linker's
  script-required error. Updated both cartridge regressions and both example
  Makefiles to omit `-T`, proving the default path in real 4096-byte builds.
* Updated staged installation to build the solid-color cartridge through the
  installed driver and installed `share/vcs/vcs_4k.cfg`.
* Passed 227/227 compile tests, 101/101 end-to-end/generic tests, both exact
  4096-byte cartridge regressions, and the staged install check.
* Added three test files and removed no files; remove.txt remains at 487 unique
  cumulative paths. Task 16e is complete; task 17 is next.


2026-07-19 07:11:04 PDT, add packed-BCD integer family
* Added `$bcd` as an unsigned one-, two-, or three-byte scalar type flag and
  defined stock `bcd8_t`, `bcd16_t`, and `bcd24_t` types for two-, four-, and
  six-digit packed decimal values. The three-byte type is a deliberate
  Atari-specific decimal exception, not restored generic 24-bit binary math.
* Added one numeric literal encoder for static initializers, automatic
  initializers, runtime assignments, aggregate/array elements, typed constants,
  and fixed arguments. Decimal `42`, hexadecimal `0x2a`, octal `052`, and binary
  `0b101010` all become packed byte `$42`; multibyte values use least-significant
  decimal pairs first. Range diagnostics enforce 0..99, 0..9999, and 0..999999.
* Added tightly scoped decimal-mode lowering for BCD `ADC`/`SBC` chains,
  including `+`, `-`, `+=`, `-=`, and valued/discarded `++`/`--`. Generated code
  executes `CLD` immediately after each operation; an execution regression
  verifies the following ordinary binary addition remains binary.
* Added BCD-aware assignment, widening/truncation, comparisons, truth tests,
  switch lowering, parameters, `bcd8_t`/`bcd16_t` returns, and ABI fingerprints.
  Rejected signed/pointer/invalid-width BCD declarations, BCD bitfields,
  multiply/divide/remainder, bitwise operations, shifts, unary minus, runtime
  BCD/binary representation changes, and `bcd24_t` returns.
* Fixed the inherited direct-copy assignment shortcut so `old := value++` and
  its decrement/prefix variants preserve the source side effect; added an
  ordinary-binary end-to-end regression in addition to BCD coverage.
* Added 27 runnable BCD-specific tests plus the generic inc/dec assignment
  regression, and extended the native VCS header smoke test to exercise the
  stock BCD declaration. Passed 250/250 compile tests, 106/106 end-to-end and
  generic tests, the staged install check, both exact 4096-byte cartridge
  regressions, and an installed-toolchain BCD cartridge smoke build.
* Documented representation, literal semantics, supported operations, decimal
  mode invariants, conversion boundaries, and the A:X return limitation. No
  files were removed; remove.txt remains at 487 cumulative paths, and the
  retained batari Basic trees were untouched.


2026-07-19 07:40:01 PDT, preserve valued increment/decrement side effects in all shortcut paths
* Confirmed the ordinary assignment bug fixed during BCD work was not the only
  inherited variant. Compact one-byte comparisons could compare `value++`
  correctly while silently skipping the increment; direct byte copies into an
  absolute ref and the optimized one-byte array-index path could do the same.
* Changed reference-lvalue classification so valued prefix/postfix increment and
  decrement expressions are not considered lvalues. This centrally forces
  side-effecting expressions through normal lowering instead of direct-copy or
  direct-index shortcuts.
* Added an explicit compact-comparison guard as defense in depth and made `ref`
  parameter calls reject `value++`/`value--` as non-lvalue arguments.
* Added `e2e_incdec_valued_storage_matrix_verify.n`, covering all four valued
  forms across globals, zero page, locals, function statics, 8/16-bit values,
  arrays, indirect lvalues, bitfields, absolute refs, nested arithmetic,
  function arguments/returns, comparisons, truth tests, and byte/word pointers.
* Added `ref_parameter_incdec_error_test.n` for the corrected lvalue diagnostic.
* Passed 251/251 compile tests, 107/107 end-to-end/generic tests, and the staged
  installation check. No files were removed; remove.txt remains at 487 unique
  cumulative paths, and the retained batari Basic trees were untouched.

2026-07-19 09:04:39 PDT, complete hard VCSC project and toolchain rename
* Renamed the public driver and companion binaries to `vcsc`, `vcsc-cc1`,
  `vcsc-as`, `vcsc-ld`, `vcsc-ar`, and `vcsc-sim`; renamed their implementation
  files without trying to use executable hyphens as C identifiers.
* Renamed all 347 language sources from `.n` to `.vcsc`, changed driver and test
  discovery accordingly, and renamed the production runtime from
  `libraries/nlib` to `libraries/runtime` with `libvcsc.a65`,
  `vcsc-runtime.inc`, `vcsc-rt0.s`, and `vcsc-zeropage.s`.
* Renamed generated compiler/runtime symbols from the obsolete N prefixes to
  `__vcsc_*` and `_vcsc_*`, changed the default installation prefix to
  `/opt/vcsc`, and changed the archive signature to the intentionally
  incompatible seven-byte `VCSCAR\x01` magic. No old-command wrappers or archive
  compatibility path remains.
* Added the canonical lowercase `vcsc` FIGlet Standard banner to all 22
  maintained Markdown, text, and Doxygen documentation files. Legal license
  texts and machine-readable `remove.txt` remain unbannered.
* Added a hard-rename regression that rejects stale `.n`, n65, and nlib artifacts,
  verifies all documentation banners and archive magic, and requires both
  retained batari Basic kernel families.
* Added 374 renamed old paths to `remove.txt`; it now contains 861 unique
  cumulative paths. Added two branding-regression files and deliberately did
  not remove or translate any batari kernel source.
* Passed 251/251 compile tests and 108/108 end-to-end/generic tests, both exact
  4096-byte cartridge regressions, and the staged installation check.


2026-07-19 11:55:11 PDT, replace register returns with callee-owned memory
* Replaced the A:X scalar/pointer return ABI with exact-sized hidden
  `function$__return` zero-page objects owned by each value-returning function.
  One- and two-byte binary values, pointers, and one-, two-, or three-byte BCD
  values now use the same memory-backed mechanism.
* Changed value epilogues to plain `RTS`; callers restore the frame pointer
  normally and consume the callee return object directly instead of preserving,
  spilling, or reconstructing A:X. Ignored results do not import or copy the
  hidden object.
* Exported non-static return objects and imported them only when an external
  call's value is consumed, so separately compiled functions follow the same
  ABI without creating unnecessary cross-object references. Static functions
  retain private return objects.
* Changed ABI metadata from `return_ax` to `return_memory` and updated compiler,
  runtime, and ABI documentation. Added same-translation-unit and
  cross-translation-unit `bcd24_t` return execution tests, plus code-generation
  tests for static, external, ignored, one-byte, and two-byte return cases.
* Added scalar-width roadmap task 18 before the retained batari Basic conversion
  inventory: generalize one- through four-byte scalar machinery, then add
  `int24_t`/`uint24_t`, `int32_t`/`uint32_t`, and `bcd32_t`. Marked memory returns
  complete as task 18a and left batari conversion as task 19.
* Updated the main README to identify VCSC as the Atari-focused child of the N
  parent project and link `https://github.com/GorillaSapiens/n`.
* Passed 254/254 compile tests, 110/110 end-to-end/generic tests, both exact
  4096-byte cartridge regressions, the staged installation check, and an
  installed-toolchain 4096-byte cartridge using a returned `bcd24_t`. Added six
  superseded test paths to `remove.txt`, which now contains 867 unique cumulative
  paths. The retained batari Basic trees were untouched.


2026-07-19 12:20:05 PDT, remove inherited runtime frame pointer
* Replaced runtime scratch-base redirection with a compile-time active-lease
  stack. Scratch helpers now address the selected linker-resolved
  `__vcsc_scratch_N` symbol directly and prepare `ptr0`/`ptr1` only for dynamic
  or offsets beyond the direct indexed range.
* Removed `_vcsc_fp` from the zero-page runtime workspace, startup initialization,
  assembler include aliases, compiler reserved names, peephole scratch tracking,
  and ordinary call lowering. Deleted `libraries/runtime/asm/fp2ptr.asm` and the
  generated helper family.
* Removed frame-pointer save/restore around argument evaluation and JSR. Ordinary
  generated calls now use only the hardware stack's two-byte return address, so
  call-graph RAM reservation changed from four bytes to two bytes per linked
  source call depth. Images with runtime initializer functions add one fixed
  two-byte allowance for startup's preserved init-table cursor.
* Reclaimed two bytes of RIOT RAM from the stock runtime workspace, now 16 bytes.
  Updated VCS map expectations: the two-deep 4K example reserves $FC-$FF and the
  three-deep Ode example reserves $FA-$FF.
* Renamed compiler helper APIs and stale comments from fp-relative terminology to
  direct scratch terminology. The ordinary source identifier `fp` is legal again.
* Added `fp_removed.test`, which compiles an `fp` variable, checks direct static
  scratch addressing and push-free calls, scans runtime/linker sources, and
  verifies the rebuilt archive contains no fp helper.
* Passed 254/254 compile tests and 112/112 end-to-end/generic tests. The staged
  installation check and both exact 4096-byte cartridge regressions passed.
  Added two removed paths to `remove.txt`, now 869 unique cumulative paths. The
  retained batari Basic trees were unchanged.

End-of-session checklist
------------------------
Before returning another modified archive:

[x] Update this file and the TODO ordering.
[x] Record every changed, added, and intentionally removed source/test file.
[x] Keep remove.txt synchronized with intentional removals.
[x] Record exact build/test totals and any known failures.
[x] Run make clean and remove test-runner scratch before packaging.
[x] Repack with the same archive-root layout.

2026-07-19 13:24:33 PDT, complete scalar-width generalization task 18c
-----------------------------------------------------------------------
* Allow ordinary signed and unsigned little-endian integer type declarations
  from one through four bytes while leaving the stock public names unchanged.
* Generalize function-return validation and hidden return objects through four
  bytes.
* Raise literal-width analysis to 32 bits so typed 24/32-bit contexts can encode
  full-width constants.
* Keep add/subtract inline and width-looped; verify comparison, bitwise, shifts,
  compound assignment, increment/decrement, multiplication, division, remainder,
  extension/truncation, parameters, returns, and constant folding end to end.
* Expand the archive-selected division helper's private dividend copy from two
  bytes to four.
* Retain rejection of four-byte packed BCD until task 18f.
* Add focused 24/32-bit declaration tests, mixed-signedness rejection, and the
  `e2e_wide_scalar_core.vcsc` execution regression.

2026-07-19 14:16:00 PDT, add first-class 24-bit binary integer types
------------------------------------------------------------------------
* Add canonical `int24_t` and `uint24_t` declarations to the stock VCS interface
  and the isolated compiler-test machine fixture.
* Preserve VCSC's established width-sized literal semantics: three-byte high-bit
  patterns remain legal, while magnitudes requiring a fourth byte are rejected.
* Fix negative runtime literal initialization so values such as `int24_t x := -2`
  are encoded through the signed initializer path instead of the unsigned text
  parser.
* Add focused literal-layout, width-overflow, mixed-signedness, same-TU execution,
  and cross-translation-unit three-byte memory-return regressions. Extend the VCS
  installed-header smoke program to exercise both new stock names.
* Document three-byte ranges, little-endian layout, promotion/cast behavior,
  parameters, and returns throughout the public and compiler documentation.
* Pass 260/260 compile tests and 115/115 end-to-end/generic tests. The staged
  install check builds exact 4096-byte cartridges with the installed VCS headers.
  `remove.txt` remains at 871 unique cumulative paths, and the retained batari
  Basic trees are unchanged.

2026-07-19 15:03:14 PDT, clean peephole unit-test temporaries
------------------------------------------------------------------------
* Reproduced that `test/peephole_unit.pl` left
  `/tmp/VCSC_peephole_unit_<pid>` behind after every successful run because the
  harness manually created the directory and then replaced Perl with the test
  binary via `exec`.
* Use `File::Temp::tempdir(..., TMPDIR => 1, CLEANUP => 1)` and run the compiled
  child with status-preserving `system()` instead. Cleanup now runs on success,
  compile failure, test failure, and catchable child signals.
* Add `peephole_temp_cleanup.test`, which runs the harness under an isolated
  TMPDIR and rejects any surviving `VCSC_peephole_unit_*` directory.
* Pass 261/261 compile tests and all 115/115 end-to-end/generic tests. The e2e
  suite was completed with its final four cases after the execution wrapper
  timed out at 111 passes. The staged installation check and exact 4096-byte
  installed-toolchain cartridge builds also passed. `remove.txt` remains at 871
  unique paths, and the retained batari Basic trees are unchanged.


2026-07-19 16:10:12 PDT, add first-class 32-bit binary integer types
------------------------------------------------------------------------
* Add canonical `int32_t` and `uint32_t` declarations to
  `libraries/vcs/vcs.vcsc` and `test/machine_6502.vcsc` while keeping pointers
  16-bit.
* Extend `test/vcs_headers_smoke_test.vcsc` so the staged installed toolchain
  compiles ordinary 24- and 32-bit values together.
* Add `int32_stock_literals_codegen_test.vcsc`,
  `int32_positive_literal_width_error_test.vcsc`,
  `int32_negative_literal_width_error_test.vcsc`,
  `uint32_literal_width_error_test.vcsc`, and
  `int32_mixed_signed_error_test.vcsc`.
* Add `e2e_int32_stock_types_verify.vcsc` and the
  `e2e_int32_memory_return_cross_tu_verify.vcsc`/`__def.vcsc` pair. Coverage
  includes exact little-endian literal layout, signed/unsigned limits,
  fifth-byte rejection, widening, arithmetic, comparisons, signed shifts,
  multiply/divide/remainder, and same- and cross-object four-byte returns.
* Update `README.md`, `compiler/README.md`, `compiler/ABI.txt`,
  `libraries/vcs/README.md`, `NOTES.md`, and this durable context so the stock
  ordinary family now spans 8, 16, 24, and 32 bits.
* Pass 266/266 compile tests and 117/117 end-to-end/generic tests. The staged
  install check builds exact 4096-byte cartridges with the installed VCS
  headers. `remove.txt` remains at 871 unique paths, and the retained batari
  Basic trees are unchanged.

2026-07-19 16:28:50 PDT, add first-class eight-digit packed BCD
------------------------------------------------------------------------
* Add canonical `bcd32_t` declarations to `libraries/vcs/vcs.vcsc` and the
  generic compiler-test machine fixture. `$bcd` now accepts unsigned
  little-endian integer widths from one through four bytes.
* Extend function-return validation and documentation so four-byte packed BCD
  uses the existing exact-sized callee-owned memory-return ABI.
* Verify numeric literal packing across decimal, hexadecimal, octal, and binary
  spellings. `12345678` stores as `$78,$56,$34,$12`; the legal range is
  0..99999999, and 100000000 is rejected.
* Add focused compile and execution coverage for static and automatic
  initialization, assignments, arrays, structs, parameters, same- and
  cross-translation-unit returns, carry/borrow across all four bytes, decimal
  wraparound, cross-width BCD operations, comparisons, truth tests, and CLD
  restoration before following binary arithmetic.
* Extend the installed-header smoke cartridge and the raw return-memory encoding
  test to exercise `bcd32_t`.
* Pass 268/268 compile tests and 121/121 end-to-end/generic tests. The staged
  install check builds both exact 4096-byte cartridges. `remove.txt` remains at
  871 unique paths, and the retained batari Basic trees are unchanged.
