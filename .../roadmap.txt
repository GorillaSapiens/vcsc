 __   __ ___  ___   ___
 \ \ / // __|/ __| / __|
  \ V /| (__ \__ \| (__
   \_/  \___||___/ \___|

VCSC MAIN ROADMAP
=================

Purpose
-------
This file contains only unfinished main-project work and its acceptance criteria.
Completed roadmap material is historical and belongs in
`.../context-history/`. The pre-cleanup full roadmap was archived in the
2026-08-22 history file.

Immediate TODO
--------------
The first unfinished item is the default main-roadmap action when the user has
not selected another workstream.

Current next action: Item 57, add playfield-color support as an explicit renderer capability.

[ ] 57. Add playfield-color support as an explicit renderer capability.

        * Define whether colors are global, per logical row, or both.
        * Schedule every COLUPF write against the beam.
        * Preserve existing renderer output when the feature is unused.
        * Expose a clear application data/API contract.
        * Add static, motion, transition, installation, and pixel/color-oracle
          coverage for every supported profile.

[ ] 58. Add a public side-scroller/platform example.

        * Demonstrate animated player sprites, horizontal world scrolling,
          platform/playfield collision, jumping/gravity, and camera/world-state
          separation.
        * Include at least one collectible, hazard, or goal and use original assets.
        * Stay inside a documented RAM/ROM/frame budget.
        * Add deterministic emulator-backed tests for scrolling, collision,
          animation, and completion/reset behavior.

[ ] 59. Let the compiler use a command-line Stella palette file for RGB builtins.

        * Add a compiler/driver option that accepts Stella's combined 792-byte
          `stella.pal` format (128 NTSC RGB triples, 128 PAL RGB triples, and
          8 SECAM RGB triples).
        * Make the NTSC/PAL/SECAM RGB builtins use the selected palette slices
          instead of the compiled-in tables when the option is present.
        * Preserve the current compiled-in palettes exactly when no palette file
          is specified.
        * Diagnose malformed, short, long, or unreadable palette files clearly.
        * Add code-generation tests proving each standard uses its own slice and
          that the checked-in Stella test palette produces the same RGB builtin
          results as the current canonical tables.

have TJ fix stella 3EX
