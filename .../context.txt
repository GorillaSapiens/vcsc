 __   __ ___  ___   ___
 \ \ / // __|/ __| / __|
  \ V /| (__ \__ \| (__
   \_/  \___||___/ \___|

VCSC PROJECT CONTEXT
====================

Purpose
-------
This is the small new-chat bootstrap for VCSC. Keep only durable rules, current
state that materially affects ongoing work, unfinished work, and the exact next
step. Completed narratives belong in `.../context-history/YYYY-MM-DD.txt`.

Read this file once near the start of a new chat. Do not reread it every turn.
Read a focused `.../*.txt` file only when the current task actually needs that
workstream. Never read `.../context-history/` wholesale; search/open only the
specific date needed for an older decision or regression.

Context budget
--------------
`.../context.txt` has a hard 16 KiB ceiling. Focused hot documents also have
small limits enforced by `test/source_tree_hygiene.pl`.

* Replace superseded current-state text; do not append another checkpoint.
* Put completed implementation detail, dated experiments, old measurements,
  failure transcripts, and superseded plans in the daily history file.
* Keep acceptance criteria only for unfinished work.
* Prefer executable regressions and maintained source documentation as durable
  truth instead of duplicating their details here.
* When a focused roadmap has no unfinished work, move it out of hot state into
  `.../context-history/`; do not keep a closeout/tombstone beside active records.

History-entry policy
--------------------
Every meaningful completed change or decision goes in the local-date file
`.../context-history/YYYY-MM-DD.txt`. Entries are oldest first. Each entry starts
with exactly:

    YYYY-MM-DD HH:MM:SS PDT, short description

The description is mandatory, ASCII, at most 50 characters, and the complete
header is at most 72 characters. Continuation/detail lines are unrestricted.
Generate timestamps with:

    TZ=America/Vancouver date "+%Y-%m-%d %H:%M:%S %Z"

Source-lineage and workflow invariants
--------------------------------------
* The newest archive supplied by the user in the current chat is the sole source
  authority. Never silently merge it with a remembered tree from another chat.
* Do not develop the same VCSC lineage concurrently in multiple chats. If that
  happened, reconcile the divergent descendants explicitly before more work.
* Source archives have no wrapper directory; `COPYING`, `Makefile`, `compiler/`,
  `test/`, etc. are archive-root entries.
* Use top-level `make tar` for source handoff archives. Do not hand-roll the
  returned source tarball.
* Preserve `.../instruction.txt`; it is intentional project guidance.
* Use the term **renderer**, never **kernel**, for VCSC display implementations.
* Prefer targeted builds/tests while developing; run the broad suite at a real
  checkpoint or before handoff. Do not weaken a valid regression to make it pass.
* Before handoff: update only durable/open hot state, append completed work to
  daily history, clean generated residue, test, then package one latest tarball.

Project/toolchain invariants
----------------------------
VCSC is a deliberately constrained Atari VCS/2600 C-like compiler and companion
6502 toolchain. Current public tools are `vcsc`, `vcsc-cc1`, `vcsc-as`,
`vcsc-ld`, `vcsc-ar`, `vcsc-sim`, and `vcsc-disas`. Source is `.c26`, maintained
assembly is `.s26`, objects are `.o26`, and archives are `.l26`.

`compiler/ABI.txt`, `BANKSWITCHING.md`, and the maintained public/internal
component documentation are the authorities for language, ABI, renderer timing,
and cartridge contracts.
Do not re-copy those specifications into this handoff. Important general rules:
no software argument/local stack, no indirect calls/function pointers, static
non-reentrant activation storage, little-endian data, and explicit timing
annotations for beam-sensitive branches.

Open workstreams
----------------
1. **Enhanced asymmetric multisprite -- side quest**
   Read `.../enhanced_asymmetric.txt` when working on it. It is the sole enhanced
   multisprite implementation; the temporary symmetric prototype is retired.
   `lines` supports 192 (96 two-scanline bands, Y=0..95) and native PAL/SECAM
   228 (114 bands, Y=0..113). The 192 NTSC extended blanking split remains 63/13
   TIM64T. Preserve the measured same-X |delta-Y|<13 reuse prohibition and the
   ordinary P0=15/P1=12 setup reservations. See the focused hot file for the
   maintained scheduler invariants and next allocator-quality work.

2. **Main roadmap**
   `.../roadmap.txt` contains only unfinished main items. Default main-roadmap
   work begins with Item 57, playfield-color renderer support, followed by the
   side-scroller/platform example.
   Completed placement/compiler cleanup is owned by executable regressions and
   dated history rather than this hot file.
   Active workstream: `.../roadmap.txt`.

3. **Bankswitching**
   `BANKSWITCHING.md` is the public cross-bank-call ABI;
   `.../bankswitching.txt` owns mapper facts and unfinished work. C26 topology is
   authoritative; VCS mapper/linker cfg profiles are gone. The descriptor call
   ABI is complete through FC, F0, and constrained-state E0; each migrated mapper
   owns `bankcall.s26` plus variable-length `entry.s26` reset normalization.
   Entry may be empty when hardware already supplies the canonical startup state.
   Ordinary direct `JMP` is a hard same-bank constraint: VCSC no longer synthesizes
   mapper-changing application JMP trampolines. The sole mapper-changing one-way
   JMP mechanism is reset/startup: replicated vector bridges run mapper `entry.s26`
   as needed, then perform the ordinary absolute jump to the reset handler.
   Cross-bank application control flow is by bankcall/return; FE remains special.
   Linker ROM storage uses independent physical planes and bank-aware relocation;
   the 256-bank 3F/3E/3EX/FC regressions own the large-mapper boundary contracts.

   The F4SC field diagnostic remains bank-local by controller; only joystick owns
   TIA/switch/collision work. Four-paddle sampling snapshots INPT0-INPT3 together
   and preserves stable VBLANK/visible/overscan phase across multi-frame reads.


4. **Disassembler**
   `.../disassembler.txt` preserves the exact-round-trip contract and unfinished
   work only. Mapper-hypothesis migration is current: A1 recursive duplicate-view
   provenance/fill accounting and A2 preselection mapper enumeration are complete;
   A3 exhaustive hypothesis-local execution is next. 2IN1/4IN1/8IN1/etc. now live
   in the same preselection hypothesis set as ordinary mappers; `--container` and
   `.gameNN.s26` sidecars remain compatibility override/presentation plumbing. GL real-corpus
   closeout, CM, DPC+, and CDF-family support remain behind this migration.

5. **Zero static initializer placement**
   Mutable all-zero link-time static initializers are emitted directly as
   BSS/ZEROPAGE rather than DATA. This avoids pointless ROM copies and lets more
   programs select the compact startup. Const/readonly objects and pointer
   initializers that require backing objects retain their required storage
   semantics. Regression tests own the detailed placement/footprint expectations.

Startup selection
-----------------
Stock VCS startup is deliberately three-way. `vcsc-rt1-simple.s26` is the
smallest path for ordinary RIOT BSS with no DATA/runtime initializer work; it
retains the historical blanket RIOT+TIA clear. `vcsc-rt1-data.s26` is selected
for initialized DATA and/or runtime initializers when every startup-zeroed object
can be satisfied by a blanket RIOT clear and no `noinit` object exists; it uses
copy/init tables but has no generic ZERO-table walker and remains TIA-neutral,
preserving the previous full-startup behavior of initialized-data carts.
`vcsc-rt0.s26` is the full table-driven path used whenever `noinit` exists or
startup-zeroed BSS lives in split/non-RIOT writable memory. DATA+noinit therefore
uses full startup. The diagnostic's strong reset probe stores its
`noinit cartram` 2600/7800 latch and tail-jumps to `__vcsc_startup_full`; normal
startup owns ordinary BSS initialization.

Installed/release layout
------------------------
Installed and binary-release trees deliberately preserve the source library
hierarchy under the package prefix: `libraries/vcs` and `libraries/runtime`.
Assembler configuration files live in top-level `cfg`, tools in `bin`, and
examples/addons retain their corresponding top-level names. Do not recreate the
historical `share/`, `include/`, or `lib/` buckets. `packaging/install.manifest`
owns the installed libraries and release payload. Installed example Makefiles
retain `$(ROOT)/libraries/vcs`; only their driver path is rewritten to
`$(ROOT)/bin/vcsc[.exe]`. Driver/runtime/profile lookup uses the same
`libraries/...` relative paths in source and installed trees.

Human-readable linked listings
------------------------------
Final `.lst` sidecars now preserve C26/S26 source provenance through compile,
assemble, object, and link. Each source statement is followed by readable
generated 6502 assembly with final linked logical address and emitted bytes;
resolved 16-bit operands are shown so split-address mappings such as Superchip
read/write aliases are directly visible. Synthetic material is labeled rather
than assigned false provenance. Source metadata is transparent to peephole
optimization, and focused regressions lock source/assembly/address/byte
correlation plus an F8SC read/write-alias example. See `linker/README.md`.

Last validation state
---------------------
The previously completed bankswitching/cfgectomy/linker work is regression-locked.
Current three-startup/noinit validation has 537/537 compile-only tests, all 26
`linker_*.pl` e2e tests, the focused startup/diagnostic/Superchip/package coverage,
and 352/364 broad e2e cases passing on the final tree. The only uncompleted broad
cases are the three max-size 3E/3EX/3F diagnostics, the diagnostic paddle-sampling
oracle, and `vcs_examples_build_1of8.test` through `_8of8.test`; each exceeded the
available execution window rather than reporting a failure. All ordinary VCS-tail
regressions found during the broad run were diagnosed and fixed. Detailed completed
implementation facts live in public docs, executable regressions, and dated history
rather than here.

Current Tanks cleanup is complete at the legitimate beam-asm boundary while preserving the plain 4K target. The current real build is 4024/4090 ROM and 120/128 RAM (66 ROM bytes, 8 RAM bytes free). `test/vcs_tanks.pl` locks coordinate, heading/reflection, missile, arena-bound, and bottom-wall GRP behavior.

Immediate TODO
--------------
The incremental asm-to-C26 cleanup converted all ordinary game logic and position-control arithmetic. `tanks_move_object()` is C26 at exactly the old asm size ($56/86 bytes); `tanks_random()` is C26 at exactly 18 bytes; `tanks_process_knockback()` is C26 after general indexed-X/range/update codegen work; `tanks_prepare_graphics()` is C26 and is 11 ROM bytes smaller than the former asm; `tanks_compute_player_position()` is readable C26 at $2c/44 bytes, exactly matching the former register-crafted asm. Explicit Tanks asm is 167 reviewed statements, confined to the actual beam/cycle-sensitive `tanks_position_missiles()`, `tanks_position_players_after_score()`, and `tanks_draw()` paths. Do not force those remaining hardware-timing routines into C26 merely to reach zero asm.

General compiler/codegen work from this cleanup is regression-locked in `test/`: runtime-indexed byte stores/copies/compound updates, direct array,X use for X-backed counted-loop induction variables, safe nested `if`/`break` counted-loop analysis, idiomatic postfix `i++` counted-loop recognition, byte immediate/logical comparisons in X-backed loops, compact byte range tests, live A/N/Z reuse across adjacent assignment/condition and arithmetic statements, direct byte constant-minus and scalar compound ALU forms, update-then-range-adjust folding, and `while (u8 >= constant) { u8 -= constant; ... }` reuse of live A/carry. The valued prefix/postfix ++/-- side-effect case is explicitly excluded from unsafe N/Z reuse after a broad e2e regression exposed it. Cross-control-flow direct-byte lifetime lowering now also keeps a normalization state in A and an immediate-initialized companion byte in Y across a repeated-subtraction loop, optional threshold adjustment, and following immediate arithmetic, then spills each once. This is structural and source-name-independent; `test/u8_register_lifetime_codegen_test.c26` locks the generic form. Full compiler compile-only validation is 525/525 passing at this checkpoint.

The user subsequently completed the full 356-test e2e suite and found three regressions: one semantic-use metadata provenance bug plus two stale exact-ROM guards. All three are now fixed in focused tests. The direct-byte statement-chain optimizer now emits read/write metadata from each statement's own lvalue/source location rather than reusing the first statement's location. The all-five public example is legitimately 3570 bytes (520 free), and the animated gallery is legitimately 3238 bytes (852 free); the latter four-byte reduction is the expected scalar-chain removal of one `STA`/`LDA` pair in `main`. Compile-only validation is now 525/525 after the register-lifetime addition. A local post-fix e2e rerun passed 1-225/356, and resumed tail validation passed 30 more non-maximal VCS e2e cases before the slow VCS-heavy remainder exceeded the execution window; an uninterrupted 356/356 rerun remains desirable.

Tanks cleanup is closed at the legitimate beam-asm boundary. `forbidcountasm:` is enforced by the compile-test harness as a literal `PATTERN|MAX` occurrence ceiling and has runner self-coverage; `page_pointer_offset_reuse_codegen_test` now locks the accepted three `lda sprite` occurrences. The compact `.flex` linker policy remains regression-locked. Two exact-address guards exposed by that policy were stale rather than semantic: OMNI now checks that explicitly pinned `main` stays in bank0 without requiring offset $000, and the pre-template six-digit score accepts its now-compact $F000 reset placement while retaining emulator timing/entry verification. Compile-only validation is 525/525. Before the BCD-matrix addition, a broad e2e rerun was clean through 227/358 before the environment window hit the maximal VCS-heavy tail; the new matrix adds one e2e case, so the current inventory is 359. focused OMNI, six-digit-score, compactness, and forbidcountasm tests all pass. Main-roadmap Item 56 diagnostic non-beam asm cleanup is complete; compile-only is 528/528 and full e2e is 364/364. Item 57 playfield-color renderer support is next.

The Item-55 direct-u8 immediate-comparison optimization must not consume single
subscript lvalues. Pointer locals may live in ordinary BSS, and 6502 `(zp),Y`
requires zero-page pointer storage; pointer-subscript comparisons therefore stay
on the established lowering that copies through `ptr0`.

Explicit packed-BCD mutation coverage now lives in `test/vcs_bcd_ram_cartram_operators.pl`: all `bcd8_t`, `bcd16_t`, `bcd24_t`, and `bcd32_t` widths are runtime-checked in both ordinary Atari RAM and split-address Superchip `cartram` for `+=`, `-=`, prefix/postfix `++`, and prefix/postfix `--` (48 cases). The F4SC fixture pins one storage/width case per bank; the wrapper also verifies the actual linker memory regions. BCD-focused e2e validation is 13/13 and compile-only remains 525/525. Preserve Tanks readability: one statement per line and one graphics byte per line. Do not resurrect the RIOT-RAM sprite cache or `$xxDD`/main-phase theory.
