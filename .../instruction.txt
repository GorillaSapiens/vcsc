We’re working on this VCSC tarball. This is a toolchain for a retrocomputing project: a compiler, assembler/linker support, and disassembler for the 1970s-era Atari 2600 game console.

Unpack it and first read `.../README.md`, then read all of `.../context.txt` once. Do not reread `context.txt` wholesale on later turns unless it materially changed. Read another `.../*.txt` file only when the current task actually needs that workstream. Never read `.../context-history/` wholesale; search/open only the specific date needed for an older decision, regression, measurement, or discarded approach. The `.deb` files are there in case you need them to build or test in your container.

Assume the available tool-execution window may be short. Optimize for useful progress within that window:

* Resume the active task immediately after the required bootstrap. Do not broadly explore unrelated parts of the tree.
* Make substantive progress before running the entire test suite. Prefer the smallest relevant build and focused tests while developing; run the full suite at a meaningful checkpoint, when broad regressions are plausible, or before final handoff.
* If a test fails, diagnose and continue fixing it rather than stopping merely to report the failure.
* Avoid repeatedly rebuilding or retesting unaffected components unless needed to establish correctness.
* Keep hot developer records small. `.../context.txt` contains only durable cross-project/current state and the immediate next step. Focused `.../*.txt` files contain durable workstream invariants and unfinished items only.
* Put every completed change, decision, experiment, obsolete checkpoint, old measurement, and superseded plan in that day's `.../context-history/YYYY-MM-DD.txt`. Replace stale hot-state text instead of appending history to it.
* Update the relevant hot roadmap/design file only when unfinished status, a durable invariant, or the next action changes. Do not preserve completed checklist prose there merely for archaeology; history and tests own that.
* Prefer executable regressions as durable memory of implementation facts. Do not make hygiene tests depend on completed prose staying in hot context files.
* Every turn should end with the creation of a handoff tarball in case chat length limits are reached.
* When handing work back, provide only one tarball containing the latest complete state unless I explicitly request otherwise.
* Tarball download link should come at the beginning of your response to avoid bugs with links at the end of the response.
* If the execution window ends before the whole task is complete, preserve the unfinished state and exact next step in the relevant compact hot file; record completed work separately in daily history.
* Do not waste execution time narrating routine operations. Briefly report important findings, decisions, regressions, or blockers while continuing the task.

Preserve existing project conventions unless the task explicitly requires changing them. Prefer fixing root causes over adding narrow exceptions or test-specific hacks. Do not weaken tests merely to make them pass unless the test itself is demonstrably wrong.

