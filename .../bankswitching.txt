VCSC BANKSWITCHING AND SUPERCHIP PLAN
=====================================

Status
------
This is the detailed design and implementation roadmap for the broad project
roadmap items covering 8K-and-larger cartridge bankswitching and Superchip RAM.
It is an internal planning document, not installed user documentation. The
main roadmap in `.../context.txt` remains authoritative for project
ordering; this file owns the detailed order within those milestones.

The first implementation family is the ordinary full-window Atari schemes:

    F8      2 x 4K banks   8K total
    F6      4 x 4K banks  16K total
    F4      8 x 4K banks  32K total

Their Superchip variants follow after ordinary banking is stable:

    F8SC    2 x 4K banks plus shared 128-byte cartridge RAM
    F6SC    4 x 4K banks plus shared 128-byte cartridge RAM
    F4SC    8 x 4K banks plus shared 128-byte cartridge RAM

Segmented and unusual schemes such as 3F, 3E, E0, E7, FE, UA, and 0840 are not
part of the first implementation. The common model should avoid blocking them,
but it must not pretend that they are merely differently sized F8 cartridges.
DPC, DPC+, CDF, and coprocessor-assisted formats are separate projects.

Core model: use the missing address lines as the bank namespace
---------------------------------------------------------------
The 6507 has a 16-bit internal program counter but exposes only thirteen address
lines to the cartridge. A13-A15 do not reach the cartridge. VCSC can therefore
assign every physical 4K bank a distinct ordinary 16-bit logical range while
the cartridge sees every one of those ranges as the same $1000-$1FFF window.

Three bank identities must be kept separate.  Using the bare phrase "bank 0"
without saying which identity is meant is prohibited in this document and
should be avoided in diagnostics and user documentation:

1. VCSC logical bank name

   This is the source/linker namespace.  BANK0 is deliberately the home bank at
   $F000, BANK1 is at $D000, and so on.  `main`, startup, and the mandatory
   runtime live in VCSC BANK0.

2. Physical/file bank index

   This is the zero-based 4K chunk number in the emitted cartridge image.
   Physical/file bank 0 is the first 4096 bytes on disk.  Because VCSC writes
   banks in ascending logical-address order, physical/file bank 0 is the
   highest-numbered VCSC logical bank, not VCSC BANK0.  VCSC BANK0 is the last
   physical/file bank.

3. Mapper selector hotspot

   F8/F6/F4 selectors increase with physical/file bank index.  The first
   selector chooses the first 4K chunk on disk, the next selector chooses the
   second chunk, and so forth.  Since VCSC logical bank numbers run in the
   opposite direction, selector addresses decrease as VCSC BANKn increases.

Use the established descending VCSC logical convention:

    VCSC logical bank    linker range
    -----------------    ------------
    BANK0                $F000-$FFFF
    BANK1                $D000-$DFFF
    BANK2                $B000-$BFFF
    BANK3                $9000-$9FFF
    BANK4                $7000-$7FFF
    BANK5                $5000-$5FFF
    BANK6                $3000-$3FFF
    BANK7                $1000-$1FFF

Equivalently:

    logical_base(BANKn) = $F000 - (n * $2000)

F8 uses VCSC BANK0 and BANK1. F6 uses BANK0 through BANK3. F4 uses BANK0
through BANK7. Ordinary linker symbols and relocations remain 16-bit. No far
pointer or bank-qualified C pointer is required for this family.

The cartridge file is written in ascending logical-address order, beginning
with the lowest-address bank and ending with the highest-address bank. Therefore
VCSC BANK0 is always the final 4K chunk in the file:

    mapper  file index  VCSC bank  linker range  selector hotspot
    ------  ----------  ---------  ------------  ----------------
    F8      0           BANK1      $D000-$DFFF   $1FF8
            1           BANK0      $F000-$FFFF   $1FF9

    F6      0           BANK3      $9000-$9FFF   $1FF6
            1           BANK2      $B000-$BFFF   $1FF7
            2           BANK1      $D000-$DFFF   $1FF8
            3           BANK0      $F000-$FFFF   $1FF9

    F4      0           BANK7      $1000-$1FFF   $1FF4
            1           BANK6      $3000-$3FFF   $1FF5
            2           BANK5      $5000-$5FFF   $1FF6
            3           BANK4      $7000-$7FFF   $1FF7
            4           BANK3      $9000-$9FFF   $1FF8
            5           BANK2      $B000-$BFFF   $1FF9
            6           BANK1      $D000-$DFFF   $1FFA
            7           BANK0      $F000-$FFFF   $1FFB

The equivalent formulas for these full-window schemes are:

    file_index(BANKn) = bank_count - 1 - n
    hotspot(BANKn)    = first_mapper_hotspot + file_index(BANKn)

where the first mapper hotspot is $1FF8 for F8, $1FF6 for F6, and $1FF4 for
F4.  Thus the startup/home hotspot is always the last hotspot in the mapper's
range: $1FF9 for F8 and F6, and $1FFB for F4.

The linker emits those banks contiguously without the unused logical gaps
between $1FFF and $3000, $3FFF and $5000, and so on. File position, VCSC logical
address, and selector hotspot are three separate facts. The currently implemented cfg records all three relationships explicitly and
the linker validates the complete table rather than guessing from a name or
relying on declaration order. Roadmap items 24 through 27 replace that duplicated
cfg description with ordinary C26 topology metadata while preserving these same
three independent identities.

This convention also matches the common cartridge layout in which the final
chunk contains the $F000 home/reset code.  Existing ROMs are not normative for
VCSC naming: some assemble both physical banks at $F000, some use other mirrors,
and external documentation may call the first chunk "bank 0".  In VCSC,
uppercase BANK0 always means the $F000 home bank, never merely the first bytes
on disk.

For the first public F8/F6/F4 family, VCSC BANK0 is the required startup/home
bank and `main` always resides there. A later mapper family may revisit the
startup-bank rule as a separate decision.

Current cfg implementation model
--------------------------------
The implemented system has separate public linker configurations for the common schemes:

    vcs_8k_f8.cfg
    vcs_16k_f6.cfg
    vcs_32k_f4.cfg
    vcs_8k_f8sc.cfg
    vcs_16k_f6sc.cfg
    vcs_32k_f4sc.cfg

The first implemented cfg spelling uses `CARTRIDGE` and `BANKS` blocks plus a
`bank=NAME` property on cartridge-output `MEMORY` regions:

    CARTRIDGE {
        mapper = F8;
        fillval = $FF;
        trampoline = $0F00;
        trampolinesize = $00E0;
        vectorbridge = $0FE0;
    }

    BANKS {
        BANK0: start=$F000, size=$1000, hotspot=$1FF9, startup=yes;
        BANK1: start=$D000, size=$1000, hotspot=$1FF8, startup=no;
    }

    MEMORY {
        bank1:              start=$D000, size=$0F00, type=ro, bank=BANK1;
        BANK1_TRAMPOLINE:    start=$DF00, size=$00E0,          bank=BANK1;
        BANK1_VECTOR_BRIDGE: start=$DFE0, size=$0012,          bank=BANK1;
        BANK1_TAIL:          start=$DFF2, size=$0008,          bank=BANK1;
        BANK1_VECTORS:       start=$DFFA, size=$0006,          bank=BANK1;
        ROM:                 start=$F000, size=$0F00, type=ro, bank=BANK0;
        BANK0_TRAMPOLINE:    start=$FF00, size=$00E0,          bank=BANK0;
        BANK0_VECTOR_BRIDGE: start=$FFE0, size=$0012,          bank=BANK0;
        BANK0_TAIL:          start=$FFF2, size=$0008,          bank=BANK0;
        BANK0_VECTORS:       start=$FFFA, size=$0006,          bank=BANK0;
        /* shared RAM regions omitted */
    }

A bank description tells the linker the VCSC logical bank name and 16-bit range,
its mapper selector hotspot, whether it is the startup bank, and the full 4K
physical-bank size.  The output writer derives physical/file index from
ascending logical address, not from the numeric suffix in `BANKn`.  `MEMORY`
entries subdivide each full physical unit into allocatable ROM and
non-allocatable holes such as hotspots, vectors, and the common trampoline
corridor. `CARTRIDGE trampoline` gives the corridor's low-twelve-bit start and
`trampolinesize` gives its reserved capacity. Both are required for banked
profiles, must fit inside one 4K bank, and may not overlap the vector bridge,
vectors, or any selector hotspot.

The cfg parser rejects unknown blocks and properties. Silently ignoring a
misspelled bank or hotspot property would create a plausible-looking but dead
cartridge. MEMORY, SEGMENTS, and BANKS storage is dynamically sized rather than
limited to sixteen entries.

Every selector hotspot is reserved at the same low twelve-bit offset in every
physical bank, not merely in the bank selected by that hotspot. The linker
rejects a configuration when any ordinary allocatable code/data segment region
covers any declared selector byte. Vector-only ownership may use fixed bytes
which overlap mapper hotspots where the mapper requires it, but ordinary code
must never be placed there.

The implementation should model a physical bank as one 4K output unit even when
its allocatable ROM is split into ordinary code, a bridge area, reserved hotspot
bytes, and vectors. A bank may contain non-allocatable holes, but its emitted
file chunk remains exactly 4096 bytes.

Planned C26 topology model and cfg retirement
------------------------------------------------
The cfg files are a historical implementation mechanism, not the desired final
source of cartridge truth. They currently repeat facts already present in C26
`mem` declarations and hide physical image topology in a second syntax. The
planned model moves the complete public cartridge description into ordinary C26
metadata and leaves cfg support only as a temporary compatibility path and,
possibly, a future escape hatch for unusual non-VCS targets.

The design separates two declarations with deliberately different jobs:

* `bank` describes one independently emitted cartridge-image unit, its location
  in the output file, the part of that unit mapped into logical CPU space, and,
  when applicable, the selector which makes that mapping active. A bank may be
  selector-controlled or directly mapped.
* `mem` describes only bytes which are available to the allocator. This adopts
  the strict "allocatable bytes only" interpretation: vector bytes, generated
  bridges, mapper-reserved bytes, RAM-port prefixes, and other holes are not
  included in a `mem` region merely because they lie inside an output bank.

`bank` and `mem` use separate namespaces. The same spelling may be used in both:

    bank bank1 { /* output and mapping topology */ };
    mem  bank1 { /* source-placement region */ };

A source declaration modifier such as `bank1 void logic(void)` always resolves
in the `mem` namespace. A bank is not a source placement qualifier. Neither
namespace gives semantic meaning to the spelling `bank0`, `bank1`, `ram`, or any
other name. Matching names are convenient for people, but the linker must derive
behavior entirely from declared values. For example, `bank fred` may contain
`mem bank1` when their ranges establish that relationship.

Do not add a magic declaration such as `cart f8;`. A file named `f8.c26` or
`f8sc.c26` may be a convenient installed library file, but its filename and
included path have no linker semantics. The file must contain the actual bank,
allocation, output, and generated-corridor declarations. A programmer or build
may include that file or compile it as a configuration-only C26 input; identical
declarations from several translation units coalesce, while conflicting
same-name declarations are fatal.

The exact parser spelling is locked by roadmap item 24, but the intended facts
are illustrated by this F8SC description:

    cartridge {
        $fill:                  0xff
        $trampoline_offset:     0x0f00
        $trampoline_size:       0x00e0
        $vector_bridge_offset:  0x0fe0
        $vector_bridge_size:    0x0012
        $vectors_offset:        0x0ffa
        $vectors_size:          0x0006
    };

    bank bank0 {
        $image_size:    0x1000
        $file_index:    1
        $image_offset:  0x0100
        $link_start:    0xf100
        $cpu_start:     0xf100
        $map_size:      0x0f00
        $select_access: 0x1ff9
        $startup
    };

    bank bank1 {
        $image_size:    0x1000
        $file_index:    0
        $image_offset:  0x0100
        $link_start:    0xd100
        $cpu_start:     0xf100
        $map_size:      0x0f00
        $select_access: 0x1ff8
    };

    mem bank0 {
        $start:    0xf100
        $size:     0x0e00
        $ro
        $priority: 2
    };

    mem bank1 {
        $start: 0xd100
        $size:  0x0e00
        $ro
    };

    mem superchip {
        $read_start:  0xf080
        $write_start: 0xf000
        $size:        0x0080
        $rw
    };

The fields have these intended meanings:

* `$image_size` is the complete physical file chunk for that bank. F8 and F8SC
  both emit 4096-byte chunks.
* `$file_index` is the physical chunk number in the output image. It is explicit;
  declaration order, numeric suffixes, and logical-address sorting are not hidden
  substitutes for it.
* `$image_offset` is the byte offset within the physical chunk at which the
  logical mapping begins.
* `$link_start` and `$map_size` define the synthetic linker range which uniquely
  distinguishes the bank's contents during linking.
* `$cpu_start` and `$map_size` define where those bytes appear to the 6507.
* `$select_access`, when present, is an any-bus-access selector hotspot matching
  the current F8/F6/F4 hardware and the linker's state-preserving `STA hotspot`
  bridges. A later mapper may require separately described read, write, masked,
  or value-qualified selectors; that must extend the topology language rather
  than overload names.
* Absence of `$select_access` means the bank is directly mapped. Its logical
  mapping is simultaneously available with every other nonoverlapping direct
  bank, and references to it never acquire selector or trampoline code.
* `$startup` marks the one selector-controlled bank whose reset/runtime/main
  placement contract is used by the current full-window family. A direct-only
  topology does not require a startup-bank marker; ordinary vectors and placement
  priority determine its entry layout.
* The `cartridge` block gives output-wide fill and explicit locations/capacities
  for linker-generated replicated material. Direct-only topologies which need no
  mapper trampolines or reset bridges may omit those reservations and retain only
  the output properties they actually use.

Plain F8 and F8SC therefore have genuinely different bank declarations. Plain
F8 maps the complete physical bank as ROM:

    bank bank0 {
        $image_size:    0x1000
        $file_index:    1
        $image_offset:  0x0000
        $link_start:    0xf000
        $cpu_start:     0xf000
        $map_size:      0x1000
        $select_access: 0x1ff9
        $startup
    };

    mem bank0 { $start:0xf000 $size:0x0f00 $ro $priority:2 };

F8SC leaves the first $100 physical bytes outside the switched-ROM mapping and
begins ROM at `$x100`. Its allocatable region begins at `$x100` and still ends at
`$xEFF`, because the final `$100` bytes are reserved for generated bank-call and
vector machinery. Thus the model records all three distinct extents:

    physical F8SC chunk:        image offsets $000-$FFF
    switched F8SC ROM mapping:  image offsets $100-$FFF
    ordinary allocatable ROM:   image offsets $100-$EFF

This distinction is how the linker knows that Superchip accesses do not require
hotspot code. Output ownership is derived from allocator-region containment in a
bank's synthetic mapped link range, not from a numeric CPU address and not from
a region name. For each `mem` region with an allocatable link range, the linker
finds containing bank ranges using:

    bank.link_start <= mem.start

and

    mem.start + mem.size <= bank.link_start + bank.map_size

Exactly one containing bank gives the region one output owner. No containing
bank makes it shared/unpackaged by that bank table. More than one containing bank
is an ambiguous target configuration and is rejected. The inference uses the
synthetic link range, never the CPU-visible mirror, because selector-controlled
banks may share the same CPU window.

For the F8SC example:

    mem bank0:       $F100-$FEFF  is inside bank0 $F100-$FFFF
    mem bank1:       $D100-$DEFF  is inside bank1 $D100-$DFFF
    mem superchip:   read $F080-$F0FF, write $F000-$F07F

The Superchip aliases lie outside every switched-ROM link range and therefore
have no owning bank. They are one shared split-address allocation. Relocations
to that region remain ordinary direct reads/writes from every selected bank;
they never generate a selector access.

Output ownership and switch behavior are separate facts:

* A region owned by a directly mapped bank is always directly addressable. Calls,
  jumps, and data references between distinct direct banks use ordinary absolute
  addresses and never use bridges.
* A region owned by a selector-controlled bank participates in the current
  selected-bank state. Transfers between different selector-controlled banks use
  the declared selector and generated bridge machinery.
* A region with no output-bank owner is shared/nonbanked and never causes a bank
  transition by itself.

A direct-bank topology can therefore use the same declaration machinery purely
for deterministic image packaging. For example:

    bank bank1 {
        $image_size:   0x1000
        $file_index:   0
        $image_offset: 0x0000
        $link_start:   0x3000
        $cpu_start:    0x3000
        $map_size:     0x1000
    };

    bank bank0 {
        $image_size:   0x1000
        $file_index:   1
        $image_offset: 0x0000
        $link_start:   0xf000
        $cpu_start:    0xf000
        $map_size:     0x1000
    };

    mem bank1 { $start:0x3000 $size:0x1000 $ro };
    mem bank0 { $start:0xf000 $size:0x0ffa $ro $priority:2 };

This emits two fixed 4K chunks in declared file order. The first chunk represents
logical `$3000-$3FFF`; the second represents `$F000-$FFFF`, with its final six
bytes reserved for ordinary vectors. Both mappings are simultaneously available.
A call from `$Fxxx` to `$3xxx` is an ordinary `JSR $3xxx`, not a cross-bank call.
The output format may remain a flat concatenation when the consumer already knows
the target profile; a future self-describing container may add a range table
without changing the linker-address or placement model.

No explicit `$bank` property is needed on `mem`. The linker validates that every
owned allocation lies wholly inside exactly one mapped link range, that mapped
link ranges of simultaneously available direct banks do not overlap, that
selector-controlled synthetic ranges are unambiguous, that
`image_offset + map_size <= image_size`, that file indices are unique and dense,
and that selector triggers are valid and unique for the profile. It requires
exactly one startup bank only when selector-controlled banks are present. It also
maps allocator and generated ranges back to physical image offsets and rejects
any collision between ordinary allocation, RAM-port prefixes, trampoline tables,
vector bridges, vectors, selector bytes, or other declared reservations.

Once `mem` is authoritative, ordinary segment routing is mechanical rather than
cfg-authored duplication:

    CODE.region and RODATA.region -> mem region
    BSS.region                    -> writable mem region
    DATA.region                   -> load in default read-only mem,
                                     run in writable mem region

Unqualified CODE/RODATA/DATA/BSS/STARTUP placement is derived from region access,
priority, startup-bank ownership where applicable, and existing language rules.
The linker must continue to preserve private movable layouts, hard pins,
alignment/page constraints, split read/write aliases, and deterministic
placement. The map reports both the `mem` region and its inferred output bank,
including whether that bank is direct or selector-controlled, so the inference
is visible rather than mystical.

Renderer and hand-written assembly constraints are not cartridge topology and
must not be moved into a renderer x mapper matrix of target files. Segment
region requirements, alignment/page constraints, and hidden hardware-stack use
belong in object metadata emitted by the source or assembler. For example, the
standard renderer should carry its own bank-local segment and 256-byte alignment
requirements, while the cartridge C26 file supplies only cartridge topology and
allocatable regions. This is the composition mechanism required before a banked
renderer profile is added.

The current cfg implementation remains normative until roadmap items 24 through
27 are completed and differential tests prove identical images, placements,
bridges, maps, simulator behavior, and Stella behavior. Completed roadmap items
which mention cfg describe the implementation that was actually certified; they
are not retroactively rewritten as though the C26 topology model already exists.

Per-bank vectors and reset bridges
----------------------------------
Per-bank vectors and reset bridges are an early foundation item, not cleanup at
the end. Power-up bank state must not be assumed.

The implemented `CARTRIDGE vectorbridge` property gives one low-twelve-bit
corridor offset. The first implementation reserves eighteen bytes there in every
physical bank and emits three byte-identical six-byte entries:

    NMI entry:      BIT BANK0_HOTSPOT; JMP __nmi
    RESET entry:    BIT BANK0_HOTSPOT; JMP __reset
    IRQ/BRK entry:  BIT BANK0_HOTSPOT; JMP __irqbrk

Every physical bank also receives the same six vector bytes at its final six
physical offsets. All three vector words use BANK0's logical mirror of the
common entries, for example $FFE0, $FFE6, and $FFEC when `vectorbridge=$0FE0`.
The currently selected physical bank ignores those upper logical address bits,
fetches the identical entry at the same cartridge offset, and the entry selects
BANK0 before jumping to the ordinary runtime handler. BANK0 runs the same
sequence; selecting the already selected bank is harmless.

Using one identical vector image is deliberate. F4's $1FFA and $1FFB selector
hotspots overlap the NMI vector bytes.  Under the table above, the low-byte
fetch at $1FFA selects physical/file bank 6, which is VCSC BANK1; the high-byte
fetch at $1FFB then selects physical/file bank 7, which is VCSC BANK0.  Every
bank contains the same vector bytes, so the word is deterministic despite the
mid-vector bank changes, and the NMI fetch itself finishes with BANK0 selected.
The common NMI entry selects BANK0 again defensively before entering `__nmi`.
RESET and IRQ/BRK follow the same general bridge path, so BRK remains safe even
though the 6507 package exposes no ordinary NMI/IRQ inputs.

Ordinary placement must never consume the vector corridor, the six final vector
bytes, or any mapper hotspot. The linker rejects an allocatable code/data region
covering the configured eighteen-byte corridor or any selector byte, and it
rejects generated bridge/vector output which would overwrite placed material.
The normal DATA/BSS initialization and `main` entry live only in BANK0 rather
than being duplicated wholesale.

Bank-aware relocation rules
---------------------------
A declared output-bank identity, not just an arithmetic test on upper address
bits, determines whether two ROM locations share one selector-controlled bank.
The current implementation obtains that identity from cfg ownership; roadmap
item 25 derives it from C26 `mem` containment within C26 `bank` mapped link
ranges. Direct banks remain simultaneously addressable and therefore do not use
this selected-bank test. This also handles Superchip banks whose allocatable ROM
begins at $x100.

References from banked ROM obey these rules:

* A reference to ROM in the same bank is emitted normally.
* A reference to RIOT RAM, TIA, RIOT registers, or another explicitly shared
  nonbanked address is emitted normally from every bank.
* A direct cross-bank JSR is redirected through a generated call bridge.
* A direct cross-bank JMP is redirected through a generated one-way bridge.
* A cross-bank conditional branch is an error.
* A cross-bank ROM load, address constant relocation, pointer initializer, or
  other data reference is an error.
* Programmer-authored indirect cross-bank control flow remains unsupported.
  The linker-generated call/jump machinery may use an indirect JMP internally,
  but VCSC still rejects function pointers, so ordinary compiled calls remain
  visible to the linker.

The assembler/object format identifies direct control-transfer relocations as
JSR, JMP, or relaxed conditional branch. The two otherwise-unused low bits of
the o26 relocation type carry this intent without changing relocation
arithmetic or the version-2 object format. The linker does not infer semantics
by peeking at an opcode byte next to a generic word relocation. Existing
low-byte, high-byte, and word data relocations remain ordinary data references
and therefore cannot cross banks. Ordinary two-byte relative branches remain in
the B26 branch table and are checked from their final source and target
addresses after placement.

Roadmap items 5 and 6 install direct cross-bank JMP and JSR trampolines. A
proven direct transfer is rewritten to the source bank's logical mirror of a
deduplicated entry in the common replicated table. JMP entries are deduplicated
by final target and destination hotspot. JSR entries also include the statically
known source hotspot because their embedded return stub must restore that bank.
The linker never emits a raw mirrored cross-bank target address. This generated
control-flow support is intentionally different from the permanent prohibition
on cross-bank ROM data and conditional branches.

A programmer can always defeat relocation checks with a manually written raw
numeric address. That does not make such access supported; diagnostics cover
symbolic relocations the toolchain can prove.

Cross-bank call and jump bridges
--------------------------------
The complete linker-generated bridge table must occupy the same low twelve-bit
cartridge offsets and contain exactly the same bytes in every physical bank.
It is one common replicated image, not a separately generated per-bank table.
After a hotspot access changes the selected bank, instruction fetching continues
at the next byte in the newly selected bank; byte-for-byte identity is therefore
what makes every continuation safe.

The 6502 has no indirect JSR instruction, but a normal JSR can call a fixed
entry whose own JSR creates the synthetic return address before the entry
ultimately performs an indirect JMP. That is the generic cross-bank call
mechanism. The implemented fifteen-byte entry is:

    generated entry for source BANKs and target $DABC:
        JSR  body
    return_to_source:
        STA  source_hotspot
        RTS
    body:
        STA  destination_hotspot
        JMP  (BANK0-mirror address of inline_target)
    inline_target:
        .word $DABC

The call site's ordinary JSR first places its real return address on the
hardware stack. The generated entry's JSR then places the address immediately
before `return_to_source` above it, without modifying A, X, Y, or the processor
flags. `body` selects the target bank and enters the callee with the indirect
JMP. The target function's RTS increments the synthetic address and reaches the
identical return stub. That stub stores A to the caller's hotspot, preserving A
and flags while restoring the source bank, and executes a second RTS through the
call site's original return address.

Both the internal JSR operand and indirect-JMP pointer are encoded through
BANK0's logical mirror of the common table. The CPU may therefore change its
upper logical address bits while the cartridge continues to see the same low
twelve-bit offset. Every physical bank contains the same bytes at that offset,
so the entry remains valid before and after either bank switch. Entries are
deduplicated by source hotspot, destination hotspot, and final target. Nested
cross-bank calls naturally nest the two return addresses in LIFO order.

The implemented direct-JMP entry does not need a shared RAM pointer. Each
specialized eight-byte table entry carries its own immutable target word:

    generated entry for target $DABC:
        STA  destination_hotspot
        JMP  (BANK0-mirror address of inline_target)
    inline_target:
        .word $DABC

`STA` is deliberate: F8/F6/F4 switch on an access to the hotspot, and a store
changes neither A nor the processor flags. A direct JMP therefore preserves the
machine state that the original instruction would have preserved. The indirect
pointer operand always uses BANK0's logical mirror of the inline word. A13-A15
do not reach the cartridge, so that address reads the same low-twelve-bit table
offset from whichever physical bank is selected after the hotspot store. The
complete entry bytes are consequently identical in every bank.

The linker deduplicates entries by final target address and destination hotspot.
Every call site to one target jumps to its own source bank's logical mirror of
the same entry offset. The table allocator inserts a fill byte when needed so an
inline target word never begins at page offset $FF and cannot trigger the NMOS
6502/6507 indirect-JMP page-wrap bug. Corridor exhaustion is a link error which
reports the reserved range, target, entry count, and consumed bytes.

A cross-bank JMP never restores the source bank. It selects the destination and
continues at the final logical target. A cross-bank JSR restores the source bank
through its embedded synthetic return stub.

The linker reserves the common bridge corridor at the same physical offset in
every bank and emits one byte-identical table into every bank. Eight-byte JMP
entries and fifteen-byte source-aware JSR entries share that corridor. The map
reports occupied and total replicated bytes, separate entry counts and sizes,
and each transfer's source/destination identity. Exhausting the corridor is a
clear link error. The allocator inserts fill when either entry's inline target
word would begin at page offset `$FF`, avoiding the NMOS indirect-JMP wrap bug.

A cross-bank call keeps one additional hardware-stack return address live while
the destination function runs. For compiler-described call edges, the linker
computes both the ordinary longest function depth and a weighted depth which
adds one return-address slot for every simultaneously active cross-bank edge.
`callstack = callgraph` reserves two bytes per weighted slot and exports
`__call_stack_weighted_depth` and `__call_stack_bank_extra_slots` alongside the
existing stack symbols. Calls hidden in hand-written or separately assembled
code remain the integration's responsibility through `callstack_extra`.

Cross-bank data policy
----------------------
The first implementation deliberately has no far ROM data reads. Code and the
ROM tables it directly uses must live in the same bank. Data shared by multiple
banks may be duplicated or deliberately placed with each caller. A relocation
from one bank to a const object in another bank is a hard error, not an implicit
bank switch.

Ordinary C pointers stay 16-bit and bank-local for ROM. Taking the address of a
ROM object in another bank is rejected when represented by a relocation. No
hidden bank byte is added to pointer objects or function parameters.

Bank placement policy
---------------------
VCSC already has named `mem` regions and uses a region name as a declaration
modifier for objects. The same mechanism is now the source spelling for explicit
function placement; do not add special `bank0` through `bank7` language keywords.
For an ordinary F8 bank:

    mem bank1 { $start:0xD000 $size:0x0F00 $ro };

    bank1 void some_function(void) {
        /* ... */
    }

For a Superchip profile, the source `mem` declaration describes the allocatable
ROM interval, not the complete emitted physical bank. The RAM-port prefix and
the common `$xF00` trampoline corridor are excluded, leaving `$x100-$xEFF`:

    mem bank1 { $start:0xD100 $size:0x0E00 $ro };

The current cfg/image model still emits the complete 4K physical bank and fills
its reserved `$D000-$D0FF` prefix separately. The planned C26 `bank` declaration
expresses the same full image size, mapped-ROM offset, and mapped-ROM size. A
`$size:0x0F00` region would collide
with the trampoline corridor; `$size:0x1000` would run beyond the logical bank.

Named region modifiers work on function definitions and produce private
layouts such as `CODE.bank1.__vcsc_function$some_function`. The assembler keeps
procedures in any `CODE` or `CODE.*` segment independently movable, and the
linker resolves the longest matching segment rule so `CODE.bank1` controls that
private layout. A declaration and its definition must use the same named region.
An inline function cannot use a region modifier because inline expansion has no
independently placeable linker layout.

Named `$ro` regions also pin constant ROM objects. For example:

    bank1 const uint8_t level_table[4] := { 1, 2, 3, 4 };

is emitted as `RODATA.bank1.__vcsc_object$level_table`. A definition in a `$ro`
region must be `const` and must have a link-time initializer; it cannot fall back
to DATA/BSS plus startup writes. This keeps the source declaration, o26 private
layout, and linker bank affinity honest. Ordinary writable named regions retain
their existing DATA/BSS behavior.

Unmarked private `CODE.__vcsc_function$...` and
`RODATA.__vcsc_object$...`/`RODATA.__vcsc_page$...` layouts are candidates for
automatic placement. Source-level `main` normally remains unqualified: the
linker pins its private layout to the unique BANKS entry marked `startup=yes`
and chooses that bank's ordinary allocatable ROM region. An explicit read-only
`mem` modifier on `main` is permitted only when its MEMORY region belongs to
that same configured startup bank. The compiler does not interpret names such
as `bank0`; only the linker has the BANKS/MEMORY relationship needed to enforce
the rule. Startup/non-private runtime material and private runtime functions
whose reserved implementation names begin with `_` are pinned by the same
startup-bank property. The public full-window profiles happen to call that bank
BANK0, but neither placement nor diagnostics depend on that spelling.

The implemented placement policy is constrained and deterministic:

* Named `mem` modifiers are hard pins for functions and ROM objects. A pinned
  private layout keeps the exact named MEMORY region, not merely the bank.
* Cross-bank ROM data is forbidden. Every relocation between ROM layouts other
  than a trampoline-eligible direct JSR/JMP, plus every retained branch edge,
  forms a hard same-bank relation. The linker collapses the transitive closure
  into indivisible placement components before assigning addresses.
* A hard component inherits any explicit or mandatory bank pin carried by one
  of its members. Conflicting pins in one component are a fatal diagnostic that
  names the component, layout, object, and incompatible banks.
* Direct JSR and direct JMP edges are soft relations because the replicated
  trampoline table can implement them. Repeated edges accumulate; the initial
  cost uses the actual per-entry replicated-table payload, 15 for JSR and 8 for
  JMP. This is a deterministic placement heuristic, not a claim about dynamic
  call frequency.
* Before packing movable components, the linker charges each ordinary ROM
  region for fixed initialized-data images and generated copy/zero/init tables.
  The largest `type=ro` MEMORY region owned by each bank is that bank's default
  automatic-placement region; named pins still use their exact region.
* Pinned components are assigned first in stable input order. Unpinned
  components are considered by decreasing byte size, then decreasing soft-edge
  degree, then stable object/layout order. Among banks with enough preliminary
  capacity, the chosen bank minimizes weighted edges cut against components
  already assigned. Ties prefer the startup bank, then the higher logical bank
  address, then the bank name.
* The preliminary budget catches clear capacity failures early. The normal
  layout allocator remains authoritative for alignment, page containment,
  branch-page constraints, and any holes those rules create.
* If no bank can hold a component, the diagnostic reports its size and the free
  ordinary-ROM capacity in every candidate bank. The linker never breaks a hard
  component, moves a hard pin, duplicates a function, or invents a cross-bank
  ROM-data access to make the image fit.

This first placer intentionally has no second placement language, explicit
same-bank-group attribute, translation-unit preference, profile-guided call
frequency, or local-search pass. Those can be added only when measured programs
show a need. Existing hard data edges already express the most important
same-bank grouping, while named `mem` modifiers give programmers a direct
steering wheel for timing-critical or architectural decisions.

The call graph alone is insufficient: ROM-data edges, explicit pins, reserved
trampoline/vector space, generated startup tables, and per-bank capacity all
matter. Conversely, requiring programmers to place everything manually throws
away whole-program information the linker already has. The hybrid policy keeps
normal use sane while preserving precise control for timing-critical code,
fixed architecture, debugging, and reproducible experiments.

A ROM table used by functions in several banks pulls those functions and the
table into one hard component unless the table is explicitly duplicated. If
that component carries incompatible pins or cannot fit, the link fails. The
linker never silently synthesizes a far ROM read. Functions and objects are not
duplicated automatically in this implementation.

Archive selection remains normal symbol-driven selection. Placement runs only
after the final object/archive-member set is known. The map's `BANK PLACEMENT`
section reports every component, pinned versus automatic assignment, owning
bank and concrete MEMORY region, component byte cost, incident cut weight, and
originating object. The `TRAMPOLINES` section then reports the actual generated
bridges. This makes placement reproducible and auditable rather than linker
astrology.

Superchip RAM through split-address named memory
-----------------------------------------------------
Superchip storage is allocator-owned named memory, not an absolute external
alias. The SC machine/profile header defines the region conceptually as:

    mem superchip {
        $read_start:  0xF080
        $write_start: 0xF000
        $size:        0x80
        $rw
    };

Applications declare ordinary owned objects with the region modifier:

    superchip uint8_t byte;
    superchip uint8_t buffer[32];

Loads relocate through the read alias, stores and initialization through the
write alias, and map accounting counts the physical bytes once across all ROM
banks. The current SC cfg profiles reserve each bank's $x000-$x0FF RAM-port prefix and
begin allocatable ROM at $x100 while still emitting complete 4K physical banks.
In the planned C26 topology model, the `bank` mapping begins at image offset
`$0100` and CPU/link address `$x100`; this explicit mapping, not the word
`superchip`, keeps the RAM aliases shared and outside selector-controlled ROM.

A non-owning `@[read/write]` absolute external binding may not overlap these or
any other allocator-managed windows. The linker compares the binding's complete
readable extent with managed read/ordinary windows and its complete writable
extent with managed write/ordinary windows. Raw hardware bindings remain valid
only in addresses deliberately left outside linker-managed `MEMORY`; storage
inside a named region must be declared through that region so the allocator can
reserve it.

The source header supplies the region name and aliases so an application does
not repeat mapper constants. The linker treats the region as one shared
128-byte physical allocation, not BANK0-owned storage merely because the
canonical aliases use the `$Fxxx` mirror. Code in every selected bank may use
the same allocated object without a bank switch.

Normal cold-start startup initializes automatically allocated SC DATA/BSS just
as it initializes RIOT RAM, but stores through the write window. Bank switching
must not disturb SC RAM contents. Warm-reset behavior should follow the normal
VCSC startup contract unless a later explicit no-reinitialize entry point is
added.

Map, listing, and diagnostics
-----------------------------
A banked map should report at least:

* mapper/profile name and exact output size;
* each bank's logical range, selector hotspot, startup status, physical file
  offset, used bytes, free bytes, and reserved bytes;
* output order, proving that the lowest logical bank is written first and BANK0
  is written last;
* every per-bank vector and reset bridge;
* the byte-identical common trampoline table, its physical offset, reserved
  capacity, occupied and replicated size, inline target-passing ABI, direct-JMP
  entries, and later source entries/return stubs;
* every generated call/jump target entry, its source bank, destination bank,
  target, logical addresses, and byte cost;
* bridge corridor occupancy;
* weighted hardware-stack depth including cross-bank call edges;
* every placement component, its pinned or automatic status, capacity cost,
  and weighted call edges cut by the placer;
* every rejected or unresolved cross-bank data relocation with source and
  target symbols;
* Superchip RAM physical occupancy and read/write aliases when present.

Listings and symbol sidecars should retain ordinary 16-bit logical addresses.
Where ambiguity matters, they should additionally show the bank name and output
file offset. The raw `.bin` remains a contiguous cartridge image in the
ascending-logical-address bank order defined above.

Testing strategy
----------------
Every implementation slice needs both structural and execution tests.

Structural tests should verify exact image size, bank order, fill bytes, vectors,
hotspot reservations, logical symbol addresses, physical file offsets, bridge
bytes, deterministic placement, map output, and useful errors. Tests must prove
that BANK0 is the final physical bank in F8, F6, and F4 output.

Stella is the authoritative end-to-end execution environment for bank
switching and Superchip behavior.  `vcsc-sim` now consumes the public cfg
`CARTRIDGE`/`BANKS` descriptions and executes raw F8/F6/F4 images, which gives
fast deterministic RAM-signature and forced-start-bank diagnostics.  That
VCSC-owned mapper model is not independent evidence: finished cartridge images
must also run in Stella's F8/F6/F4 and later F8SC/F6SC/F4SC implementations.

Create a visible diagnostic cartridge, parameterized or separately built for
F8, F6, and F4, which:

* is run once with every physical/file bank forced as the initial power-on bank
  and also in repeated developer-mode runs with randomized startup-bank state;
* resets correctly from every initial bank, enters BANK0, and calls `main` there;
* exercises every possible ordered source-bank to destination-bank transition,
  including self-selection where useful, through direct selector accesses,
  cross-bank JMP, cross-bank JSR/return, and nested cross-bank calls;
* writes known source-bank, destination-bank, transition-number, and phase
  values into RIOT RAM before and after each switch, and verifies those values,
  the restored source bank, and the hardware-stack balance after each return;
* renders a distinctive PASS or FAIL sprite for every transition in a stable
  frame.  A failure indication should identify at least the source bank,
  destination bank, and failed phase so a screenshot is diagnostically useful;
* emits a final unmistakable all-pass frame only after every transition has
  completed, allowing the existing Stella screenshot/oracle machinery to grade
  the actual cartridge;
* retains exact frame length and performs mapper changes outside critical
  visible timing.

The Superchip versions of the diagnostic must additionally write distinct
patterns through the write window, read them back through the read window,
carry them across every tested bank transition, and display separate PASS/FAIL
results for alias direction, persistence, initialization, and bounds.  Stella
must be used to test both bank switching and Superchip operation; a local
interpreter that repeats VCSC's own assumptions is not sufficient certification.

Negative tests should cover cross-bank ROM reads, pointers, low/high-byte data
relocations, conditional branches, indirect control flow, bridge exhaustion,
invalid or duplicate hotspots, overlapping banks, malformed output order,
missing per-bank vectors, unknown cfg properties, and ROM placement in an SC
RAM-port prefix.

Detailed roadmap
----------------
The first unfinished leaf in this file is the next bankswitching-specific action
when main roadmap item 29 becomes active.

[x] 0. Record and lock the initial full-window design decisions.

       * Use descending mirrored logical ranges with BANK0 at $F000.
       * Keep ordinary 16-bit symbols and bank-local ROM pointers.
       * Reject cross-bank ROM data references.
       * Write physical banks from lowest logical address to highest, placing
         BANK0 last in the cartridge file.
       * Use existing split-address refs as the explicit Superchip baseline.

[x] 1. Extend existing named `mem` region modifiers to function code.

       * Emit a function marked `bank1` into `CODE.bank1` and preserve each
         procedure as an independent private o26 layout.
       * Resolve `CODE.bank1.__vcsc_function$NAME` through the exact
         `CODE.bank1` linker segment rule before falling back to `CODE`.
       * Emit and validate the same source mem-region metadata used by objects.
       * Require declarations and definitions to agree on their region.
       * Reject region modifiers on inline functions and explicit numbered
         nonzero-bank placement of `main`.
       * Test ordinary and Superchip-sized function regions, including the
         correct `$D100+$0E00` allocatable SC interval.

[x] 2. Extend the cfg parser and linker image model for multiple full 4K banks.

       * Add explicit CARTRIDGE mapper/fill metadata, BANKS
         start/size/hotspot/startup metadata, and MEMORY `bank=NAME` ownership.
       * Replace fixed MEMORY and SEGMENTS tables with dynamic storage and add a
         dynamic BANKS table.
       * Reject unknown cfg properties, malformed entries, unsupported or
         incomplete F8/F6/F4 bank sets, conflicting banks, overlapping bank
         MEMORY regions, and non-binary banked output.
       * Validate VCSC logical bank name/range, physical file index, and hotspot
         as three distinct identities.  The first mapper hotspot selects file
         chunk 0; VCSC BANK0 is the final chunk and therefore uses $1FF9 for
         F8/F6 and $1FFB for F4.
       * Reserve every selector hotspot at the same physical offset in every
         bank by rejecting any ordinary allocatable segment region which covers
         one; hotspot bytes may not become code or ordinary ROM data.
       * Group allocatable regions, reserved holes, and vectors into exact 4K
         physical output units.
       * Emit bank units in ascending logical-address order with no logical gaps
         in the file; prove BANK0 is last.
       * Preserve the current unbanked `vcs_4k.cfg` output byte-for-byte.

[x] 3. Add per-bank vectors and same-offset reset bridges.

       * Require a `CARTRIDGE vectorbridge` low-twelve-bit offset and reserve the
         same eighteen-byte NMI/RESET/IRQ corridor in every bank.
       * Emit byte-identical `BIT BANK0_HOTSPOT; JMP handler` entries and
         byte-identical vector words in every selected physical bank.
       * Route all vectors through BANK0 before entering `__nmi`, `__reset`, or
         `__irqbrk`; keep startup and `main` in BANK0.
       * Test all three vectors from every possible initial bank under F8, F6,
         and F4, including F4's NMI-vector/selector overlap.
       * Reject missing, vector-overlapping, selector-overlapping, or
         ordinary-code-overlapping bridge corridors.

[x] 4. Add bank-aware relocation classification and validation.

       * Preserve JSR, JMP, and conditional-branch relocation intent in o26.
       * Identify same-bank, shared-memory, and cross-bank references from cfg
         bank identities.
       * Continue emitting same-bank ROM and shared RAM/hardware references
         normally.
       * Reject cross-bank branches, data reads, address constants, pointer
         initializers, and low/high/word data relocations with source-located
         diagnostics.
       * This validation slice originally rejected cross-bank direct JMP/JSR
         with distinct pending-trampoline diagnostics rather than emitting a
         wrong raw mirror address or misclassifying the relocation as data.
         Items 5 and 6 now rewrite direct JMP and JSR through the common table.
       * Diagnose object, movable source layout, final source address/bank,
         target symbol/layout, final target address, and target bank.

[x] 5. Add the byte-identical common trampoline table and cross-bank JMP.

       * Require `CARTRIDGE trampoline` and `trampolinesize`; reserve that
         deterministic low-twelve-bit corridor in every bank and reject overlap
         with ordinary allocation, vector bridges, vectors, or hotspots.
       * Use an eight-byte state-preserving target ABI per deduplicated entry:
         `STA destination_hotspot; JMP (inline_target); .word target`, with the
         indirect operand encoded through BANK0's logical mirror.
       * Redirect each direct cross-bank JMP to the source bank's mirror of the
         shared entry offset. Copy the exact occupied table bytes into every
         physical bank and leave the rest of the reserved corridor filled.
       * Avoid the NMOS indirect-JMP page-wrap bug, deduplicate equivalent
         targets, report reserved/occupied/replicated and per-entry costs, and
         diagnose corridor exhaustion.

[x] 6. Add JSR-to-indirect-JMP cross-bank calls and return stubs.

       * Redirect a direct cross-bank JSR to a fifteen-byte source-aware target
         entry in the common replicated table.
       * Use an internal JSR to create the synthetic return address without
         clobbering A, X, Y, or flags; select the target bank and enter the
         callee with `JMP (inline_target)`.
       * Restore the caller's bank with an embedded identical `STA hotspot; RTS`
         stub, then RTS through the original call-site return address.
       * Deduplicate by source bank, destination bank, and final target; support
         nested cross-bank calls and avoid the NMOS indirect-JMP page-wrap bug.
       * Add one weighted hardware-stack slot for every active compiler-described
         cross-bank edge, reserve two bytes per slot, and report/export the
         ordinary and weighted depths. Assembly-hidden calls still require
         `callstack_extra`.

[x] 7. Add placement constraints and deterministic automatic bank placement.

       * Pin `main`, startup/non-private runtime layouts, and reserved-name
         private runtime functions to BANK0; keep named `mem` modifiers as hard
         function/object pins.
       * Emit initialized `const` objects in named `$ro` regions as private
         `RODATA.region` layouts. Reject mutable definitions and runtime-only
         initializers in read-only regions.
       * Build indivisible hard components from every cross-bank-forbidden ROM
         relocation and retained branch, propagating explicit and mandatory
         pins and diagnosing contradictions before final layout.
       * Treat direct JSR/JMP relationships as soft edges weighted by their
         15-byte/8-byte bridge payload. Reserve fixed ROM/startup-table capacity,
         place pins first, then use deterministic size/degree/stable ordering and
         minimum incremental cut cost with documented bank tie-breaks.
       * Report component identity, pinned versus automatic members, concrete
         bank/MEMORY assignment, byte cost, incident cut weight, and resulting
         trampoline entries. Diagnose per-bank capacity when a component cannot
         fit; preserve final allocator checks for alignment and page constraints.
       * Cover deterministic repeated links, capacity spill, hard data affinity,
         contradictory pins, `main` residency, generated bridge count, and a
         source-compiled named `$ro` object whose unpinned reader follows it.

[x] 8. Add and certify `vcs_8k_f8.cfg`.

       * Install a public two-bank profile with BANK1 first and BANK0 last in
         the file, ordinary allocation ending at `$xEFF`, and common trampoline,
         vector-bridge, tail, hotspot, and vector reservations in both banks.
       * Provide `bank0` and `bank1` MEMORY/SEGMENT names for hard source pins
         while leaving unmarked CODE/RODATA eligible for deterministic placement.
       * Certify reset from either initial file chunk, `main` and runtime in BANK0,
         automatic and pinned placement, cross-bank JMP, JSR-to-indirect-JMP calls,
         nested return, byte-identical common corridors, vectors, selector use,
         map output, staged installation, and exact 8192-byte image size.

[x] 9. Add F6 and F4 through the same implementation.

       * Add and install `vcs_16k_f6.cfg` and `vcs_32k_f4.cfg` without
         mapper-specific linker forks.
       * Exercise every logical bank and every selector with nested cross-bank
         calls, reset from every initially selected physical chunk, exact file
         order, byte-identical trampoline/vector corridors, reserved hotspot
         bytes, F4's NMI-vector/selector overlap, and exact 16K/32K sizes.
       * Keep BANK0 at $F000 and last in both files; certify F6 physical/file
         BANK3..BANK0 order and F4 physical/file BANK7..BANK0 order.
       * Install/uninstall both public profiles and compile staged diagnostics
         through each profile in `make installcheck`.

[x] 10. Make archive selection, listings, map output, simulator execution,
       and Stella certification fully bank-aware.

       * Preserve normal lazy archive-member selection and report selected
         `archive.l26(member.o26)` origins, logical bank placement, final symbol/
         listing addresses, and every generated bridge.
       * Teach `vcsc-sim` to parse the same F8/F6/F4 `CARTRIDGE` and `BANKS`
         descriptions, load complete raw `.bin` chunks in physical/file order,
         switch on configured hotspot reads/writes, fetch reset vectors through
         the initially selected chunk, and expose physical `--start-bank`,
         `--stop-pc`, and RAM-dump controls for deterministic diagnostics.
       * Add the reusable `bankswitching_diagnostic_suite.c26` plus the editable
         `examples/09_bankswitching/01_diagnostic` wrapper.  Emit one cartridge
         per mapper rather than one cartridge per transition: F8, F6, and F4.
         Each cartridge internally exercises every possible ordered source-bank
         to destination-bank transition through direct JMP, performs a same-bank
         JSR/RTS check from every source, adds a nested BANK0-to-BANK1 call and
         return, writes known RIOT-RAM signatures, verifies stack balance, and
         settles on a stable final green-background/white-P or dark-red-
         background/white-F frame. The P and F are the exact default ASCII
         glyphs, loaded through ordinary indexed syntax which must remain
         absolute for relocatable ROM, and changed only on a scanline boundary.
         The public example also emits one deliberately poisoned F8SC image and
         every displayed result frame is exactly 262 scanlines.
       * Run the complete matrix in Stella, force every physical startup bank,
         and run developer-mode randomized startup-bank trials.  The top-level
         `stella-bank-test` target grades actual Stella snapshots; ordinary
         `make test` runs the full matrix through the cfg-driven simulator.
       * Keep extension of this same Stella diagnostic framework to the SC
         profiles mandatory when they land, proving read-window/write-window
         direction, initialization, bounds, and Superchip persistence across
         every bank transition.

[x] 11. Add explicit-binding Superchip profiles.

       * Add `vcs_8k_f8sc.cfg`, `vcs_16k_f6sc.cfg`, and `vcs_32k_f4sc.cfg`.
       * Begin ordinary ROM allocation at $x100 in every bank, reserve the RAM
         port prefix, and still emit a complete 4K physical bank.
       * Publish scheme constants/helpers for existing
         `uint8_t object@[read/write]` declarations.
       * Verify shared 128-byte persistence across every bank switch and exact
         8K/16K/32K output sizes, using Stella diagnostic cartridges in addition
         to structural linker tests.
       * Implemented profiles reserve `$x000-$x0FF`, allocate ordinary ROM only
         in `$x100-$xEFF`, install `superchip.c26` with the allocatable
         `mem superchip` region, keep the diagnostic suite's whole-window
         absolute alias private to that suite, and reject SC cfgs whose bank-owned
         read-only regions overlap the RAM ports. Structural tests also mutate
         each public profile to expose the prefix and require the linker to
         reject it. The three Superchip diagnostic cartridges internally run
         their complete transition matrices; the simulator proves them from
         every physical startup chunk, and Stella grades the same stable visible
         PASS/FAIL result under forced and randomized startup-bank selection.

[x] 12. Add Automatic allocation of variables into Superchip RAM.

       * Define `mem superchip { $read_start:0xF080 $write_start:0xF000
         $size:0x80 $rw };` through the SC machine/profile header and allocate
         objects with ordinary named-region syntax such as
         `superchip uint8_t foo;` and `superchip uint8_t buffer[32];`.
       * Lock the existing split-address order: explicit bindings use
         `@[read_address/write_address]`, so the first SC byte is equivalent to
         `uint8_t foo@[0xF080/0xF000];`.  Loads use the read alias; stores,
         initializer copies, and BSS clearing use the write alias.
       * Preserve both aliases through compiler metadata, o26, linker layout,
         loads, stores, initialization, and map output while treating the region
         as shared across all selected banks rather than owned by BANK0.
       * Start with persistent globals and arrays.
       * Reject address-taking, pointer decay, and unsupported `ref` passing.
       * Count physical occupancy once and provide deterministic overflow
         diagnostics.
       * Implemented compiler metadata carries both aliases through private
         `BSS.superchip`/`DATA.superchip` layouts. Unsuffixed direct accesses
         retain absolute addressing, direct and runtime array indexing select
         the correct alias, and invalid address-taking, decay, `ref`, local,
         parameter, function, split-address bitfield-write, and bank-owned
         split-region uses fail explicitly.
       * The linker allocates shared split storage once at the read alias,
         translates startup DATA copies and BSS clearing to the write alias,
         exempts both aliases from cross-bank ROM rewriting, validates all cfg
         properties, reports both addresses, and names the first overflowing
         object deterministically.
       * F8SC, F6SC, and F4SC simulator regressions start from every physical
         bank and prove initialized DATA, zeroed BSS, dynamic indexing, alias
         direction, persistence across calls in every logical bank, exact image
         sizes, and one 34-byte physical occupancy report. Authoritative Stella,
         installed-toolchain, and reset-lifecycle certification remains item 23.
       * Parameters and function return storage remain implementation staging
         restrictions, not intended final language semantics. Items 14, 15, and 20
         remove those restrictions. Item 13 has removed the local-object
         restriction, including function-scope static storage.

[x] 13. Add Superchip-backed local variables.

       * Permit ordinary automatic and function-scope `static` scalars, arrays,
         and other already-supported local object forms to select a split-address
         writable region, for example `superchip uint8_t scratch;` or
         `static superchip uint8_t calls;` inside a function body.
       * Keep the existing non-reentrant, symbol-backed automatic-local model.
         Reads use the region's read alias; writes and function-entry initializers
         use its write alias. No runtime stack or new lifetime mechanism is
         required. Function-scope static objects instead use persistent
         `BSS.<region>` or `DATA.<region>` storage and the existing one-time
         startup initialization machinery.
       * Reuse the existing alias-aware lowering for assignment, compound
         assignment, increment/decrement, and indexed access. Extend the formerly
         rejected split-address bitfield update so it evaluates the lvalue once,
         loads through the read alias, and stores through the write alias; never
         emit a single-address 6502 read-modify-write for a split-address object.
       * Preserve region identity and both aliases through inline-expansion-private
         locals where inline locals are otherwise legal, and count each physical
         allocation only once in map and overflow reporting.
       * Test direct and indexed reads/writes, initializers, nested calls, inline
         expansions, cross-bank use, exact map occupancy, deterministic overflow,
         and rejection only where the underlying local object form is itself
         unsupported.

       Completed 2026-08-04:

       * Automatic scalars, arrays, structures, packed bitfields, and inline-
         expansion-private locals may use a split writable region. They retain
         fixed non-reentrant backing symbols in function activation layouts.
       * Function-entry declaration initializers and every generated store use the
         write alias; generated reads use the read alias. The existing alias-aware
         assignment/index/increment paths are reused.
       * Split bitfield writes now evaluate the destination once, copy the physical
         storage byte through the read alias, edit it in compiler scratch, and
         store it through the write alias.
       * Linker activation zeroing now translates split activation destinations to
         the write window. Map accounting counts each physical local byte once.
       * F8SC, F6SC, and F4SC execution tests cover direct/runtime indexing,
         initializers, compound assignment, increment, bitfields, inline locals,
         repeated nested cross-bank calls, exact eleven-byte occupancy, every
         physical startup bank, and deterministic activation overflow.
       * Function-scope `static` split objects use persistent `BSS.superchip` or
         `DATA.superchip`, never activation overlays. Uninitialized storage is
         cleared at startup, link-time initializers use the DATA copy path, and
         nonconstant initializers run once in the translation-unit startup
         initializer; all destinations use the write alias. F8SC/F6SC/F4SC tests
         prove persistence across repeated calls and every physical startup bank.
       * A mapper-independent regression uses `banana`, `pair`, and `orange`
         split regions with different sizes, unaligned and noncontiguous aliases,
         and both read-above-write and read-below-write layouts. It proves that
         compiler metadata, linker allocation, activation/static storage, startup
         initialization, bitfield lowering, simulator aliasing, and overflow
         reporting are driven by `mem`/cfg attributes rather than the region name
         or a fixed `$F000/$F080` ordering.

[x] 14. Add Superchip-backed value parameters.

       * Permit a value parameter to select the split-address writable region, for
         example `void consume(superchip uint16_t value)`.
       * Allocate the existing callee-owned parameter object in that region. The
         caller writes the argument through the write alias before `JSR`; the
         callee reads it through the read alias, and assignments to the parameter
         write through the write alias.
       * Carry the parameter region and both aliases in function ABI metadata so
         calls work identically within one translation unit, across separate
         compilation, and through generated cross-bank trampolines. Preserve the
         existing non-reentrancy and call-cycle rules for symbol-backed parameters.
       * Do not conflate value parameters with ordinary `ref` parameters.
         Items 17 through 19 add directional one-address pointers and `ref`
         binding rules without introducing a built-in fat-pointer ABI.
       * Test one- through four-byte values, BCD values, caller-side expression
         evaluation, callee reads and writes, repeated and nested calls, separate
         compilation, map accounting, and overflow diagnostics.

       Completed 2026-08-04:

       * Any named split-address writable `mem` region may back a value parameter;
         no compiler or linker path recognizes the spelling `superchip`. The
         parameter remains an exact-sized callee-owned symbol in the function's
         region-local activation layout.
       * Direct calls evaluate argument expressions left to right and use
         selective caller staging. A converted argument is copied immediately to
         its callee-owned object unless it must survive a function call in a later
         argument; only those earlier values remain staged until all evaluations
         finish. Split parameters use their configured write alias for either
         copy path. Callee loads use the read alias and parameter assignments use
         the write alias. Inline-expansion-private parameters use the same alias-
         aware storage rule without gaining a callable ABI.
       * Function signatures and ABI fingerprints now include each value
         parameter's region identity; linker-visible fingerprints additionally
         carry the read and write starts plus ordinary type representation.
         Declarations and definitions therefore cannot disagree about split
         storage either within one translation unit or across separate builds,
         even when their value widths match.
       * Split-address `ref` parameters remain rejected pending item 19, with a
         focused diagnostic: a normal pointer cannot represent distinct load and
         store addresses.
       * F8SC, F6SC, and F4SC tests cover one- through four-byte ordinary and BCD
         values, caller-side calls during argument evaluation, callee mutation,
         inline expansion, nested and cross-bank calls, every physical startup
         bank, exact twenty-one-byte activation occupancy, and deterministic
         overflow. A separately compiled mapper-independent test uses `banana`,
         `pair`, and `orange` regions with different sizes, noncontiguous aliases,
         and both alias orderings; it also proves linker-visible ABI mismatch
         diagnostics name the conflicting parameter regions.
       * `call_selective_staging.pl` additionally proves call-free argument lists
         reserve only one reusable slot sized for the largest argument, while a
         nested-call list reserves exactly the values which cross a later call
         plus that reusable slot. Existing sibling-overlay execution coverage
         proves the reduced staging remains safe.

[x] 15. Add Superchip-backed function return storage.

       * Permit a non-void function's existing hidden `function$__return` object
         to select the split-address region, using syntax such as
         `superchip uint8_t function(void)`. This extends the existing memory-return
         ABI; it does not introduce register returns or a second calling convention.
       * Lower `return expression;` to stores through the write alias. Callers read
         the result through the read alias after the call. Preserve exact-sized
         return storage for every already-supported return width and type which
         fits in the selected region.
       * Keep `$$` as the source-level lvalue for that same return object. Reads
         from `$$` must use the read alias; assignments and compound assignments to
         `$$` must use the write alias. A write followed by a read of `$$` inside
         the callee must observe the value just written.
       * Carry the result region and both aliases through declarations, definitions,
         ABI metadata, separate compilation, generated cross-bank calls, map output,
         activation layout, and deterministic overflow diagnostics. Item 20 handles
         combining result storage with an explicit code-placement region.
       * Add execution tests which explicitly write `$$`, read it back, modify it,
         use a bare `return;`, and verify the caller receives the final value. Cover
         at least one-, two-, three-, and four-byte returns, ordinary and BCD values,
         same-bank and cross-bank calls, and all three SC profiles.

       Completed 2026-08-04:

       * Any named writable split-address `mem` region may back the existing
         exact-sized hidden `function$__return` object. The implementation derives
         the read symbol, write expression, size, and region-local activation
         segment from ordinary `mem` metadata; no compiler or linker path recognizes
         the spelling `superchip` or assumes a particular alias order or distance.
       * `return expression;`, direct assignment to `$$`, and compound assignment to
         `$$` store through the configured write alias. Reads of `$$` inside the
         callee and result copies in the caller use the configured read alias. A bare
         `return;` preserves the value already stored in the object. The epilogue
         remains an ordinary `RTS`, and ignored results require no caller copy.
       * Split return objects use absolute import/export rather than zero-page
         import/export. Function contracts and linker ABI fingerprints carry the
         result region plus both window starts, so declarations and definitions
         cannot silently disagree within one translation unit or across separate
         compilation. A writable result modifier on a `void` function is rejected.
       * The return object participates in the selected region's ordinary call-graph
         activation overlay and map accounting. Sibling result objects reuse the
         same physical bytes, caller/callee paths remain disjoint, read and write
         windows count once, and an over-capacity path produces the same deterministic
         region-overflow diagnostic as locals and parameters.
       * `superchip_function_returns.pl` covers one- through four-byte ordinary and
         BCD returns, explicit `$$` write/read/update and bare-return paths, same-bank
         calls, generated cross-bank JSR trampolines, every physical starting bank,
         both aliases, exact four-byte physical occupancy, and deterministic overflow
         under F8SC, F6SC, and F4SC. `split_memory_function_returns.pl` separately
         compiles callers and definitions using `banana`, `pair`, and `orange`, with
         different sizes, reversed window ordering, noncontiguous aliases, ordinary
         integer and pointer results, map/symbol checks, result-region ABI mismatch,
         and per-region overflow coverage.
       * A split result modifier currently selects only return storage; the function
         body's code remains automatically placed. Item 20 adds the independent
         code-region/result-region partition needed to combine writable result
         storage with an explicit read-only code-placement modifier.

[x] 16. Make `@[read_address/write_address]` imply an absolute external
       object binding without `ref`.

       * Accept declarations such as `uint8_t VSYNC @[none/0x00];`,
         `uint8_t CXM0P @[0x30/none];`, and
         `uint8_t SWCHA @[0x0280/0x0280];`. The address annotation itself means
         that the object names pre-existing storage: allocate no bytes, emit no
         initializer, and preserve the supplied read and write locations exactly.
       * Reserve `ref` for parameter-passing semantics. Migrate the installed
         hardware headers, examples, tests, and documentation away from
         `ref T object @[read/write]`, then diagnose `ref` on an object definition
         or declaration rather than retaining two spellings for the same binding.
       * Preserve `none` independently on either side and carry every absolute
         binding through the same generic lvalue metadata used by split `mem`
         objects. Reads require a read location, writes require a write location,
         and read-modify-write operations require both; do not special-case
         hardware-register names or the `ref` spelling.
       * Reject initializers, allocation modifiers, and other contradictory storage
         requests on absolute external bindings. Require compatible redeclarations
         and separate-compilation metadata to agree on type and both addresses.
       * Add migration and diagnostic tests for readable-only, writable-only,
         same-address read/write, and differing-address bindings, including array
         and aggregate object declarations where those forms are already legal.
       * Implemented file-scope and block-scope `@...` declarations as non-owning
         absolute bindings without `ref`; installed TIA, RIOT, and Superchip
         headers and all examples now use the shorter spelling. Object-level and
         function-level `ref` are diagnosed, while parameter `ref` remains intact.
       * Absolute bindings allocate no segment bytes, reject initializers and
         storage/linkage/use-contract modifiers, preserve `none` independently,
         and retain the existing generic direct/index/member/inline-assembly
         read/write lowering. Aggregate members cannot themselves carry an
         absolute binding, but aggregate and array objects may.
       * Same-translation-unit redeclarations compare canonical numeric addresses
         as values rather than spellings. Linker-visible ABI fingerprints record
         the object type plus canonical read and write addresses, so separately
         compiled declarations with different locations fail deterministically.
       * Because installed hardware headers now contribute ABI records for every
         absolute binding, the assembler's `.o26` export writer now honors the
         format's existing 16-bit export count instead of imposing an unrelated
         255-symbol ceiling. This preserves complete metadata even in large generated
         overflow fixtures and ordinary translation units which include `vcs.c26`.
       * Coverage includes global and local bindings, readable-only, writable-only,
         same-address and split-address objects, arrays and aggregate objects,
         legacy `ref` diagnostics, contradictory modifiers, forbidden
         initialization, compatible redeclarations, and cross-object ABI mismatch.
       * Absolute bindings now emit linker-visible range metadata for their full
         object size. Before layout, `vcsc-ld` rejects a readable range which
         overlaps any configured allocator-managed read/ordinary window and a
         writable range which overlaps any configured allocator-managed
         write/ordinary window. The check is generic, directional, and covers
         block-scope bindings as well as separately compiled file-scope bindings.
       * Convert the bankswitching diagnostic's complete Superchip probe from a
         non-owning absolute alias to `superchip uint8_t ...[128]`, so the probe
         owns and reserves the entire physical region. Keep TIA and RIOT hardware
         bindings valid because their addresses lie outside the VCS profiles'
         allocator-managed RAM and ROM windows.
       * Add ordinary/split/reversed/noncontiguous overlap tests, complete-array
         boundary checks, local-binding coverage, and an external-hole cfg for
         tests that intentionally model raw helper buffers outside managed memory.

[x] 17. Add `writeonly` as the write-side access qualifier.

       * Define `writeonly T *` as a one-address pointer through which the pointed-
         to object may be written but not read. Permit copying and the ordinary
         pointer comparison/arithmetic operations VCSC already supports, indexing
         used as a store destination, and direct assignment through the pointer.
       * Reject every operation which obtains a value through a `writeonly`
         lvalue, including ordinary loads, compound assignment, increment/
         decrement, and packed-bitfield updates which must preserve surrounding
         bits. Keep `const` and `writeonly` mutually exclusive on the same pointee.
       * Permit an ordinary readable/writable `T *` to convert implicitly to
         `writeonly T *`, just as it may lose write capability when converted to
         `const T *`. Reject implicit conversion from either restricted pointer
         back to ordinary `T *` or between `const T *` and `writeonly T *`.
       * Carry access qualification through declarations, assignments, casts if
         supported, function signatures, ABI fingerprints, structure layout, and
         separate compilation. The qualifier changes permitted access, not pointer
         width: ordinary, `const`, and `writeonly` pointers remain one-address
         pointers.
       * Add positive store/index/arithmetic tests and focused diagnostics for
         hidden reads, forbidden conversions, bitfield writes, qualification
         mismatch across declarations, and attempts to combine `const` with
         `writeonly`.

       Completed 2026-08-04:

       * `writeonly T *` is a real one-address pointer capability. Pointer values
         remain two bytes and retain ordinary copy, comparison, indexing, and
         arithmetic behavior, while dereference is accepted only as a pure store
         destination.
       * Generic lvalue access checks reject loads through `writeonly`, stores
         through `const`, and hidden reads caused by compound assignment,
         increment/decrement, or packed-bitfield preservation. The rules follow
         pointer-derived lvalue metadata rather than symbol or region names.
       * Ordinary read/write pointers implicitly convert to either `const` or
         `writeonly`. Restricted pointers cannot implicitly regain full access or
         convert between the read-only and write-only forms. `const writeonly`
         and non-pointer `writeonly` declarations are diagnosed.
       * Access capability is preserved through locals, globals, expressions,
         assignments, initializers, returns, value parameters, aggregate members,
         declarations, and separate-compilation ABI fingerprints. Function and
         object redeclarations must agree independently of the unchanged pointer
         width.
       * Compile-only coverage exercises stores, indexing, arithmetic, comparison,
         every forbidden read/conversion, aggregate and function mismatches, and
         file-scope pointer expressions. End-to-end execution proves generated
         stores and the two-byte representation; linker failures prove pointer and
         aggregate ABI qualification mismatches across translation units.

[x] 18. Add explicit read- and write-address projection operators `&<` and `&>`.

       * Define `&<lvalue` to return the lvalue's read address as `const T *`, and
         `&>lvalue` to return its write address as `writeonly T *`. Evaluate the
         lvalue location once and do not load from or store to the object merely to
         form either address.
       * Lex `&<` and `&>` as indivisible multi-character unary operators with
         the same precedence and associativity as ordinary unary `&`. Require the
         characters to be adjacent: `& <foo` and `& >foo` are not alternate
         spellings, and neither spaced form creates a competing unary-address
         interpretation.
       * Drive both operators entirely from generic lvalue read/write metadata.
         `&<` is an error when the read location is `none`; `&>` is an error when
         the write location is `none`. This applies equally to named `mem` objects,
         absolute bindings such as `uint8_t VSYNC @[none/0x00];`, array elements,
         structure members, and dereferenced lvalues.
       * Make no assumption about region spelling, window order, contiguity,
         distance, alignment, or size. For an ordinary one-address object the two
         projected pointer values may be numerically equal while retaining their
         different access qualifications.
       * Keep plain `&` for objects which have one conventional address compatible
         with the resulting ordinary pointer type. Reject plain `&` when distinct
         read and write locations cannot be represented by one pointer, and direct
         the diagnostic to `&<` and `&>`. Keep implicit decay of a split-address
         array invalid; use an explicit directional projection such as `&<array[0]`
         or `&>array[0]`. Expressions such as `&<*p` and `&>*p` remain valid
         compositional spellings when the dereferenced lvalue has the required
         access path.
       * Do not add a built-in split/fat pointer in this slice. A program which must
         retain both addresses may place the two projections in an ordinary user-
         defined structure. Leave a language-defined paired-pointer type deferred
         until a concrete use case justifies its ABI and optimizer cost.
       * Test ordinary, read-only, write-only, same-address, reversed-window, and
         noncontiguous split objects using unrelated fruit-named regions and
         explicit absolute bindings. Verify `none` diagnostics and ensure the
         generated pointer is the exact configured read or write address.

       Completed 2026-08-04:

       * Add adjacent unary `&<` and `&>` tokens at ordinary address-of precedence.
         `&<` returns the selected lvalue read address as `const T *`; `&>`
         returns its write address as `writeonly T *`. Spaced `& <` and `& >`
         forms remain ordinary tokens and are rejected rather than reinterpreted.
       * Lower both operators through generic lvalue metadata for ordinary,
         split-region, absolute, indexed, member, and pointer-derived lvalues.
         Runtime location expressions are evaluated once, and constant
         initializers retain symbolic alias relocations and offsets without an
         object load or store.
       * Keep plain `&` limited to one conventional readable/writable address.
         Split and one-sided objects diagnose the unrepresentable ordinary
         pointer and direct source to `&<` or `&>`. Split-array implicit decay
         remains forbidden.
       * Preserve pointer capability in expression typing: read projections are
         `const`, write projections are `writeonly`, and neither implicitly
         regains ordinary read/write access. Missing directions, forbidden
         pointer-derived access, and bitfield projections are diagnosed.
       * Add exact-address execution coverage using `banana`, reversed `pair`,
         and noncontiguous `orange` regions plus ordinary, same-address, split,
         read-only, and write-only absolute bindings. Compile failures cover
         adjacency, `none`, capability recovery, const/writeonly dereference
         direction, and bitfields.

[x] 19. Extend one-address `ref` parameters with directional access capability.

       * Define three parameter contracts: `ref const T` accepts a readable lvalue
         and passes its read address; `ref writeonly T` accepts a writable lvalue
         and passes its write address; ordinary `ref T` accepts only an lvalue whose
         read and write locations both exist and are identical.
       * Continue to represent every `ref` argument with one ordinary pointer-sized
         address. Do not create an implicit fat pointer and do not choose one side
         of a genuinely split object for unqualified `ref T`.
       * Enforce the declared capability inside the callee. `ref const T` may be
         read but not written; `ref writeonly T` may be written but not read or
         used for read-modify-write; `ref T` retains ordinary read/write behavior.
         Select the argument address from the parameter type, never from callee-body
         analysis, so separate compilation and ABI behavior remain deterministic.
       * Permit calls such as `inspect(slug)` for `ref const T` and `output(slug)`
         for `ref writeonly T` when `slug` is split, while rejecting `modify(slug)`
         for ordinary `ref T`. Apply the same rules to absolute bindings with
         `none`, for example allowing a writable-only VSYNC binding to a
         `ref writeonly` parameter but rejecting readable or ordinary `ref` use.
       * Include access capability in function type compatibility and ABI
         fingerprints. Diagnose incompatible declarations, forbidden qualifier
         conversions, non-lvalue arguments, missing access locations, and unequal
         read/write addresses with the actual object and parameter names.
       * Test same-TU and separately compiled calls, arrays and subobjects,
         ordinary and split `mem` regions, absolute hardware-style bindings,
         cross-bank calls, and both successful directional binding and every
         rejected capability mismatch.

       Completed 2026-08-05:
       * Add `ref const T`, `ref writeonly T`, and ordinary `ref T` as distinct
         one-address contracts. Call lowering selects the read alias, write alias,
         or one proven shared address from the formal parameter capability; it
         never manufactures a fat pointer or guesses one side for ordinary `ref`.
       * Carry ref capability through callee lvalue checks, forwarding conversions,
         same-translation-unit function compatibility, and linker ABI metadata.
         Read/write refs may narrow to either restricted form; restricted refs may
         neither regain read/write access nor exchange const/writeonly capability.
       * Add exact-address execution, inline and separately compiled calls, arrays,
         structures and members, generic reversed/noncontiguous split regions,
         one-sided and split absolute bindings, same-TU and cross-object mismatch
         diagnostics, forbidden-operation tests, and an F8 cross-bank trampoline
         regression.

[x] 20. Generalize function region modifiers into a code-region set and one
       result-storage region using `mem` attributes.

       * Classify every named region modifier from its declared `mem` properties,
         never from a hard-coded spelling such as `bank0` or `superchip`. For a
         function declaration, read-only regions describe code placement; one
         writable region describes the hidden return object's storage.
       * Permit an order-insensitive mixture such as
         `bank0 superchip uint8_t function(void)`, with the function body in the
         read-only region and `function$__return` in the writable region. Preserve
         the read-only code-region set and writable result region as separate ABI
         facts even though this item emits at most one function-body copy.
       * Allow at most one writable result region, reject one on a `void` function,
         and diagnose duplicate, unknown, contradictory, or unsuitable region
         modifiers. With no writable modifier, retain the normal default return
         region; with no read-only modifier, retain automatic code placement.
       * Permit the semantic representation of more than one read-only code region,
         but until item 21 implements replication, reject a function which requests
         more than one with a deterministic diagnostic stating that multi-region
         code placement requires item 21. Do not partially duplicate or silently
         choose one region in this item.
       * Require declarations and definitions to agree independently on the code-
         region set and result-storage region. Map output must report the one actual
         code placement separately from the one return object.
       * Add order-independence and arbitrary-region-name tests, single-code-region
         plus writable-result tests, declaration/definition mismatch tests, and the
         explicit deferred-multi-region diagnostic consumed by item 21.

       Completed 2026-08-05:
       * Classify every named function modifier from its `mem` declaration:
         read-only regions form the code-placement set and one writable region
         selects the hidden result object. Modifier spelling and order have no
         semantic role; ordinary and split-address writable regions both work.
       * Emit code and result storage independently, preserve automatic/default
         placement when either role is absent, and compare the two facts
         independently in same-translation-unit compatibility and linker ABI
         metadata. Map output now shows the actual CODE layout separately from
         ordinary or split return-object activation storage.
       * Reject duplicate modifiers, multiple writable result regions, writable
         storage on `void`, inline region contracts, and multiple read-only code
         regions with the explicit item-21 deferral diagnostic. Add arbitrary-name,
         order-independence, code-plus-result, ordinary/split result, map-placement,
         and separate-object ABI mismatch coverage.

[x] 21. Add explicit multi-bank duplication for immutable objects and functions.

       * Accept an order-insensitive set of named bank-region modifiers, for
         example `bank0 bank1 const uint8_t table[] := { ... };`, and emit one
         identical ROM copy in every listed bank.
       * Bind each ordinary reference to the copy in the referencing function's
         current bank.  A reference from a bank without a declared copy is an
         error; the copies need not share one low-twelve-bit offset.
       * Permit the same syntax on functions, using item 20's region-role
         partition. Emit one function body per read-only region and resolve calls
         from a bank containing a copy to that local copy before considering a
         cross-bank trampoline. A writable modifier, when present, selects one
         shared return object rather than another function-body copy.
       * Require every declaration and definition to specify the same code-region
         set and result-storage region regardless of order; reject duplicate region
         names and mutable object duplication until explicit coherence semantics
         exist.
       * Combining `inline` with any named bank/memory-region specification,
         whether single-bank or multi-bank, is always an error.  Inline bodies
         have caller-selected placement and therefore cannot carry an independent
         bank-placement contract.
       * Report every replicated copy and its total physical ROM cost in the map,
         and test local binding, mixed pinned/unpinned callers, the complete
         `bank0 bank1 superchip` function form, function pointers remaining
         unsupported, and deterministic diagnostics for missing copies.

       Completed 2026-08-05:
       * Accept order-insensitive multi-region `$ro` contracts for immutable
         file-scope objects and non-inline functions. Emit one compiler body or
         initializer plus linker-visible replication metadata, then materialize
         independently placed private copies in every named bank region.
       * Resolve object relocations to the copy in the referencing function's bank
         and constrain automatic readers to banks with a local copy. Diagnose a
         pinned reader whose bank has no object copy. Resolve direct function calls
         to a local body first, with deterministic cross-bank trampoline fallback
         to the primary body when the caller's bank has no copy.
       * Preserve one logical symbol and one optional shared writable result object
         across all function bodies. Compare sorted function code-region sets,
         result storage, and replicated-object region sets in same-translation-unit
         and linker ABI contracts. Keep function pointers unsupported.
       * Reject duplicate regions, mutable object duplication, non-read-only or
         split-address replica regions, inline region contracts, and unusable
         unbanked/missing linker regions. Report every physical copy, load address,
         per-symbol cost, and aggregate replicated-ROM cost in the map.
       * Add F8SC local-copy and `bank0 bank1 superchip` execution, mixed automatic
         placement, F6 missing-copy/fallback and three-copy execution, separate-
         object matching, independent ABI mismatch, and compiler rejection coverage.
         Update compiler, ABI, linker, VCS-library, test-inventory, source-hygiene,
         and roadmap documentation.

[x] 22. Coalesce a single returned automatic variable with `$$`.

       * For a value-returning function where every reachable return is exactly
         `return variable;` for the same automatic local variable, allow that local
         and the hidden return object to occupy the same byte or bytes. Bind the
         source variable and `$$` as aliases of one allocation and eliminate the
         final copy into `function$__return`.
       * Apply the optimization only when the variable and return object have
         identical types and identical resolved storage specifications. The resolved
         storage specification includes memory-region identity, address size,
         placement class, and the complete read/write alias mapping. An explicit
         storage modifier must match exactly and must not be discarded merely because
         two regions currently map to the same physical addresses. Also require that
         the variable's address does not escape and that source code does not
         explicitly read or write `$$` in a way which would make the two objects
         observably distinct. Otherwise keep the existing separate allocations and
         copy.
       * When the return object is Superchip-backed, place the coalesced local in
         the same split-address allocation: reads use the read alias and writes use
         the write alias. Preserve the externally visible `function$__return` ABI
         symbol and report the local as coalesced with it in the map.
       * Test straight-line and branched functions, one- through four-byte values,
         BCD values, and Superchip return storage. Verify one physical allocation,
         absence of the redundant return copy, correct caller-visible results, and
         deterministic fallback for different return variables, non-variable return
         expressions, explicit `$$` access, escaping addresses, type mismatches, or
         any storage-specification mismatch. Include same-type locals whose explicit
         regions differ, including distinct regions which happen to map to the same
         physical addresses. Update language, ABI, map, and optimization documentation.

       Completed 2026-08-05:
       * Detect a single automatic local named by every return expression and alias
         it with the existing hidden return object only after exact type, const,
         pointer-access, width, named-region, placement-class, and split-alias
         checks. Keep by-value uses eligible while treating address-taking, `ref`
         binding, explicit `$$`, and inline assembly as observable escape hazards.
       * Emit no separate local allocation and no final return copy. Preserve the
         `function$__return` symbol and all existing caller behavior. Ordinary
         zero-page, named absolute, and split-address result storage all use the
         same activation-overlay allocation; split writes and reads retain their
         distinct aliases.
       * Add relocated linker metadata and a `RETURN COALESCING` map section with
         function, local, ABI return symbol, region, read/write addresses, width,
         and defining object. Count the physical storage once.
       * Add straight-line, branched, one- through four-byte binary and packed-BCD,
         ordinary absolute, Superchip-style split, safe by-value, and deterministic
         fallback coverage for every required mismatch or escape case. Update the
         compiler, ABI, linker, VCS-library, test-inventory, source-hygiene, and
         roadmap documentation.

[x] 23. Integrate automatic Superchip initialization and lifecycle tests.

       * Clear SC BSS and copy SC DATA through the write window during normal
         startup.
       * Prove that bank switches preserve SC contents.
       * Define reset/reinitialization behavior explicitly.
       * Add map, simulator, installed-toolchain, and authoritative Stella
         diagnostic-cart coverage, including a visible PASS/FAIL result for
         initialization, alias direction, persistence, and overflow handling.
       * Consider activation-overlay placement only after persistent allocation
         is stable and measured.

       Completed 2026-08-05:

       * Generalize linker startup records to carry distinct readable and writable
         runtime addresses. DATA copies and BSS clears use the write alias, while
         `STARTUP INITIALIZATION` map output reports load/read/write addresses,
         size, split status, and the explicit every-reset policy.
       * Define the lifecycle: physical power-on contents are unspecified; every
         entry through reset restores all allocated BSS/DATA declarations; mapper
         bank switches preserve shared Superchip bytes; unallocated bytes have no
         VCSC contract. Keep automatic locals in the already completed call-graph
         activation overlay rather than inventing a separate SC overlay policy.
       * Extend `vcsc-sim` with hostile split-memory fill and one-shot reset-at-PC
         controls. The reset preserves RAM and current bank selection before the
         vector fetch, exercising the real generated reset bridge and startup path.
       * Fold lifecycle coverage into the existing six complete-matrix diagnostic
         cartridges. SC builds own all 128 bytes as mixed BSS and DATA, validate
         startup, aliases, bounds, and matrix-wide persistence, poison the region,
         reset, and require a second PASS. Convert the existing poisoned reference
         to F8SC instead of adding an eighth cartridge.
       * Make Stella press console Reset before snapshotting every SC result and
         grade the second PASS/FAIL frame. Add installed-toolchain, map, hostile-
         fill, reset, overflow, documentation, and source-hygiene coverage.

[x] 24. Define generic C26 cartridge-output and bank topology.

       * Add separate `cartridge` and `bank` declaration namespaces to C26 and
         carry their complete declarations through compiler metadata and o26.
         The exact grammar must express fill, generated trampoline/vector ranges,
         physical image size, physical file index, mapped image offset, synthetic
         link start, CPU-visible start, mapped size, optional selector trigger,
         and optional startup status. Do not add a magic mapper keyword such as
         `cart f8`.
       * Define a bank without a selector as directly mapped and simultaneously
         available with every nonoverlapping direct bank. Define a bank with a
         selector as selector-controlled. Selector absence must never be treated
         as an incomplete declaration or cause generated switch code.
       * Permit the same identifier in the `bank` and `mem` namespaces. Source
         placement modifiers always resolve to `mem`; bank names are configuration
         handles only. Reject all name-derived behavior.
       * Use `$select_access` for the current any-access F8/F6/F4 hotspots and leave
         an explicit extension path for read-, write-, masked-, or value-qualified
         future selectors. Generated bridges must continue using the declared
         trigger.
       * Merge byte-identical declarations across translation units and reject
         conflicting declarations with source locations. Validate unique/dense
         file indices, physical bounds, nonoverlapping direct mappings, unambiguous
         selector-controlled mappings, selector validity, generated-range bounds,
         and exactly one startup bank when selector-controlled banks exist.
       * Add parser, metadata, malformed-topology, separate-compilation, direct-
         bank packaging, switched-bank, map, and source-hygiene tests. Keep current
         cfg behavior available during migration.

       Completed 2026-08-05:
       * Add exact C26 `cartridge { ... };` and `bank name { ... };` syntax
         with separate `bank` and `mem` namespaces. Emit fixed-version compiler
         metadata for fill/generated ranges and each bank's physical image, file
         index, image offset, synthetic link mapping, CPU mapping, selector, and
         startup status. Identical declarations merge across translation units;
         conflicting declarations identify both defining objects.
       * Validate dense unique file indices, image/mapping bounds, nonoverlapping
         link mappings, direct CPU mappings, selector ranges and uniqueness,
         generated-range bounds/overlap, and selector startup cardinality. Keep
         direct banks selector-free and reject mixed direct/selector topologies
         until an explicit window model exists.
       * Package direct-bank output in declared file order without generating any
         selector, bridge, or trampoline code. For selector-controlled topology,
         require semantic agreement with the retained cfg during migration and use
         the declared image layout for flat output while preserving existing F8
         bridge/trampoline behavior.
       * Add `C26 CARTRIDGE TOPOLOGY` map reporting, parser/metadata/error coverage,
         same-name `bank`/`mem` coverage, separate-compilation merge/conflict tests,
         direct two-chunk packaging and absolute cross-chunk calls, switched F8
         integration, malformed-topology diagnostics, and grammar coverage.

[x] 25. Make C26 `mem` declarations authoritative and derive output-bank ownership.

       * Adopt the allocatable-bytes-only interpretation of `mem`. Do not include
         RAM-port prefixes, trampoline corridors, bridges, vectors, selector bytes,
         or fill holes in an allocatable region.
       * Remove the need for cfg MEMORY duplication and for a `mem $bank` property.
         Infer one output owner only when a region's complete synthetic link range
         lies inside exactly one declared bank mapped link range. Treat no match as
         shared/unpackaged and multiple matches as a configuration error. Never
         infer ownership from names or CPU-visible mirrors.
       * Keep output ownership separate from access behavior. References between
         direct banks remain ordinary absolute references. References between
         distinct selector-controlled banks use selector/trampoline machinery.
         Shared regions never cause a transition by themselves.
       * Prove that F8SC split aliases at `$F000/$F080` remain shared because the
         switched-ROM bank mappings begin at `$x100`; accesses to Superchip objects
         must never acquire selector code. Test deliberately mismatched bank/mem
         names to prove names are irrelevant.
       * Synthesize ordinary CODE, RODATA, DATA, BSS, STARTUP, private-layout, and
         region-suffixed routing from authoritative `mem` metadata, access type,
         priority, startup-bank ownership where applicable, and existing placement
         rules. Preserve split aliases, alignment, page constraints, and
         deterministic allocation.
       * Report each region's inferred output bank and direct/switched/shared mode
         in the map and add differential tests against current cfg MEMORY/SEGMENTS
         results.

       Completed 2026-08-05:
       * Emit one versioned authoritative metadata record for every complete C26
         `mem` declaration, even when no object currently uses the region. Merge
         identical declarations after archive selection and reject cross-object
         conflicts with both original C26 locations. Preserve empty policy-only
         `mem name { };` declarations without creating allocator bytes.
       * Create or overwrite linker allocator regions from C26 start/read/write,
         size, access type, and priority. Repeated cfg MEMORY entries retain only
         migration-era operational properties such as call-stack reservation,
         file backing, and fill policy; stale addresses, sizes, types, or absent
         entries no longer override source declarations.
       * Infer output ownership solely by unique containment of a region's complete
         synthetic range in one C26 bank mapping. Record direct, switched, or
         shared mode independently from names and CPU-visible aliases; diagnose
         ambiguous containment with the candidate bank names. Selector-controlled
         owners are matched to the retained mapper mechanics by declared hotspot
         during migration.
       * Synthesize STARTUP, CODE, RODATA, DATA, BSS, ZEROPAGE, and every ordinary
         region-suffixed route from authoritative memory metadata. Choose defaults
         by source priority and startup-bank ownership, preserve explicit cfg
         alignment/start constraints, retain split read/write aliases, and keep
         deterministic region-local activation allocation.
       * Adopt allocatable-bytes-only VCS ROM declarations: the unbanked default is
         `$F000-$FFF9`; banked diagnostics suppress it and declare their actual
         ordinary or SC bank ranges. F8SC topology maps switched ROM from `$x100`,
         so Superchip `$F080/$F000` remains ownerless/shared and its generated
         loads/stores contain no hotspot access.
       * Add an allocator-free direct-bank link, mismatched bank/mem-name coverage,
         a minimal-hardware-only F8SC cfg, direct Superchip alias byte checks, real
         switched-call coverage, full-cfg/minimal-cfg byte equality, unused and
         separate-object declaration metadata, stale/missing cfg success cases,
         ambiguous-owner diagnostics, map reporting, installed diagnostic builds,
         documentation, and source-tree hygiene checks.

[x] 26. Migrate existing cartridge profiles and add a direct-bank packaging profile.

       * Provide explicit 4K, F8, F6, F4, F8SC, F6SC, and F4SC topology files whose
         contents, not filenames, define behavior. A build may include or compile
         one as a configuration-only C26 input.
       * For ordinary F8/F6/F4, map the complete 4K physical chunk as switched ROM
         and expose only the `$x000-$xEFF` allocator region. For SC variants, keep
         a 4K physical chunk but map ROM only from physical offset `$0100` and CPU/
         link address `$x100`, expose `$x100-$xEFF` for allocation, and declare one
         shared 128-byte split-address `mem` region.
       * Add one generic direct-bank profile containing at least two nonoverlapping
         logical mappings and no selectors. Prove flat chunk ordering, fill,
         ordinary absolute calls/jumps/data references across direct banks, vector
         reservation, and the complete absence of selector/trampoline output.
       * Preserve exact file order, image sizes, selector behavior, bridges,
         vectors, startup placement, simulator behavior, Stella behavior, map
         accounting, and installed-toolchain results for every existing diagnostic.
       * Retain cfg parsing temporarily for compatibility and run byte-for-byte or
         semantically exact differential tests. Remove cfg use from public VCS
         builds only after all migrated profiles certify cleanly.

       Completed 2026-08-05:
       * Add explicit installed `vcs_2k.c26`, `vcs_4k.c26`, `vcs_8k_f8.c26`,
         `vcs_16k_f6.c26`, `vcs_32k_f4.c26`, and matching F8SC/F6SC/F4SC
         profiles. Each file contains the complete inspectable cartridge, bank,
         and allocatable-memory declarations; behavior is derived solely from
         those values rather than the filename.
       * Move the conventional unbanked ROM declaration out of `vcs.c26` and
         into `vcs_4k.c26`. The machine header now describes only common types,
         RIOT RAM, and hardware registers. A cartridge profile may therefore be
         included for source-visible named placement or compiled independently
         as a configuration-only input without a suppression macro.
       * Reduce public-build linker policy to `vcs.cfg`, which retains dynamic
         RIOT hardware-stack reservation but no cartridge, bank, allocator, or
         ordinary segment topology. When C26 topology is present, construct the
         direct or selector-controlled internal bank model from it and ignore
         legacy cfg CARTRIDGE/BANKS mechanics. Retain the old profile cfg files
         only for compatibility, differential certification, and simulator
         mapper selection.
       * Make command-line objects carrying complete cartridge, bank, or memory
         metadata survive selection even when they export no referenced program
         symbol. Preserve the requirement that source using `bankN` placement
         include the corresponding profile so the compiler sees that `mem` name.
       * Add the selector-free `vcs_direct_8k.c26` profile with two directly
         mapped 4K chunks. Certify explicit file order, fill, vector reservation,
         ordinary absolute cross-chunk JSR/data access, and the absence of
         hotspot, bank-placement, bridge, and trampoline output.
       * Migrate the public seven-cartridge bank diagnostic and installed builds
         to C26 topology through reduced `vcs.cfg`. Add the conventional direct
         2K `$F800-$FFFF` profile and preserve exact 2K/4K/8K/16K/32K
         output, startup bank, selectors, generated bridges/vectors, Superchip
         prefix and shared aliases, simulator execution, and map accounting.
       * Add differential coverage proving every migrated C26 profile emits the
         same bytes as its retained legacy cfg, driver-default 4K equality,
         configuration-only input selection, installation/uninstallation,
         documentation, and source-tree hygiene.

[ ] 27. Move component-specific placement constraints out of cfg files.

       * Add source/assembler object metadata for required `mem` region, alignment,
         page containment, private segment routing, and hidden hardware-stack use
         such as the standard renderer's current `callstack_extra` allowance.
       * Make each renderer or assembly component carry its own constraints. The
         cartridge topology file must describe cartridge hardware and allocatable
         memory only; it must not know which renderer is linked into the program.
       * Migrate the existing unbanked standard-renderer profile and prove that its
         image, timing, stack reservation, segment placement, and diagnostics match
         the current renderer-specific cfg. Add conflict and malformed-metadata tests.
       * Define the source-tree rule which forbids new renderer x mapper cfg products.

[ ] 28. Add the first composable banked profile for the maintained standard renderer.

       * Use the existing standard all-five P0/P1/M0/M1/Ball renderer, not
         multisprite. Multisprite remains deferred until the main-roadmap
         multisprite source contract and minimal unbanked profile are complete.
       * Compose one renderer object carrying its own constraints with the generic
         F8 C26 cartridge topology. Keep renderer code and all beam-critical ROM
         data in the startup bank; place real game/overscan work in another bank and
         switch only while VBLANK is asserted. Prove return to the renderer bank
         before the next beam-critical phase.
       * Exercise the same renderer/component description against F6 and F4 in
         private build/test coverage rather than creating public profile copies.
         Add one F8SC case which keeps timing-critical renderer workspace in RIOT
         RAM while using shared Superchip RAM for non-critical game state.
       * Measure per-bank ROM, replicated bridge bytes, switch/call cycles, RIOT
         RAM, Superchip RAM, hardware-stack depth, frame length, and raster identity
         against the unbanked profile. Keep public diagnostic count consolidated.
       * Add map assertions, installed-toolchain coverage, authoritative Stella
         raster/timing coverage, documentation, and source-hygiene enforcement.

[ ] 29. Improve automatic placement heuristics without changing semantics.

       * Measure whether call-frequency weighting, component ordering, or local
         moves materially reduce bridge count, cycles, and hardware-stack cost.
       * Keep all explicit pins and hard ROM-data components inviolate.
       * Preserve deterministic output and offer a diagnostic mode which explains
         why each component was placed where it was.
       * Permit a simple-placement mode for debugging and reproducibility; never
         fall back to invented cross-bank data access.

[ ] 30. Consider another mapper family only when a concrete program requires it.

       * Do not build a universal mapper-description language merely to reproduce
         every historical circuit. Full-window selector-controlled banks, direct
         banks, shared memory, and explicit replication are the maintained model.
       * If a real program eventually requires independently switched windows,
         value-controlled selection, banked RAM, or another unsupported behavior,
         design that family separately rather than distorting the current bank
         abstraction in advance.
       * Reuse logical mapping, vector, output, relocation, and reporting pieces
         where honest; add a true window or device model only when the chosen
         hardware semantics actually require one.

Decisions intentionally deferred
--------------------------------
The following are not settled by this document:

* whether cfg remains a supported generic escape hatch after all public VCS
  profiles migrate, or becomes test-only compatibility code;
* whether a future self-describing image container is warranted beyond the
  current deterministic flat concatenation of declared output-bank chunks;
* the exact size/location of the common bridge corridor beyond the currently
  certified full-window values;
* whether non-public or future mapper profiles may use a startup bank other
  than BANK0 while still keeping public full-window profiles and `main` in BANK0;
* whether a concrete use case eventually justifies a built-in split/fat
  pointer rather than explicit `&<`/`&>` projections stored in a user-defined
  structure;
* whether measured programs justify profile-guided edge weights, explicit
  same-bank groups, translation-unit preferences, or a deterministic local-search
  pass beyond the completed greedy placer.

Those choices should be made in the roadmap slice that can implement and test
them, not guessed here and fossilized without evidence.
