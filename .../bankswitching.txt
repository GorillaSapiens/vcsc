VCSC BANKSWITCHING: DURABLE MODEL AND OPEN WORK
==============================================

Purpose
-------
Keep only cartridge-model rules that future mapper work must not rediscover and
unfinished bankswitching items. Completed implementation narratives are archived
under `.../context-history/`; the pre-cleanup full document is in 2026-08-22.

Durable identity model
----------------------
Never use bare "bank 0" without saying which identity is meant. Keep these three
facts separate:

1. **VCSC logical bank** -- source/linker namespace. For the classic full-window
   family BANK0 is the $F000 home/startup bank, BANK1 is $D000, then descending
   by $2000.
2. **Physical/file bank index** -- zero-based emitted chunk number. Output is in
   ascending logical-address order, so VCSC BANK0 is always the final 4K chunk
   in the file for F8/F6/F4.
3. **Mapper selector hotspot** -- hardware address selecting a physical chunk.

Classic full-window mapping:

    mapper  file index  VCSC bank  linker range  selector
    ------  ----------  ---------  ------------  --------
    F8      0           BANK1      $D000-$DFFF   $1FF8
            1           BANK0      $F000-$FFFF   $1FF9

    F6      0           BANK3      $9000-$9FFF   $1FF6
            1           BANK2      $B000-$BFFF   $1FF7
            2           BANK1      $D000-$DFFF   $1FF8
            3           BANK0      $F000-$FFFF   $1FF9

    F4      0           BANK7      $1000-$1FFF   $1FF4
            1           BANK6      $3000-$3FFF   $1FF5
            2           BANK5      $5000-$5FFF   $1FF6
            3           BANK4      $7000-$7FFF   $1FF7
            4           BANK3      $9000-$9FFF   $1FF8
            5           BANK2      $B000-$BFFF   $1FF9
            6           BANK1      $D000-$DFFF   $1FFA
            7           BANK0      $F000-$FFFF   $1FFB

    file_index(BANKn) = bank_count - 1 - n

E0 is segmented rather than full-window: eight physical 1K chunks feed three
independently selectable windows plus a fixed top chunk. `$1000-$13FF` selects
with `$1FE0-$1FE7`, `$1400-$17FF` with `$1FE8-$1FEF`, and `$1800-$1BFF` with
`$1FF0-$1FF7`; `$1C00-$1FFF` is always physical bank 7. Power-on mapping is
physical banks 4/5/6/7. One physical chunk may appear in more than one window,
so do not model E0 as one selector per bank or as four F8-style whole windows.

FE/SCABS is also not an F8-style hotspot mapper. The released two-bank
layout stores physical/file bank 0 as the `$F000-$FFFF` startup view and
physical/file bank 1 as the `$D000-$DFFF` alternate view. Any access to mirrored
`$01FE` arms a latch; the **following** bus cycle's data selects the bank
(`E/F` high nybbles select physical bank 0, `C/D` select physical bank 1).
With `SP=$FF`, a top-level `JSR $Dxxx` pushes its low return byte to `$01FE` and
the following target-high fetch selects bank 1. `RTS` later reads `$01FE`,
arming the same latch so the following caller-high stack byte restores bank 0.
VCSC therefore pins automatic FE ROM to startup bank 0, permits explicit bank-1
placement, and only lowers a cross-bank JSR directly when its caller is
`main`; cross-bank data, branches, jumps, and nested calls are rejected rather
than pretending FE has a generic trampoline corridor.

WD/Wickstead Design physically uses eight 1K chunks and eight complete
four-segment arrangements. Power-on state 0 is `0,0,1,3`; reads of TIA
`$30-$3F` select states 0-7 only after the hardware delay. Ordinary 6502 code is
not position independent, so the compiler ABI deliberately exposes only the two
complementary relocation-safe arrangements: logical bank0 is state 1
`0,1,2,3`, selected by read `$39`/descriptor 1; logical bank1 is state 2
`4,5,6,7`, selected by read `$3A`/descriptor 2. The other six states remain
real simulator/hardware behavior for deliberate assembly but are outside the
compiler ABI. WD owns 64 bytes of always-live split RAM: reads `$1000-$103F`,
writes `$1040-$107F`; both logical banks reserve the hidden first `$80` bytes.
The corrected public 8192-byte `WD` image remains distinct from historical
8195-byte `WDSW`. Public WD C26 code uses the `$40-$7F` TIA mirror.

Classic 3F and 3E reserve the low TIA write page for mapper control. Public
profiles therefore bind ordinary TIA accesses through the equivalent `$40-$7F`
mirror; 3E mapper selection itself remains literal `$3E`/`$3F`. Do not depend
on an emulator forwarding non-hotspot low-page writes to the TIA.

FA2 extends the FA split-RAM model to six or seven directly selected 4K ROM
banks. Public VCSC profiles are clean 24K and 28K payloads: physical/file bank 0
is the startup bank, `$1FF5-$1FFA` select banks 0-5, optional `$1FFB` selects bank
6, and the first `$200` bytes of every selected bank are hidden by the 256-byte
cartridge-RAM ports (write `$1000-$10FF`, read `$1100-$11FF`). VCSC emits the
native 24K/28K payload rather than Harmony's 29K/32K wrapper forms. Harmony
`$1FF4` flash persistence is optional external behavior and is not part of the
core compiler/linker/simulator contract.

DPC is F8 program banking plus two non-CPU-addressable image regions. Physical
file chunks 0/1 are the ordinary 4K F8 program banks; file chunk 2 is 2048 bytes
of display data consumed by the DPC fetchers; file chunk 3 is the conventional
255-byte Poly8 sequence tail. Model the latter two as `$data_only` topology banks
backed by read-only `$data_bank:bankN` allocator regions, never by invented 6507
addresses. The runtime register window is `$1000-$107F`, so the program banks map
only `$1080-$1FFF`. The Poly8 generator is hardware state: resetting the RNG sets
it to 1 and each DPC access clocks it before the addressed register operation.
The 255-byte image tail is therefore reference/format data, not CPU-mapped ROM.

Ordinary linker symbols remain 16-bit. Do not invent far pointers for this
family. Selector-controlled families, segmented-window families, direct islands,
and coprocessor formats are different hardware models and must not be forced into
an F8-shaped abstraction.

C26 topology/placement rules
----------------------------
* Cartridge **bank** topology and allocatable **mem** regions are separate
  namespaces and separate concepts. A bank describes an emitted/mapped hardware
  unit; `mem` describes bytes available to the allocator.
* Names such as `bank0` have no magic compiler meaning. Startup/home behavior
  comes from topology metadata, not spelling.
* Explicit source placement is a hard pin. Automatic placement may move ordinary
  CODE/RODATA only where topology and timing constraints permit it.
* Selector-free direct regions are ordinary 16-bit address spaces and do not
  require generated bank-switch trampolines.
* Selector-controlled cross-bank calls use generated transitions; preserve
  hardware-return-depth accounting and branch/page timing constraints. Ordinary
  direct `JMP` is a hard same-bank constraint and never gets a mapper trampoline.
  The only mapper-changing one-way JMP is the replicated reset/vector bridge:
  mapper `entry.s26` normalizes startup state, then the bridge JMPs to the normal
  handler. Mapper
  diagnostics must distinguish "every selector was touched" from exhaustive
  ordered source/destination bridge coverage. The durable regression is
  `test/vcs_bankswitching_call_matrix.pl`: one ROM per source bank calls every
  destination bank so every legal generated JSR/RTS pair is executed. The legacy JSR path emits one 15-byte replicated entry per unique source-bank/
  target-function pair, so target diversity consumes the common corridor. Migrated
  profiles use the fixed inline-target block instead; keep the legacy path only
  for mapper families/lower-level inputs not yet migrated.
* Split-address cartridge RAM keeps distinct read/write aliases. Do not collapse
  them into one address for RMW or initialization semantics.
* Mapper hotspots, vector bridges, RAM-port prefixes, signatures, and other
  nonallocatable holes are not ordinary ROM placement space.
* `$data_only` banks have file identity and size but no CPU/link address, selector,
  or startup state. Their `$data_bank:NAME` read-only `mem` regions allocate in
  bank-local file-offset space; code and ordinary 6507 relocations into them are
  errors.

Mapper signature convention
---------------------------
Public VCSC cartridge profiles reserve four bytes at logical `$xFF8-$xFFB` in
the final CPU-mapped program bank. This normally is the final file chunk; DPC is
the exception because its data-only chunks follow the program ROM. Short
signatures are padded with ASCII NUL, never space or underscore. Current
signatures include:

    4KSC  F8\0\0  F8SC  F6\0\0  F6SC  F4\0\0  F4SC  FA\0\0
    CV\0\0  OMNI  JANE  0840  UA\0\0  UASW  0FA0  E0\0\0  FE\0\0  WD\0\0  DPC\0  FA2\0  3F\0\0  3E\0\0  FC\0\0

The bytes are metadata; real hardware switches because the hotspot address is
accessed, not because of the stored byte value.

Descriptor ABI contract
-----------------------
Generic inline-target cross-bank JSR contract
---------------------------------------------
Public ABI authority: `BANKSWITCHING.md`. Do not duplicate the full contract in
this hot file. The target ABI is a six-byte cross-bank call: JSR plus a
three-byte `.banktarget` payload containing the 16-bit target CPU address and one
opaque mapper-defined destination descriptor. Each mapper-specific replicated entry/return saves the one-byte source state
needed for restoration. Most mappers bake it into each bank copy; E0 instead
pushes its dynamic canonical state byte because resident banks 6/7 do not reveal
which lower pair is selected. The return half restores that byte/state before
the final RTS to the unchanged original 16-bit logical return PC. Same-bank
calls remain ordinary JSRs. Neither source
nor destination bank identity may be inferred from the address once this ABI is
implemented.

The descriptor is mapper-owned. Useful values include selector-hotspot low
bytes, selector write values, indexed-selector offsets, or simple bank IDs. The
linker carries the destination byte but does not interpret it. One byte must be
sufficient to select/restore every compiled-code bank participating in
`$bankcall`; otherwise that mapper needs a different or future wider ABI.
The source descriptor occupies one extra hardware-stack byte per active
cross-bank call. The inline payload grows from two to three bytes, so stacked-PC
continuation adjustment must be carry-correct by +3 and page/bank-edge tests must
move accordingly.

Implementation checkpoint: the compiler/assembler/linker use the six-byte
descriptor ABI and `__bankmeta$V3`. F8/F8SC/F6/F6SC/F4/F4SC, FA, DPC,
FA2-24/28, JANE, 0840, UA, UASW, 0FA0, WD, 3F, 3E, FC, F0, and E0 consume it end to end and
those migrated profiles declare `$bankcall` unconditionally. They own
`bankcall.s26` and variable-length `entry.s26`; maintained entry bytes run before
the normal vector handler. Selector-normalizing hooks use raw `op0C` for fixed
selector reads, avoiding an assembler `--illegal` dependency. Zero-length entry
fragments are valid; 3F, 3E, FC, F0, and E0 use one where hardware already guarantees the startup mapping.
FA2 uses
descriptors `$F5-$FA/$FB` and raw `op1C $1F00,X`; JANE uses
`$F0/$F1/$F8/$F9` and raw `op1C $1F00,X`; 0840 uses `$00/$40` and raw
`op1C $0800,X`; UA/UASW use `$20/$40` and raw `op1C $0200,X`, with UASW
reversing the association; 0FA0 uses `$A0/$C0` and raw `op1C $0F00,X`; WD
uses descriptors 1/2 and raw `op1C $0038,X` to choose hardware states 1/2.
3F lower banks use selector-value descriptors `$00-$FE`; fixed uses `$FF` as a
no-switch sentinel and writes lower selectors with `STX $3F`. FC descriptors are
physical bank IDs `$00-$FF`; the trampoline stages low bits at `$1FF8`, high bits
at `$1FF9`, and commits via `$1FFC`. F0 uses physical bank IDs `$00-$0F`;
its replicated trampoline advances `$1FF0` `(destination-source)&15` times and
uses the inverse modulo-16 distance on return. F0 powers up in physical bank 15,
and its code allocation stops below `$FFF0` so ordinary code cannot trip the
hotspot by instruction fetch. The common blocks use 68 active bytes with 72 reserved
(`$048`); 3F uses 74 active bytes with 80 reserved (`$050`); FC reserves 112 bytes (`$070`);
F0 reserves 96 bytes (`$060`). Only the descriptor ABI is retained for
migrated inline mappers.

Migration acceptance for each remaining mapper: consume the destination
descriptor directly; bake and push the source descriptor; restore from that byte
on return; remove PC-to-bank decoding; preserve A:X, nested LIFO behavior,
same-bank ordinary JSRs, complete ordered call-matrix coverage, and zero
per-target JSR entries. Descriptor-specific bytes may differ between bank copies
only where they are consumed safely before a switch.

Field-diagnostic timing fallout from F4SC descriptor conversion is resolved.
The diagnostic now has bank-local joystick, paddle, keypad, and driving frame
paths and bank-local deadline pollers; only joystick retains TIA/switch/collision
work. Leaving joystick clears AUDV0/AUDV1. Paddle uses an aligned four-INPT
snapshot with one timestamp; channel-1 completion is committed on a blank
alignment line so multi-frame high-resistance completion cannot shift visible
GRP0 timing. NTSC 0..255 thresholds, independent displayed-95 high-end cases,
and NTSC/PAL/SECAM moving-input timing pass.

Automatic-call mapper migration is complete.

E0 compiler topology is deliberately constrained to three canonical states:
`[0,1,6,7]`, `[2,3,6,7]`, and `[4,5,6,7]`. Banks 0/2/4 are linked at `$1000`,
banks 1/3/5 at `$1400`, bank 6 at `$1800`, and fixed bank 7 at `$1C00`.
Descriptors 0/1/2 select the lower pair; resident banks 6/7 use `$FF`. One RIOT
RAM byte tracks only the canonical state so nested calls made by resident code
can restore the caller's lower pair. Hardware startup `[4,5,6,7]` is already
state 2, so E0 entry code is empty. Arbitrary E0 window arrangements are outside
the automatic-call ABI. The maintained diagnostic covers all 8x8 ordered calls,
resident-bank nested restoration, A:X preservation, legal same-state ROM data
references, rejection of cross-state ROM data, timing, autodetection, and exact
disassembly roundtrip.

Deferred design questions
-------------------------
Decide only when a concrete task needs them: long-term cfg compatibility policy,
a self-describing image container, future startup-bank conventions, fat/split
pointer support, and profile-guided placement heuristics.
