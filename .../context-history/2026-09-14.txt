2026-09-14 09:10:15 PDT, correct split-RAM bus semantics
Audited A5/A7 and detached speculative memory semantics after repeated mapper
regression whack-a-mole. The prior split-RAM model was electrically wrong:
writes to a read alias are legal ignored cycles, while reads from a write alias
return an unpredictable read-from-write-port value and overwrite the backing RAM
byte. RMW is therefore legal on either alias and is now diagnostic/neutral--it
neither vetoes nor proves Superchip, FA/FA2, CV, WD/WDSW, E7, or 3E. Only pure
direction-correct split-RAM accesses may contribute positive mapper evidence.

Separated A5 bus legality from detached-island usefulness. Writes to mapped ROM
remain legal for mapper viability but are not meaningful speculative write sinks;
this fixes the plain-F8 speculative Superchip-store regression without restoring
the false ROM-write contradiction. Added a direct established ROM-RMW legality
regression instead of using speculative-island promotion as a proxy for bus
validity.

Cartridge-RAM provenance now models the hardware side effect: an exact read from
a write alias destroys the prior backing-byte source; unknown absolute indexing
enumerates all 256 possible aliases, and unresolved indirect reads conservatively
drop split-RAM provenance. Writes to read aliases preserve the old backing source.
New FA regressions pin exact RWP destruction, unknown-index destruction, and
ignored read-alias writes.

Removed obsolete split-RAM-RMW hard-failure contracts and renamed stale fixtures.
The complete current generated disassembler corpus round-trips 206/206 byte-exact,
the full assertion tail reports `vcsc-disas regression suite ok`, and the
hardening suite passes. Also corrected two buried JANE/F6 hypothesis-count checks
from 6 to 7 after CM became a legitimate 16K hypothesis.

2026-09-14 09:58:00 PDT, finish split-RAM provenance and graphics audit
Closed two remaining abstraction leaks after re-reading the project instruction
contract. Unknown absolute-index stores now enumerate possible effective
addresses and invalidate any split-RAM backing-byte provenance they can alter;
unresolved destructive reads remain conservative. CM read-mode upper-RAM writes
are legal no-ops and therefore are not meaningful detached speculative sinks.

Static graphics/color recovery now requires the source address to be definitely
ROM under the selected mapper. This prevents Superchip/native split RAM and
configuration-dependent E7/3E/GL/CM overlays from misclassifying hidden physical
ROM bytes as GRP/PF graphics. Added regressions for exact and indexed destructive
write-port reads, ignored read-alias writes, unknown-index RAM stores, and an SC
hidden-ROM-to-GRP false positive.

The generated disassembler corpus is now 208/208 exact round trips. The complete
main assertion tail reports `vcsc-disas regression suite ok`; the intentional
`speculative_without_reset.bin` hard failure remains expected.

2026-09-14 10:28:00 PDT, Perl requirement and disassembler timeout
Perl is an ordinary VCSC source-build requirement. Removed the accidental
special treatment of compiler/coverage_map.h as if it supported a partial
Perl-free bootstrap: coverage_map.h is generated from parser.y by coverage.pl,
is ignored as generated output, make clean removes it, and the Linux/Windows
package targets no longer regenerate it after their final clean. README.md,
LINUX.md, and WINDOWS.md now list Perl explicitly among build prerequisites.

The main vcsc_disassembler.pl regression had grown far beyond the generic
45-second per-test budget because it generates and round-trips more than 200 ROM
fixtures plus mapper/speculation/presentation assertions. A standalone measured
run completed successfully in 139.54 seconds, so the test now declares a
600-second test-local timeout. The global 45-second default is unchanged; the
larger ceiling is intentionally isolated to this heavyweight integration test
so parallel-suite CPU contention does not create false timeouts.

2026-09-14 11:17:27 PDT, finish ROM and half-heart score renderer
Replaced the 58-byte self-modifying RIOT-RAM heart renderer with hard-page ROM
renderers and reduced one component instance to 8 RIOT-RAM bytes. The twelve
full-heart renderers preserve the certified 0..11 footprint and their existing
Stella hashes. Added `half` state support through 11.5 hearts: 0.5 uses one P0
right-half sprite, while 1.5..11.5 use Ball plus M0 without moving any full heart.

Corrected the overlay positioner root cause rather than carrying score-specific
phase hacks. Ball/M0 table lookup and clamping now happen before WSYNC so the
post-WSYNC divide-by-15 RESP/HMxx sequence has the same beam phase as the
maintained standard positioner. Independent pixel-oracle checks prove all twelve
half states are exactly N full hearts plus the right half of the next slot, with
no stray Ball/M0 pixels. Full-state hashes remain unchanged.

Moved the public heart example to 4K and made it cycle through half-heart steps.
The example currently links at 2540 ROM bytes and 20 total RAM bytes, of which 8
belong to the heart component. Extended structural and Stella regressions to all
24 states and added a fast private-Xvfb snapshot path so the 24-launch visual
certification does not repeatedly invoke xkbcomp. Focused structural and public-example builds pass, the final Stella run passes all 24 states, and branding, source-tree hygiene, context-history format, and Perl syntax checks pass.

2026-09-14 12:03:42 PDT, stabilize example-shard timeout budget
The 0..11.5 heart example inserted early in the sorted editable-example list and
therefore rotated every later `index % 8` round-robin assignment by one shard.
That moved the previously heavyweight shard-1 mapper/diagnostic workload into
shard 2, but only shard 1 had a 180-second ceiling; shards 2..8 still used 90
seconds. A complete user suite consequently passed 365/366 with shard 2 timing
out despite no build or round-trip failure.

Kept the existing eight-way round-robin parallelism and made all eight shard
descriptors use the same 180-second ceiling. This is intentionally uniform:
future example insertion/removal may rotate the heavy group to any shard number.
Documented that contract in test/README.md and made source_tree_hygiene.pl lock
the uniform ceiling. On the current tree shard 2 completes 14/106 editable
examples plus byte-exact disassembler round trips in about 38 seconds in the
container; the focused test-runner shard and source-tree hygiene both pass.

2026-09-14 15:14:56 PDT, left-half heart geometry and 11-heart cap
The heart meter now exposes the conventional sequence 0, 0.5, ... 10.5, 11;
11.5 is deliberately not a state. At score 11, `half` is ignored visually and
the half-control path renders the same eleven-heart raster as the full path.
The public example was changed to step directly from 10.5 to 11 to 0.

Replaced the Ball/M0 right-half overlay with player-only left-half rendering.
0.5 uses the high nibble of the normal P0 heart glyph. 1.5..9.5 preserve the
Jentzsch row schedule and use stable SAX stores with X=$F0 to write A&$F0 into
the next P0/P1 copy. 10.5 uses an unrolled six-row renderer because the ten-heart
loop has no spare four-cycle slot for SAX. The fixed-cycle dispatcher now
cycle-balances X=0 for full renderers against X=$F0 for half renderers. Obsolete
Ball/M0 position/motion tables and overlay code were removed; one component
instance remains 8 RIOT-RAM bytes.

Stella 7.0 validation passed all twelve full states plus all twelve half-control
combinations. The twelve historical full hashes are unchanged. Independent
pixel comparison of every 0.5..10.5 screenshot against adjacent full-score
screenshots proved each half raster is exactly N full hearts plus the left eight
pixels of heart N+1, with zero mismatched pixels; score 11 with `half=1` hashes
identically to ordinary score 11. Updated the structural regression, Stella
golden hashes, library documentation, focused heart note, hot context, and the
public example. `vcs_heart_score.pl`, `vcs_heart_score_stella.pl`, and the public
example build all pass; the example currently uses 2812 ROM bytes and 20 total
RAM bytes.


2026-09-14 16:32:26 PDT, heart composition demos
Added two interactive 4K cartridges under `examples/01_basic/14_heart_score`
that compose the seven-line heart meter with the maintained 181-line
`player_color` renderer in both visible orders. A four-line blank separator
makes 181 + 4 + 7 = 192 visible scanlines. The gameplay scene is intentionally
static so the actual renderers fit together in plain 4K rather than introducing
bankswitching solely for a composition diagnostic. Both images currently link
at 3876/4090 ROM bytes and 43/128 RAM bytes.

Right joystick UP/DOWN changes the meter by one half-heart per neutral-separated
press across 0, 0.5, ... 10.5, 11; endpoint input clamps. The examples start at
5.5 and Reset restarts the scene. A new focused e2e regression builds both ROMs,
checks composition order/handshake and the right-controller contract. The Stella
7.0 certification now also snapshots both top/bottom compositions at neutral,
held UP, and held DOWN; from 5.5 those held inputs produce exactly 6.0 and 5.0.
The focused example, core heart, and extended Stella tests pass. Both new ROMs
also disassemble/reassemble byte-exact (2/2). A broad e2e run reached 229/367
with zero failures before the container's 300-second execution cutoff; a later
all-example shard run completed shards 1-3 cleanly before its separate wall-time
cutoff, so no source failure was observed in either interrupted broad run.

2026-09-14 17:46:19 PDT, make heart draw order independent
The bottom-mounted interactive heart demo exposed a real integration defect in
low states 0.5 through 3.0. Those states use singleton P0/P1 copies, and the
component had positioned those copies during VBLANK. A visible player renderer
running before the score legitimately rewrote RESP0/RESP1, so the later heart
draw inherited gameplay X positions. States 3.5 and above happened to repair
their positions with their own timed RESP stream, which hid the bad ownership
assumption in the original composition snapshots.

Moved the singleton base placement into the heart draw's existing visible setup
line without increasing the seven-line footprint or RIOT-RAM use. The setup
clears inherited graphics, issues absolute RESP0/RESP1 strobes four CPU cycles
apart, applies a late $50 HMOVE with missile/Ball motion neutralized, and then
restores the score-dependent Jentzsch NUSIZ/HMP state during the fixed 38-cycle
prelude. The renderer therefore owns horizontal player placement at the point it
is consumed instead of depending on VBLANK state surviving another renderer.
The draw contract now records seven HMOVE strobes rather than six.

All pre-existing Stella hashes still pass, including all twelve historical full
states, every half-control state, and the existing above/below 5.0/5.5/6.0
composition snapshots. Added bottom-order Stella regressions for 0.5, 1.0, 1.5,
2.0, 2.5, and 3.0; their score rows are masked and hashed independently of the
gameplay scene. The two interactive examples now permit compile-time initial
score/half overrides solely to make those deterministic composition checks easy.

2026-09-14 18:40:37 PDT, restore heart demo gameplay controls
The heart-score above/below cartridges were labeled interactive but their
heart-specific application body had deliberately removed the normal gameplay
controls to save ROM. Restored the maintained player-color interaction model:
left joystick moves the selected P0/P1/Ball and SELECT cycles the selected
object, while the right joystick remains dedicated to half-heart health changes.
The sprites intentionally keep a single static frame so both real renderers plus
both controller paths still fit in ordinary 4K.

To recover ROM without deleting behavior, the non-beam object-selection,
movement, and aggregate control helpers are ordinary overscan functions rather
than one large inline expansion. Both top and bottom carts now link at 3974/4090
ROM bytes with the same 47-byte total RAM use. Existing neutral and right-stick
Stella hashes are unchanged. New Stella hashes pin held left-stick RIGHT visibly
moving the initially selected P0 in both composition orders.

2026-09-14 22:15:00 PDT, heart capacity boxes and F8 composition
Extended `heart_score_component.c26` with a separately colored playfield box
background. Public `boxes` selects 0..11 capacity slots and `box_color` supplies
one color for every enabled box; each box is 8 pixels wide on the heart meter's
existing 12-pixel pitch. VBLANK clamps full/half health to capacity. The box
stream uses asymmetric PF1/PF2 bytes with fixed-cycle left/right transitions,
so capacity changes alter data rather than horizontal timing. Dense 8.5, 9.5,
and 10.5 half-heart cases are unrolled to fit both the half-player write and
playfield transition without moving established heart pixels. A component now
uses 16 RIOT-RAM bytes and still consumes exactly seven visible scanlines.

Stella 7.0 retained all 24 existing full/half heart-only raster hashes when box
color is hidden. Separate visible-box snapshots lock every capacity from 0
through 11. The interactive above/below demos now start at 5.5 hearts with eight
boxes; right joystick UP/DOWN changes health by half-heart and LEFT/RIGHT changes
capacity by one box with the existing neutral-required latch. Reducing capacity
clamps health. Left-stick P0/P1/Ball movement and SELECT cycling remain intact.
The expanded component plus fully interactive player-color application no longer
fits honestly in 4K, so both composition diagnostics use F8: application/main in
bank0, heart dispatcher and renderer targets in bank1 via `banked:=1`. The
above-score gap restores gameplay COLUPF/CTRLPF after the heart component's
playfield ownership. Both F8 ROMs disassemble/reassemble byte-exactly (2/2).
