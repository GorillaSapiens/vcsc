2026-08-28 08:39:46 PDT, P1 diagnostic RESP phase fix
The P1 detail corruption bisected by the user to badpatch.patch / commit
4785eb36... was isolated to the animated TIA panel's P0 RESP0 phase after the
dense collision-icon rows. The renderer data, pointers, RAM workspace, VDEL,
NUSIZ, and GRP latch flushes were not corrupting the glyph bytes; moving the P0
RESP0 strobe itself was the reliable discriminator.

The final fix adds one NOP before that P0 RESP0 and changes the positioning
routine's raw P0 coordinate from 32 to 26 while retaining the normal fine-motion
lookup. This keeps the visible P0 collision target at the original x=32 and
therefore preserves the existing collision-lane golden pixels, while moving the
RESP strobe off the phase that produced the next-frame extra player copy.

The Stella regression now checks rectangle (178,122)-(209,126) for darkness
before masking the two live controller-detail rows. It caught the original bug
and rejected several startup-sensitive false fixes. The final source passed
repeated fresh NTSC joystick captures, all 12 Stella 7.0 standard/controller
reference cases, vcs_diagnostic_cartridge.pl, and example_assembly_allowlist.pl.
The latter fixture was deliberately repinned to the reviewed diagnostic asm
SHA-256 c4f15584425aa56d47bc1d25506d5e3e13190c9d71be51fedf534cf420ebc4a7.

A broad jobs=8 e2e rerun after the fix reached 239/327 with every reported case
passing before the container command timeout. The change-specific diagnostic,
assembly allowlist, VSYNC contract, source-tree hygiene, history-format, and all
12 Stella reference checks had already passed separately.

2026-08-28 10:25:15 PDT, P1 staging supersedes RESP theory
The earlier 08:39 RESP0-phase closeout is superseded. User testing showed the
P1 corruption still present, and the surrounding six-glyph rows made a
cross-frame TIA-state explanation implausible. The TIA panel P0 positioning was
returned to its original x=32 behavior.

Fresh Stella launches of the same bad cartridge produced several different P1
rasters. Hardcoding DETAIL1 pair indices was stable, and normal font pointer
lookup using fixed indices was stable. Private collision workspace, a dedicated
Superchip reader, and direct assembly stores to the Superchip row did not fix
it. Moving only the six live DETAIL1 pair indices from Superchip cartridge RAM
to ordinary RIOT RAM produced 12/12 stable fresh Stella boots. This isolates the
active problem to live DETAIL1 cartridge-RAM staging/readback rather than the
font tables, pointer lookup tables, or six-glyph renderer.

The current source adds `diagnostic_detail1_row[6]` in ordinary RAM plus a
dedicated `diagnostic_prepare_detail1_row()` pointer-expansion path. A leading
WSYNC explicitly preserves the scanline consumed by the former generic prepare
path. The Stella regression keeps a hard dark-rectangle oracle on the corrupt P1
trailing area before masking the live controller rows.

Focused tests currently pass: vcs_diagnostic_cartridge.pl, vcsc_branding.pl,
source_tree_hygiene.pl, and context_history_format.pl. The 12-case Stella matrix
and full 327-test suite still need rerunning before this is certified.

2026-08-28 10:35:46 PDT, Add four UI-phase diagnostic ROMs
The user observed that holding right joystick fire could reveal the correct P1
glyph beneath the corruption, suggesting frame-phased behavior. The diagnostic
UI scheduler already runs a four-frame cycle and phase 2 owns DETAIL1 updates.

Added `DIAGNOSTIC_TEST_UI_PHASE` debug builds and changed
examples/19_diagnostic/01_diagnostic/Makefile so plain `make` produces exactly
four F4SC ROMs: vcsc_diagnostic_phase0.bin through phase3.bin. Each build forces
its selected UI phase immediately before and after dispatch, making that same
phase execute on every steady-state frame. `make play0` through `make play3`
launch individual variants; README documents the temporary diagnostic mode.
All four variants build successfully and produce distinct binaries.

The diagnostic assembly allowlist was stale from the earlier DETAIL1 changes:
the source now contains 615 asm statements. Repinned its reviewed digest to
e7f6b0bf71bf8d403940deff48962f82217336f5ca33eab9c31cac134bfc3af8.
Focused diagnostic-cartridge, assembly-allowlist, source-tree-hygiene, and
branding tests pass.

2026-08-28 10:58:00 PDT, Replace phase lock with skip ROMs
The user reported that the four phase-locked diagnostic ROMs were not useful:
each one showed either a blank P1 or an uncorrupted P1. That means pinning a
single scheduler phase does not exercise the transition that corrupts the
normal four-phase cycle.

Replaced the temporary `DIAGNOSTIC_TEST_UI_PHASE` experiment with
`DIAGNOSTIC_SKIP_UI_PHASE`. examples/19_diagnostic/01_diagnostic/Makefile now
builds vcsc_diagnostic_skip0.bin through skip3.bin, and each build omits one
steady-state UI phase while preserving the other three in order. `make play0`
through `make play3` launch the four skip variants; README and hot context now
describe the new experiment.

2026-08-28 11:12:00 PDT, Correct skip-phase experiment cadence
The user reported that all four first-generation skip-one-phase ROMs showed
only a blank or perfectly formed P1. Inspection found the experiment itself
changed the scheduler from its normal four-frame 0,1,2,3 cadence to a
three-frame loop by advancing over the omitted phase before dispatch. That
made the result ambiguous because frame phasing, not just phase work, changed.

Reworked `DIAGNOSTIC_SKIP_UI_PHASE` so every phase still consumes one frame and
advances normally; only the selected phase's work is suppressed. DETAIL1 is
seeded with a neutral valid P1 row so skip2 remains visible without its normal
refresh. The debug skip selector lives in cartridge RAM and is initialized by
fixed-size handwritten `LDA #imm` / `STA` assembly. All four resulting 32 KiB
ROMs now have identical memory maps and differ at exactly one byte (offset
30708), whose value is 0, 1, 2, or 3. This removes code-layout and collapsed-
cadence confounds from the next hardware/Stella comparison.

Focused `vcs_diagnostic_cartridge.pl`, `example_assembly_allowlist.pl`,
`source_tree_hygiene.pl`, `vcsc_branding.pl`, and `context_history_format.pl`
pass. The diagnostic asm allowlist is repinned to 617 reviewed statements,
digest 5ad48e98faa2f4889fe35b32193d4f1e3e71ba83affe93d39178975516fdec02.

2026-08-28 14:39:16 PDT, Restore full four-phase diagnostic
The user reported that every corrected skip-one-phase ROM showed either a blank
or perfectly formed P1; none reproduced the corruption. This rules out any one
UI phase being sufficient by itself under the preserved four-frame cadence and
identical-layout experiment.

Removed the temporary skip-phase selector, conditional scheduler suppression,
seeded debug P1 row, four-ROM Makefile targets, and debug README text. Restored
the normal single vcsc_diagnostic.bin build with the ordinary 0,1,2,3 scheduler
fully enabled. Restored the diagnostic asm allowlist to the non-debug source.
The restored source has 615 reviewed asm statements; the non-debug allowlist
digest is e7f6b0bf71bf8d403940deff48962f82217336f5ca33eab9c31cac134bfc3af8.
Focused diagnostic, assembly-allowlist, source-tree-hygiene, branding, and
history-format tests pass after restoration.

2026-08-28 15:40:11 PDT, human-readable linked listing first
Roadmap reprioritized: final linked .lst output is now Item 45, ahead of all
other main-roadmap work. The intended listing preserves original C26/S26 source
provenance through the toolchain and shows generated assembly, final linked
addresses/bank placement, and emitted bytes. Superchip read/write aliases must
be obvious in the listing. Former Items 45-47 moved to 46-48. This turn changed
only files under the developer-record directory; no source/code changes.

2026-08-28 15:45:17 PDT, document SC access regression plan

Added main-roadmap Item 46 immediately after the human-readable linked-listing
work. The permanent Superchip access suite must cover ordinary compiler-generated
cartram scalar/array accesses, constant and runtime indexes, copies and
read-modify-write forms, DATA/BSS, boundary/sentinel cases, linked read/write
alias operands, simulator/emulator readback, and 4KSC/F8SC/F6SC/F4SC diagnostic
coverage. Later roadmap items were renumbered 47-49.

2026-08-28 16:28:19 PDT, complete human-readable linked listing
Final linked .lst sidecars now carry maintained C26/S26 source provenance through
compiler, assembler, O26, relocation, and final bank placement. Source statements
are printed before their emitted instructions with final logical addresses,
relocated bytes, readable generated 6502 assembly, and resolved 16-bit operands.
This makes split-address mappings visible directly; the focused F8SC regression
shows the generated Superchip store through the write alias and load through the
read alias, separated by exactly 0x80. Compiler-generated or otherwise
unattributed material is labeled rather than assigned false source ownership,
and legacy/Stella RAM-constant information remains in the same sidecar.

Provenance markers initially interfered with assembly peephole matching and
changed several renderer ROM footprints. They are now optimization-transparent.
A dedicated peephole-source-toggle regression verifies that source provenance
does not change optimized generated assembly after metadata is stripped.
Documentation in linker/README.md, driver/README.md, and test/README.md records
the final listing contract and normal build behavior.

Validation on the completed implementation: all 502 compile-phase tests pass.
The complete 328-case e2e set also passes on this source revision; because the
single long runner exceeded the container command window after 240 cases, the
remaining 88 were completed in ordered batches. Item 45 is complete and removed
from the unfinished roadmap; Item 46 (exhaustive Superchip RAM access
regressions) is now the main-roadmap next action.

2026-08-28 17:49:14 PDT, document linker dummy-read hazard
The diagnostic P1 corruption is now attributed to NMOS 6502 page-cross dummy
reads from a wrapped absolute,Y reposition-table lookup. With the table linked
at $F14D the encoded base was $F05D; Y=$F3 and Y=$F1 reached final ROM addresses
$F150/$F14E but ghost-read $F050/$F04E, which are Superchip write-port aliases
for DETAIL1 bytes 5 and 3. Normal CPU write traps were clean because the
corruption is caused by read bus cycles with destructive SC write-port side
effects. RIOT zero-page/absolute controls and a no-page-cross SC control rule
out the earlier timing and direct cartram-lowering theories.

Created `.../superchip_dummy_read_hazard.txt` as the detailed active design
record. Reprioritized the main roadmap so Item 46 is a generic linker fix:
mapper-declared hazardous-read ranges, real 6502 dummy-cycle analysis on final
relocations, placement constraints that preserve instruction timing, and hard
source-correlated errors when no safe placement exists. The exhaustive direct
Superchip access suite moves to Item 47; later main items shift to 48-50. The
current diagnostic RIOT workaround remains temporary and must be removed only
after the generic linker solution is proven.

2026-08-28 20:16:38 PDT, complete linker dummy-read hazard fix
Main-roadmap Item 46 is complete. Added C26 `$read_hazard` metadata and
legacy cfg `read_hazard = yes`; split-address regions treat their write alias
as the CPU-read side-effect range. Superchip 4KSC/F8SC/F6SC/F4SC, CBS FA/RAM
Plus, and CommaVid CV declarations now mark their destructive write ports, and
the legacy F8SC/F6SC/F4SC/FA/CV cfg files carry the same operational metadata.

The linker now evaluates statically knowable NMOS 6502 bus reads from final
linked bytes. Its explicit 256-entry addressing-mode model covers every opcode
byte, including unofficial encodings and handwritten `opXX` assembly. The
model includes instruction/operand fetches, direct reads/RMWs, zero-page
indexed dummy reads, absolute X/Y page-cross reads, indexed store/RMW pre-reads,
(zp,X) and (zp),Y pointer reads, JMP-indirect vector reads, branch dummy reads,
stack reads, JSR, BRK/vector reads, implied/accumulator next-PC reads, and
KIL/JAM cycles. Runtime-computed indirect effective addresses and return targets
remain inherently unknowable and are not guessed.

Relocatable referenced layouts receive read-hazard placement constraints rather
than instruction rewrites. Fixed unsafe instructions fail with final-PC,
operand, cycle, mapper range, referenced-symbol, and source/assembly provenance.
A stable indexed STA pre-read of exactly the physical write-port byte immediately
overwritten by that same STA is allowed; wrong-high-byte dummy reads, unstable
unofficial indexed stores, and RMW pre-reads remain hazards.

Added `test/linker_dummy_read_hazards.pl`. It builds raw handwritten assembly
for all 256 opcode bytes and exercises representative hazardous cycles in every
statically analyzable family, plus movable-placement and fixed-provenance cases.
The diagnostic integration adds a final-linked structural oracle.

Restored `diagnostic_detail1_row[6]` to Superchip RAM. The original
beam-sensitive wrapped absolute,Y lookup is unchanged. The linker now places
`diagnostic_reposition_table` at `$F1F0`, encoding the five wrapped accesses
against `$F100` instead of the corrupting `$F05D`; Y=`$F0-$FF` therefore
stays in `$F100-$F1FF` without ghost reads of `$F04E/$F050`. DETAIL1 remains
at read `$F0CB`, write `$F04B`.

Regenerated all 12 Stella 7.0 NTSC/PAL/SECAM x joystick/paddle/keypad/driving
reference captures because the old images contained effects of the ghost-read
corruption. The corrected Superchip output was previously cross-checked against
a temporary RIOT-RAM control and matched pixel-for-pixel. The full optional
12-case Stella oracle passed after re-blessing; closeout rechecked all four NTSC
cases against those references.

Final validation: 502/502 compile-phase tests pass and 329/329 e2e tests pass.
The monolithic e2e run hit the outer execution timeout after case 241, so the
remaining ordered tail was completed in bounded batches; the two discovered
failures were stale source-contract expectations for the newly added hazard
flag and pass after being updated. All eight example-build shards, linker hazard,
Superchip/FxSC, FA, CV, diagnostic, video-standard, and disassembler/fuzz tests
are green.

2026-08-28 21:09:12 PDT, fix interactive score controls
Audited public interactive score examples after finding the group-16 combined
181-line score cartridges did not react to the right joystick. Four normal
six-digit interactives were genuinely missing score editing: group 16
score-above/score-below and group 14 multisprite 181 score-above/score-below.
The poison-score interactives are intentional no-op hostile diagnostics; the
remaining ordinary score examples already have score controls.

The near-full group-16 cartridges now integrate compact hand-written 6502
controls: Select cycles P0/P1/M0/M1/Ball, the left joystick moves the selected
object, and the right joystick uses one-action-per-press score editing with
full-neutral re-arm. Left/Right select one of six decimal digits and advance
score hue; Up/Down increment/decrement the selected packed-BCD digit. Reset
restarts the cartridge. Both orders remain unbanked 4K at 4051/4090 ROM and
124/128 RAM.

The group-14 181 multisprite score examples now use the new compact shared
`fixed_six_digit_controls_compact.c26`, preserving the same right-stick behavior
without pulling generic 24-bit runtime workspace. Both link at 3605/4090 ROM
and 101/128 RAM. `vcs_public_score_controls.pl` now includes centered score
layouts plus the group-14 and group-16 special cases, closing the structural
audit gap that let these examples escape the earlier cleanup. The assembly
allowlist pins both reviewed compact-control sources.

Validation: both focused cartridge suites, public score controls, all-five
interactive examples, assembly allowlist, and source-tree hygiene pass. Runtime
MOS6502 input oracles cover score digit selection, packed-BCD up/down changes,
color changes, hold/neutral re-arm behavior, object movement, Reset, and 262-line
timing for the changed public cartridges. All 502 compile-phase tests pass. A
broad 329-case e2e run passed 241 cases with no failures before the outer command
timeout; subsequent unrelated long-tail diagnostics also exceeded the command
window, so no claim is made here that the entire e2e tail was rerun.

2026-08-28 21:53:38 PDT, default font slashed zero
Changed default_ascii digit 0 to a slashed zero and regenerated the
decimal, uppercase-hex, and lowercase-hex subsets. Updated independent
score raster oracle tables and added a font-contract assertion for the
new default-zero pixels.

2026-08-28 22:24:05 PDT, generate mapper font subsets
Replaced hand-copied canonical font tables in the FA RAM+, 4KSC, Omni, CV,
JANE, 0840, UA/UASW, 0FA0, E0, 3F, and 3E diagnostics with checked-in
custom subset files. Added `libraries/vcs/fonts/make_font_subset.pl` for
arbitrary ordered subsets from canonical C26 fonts, with selectable bank/page
placement and license text. Each affected example Makefile has an explicit
`make fonts` regeneration target; normal builds consume the checked-in files
and do not require Perl.

Added `vcs_generated_font_subsets.pl`, which regenerates every custom subset
through its example Makefile into a temporary directory and byte-compares it
against the checked-in copy. It also verifies ordinary example builds do not
invoke Perl and locks the 0840 subset to the canonical slashed zero. UA/UASW
wrappers include their local generated fragments directly so recursive example
discovery recognizes them as include-only files instead of standalone carts.

Validation: all 502 compile-phase tests pass. All 330 e2e cases are green.
The broad eight-job e2e run exposed four resource-contention failures: the
120-second diagnostic and example-build shards 5, 6, and 8. The diagnostic and
shard 5 pass through the normal runner when run alone; shards 6 and 8 pass after
the UA wrapper include fix. The other 326 e2e cases passed in the broad run.
