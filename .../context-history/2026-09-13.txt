2026-09-13 01:21:58 PDT, RESET zero-vector BRK bootstrap
The disassembler now recognizes the deterministic stock-6507 startup used by
cartridges with RESET=$0000: the vector high-byte fetch leaves $00 on the data
bus, the opcode fetch from write-only TIA $0000 sees open-bus $00 (BRK), and
execution continues through the cartridge IRQ/BRK vector. Automatic mapper
viability and established RESET tracing both accept that path; other non-ROM
RESET targets remain invalid. Generated headers document the bootstrap.
A synthetic 4K regression pins automatic selection, startup-code promotion, and
exact reconstruction. The user-supplied six-ROM failure corpus now disassembles
automatically (five unbanked 4K, one F8) and round-trips 6/6 byte-exact. Focused
all-opcode, hardening, and deterministic-fuzz tests pass (fuzz 30/30).
The comprehensive `test/vcsc_disassembler.pl` regression suite also passes with
the new fixture, and `test/vcsc_disassembler_gl.pl` remains green.

2026-09-13 07:17:00 PDT, heart score component completed
The paused Thomas Jentzsch 11-Invaders heart-score work is complete for full
hearts. The authoritative RAM renderer was reconstructed and the previous
ROM-specialized branch discarded. Scores 4..11 already matched the centered
11-heart footprint; the remaining 1..3 defect was the singleton positioning
path. A single-copy P0 lands two TIA pixels left of the repeated-copy footprint,
so its VBLANK setup now applies one additional $A0 HMOVE correction. When both
players are singletons, P1's setup HMOVE had also dragged already-positioned P0;
P0 is now held at zero motion across that HMOVE. This fixes the root interaction
without score-specific renderers.

The maintained implementation is `libraries/vcs/heart_score_component.c26`.
It copies a 45-byte self-modifying renderer to RIOT RAM, consumes seven visible
scanlines, uses 58 bytes of instance RIOT RAM, clamps scores above 11, and draws
11 centered eight-pixel hearts at 12-TIA-pixel pitch. Stella 7.0 snapshots for
all twelve values 0..11 were verified as exact prefixes of the score-11 image;
new `test/vcs_heart_score_stella.pl` permanently locks all twelve rasters. A
structural/build regression, public 2K `examples/01_basic/14_heart_score/`
example, library documentation, and install-manifest entry were added. The four
root-level heart prototypes/probes were removed. A half-heart remains optional
future work and is not part of the full-heart API.

2026-09-13 07:21:24 PDT, heart score completion validation
Focused heart/hygiene/install/package validation passed 5/5, and the examples
build shard containing the new public example passed 13/13. The full e2e runner
was clean through 229/367 tests before the 120-second execution window ended;
no failure had occurred. This was a runner time limit, not a test failure.

2026-09-13 09:15:00 PDT, repair heart-score test integration
-------------------------------------------------------------
* Remove vcs_heart_score_stella.pl from the default e2e discovery path. Like the
  established Stella raster certifications, it is graphical/external and must
  not make `make test` require Xvfb or Stella. Add `make stella-heart-score-test`
  as its explicit target; vcs_heart_score.pl remains the normal non-Stella E2E.
* Teach vcsc_branding.pl that .../heart_score.txt is an intentional focused
  developer record and require it alongside the other canonical hot files.
* Record the next heart work: move the 45-byte self-modifying RAM kernel into
  ROM-resident variants, then synthesize half hearts with M0/M1.

2026-09-13 10:00:41 PDT, complete structural font extension
The disassembler now lets long aligned 8x8 structural font/graphics recognition
extend through rows that already have strong graphics provenance.  This fixes a
case where recovered pointer bases at $FF00 and $FF10 pessimistically marked only
digits 0-5 of a 10-glyph decimal font and prevented the structural fallback from
starting at the real table base.  Structural font output also inserts a blank
line between each eight-row glyph while retaining interior labels/comments.
Added a synthetic overlap regression plus separator checks; the supplied
3e90cf23106f2e08b2781e41299de556.bin now renders all ten decimal glyphs and
round-trips byte-for-byte (matching MD5).

2026-09-13 10:58:14 PDT, composite sprite and sink annotations
The disassembler now preserves exact TIA graphics-sink provenance in emitted
bitmap rows: GRP0/GRP1 bytes are identified as sprite data and PF0/PF1/PF2 bytes
as playfield data, including joined alternate ROM origins and one source feeding
multiple registers. The static dependency walker keeps its normal short bound;
a longer bound is used only while validating a structurally recovered composite
sprite group, avoiding broad G1 behavior changes.

A bounded interleaved-pointer setup pattern such as `STA zp,X ; SBC #stride ;
DEX ; DEX ; BPL` can now recover adjacent 8xN strips that are proven to feed a
multi-sprite renderer. Presentation emits strip-base labels plus a full-width
X/dot image preview. When the established renderer loads Y from a memory row
counter and decrements it back to the loop, the preview reverses source rows so
the comment is in displayed top-to-bottom order. Synthetic regressions cover
six 8x16 strips, exact PF0/PF1/PF2 sink accumulation, GRP alternate origins, and
byte-exact round trips. The supplied 3e90cf23106f2e08b2781e41299de556.bin
round-trips with the same MD5 and now shows the full 48x16 composite preview.

2026-09-13 11:20:17 PDT, structural-font separator regression assertion
The overlap-extended structural-font fixture already emitted all nine blank
separators between its ten glyphs.  Its regression regex counted only five
because four glyph-ending rows now carry the exact `sprite data -> GRP0` sink
annotation.  Updated that assertion to accept and verify the optional graphics
sink suffix, matching the row-format contract used by the surrounding graphics
tests.  No disassembler emission logic changed.
