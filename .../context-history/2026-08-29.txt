2026-08-29 06:14:39 PDT, narrow default slashed zero
Applied the user-supplied zero.patch.  The canonical default_ascii zero keeps
its diagonal slash but uses symmetric four-pixel top and bottom caps.  Ran
make fonts so default decimal/hex/lhex and mapper diagnostic subsets are
derived from the ASCII source of truth.  Updated the exact font-contract and
independent score-raster expectations for the intentional glyph change.
Validation: 502/502 compile tests pass; all 331 e2e cases pass, with the long
diagnostic cartridge driver run directly after the ordered suite hit the
outer command timeout.
2026-08-29 06:45:06 PDT, move bank diagnostic into example
Moved the complete F8/F6/F4/F8SC/F6SC/F4SC transition diagnostic out of
libraries/vcs and into examples/09_bankswitching/01_diagnostic. The public
library no longer installs or documents the one-off diagnostic source; staged
install checks compile the installed example against the installed general VCS
library instead.

Because the moved source is now a real example, replaced its hand-copied Big
and default ASCII glyph tables with checked-in status_font.c26 and
cart_type_font.c26 files generated from the canonical *_ascii.c26 fonts. Its
Makefile now exposes fonts and top-level make fonts regenerates both files;
ordinary builds still do not require Perl. Updated diagnostic, example-build,
generated-font, hygiene, install-layout, documentation, and reviewed-assembly
contracts for the new ownership boundary.

Validation: 502/502 compile tests pass. All 331 default e2e cases pass across
bounded runs after the monolithic parallel run hit the execution ceiling. All
eight recursive example-build/disassembly shards pass. A staged install proves
the suite is absent from share/vcs and the complete example builds from the
installed examples tree. All seven generated diagnostic ROMs are byte-identical
to builds from the former library source.
2026-08-29 07:06:44 PDT, localize narrow example helpers
Moved five helpers out of examples/common because each served only one numbered
example class: UA/UASW diagnostic support into 09_bankswitching/09_ua, compact
multisprite score controls into 14_multisprite, three-plus-three controls into
06_all_five_181, combined all-five/player-color controls into
16_all_five_player_color_181, and the PAL/SECAM 228-line multisprite body into
17_video_standards. Updated includes, Makefile dependencies, reviewed-assembly
paths, font/subset and source-shape tests, and hygiene ownership checks.
examples/common now contains only the six helpers shared across multiple
numbered example classes.

Validation: 502/502 compile tests and all 331 e2e cases pass across bounded
runs; all eight recursive example-build/disassembly shards pass.
2026-08-29 07:32:57 PDT, collision labels A through O
Replaced the field diagnostic's unreadable collision-pair microglyphs with
Half-font letters A through O. The README now carries the A-O-to-TIA-latch
legend; a latched collision adds a six-pixel underline without obscuring the
letter, while the sixteenth slot remains the exact-bitmap check mark.

The checked-in diagnostic_collision_font.c26 is now generated directly from
libraries/vcs/fonts/half_ascii.c26 by make_collision_font.pl. The diagnostic
Makefile exposes a fonts target, so top-level make fonts regenerates it, while
ordinary example builds consume the checked-in file and do not require Perl.
Removed the obsolete diagnostic_collision_objects.font source and extended the
generated-font regression for this contract.

Validation: 502/502 compile tests and all 331 default e2e cases pass across
bounded runs, including the diagnostic cartridge driver and all eight recursive
example-build/disassembly shards. Re-blessed the 12 Stella 7.0 NTSC/PAL/SECAM x
controller references for the intentional collision-raster change and rechecked
all 12 references successfully.
2026-08-29 10:35:51 PDT, exhaustive diagnostic collision sweep
Reworked the field diagnostic so the A-O panel now physically exercises all
15 unordered TIA collision pairs among M0, M1, P0, P1, BL, and PF one at a
time. Each phase clears CXCLR, renders only the requested collision sources,
reads the complete collision-register bitmap, and records pass only when the
one expected latch is present with no extras. Each successful A-O phase gains
an underline; the sixteenth check mark now requires all fifteen remembered
results to pass. The Stella certification build runs the same hardware tests
one phase per frame and freezes after the complete A-O sweep for deterministic
screenshots.

Replaced the approximate text VCSC heading with the canonical six-slice
libraries/vcs/fonts/logo_font.c26 logo. The six-hex-digit CPU fingerprint now
uses a checked-in 0-9A-F subset generated from big_ascii.c26, with local and
top-level make fonts regeneration support. Folded the 2600/7800 host identifier
into the TV-standard row to recover the raster space required by the larger
logo/fingerprint. Aligned PAL and SECAM visible-raster origin with NTSC while
preserving the standard frame lengths. Updated the diagnostic README, assembly
allowlist, generated-font contracts, exhaustive collision-phase regression,
and all twelve Stella 7.0 references.

Validation: 502/502 compile tests pass. The 331-case e2e run had only
vcs_examples_build_3of8.test time out under parallel contention; that exact
shard passes alone, so all 331 e2e cases are accounted for green. The complete
diagnostic cartridge regression passes directly. All 12 Stella 7.0
NTSC/PAL/SECAM x joystick/paddle/keypad/driving raster cases pass sequentially.

2026-08-29 11:02:05 PDT, center diagnostic heading
The field diagnostic title now renders as " DIAGNOSTIC " instead of
"DIAGNOSTIC  ". Three title-only pair-font slots were repurposed as IA/GN/OS,
so the pair table remains 127 glyphs / 637 bytes and all later pair indices,
placement, and timing remain unchanged. The diagnostic structural regression
pins the centered six-pair header. All 833 default tests pass, and all 12
Stella 7.0 NTSC/PAL/SECAM x controller raster references were refreshed and
verified.
2026-08-29 12:10:19 PDT, lock direct Superchip RAM access
Completed main-roadmap Item 47 with a focused 4KSC compiler regression rather
than duplicating the same physical Superchip RAM semantics across F8SC/F6SC/F4SC.
The new test/superchip_direct_access.pl builds ordinary C26 programs that fill
all 128 SC bytes, verify hostile-fill BSS clearing and initialized DATA copying,
exercise first/middle/last boundaries, scalar and array constant accesses,
runtime-index loads/stores, array copy, and read-modify-write, and inspect the
final linked listing so direct operands and runtime pointer bases resolve to the
$F080-$F0FF read window and $F000-$F07F write window. Simulator checks verify
both aliases expose the expected final physical bytes. No compiler change was
needed: the generated code already obeyed the SC split-window contract.

Advanced the hot main roadmap to Item 48 and documented that the FxSC mapper
variants should not multiply this compiler-lowering test unless their RAM
semantics diverge.

Validation: the new regression passes directly and through test.pl; focused
4KSC, split-memory codegen, linked-listing, and hygiene regressions are green.
All 502 compile-phase tests pass. A broad e2e rerun found no failures in the
ordinary tail regressions; the execution window prevented completing the known
heavy diagnostic/disassembler and recursive example-build tail in one sweep.
The input handoff already had all 331 prior e2e cases green, and this change did
not modify compiler/linker/runtime/example code.
