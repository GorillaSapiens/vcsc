2026-09-03 00:47:00 PDT, close out 3E simulator hardware oracle
Completed bankswitching Item 38b with `test/vcs_3e_simulator_oracle.pl`. The
fixed-bank fixture writes each selector value 0..255 to its own RAM bank through
the `$1400` write alias, leaves RAM mode via `$3F`, then reselects all 256 banks
and reads through `$1000`; this fails on selector truncation, bank aliasing, or
lost RAM contents. Separate negative fixtures require `vcsc-sim` to hard-error
on reads from the `$1400-$17FF` write alias and writes to the `$1000-$13FF` read
alias. No simulator runtime change was needed; the existing model already met
the hardware contract. Compacted `.../3e.txt` to a durable closeout, advanced
the active bankswitching roadmap to FC, and replaced hygiene's dependency on
obsolete unfinished 3E prose with the executable oracle regression.

2026-09-03 07:56:28 PDT, reopen maximum-size 3F/3E stress certification
Audited the post-migration 3F/3E regressions before starting FC. Existing tests
prove the 256-bank/512K topology endpoints, a four-bank ordered call matrix, all
256 VCSC 3E RAM selectors, and helper access through bank 255/offset $3FC, but
they do not execute a maximum-size ROM call matrix or consume/verify the entire
RAM capacity. Reopened bankswitching Item 38c to require one maximum-size 3F and
one maximum-size 3E torture cartridge: all 255 selectable lower banks execute
the full 255 x 255 ordered matrix, with the fixed/startup bank also exercised in
both directions. Added separate 3E RAM torture requirements: compiler-managed
fill/verify of current Stella's 32 x 1K / 32K RAM model and VCSC's extended 256 x
1K / 256K model. FC moved to 38d and is explicitly capped at 256 x 4K / 1 MiB
for VCSC automatic calls; F0/E0 follow, and Stella-compatible 3EX remains a
distinct later mapper as Item 38g.

2026-09-03 09:26:53 PDT, add maximum-size 3F torture diagnostic
Added `examples/09_bankswitching/18_3f_max`, a single 256 x 2K / 512K 3F
PASS/FAIL cartridge. Eight generated lower-bank assembly objects cover physical
banks 0..254 and a fixed-bank scheduler drives every ordered lower-bank pair:
255 x 255 = 65,025 calls. Same-bank pairs use direct JSR; cross-bank pairs use a
seven-byte runtime-patched inline descriptor call stub in RIOT RAM, so every
source bank can sweep all 255 destinations without 255 literal call sites. The
scheduler also exercises every lower bank -> fixed call and every fixed -> lower
call, for 65,535 counted target calls total, with bank-local target/source
identity checks and hardware-stack balance checks. The visible fixed-bank kernel
displays `pass`/`FAIL` and exactly `3F max` on line two in a measured 262-line
NTSC frame. Widened the frame-timing harness to signed 3F/3E 2K-bank images up to
512K and made its 3F/3E selector model preserve the full bank byte rather than
`value & 3`. Added `test/vcs_3f_max_diagnostic.pl`; the generic one-source
example smoke test intentionally skips this generated nine-object cartridge,
and the dedicated regression owns its 512K build, full runtime torture, and
frame timing. Maximum-size 3E ROM/RAM torture remains Item 38c.

2026-09-03 10:19:31 PDT, fix 3F max smoke-test discovery
The generic recursive example smoke test now retains the dedicated 512K 3F max
entry source during include-fragment discovery, then excludes that entry point
from one-source builds.  This keeps status_font.c26 and cart_type_font.c26
classified as include-only fragments instead of compiling them standalone before
bank255 exists, which caused the shard 1/8 and 8/8 parser failures.

The supplied 17:05 archive had also omitted the new 18_3f_max directory and its
dedicated regression because those files were untracked when make tar used the
real Git index.  Recovered only those omissions from the immediately preceding
16:59 handoff after verifying every shared source file was byte-identical.
Validation: both formerly failing example shards pass, the dedicated 512K 3F max
regression passes all 65,535 target calls and 262-line timing, source-tree hygiene
passes, and a broad make test reached 230/352 with no failures before the bounded
execution window ended.

2026-09-03 11:18:31 PDT, normalize 3F max diagnostic display
Changed `examples/09_bankswitching/18_3f_max` to use the same visible PASS/FAIL
presentation as the ordinary bankswitching diagnostics: `big_ascii.c26` through
`six_glyph_big_wide_component.c26`, with the 16-byte glyph offsets and the
standard 81 blank + 19 status + 11 mapper + 81 blank = 192 visible-line layout.
The second line remains the small exact `3F max` label. Extended the dedicated
3F-max regression to lock the big-wide renderer, big font source, and standard
spacing. Validation: generated-font contracts, assembly allowlist, source-tree
hygiene, example build shards 1/8 and 8/8, and the dedicated 512K torture all
pass; the torture still executes 65,535 target calls and produces 262-line NTSC.

2026-09-03 11:24:01 PDT, widen lowercase default m and w glyphs
Applied the supplied default-font redesign for lowercase `m` and `w`. Both now
use column 0 to gain the same seven-pixel full width already used by uppercase
`M` and `W`; regenerated all library and example font subsets with top-level
`make fonts`. Extended `vcs_font_contracts.pl` narrowly so lowercase m/w are
canonical full-width exceptions while every other default glyph retains the
normal blank-column contract. Validation: font contracts, generated subsets,
ASCII alignment, score glyph rows, the 3F-max diagnostic, and example build
shards 1/8 and 8/8 pass.

2026-09-03 12:05:11 PDT, add stable 3F max WAIT spinner during torture
Made the 512K `18_3f_max` torture scheduler resumable so the cartridge keeps a
normal NTSC frame visible while the 65,535-call matrix executes. Each frame runs
bounded batches only during blank time (10 target calls in VBLANK and 8 in
overscan), leaving all 192 visible lines to the display. The waiting screen uses
the same `$84` blue background produced by `01_basic/01_blank_screen`, shows big
wide `wait ` plus a `/ - \\ |` spinner that advances every 15 frames / 250 ms,
and retains the small `3F max` second line; completion transitions to the normal
PASS/FAIL display.

Added `external_pointers` support to `six_glyph_big_wide_component.c26`, matching
the ordinary six-glyph component so the diagnostic can own its status pointers
without carrying unused packed-BCD setup. To preserve the torture ABI's common
lower-bank `$1000` source and `$1100` target addresses, `make_torture.pl` embeds
the generated WAIT and PASS/FAIL big-font bytes plus their pointer loaders inside
physical banks 253 and 254 rather than linking separate pinned C26 objects there.
The generic example smoke test now recognizes generated font-subset C26 files as
data fragments even when consumed by a generator instead of `include`.
Validation: the dedicated 3F-max regression executes all 65,535 calls with
failure=0; 3,800 consecutive WAIT/result frames are exactly 262 scanlines;
big-wide/score/font regressions, example-build shards 1/8 and 8/8, source-tree
hygiene, and context-history format pass.

2026-09-03 12:10:21 PDT, finalize 3F max spinner regression
Updated the assembly allowlist for the resumable 3F-max diagnostic after moving
WAIT/PASS pointer setup into generated bank payloads: the entry source now has
13 reviewed asm statements with its new digest. Refreshed the hot 3F status text
to mention the stable blue wait spinner before the final PASS/FAIL screen.
Focused validation passes the assembly allowlist, full 512K/65,535-call 3F-max
runtime and 3,800-frame timing regression, font contracts/subsets, example build
shards 1/8 and 8/8, source-tree hygiene, and history-format checks.

2026-09-03 12:12:14 PDT, broad spinner handoff checkpoint
A broad make test with eight workers reached 208/352 with zero failures before
the tool execution cap stopped it. All focused tests for the changed code had
already passed, including the complete 65,535-call 3F-max run, 3,800 consecutive
262-line frames, font contracts/subsets, assembly allowlist, example shards 1/8
and 8/8, source-tree hygiene, and context-history format.

2026-09-03 12:28:03 PDT, center final 3F max PASS display
Corrected the generated bank254 PASS pointer loader in 18_3f_max from
p/a/s/s/blank/blank to the standard bankswitching blank/p/a/s/s/blank layout,
so the completed PASS word is centered by the big-wide six-glyph renderer.
The WAIT + spinner layout is unchanged. Extended vcs_3f_max_diagnostic.pl to
lock the centered PASS pointer pattern. Validation: the dedicated 512K torture
still executes all 65,535 calls with failure=0 and the 3,800-frame WAIT/result
timing run remains exactly 262 NTSC scanlines; example-build shards 1/8 and 8/8,
source-tree hygiene, and context-history format also pass.

2026-09-03 13:03:37 PDT, add Stella-compatible 3E max torture
Added examples/09_bankswitching/19_3e_max as the 512K-ROM / 32K-RAM Stella
3E stress cartridge. It retains the complete 255x255 lower-ROM ordered call
matrix plus lower<->fixed coverage and adds a fixed-bank full-RAM fill/verify
pass over all 32 x 1K banks. RAM data is one continuous maximal 16-bit Galois
LFSR stream (x^16+x^14+x^13+x^11+1, mask $B400, seed $ACE1); the generator
verifies the 65,535-state period before emitting assembly. Readback resets the
LFSR and checks every byte, so mirrored or aliased RAM banks cannot pass merely
by sharing a repeated per-bank pattern.

The cartridge keeps the 3F-max blue WAIT/spinner UI. ROM work uses 10/8
VBLANK/overscan batches; RAM uses measured-safe 20/12-byte batches. The small
3E max font is embedded in lower bank 251 and selected on one deliberate blank
handoff scanline; one-time stub setup lives in bank 252; WAIT/PASS/FAIL data
remain in banks 253/254. A 6,500-frame timing soak crosses ROM torture, RAM fill,
RAM verify, and WAIT->PASS transition with every live frame exactly 262 lines.
Added test/vcs_3e_max_diagnostic.pl and source/build/hygiene/asm-allowlist hooks.
The remaining Item 38c work is the separate VCSC-extended 256K 3E swapram
capacity torture across multiple translation units.

2026-09-03 14:35:26 PDT, fix 3E max compiler swapram and timing
Reworked `examples/09_bankswitching/19_3e_max` after the first Stella run
showed both FAIL and 262/263-line frames. The 32K RAM phase no longer uses raw
mapper RAM reads/writes. It now exposes exactly 32 one-kilobyte `$swapram`
objects and performs every byte write/read as an ordinary C26 `$swapram` lvalue,
so the compiler selects `swapram_write1`/`swapram_read1`; the generated ROM
scheduler only handles bank dispatch, LFSR state, and bounded work slicing.
One continuous `$ACE1`/`$B400` maximal 16-bit LFSR still spans all 32 banks, so
mirrored or aliased banks cannot pass from repeating per-bank data.

The compiler-managed path is materially slower than the raw mapper shortcut.
Measured RAM work was reduced to two bytes in VBLANK and two in overscan, and
result-pointer/color setup stays inside the final overscan deadline. Replaced
the old 6,500-frame partial timing check with a 22,000-assertion run that covers
the complete ROM matrix, RAM fill, RAM verify, WAIT-to-result transition, and
final result. All 21,997 checked frames are exactly 262 lines. The same timing
harness now checks final RAM-state bytes directly: failure=0, call count=$FFFF,
RAM phase=3, bank/page/offset=32/0/0, and LFSR=$5F32. This avoids a second huge
simulator-only cartridge build while proving the normal displayed cartridge
actually completes successfully.

Extended `test/vcs_3e_max_diagnostic.pl` to lock the 32 compiler-visible 1K
objects, their 0..31 swapram bank allocation, linked read/write helper presence,
full LFSR endpoint, and the complete frame/memory oracle. The generic example
smoke test treats the companion `3e_max_swapram.c26` translation unit as part of
the dedicated multi-input cartridge rather than a standalone example.

2026-09-03 14:44:00 PDT, package 3E max swapram storage
Added an examples/.gitignore exception for
`19_3e_max/3e_max_swapram_storage.s26`. The top-level `make tar` temporary-git
path honors `examples/*.s26`; without the exception the compiler-managed 32K
swapram backing source was silently omitted even though the working-tree
regression passed.

2026-09-03 21:32:35 PDT, finish Stella-compatible 3EX max
Finished the dedicated Stella-compatible 3EX path at its maximum supported
shape: 256 x 2K / 512K ROM and 256 x 1K / 256K RAM. Stella's implemented RAM
geometry is 1K per bank; the earlier 512-byte wording described discarded
future discussion and is removed from the hot roadmap. The fixed bank uses the
`$FF` bankcall descriptor/no-switch sentinel, lower ROM banks use `$00-$FE`,
RAM selects through `$3E`, ROM through `$3F`, and the linker emits Stella's
RAM-count-minus-one byte at `size-6` plus exactly two `3EX` detector strings.

Added `20_3ex_max`, split across translation units and built in parallel. Its
512K image runs the full 65,535-call ROM matrix and compiler-managed fill and
readback of every byte in all 256 RAM banks with one continuous maximal LFSR.
The regression redirects only a copy's RESET vector to a linked fast test entry
for the complete torture; a separate 3,997-frame run locks displayed execution
to 262 NTSC lines. Fixed the 3EX bank-return path for calls made while RAM masks
the lower ROM window, and integrated mapper entry generation, install/package,
branding, source hygiene, font generation, assembly allowlisting, and simulator
timing support.

The max link exposed a generic linker bug: duplicate bank-placement ZERO/BSS
estimators counted `$swapram` BSS as CPU startup-zero records, falsely rejecting
the fixed `vcsc-rt0` component. The estimators now exclude swapram consistently
with real layout. The separate pre-FC extended-3E 256K capacity-torture milestone is retired;
VCSC's extended 3E selector range remains supported and focused through bank 255.

2026-09-03 21:32:36 PDT, stabilize loaded regression suite
Fixed no-Git version generation so the first generated `version.h` keeps an
mtime equal to the UTC timestamp embedded in `VERSION`; crossing a one-second
boundary while writing the header no longer changes an otherwise clean no-Git
version on the next probe. Raised only wall-clock allowances for the exhaustive
paddle soak, eight example-build shards, and 3EX-max torture to accommodate
measured eight-worker CPU contention; test behavior and coverage are unchanged.
Validation completed with 503/503 compile-only tests and 354/354 e2e tests.

2026-09-03 21:39:43 PDT, repair installed F864 profile smoke
The final `make installcheck` exposed a cfgectomy-era packaging regression that
was not part of the ordinary test path. `f8_profile_diagnostic.c26` had become
self-contained by including `F8/mapper.c26`, while installcheck still supplied
F8/F6/F4 mapper sources as separate translation units. The F6/F4 smokes
therefore linked their selected topology together with an unintended F8 topology
and failed on conflicting bank declarations.

Made the shared fixture select F8 by default and F6/F4 through explicit
preprocessor defines, so the chosen mapper is visible in the same translation
unit as its `bankN` modifiers. Installcheck now compiles only that fixture for
all three profiles. Focused F8/F6/F4 profile regressions and source-tree hygiene
pass, and the complete staged `make installcheck` succeeds.
