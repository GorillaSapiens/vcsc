2026-09-08 08:24:41 PDT, delegate diagnostic startup clearing
-------------------------------------------
Corrected the field diagnostic's initial noinit integration. `diagnostic_boot.s26`
now does only the pre-startup 2600/7800 RIOT signature scan, stores the result
through the linked Superchip write alias for `diagnostic_boot_7800`, and tail-jumps
to `__vcsc_startup_full`. It no longer carries private RIOT, Superchip, TIA, or
stack-reset clearing loops. The shim shrank from 81 linked bytes to 44 bytes.

Both stock runtime reset providers are weak so a strong command-line pre-startup
shim can override `__reset` without preventing the selected runtime member from
being linked for its exported startup body. The linker publishes a selected weak
stock reset under the ordinary `__reset` global/sidecar name when no strong reset
exists, preserving reset-vector and map/symbol contracts. Normal startup
selection still begins with full startup and reselects compact startup only when
safe; RIOT noinit therefore still forces table-driven startup.

The diagnostic regression now requires the strong shim to chain into the weak
full stock startup, requires ordinary Superchip BSS to have ZERO records while
the noinit latch has none, and simulator probes verify the latch survives normal
startup while neighboring cartram is initialized normally. Runtime/linker/test
and diagnostic documentation were updated. Focused noinit/startup/diagnostic,
assembly allowlist, source hygiene, interrupt-runtime, and F8 profile coverage
passes 16/16; all linker_* e2e regressions pass 26/26.

2026-09-08 09:06:00 PDT, split stock startup into simple/DATA/full paths
-----------------------------------------------------------------------
Separated ordinary initialized-data startup from the generic full/noinit path.
The stock runtime now has three selectable reset bodies: `vcsc-rt1-simple.s26`
for the no-DATA/no-init-table RIOT-only case, new `vcsc-rt1-data.s26` for DATA
copies and/or runtime initializers when blanket RIOT zeroing is safe, and
`vcsc-rt0.s26` for `noinit` or any split/non-RIOT BSS that requires
object-by-object ZERO records. DATA+noinit stays on full startup.

The DATA startup imports `__copy_table` and `__init_table`, blanket-clears
ordinary RIOT RAM, copies through split write aliases, walks runtime initializers,
and tail-jumps to `main`; it has no `__zero_table` import or generic ZERO walker.
It deliberately does not clear TIA, preserving the previous behavior of carts
that used the full startup solely because they had initialized DATA. The simple
startup retains its historical RIOT+TIA clear; the full startup remains generic
and TIA-neutral. Split Superchip DATA can use the DATA startup, while split
Superchip BSS forces full startup.

Linker startup selection, startup-table generation, map policy, call-stack
transient accounting, runtime packaging/install inventory, and documentation
were updated. `vcs_startup_selection.pl` now locks simple, DATA, runtime-init,
split-DATA, split-BSS, noinit, and DATA+noinit choices. Exact-ROM baselines were
refreshed where the middle startup legitimately recovered space: the 181
interactive example is 3873/4090 bytes, the 192 public interactive example is
3399/4090, and the animated gallery is 3079/4090. The gallery stack provenance
now names the shared table-copy startup transient rather than the old full-only
reason.

Validation: compile-only 537/537; all 26 `linker_*.pl` tests; focused
startup/diagnostic/Superchip/generic-ZP/package coverage 15/15; and 268 unique
tests from the 364-case broad e2e inventory passed. Three maximal mapper tests,
the long diagnostic paddle-sampling oracle, and the later example-heavy tail
exceeded the available execution windows; no failure was observed in those
unfinished batches.

2026-09-08 09:22:53 PDT, finish three-startup broad regression pass
--------------------------------------------------
Continued the broad e2e validation after the three-way startup split. The new
DATA startup legitimately reduced several exact ROM footprints, so stale size
guards were refreshed from fresh linked maps while preserving all behavioral,
raster, RAM, placement, and timing assertions. Updated multisprite 192 from
2621 to 2504 bytes and both 181 score compositions from 3650 to 3513; big-wide
score from 1112 to 991; wide-score fixture/public from 1245/1321 to 1128/1200;
wide 181 score compositions from 2887 to 2722; F8/F6/F4 standard-renderer bank0
from 1891 to 1710 while F8SC correctly remains 1891 because split BSS forces the
full startup; and public three-plus-three score from 1538 to 1413 bytes.

Fixed `vcs_faithful_legacy_playercolors.pl` so its eleven required `.same`
branch check is scoped to the linked `RENDERER_CODE` component. The new DATA
startup itself has a legitimate `.same` RIOT-clear loop branch, so the former
global-map count was accidentally counting unrelated runtime code. The renderer
still has exactly eleven required same-page branches and one intentional crossing
branch.

Final broad validation reached 352/364 e2e cases passing. The only cases not
completed are the three max-size 3E/3EX/3F diagnostics,
`vcs_diagnostic_paddle_sampling.pl`, and `vcs_examples_build_1of8.test` through
`vcs_examples_build_8of8.test`; those exceeded the execution window and did not
report failures. Compile-only remains 537/537 from the same implementation, and
all 26 linker regressions plus the focused three-startup/noinit coverage pass.

2026-09-08 10:02:40 PDT, disassembler mapper-evidence provenance
----------------------------------------------------------------
`vcsc-disas` now preserves the complete size-compatible mapper refinement result
through final emission.  Generated headers keep the existing tested/survived
summary and add a `mapper evidence` decision line plus one line for every tested
hypothesis.  Viable hypotheses report positive evidence such as a real
bank-changing selector, switch-avoided HLT/JAM/KIL fetch, detector signature, or
explicit VCSC tail signature; rejected hypotheses report the actual rejection
stage/reason, including unreachable RESET flow, split-RAM RMW contradiction,
missing family evidence, unrescued HLT/JAM/KIL, family precedence/subsumption,
UA-variant flow, or loss to stronger mapper-specific switching.  F8 prints the
precise `$1FF8/$1FF9` selector when that bank-changing edge was established.
Ambiguous cases explicitly say when the legacy/size default was merely retained,
and the zero-survivor fallback likewise admits that no flow hypothesis survived.
`test/vcsc_disassembler.pl` locks positive F8 evidence, negative JAM rejection,
CV split-RAM elimination, 3E-over-3F subsumption, unresolved F8 fallback, and the
zero-survivor F6 fallback.  Focused disassembler regression, hardening, opcode,
and fuzz tests pass 4/4.

2026-09-08 17:57:41 PDT, disassembler success destination message
----------------------------------------------------------------
`vcsc-disas` now reports successful completion on stderr after all output work
has succeeded: `Success, output written to <path>` for file output and
`Success, output written to stdout` for `-o -`. The message is emitted only
after the main stream is written/closed and multicart sidecars succeed, so a
failed write never receives a false success message. Explicit `-o`, derived
`.s26`, and stdout output are regression-locked in `test/vcsc_disassembler.pl`.
`disassembler/roundtrip.pl` captures, validates, and consumes each success line
so bulk exact-round-trip runs remain quiet. Direct stdout-mode mapper tests were
updated to require the new success diagnostic. `disassembler/README.md` and the
disassembler durable contract document the CLI behavior.

Focused validation: `vcsc_disassembler.pl` completed 131/131 byte-exact cases
and `vcsc_disassembler_hardening.pl` passed.

2026-09-08 18:04:05 PDT, disassembler uniform failure status
-------------------------------------------------------------
`vcsc-disas` now ends every nonzero CLI exit on stderr with the uniform line
`Failure, disassembly not written`, after any specific diagnostic. Successful
help/version exits remain informational and unchanged. Early argument, input,
analysis, and semantic-hint failures use the same final status as output/open,
write/close, and multicart-sidecar failures. The CLI regression locks both a
bad-option failure and an empty-input failure, including the final status line
and absence of an output file. Public/internal disassembler documentation was
updated.

The previously untimestamped mapper-evidence provenance entry in this history
file was repaired using the archived source-file mtime: 2026-09-08 17:02:40 UTC
= 2026-09-08 10:02:40 PDT. All top-level September 8 change entries now carry
the required local timestamp header.

Validation: `vcsc_disassembler.pl` passes all 131 round-trip fixtures,
`vcsc_disassembler_hardening.pl`, `context_history_format.pl`, and
`source_tree_hygiene.pl` pass, and a direct separator audit confirms six
September 8 history entries with six timestamped headers.

2026-09-08 20:40:04 PDT, disassembler VCSC vector bridge recovery
-----------------------------------------------------------------
Fixed conservative code recovery at signed VCSC banked cartridge tails. RESET
is now the sole stock-6507 initial hardware execution root across all mapper
paths; the former unconditional NMI root is removed. Reachable BRK still
promotes the mapper-visible IRQ/BRK vector and retains the existing provable
BRK/IRQ/RTI continuation analysis.

For a banked image carrying VCSC's explicit mapper tail signature,
`vcsc-disas` now recognizes the linker's generated three-slot vector bridge only
when the complete structural invariant proves it: RESET and IRQ vectors imply
equal adjacent slot sizes, all three mapper-entry prefixes are byte-identical,
each prefix decodes cleanly, every slot ends in absolute JMP to cartridge
space, and non-final NMI vectors (when present) agree with the inferred first
slot. Those bridge slots are classified as established code without rooting or
following otherwise-unreachable NMI/IRQ handlers. A signature without the full
bridge shape promotes nothing.

The user's F8 ROM now recovers the missed final-bank IRQ bridge at `$FFEC` as
`BIT $1FF9` / `JMP $F19D` (and likewise the NMI bridge slot at `$FFE0`), while
round-tripping to the identical 8192-byte image. `test/vcsc_disassembler.pl`
adds a signed synthetic F8 linker-bridge fixture and an NMI-only 1K negative
fixture. The disassembler README and hot contract document the distinction.

Validation: the main disassembler regression passes 132 byte-exact fixtures;
`vcsc_disassembler_hardening.pl`, all-opcode round trip, deterministic fuzz
(30/30), source-tree hygiene, and context-history format pass. The supplied
`f8.bin` also passes a direct one-file exact round trip.

2026-09-08 21:12:12 PDT, fix stale UA precedence diagnostics
-----------------------------------------------------------
Mapper refinement no longer lets an identified UA/UASW family reject unrelated
8K hypotheses before stronger executable bank-switch evidence has run. UA-family
precedence is now applied only after switch-avoided-halt and mapper-specific
bank-switch filtering, so a rejected UA/UASW hypothesis can no longer appear in
the final header as having outranked E0/0840/0FA0. The diagnostic wording now
explicitly says "surviving identified UA-family evidence" when that precedence is
actually used.

`test/vcsc_disassembler.pl` adds a raw-UA-data/F8 regression that requires UA to
lose to surviving mapper-specific switching and forbids any stale
`UA-family evidence outranks` line. The supplied `f8.bin` now reports F8 as the
sole survivor, with E0/0840/UA/UASW/0FA0 rejected by surviving mapper-specific
bank-switch evidence rather than by a UA family that was later rejected.

Validation: the bundled Debian Bison/Flex packages rebuilt `vcsc-as` and
`vcsc-disas` cleanly. `test/vcsc_disassembler.pl` passes, disassembler hardening
passes, and deterministic disassembler fuzz passes 30/30. The supplied `f8.bin`
and the synthetic raw-UA-data F8 fixture both produce the corrected mapper-
evidence header.

2026-09-08 21:53:09 PDT, generalize mirror normalization
------------------------------------------------------------
Replaced the disassembler's hard-coded doubled-2K and doubled-4K cases with one
pre-inference normalization pass for power-of-two inputs. Automatic inference now
repeatedly compares exact upper/lower halves and reduces to the smallest unique
logical power-of-two image, with a hard 128-byte floor. The full physical input is
still retained and emitted byte-for-byte after the logical image, so round-trip
preservation remains exact. Explicit --mapper selection bypasses this automatic
collapse, preserving the override contract.

Unbanked 128-, 256-, and 512-byte ROMs now use the existing mirrored unbanked
mapper model alongside 1K, including correct modulo-ROM bus mapping in static,
concrete, and dynamic analysis and canonical top-mirror origins $FF80/$FF00/$FE00.
CV's doubled-2K storage form now flows through the generic mirror mechanism;
nonidentical Stella 4K CV save images keep their special RAM/padding layout.

Regressions cover native 128/256/512-byte images, a 4K file containing 32 exact
copies of one 128-byte ROM, the former doubled-2K/doubled-4K cases, and a 16K
preservation file containing one complete distinct-bank 8K F8 image twice. The
main vcsc-disas regression suite, hardening suite, all-opcode round trip, and
deterministic fuzz suite all pass; fuzz is 36/36 exact, including two each at 128/256/512 bytes.

2026-09-08 22:23:00 PDT, normalize tiny ROMs in Stella differential
------------------------------------------------------------------
`roundtrip.pl --stella` now recognizes the physical-topology names introduced
for native 128-, 256-, and 512-byte unbanked ROMs. VCSC headers
`unbanked 128-byte`, `unbanked 256-byte`, and `unbanked 512-byte` normalize to
`128B`, `256B`, and `512B`. Stella's corresponding `2K* (128B)`,
`2K* (256B)`, and `2K* (512B)` spellings normalize to the same names, extending
the existing `2K* (1K)` -> `1K` equivalence. Stella uses the 2K cartridge class
for these tiny images while preserving the physical size in its description.

The fake-Stella hardening corpus now includes native 128/256/512-byte ROMs and
requires all of them to compare as mapper matches in both ordinary `--stella`
and `--stella-strict` runs. Public disassembler documentation records the four
2K-class physical-topology equivalences. Focused disassembler hardening passes.

2026-09-08 22:47:13 PDT, align Stella with logical mirror image
----------------------------------------------------------------
`roundtrip.pl --stella` now compares mapper inference against the same logical
unique ROM that `vcsc-disas` analyzed after exact repeated-half normalization.
Previously the disassembler could correctly reduce a repeated preservation dump
while the corpus verifier still ran Stella on the larger physical file, causing
Stella's size default to create a false mapper mismatch.

The real 16K Acid Drop image `17ee23e5da931be82f733917adcb6386` is exactly
`[A,B,A,B]`: its two 8K halves are identical while A and B are distinct 4K banks.
Bundled Stella 7.0 reports `F6* (16K)` for the physical file and `F8* (8K)` for
the unique 8K `[A,B]` image. The patched verifier reports the normalization and
compares VCSC F8 against Stella F8; `--stella-strict` therefore passes without
weakening the full 16K byte-exact round-trip check.

Hardening adds a synthetic distinct-bank 8K image duplicated to 16K. Fake Stella
returns F6 for 16K and F8 for 8K, so the regression fails if the verifier ever
passes the physical duplicate to Stella again. The focused hardening test passes,
and the uploaded Acid Drop ROM passes a real bundled-Stella strict comparison.
