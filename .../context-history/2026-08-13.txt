2026-08-13 08:31:14 PDT, Add inline callsite prepass
Started main-roadmap item 31 with a focused `inline_roadmap.txt`.  Subsection 0
is complete: a pre-lowering AST census records ordinary direct-call occurrences,
unique callers, and exact call-expression nodes before any ordinary function
body is emitted.  Source-inline expansion multiplicity is modeled explicitly,
while the existing emitted call graph remains a separate deduplicated relation.
Focused unit/codegen tests pass and the full compile-side suite is 475/475.
Parameter specialization and optimizer-selected inlining are not implemented yet.

2026-08-13 10:10:42 PDT, Complete fixed ref specialization
Completed inline-roadmap subsection 1. Single-callsite static ref parameters now
bind directly to fixed caller lvalues for regular, const, and writeonly refs,
including zeropage and split read/write aliases. Binding propagates to a fixed
point through ref-call chains; unresolved runtime-index actuals retain the old
address slot. Inline-asm candidates remain conservatively vetoed pending the
later assembly-boundary proof. Added compile and E2E/map/opcode/simulator tests.
The compile suite is 476/476; the full E2E attempt reached 109/278 with no
failures before the execution limit. Also taught vcsc_branding.pl to recognize
the required inline_roadmap.txt developer record.

2026-08-13 10:35:00 PDT, Update F8SC banked size oracle
The full E2E suite exposed stale F8SC size expectations after subsection-1
split-address direct RMW lowering. The maintained banked renderer now measures
1892 bank0 bytes, 53 bank1 bytes, and 97 RIOT RAM bytes; runtime/raster behavior
is unchanged. Updated the exact-size oracle to lock the optimized result.

2026-08-13 11:12:10 PDT, Add readonly value specialization
Inline-roadmap subsection 2 is mostly complete. Single-callsite explicit-const
and inferred-readonly value formals can bind directly to stable exact-type caller
storage or integer constants, eliminating the formal RAM slot. Conservative
write/ref/address/escape/alias/conversion cases retain by-value copies. Direct
constant-formal if conditions prune dead arms; general post-binding expression
folding/DCE remains partial. Added compile and E2E RAM/semantic regressions. The
compile suite is 477/477; a full E2E attempt reached 106/279 without failure, and
focused late-suite banked/examples/branding/specialization checks pass 7/7.

2026-08-13 11:54:04 PDT, Add forced IR inliner mechanism
Implemented subsection-3 ordinary-function control-flow expansion behind the
-X inlineir test gate. Added activation-only lifetime graph metadata so inlined
callees retain RAM overlay relationships without contributing fake hardware call
stack depth. Tests cover copied parameters, early returns, nested chains, sibling
overlay, ordinary calls inside inlined bodies, simulation, and stack reduction.
General post-inline census/DCE remains partial; production selection remains
blocked on placement legality and measured profitability. Compile-side tests pass
478/478; focused integration/hygiene passes 9/9; broader E2E reached 140/280
without failure before the execution limit. `make installcheck` passes.

2026-08-13 13:45:48 PDT, Restore inline contract metadata
Subsection-3 accidentally returned from source-inline function lowering before
emitting require/recommend contract metadata. This removed template lifecycle
requirements and allowed missing hooks to link. Restored contract emission
before the source/optimizer-inline early return. Compile suite passes 478/478;
the full E2E suite passes 280/280 when run in chunks.

2026-08-13 14:26:58 PDT, Add inline placement legality gate
Completed inline-roadmap subsection 4. Added caller-aware placement legality:
hard function page containment, named code/result regions, and generated-code
.same/.cross policy on either the candidate or unique caller retain the ordinary
call. Hard branch scanning follows source-inline expansion transitively. Added
compile and E2E coverage, including a page fixture that would exceed 256 bytes
if inlined and linked same/cross branches that remain physically satisfied.
Compile-side suite passes 480/480, the complete E2E suite passes 281/281 in
bounded chunks, and make installcheck passes. Production inlining remains gated
on measured profitability.

2026-08-13 16:01:57 PDT, Add measured inline profitability
Completed inline-roadmap subsection 5 behind the internal -finline-profit path.
The driver now trial-links legal candidates against the real final image, accepts
measured ROM wins or equal-ROM stack wins without activation-RAM regression, and
retries candidates after each accepted transform. Added exact final-link win/loss,
stack, RAM-overlay, and nested-chain tests. Also fixed lazy inline-argument scratch
allocation and propagated readonly constants through specialized parameter chains.
Production selection remains off until subsection 6 ABI/reachability vetoes land.
Validation: compile-side 481/481; complete E2E 282/282 in bounded chunks; make
installcheck passes. Subsection 6 is next; production inlining remains disabled.
