2026-09-16 08:53:00 PDT, complete example reorg ER13
ER13 moved the complete ten-cartridge unofficial player-color score matrix from
`07_player_color_181_unofficial` to the peer
`04_renderers/player_color_unofficial/score_{above,below}` tree. Centered,
left, right, two-plus-two, and poison leaves are now flattened directly under
the above/below composition directories. The retired numbered top-level family
and all single-child `01_interactive` layers are gone.

Every moved Makefile still enables `-Wa,--illegals` explicitly. Source-relative
`_common` paths and Makefile root depth were adjusted for the new tree. Public
renderer documentation, `examples/README.md`, matched score/raster tests, and
the reorg stale-path checklist now name the peer renderer path directly rather
than probing or preserving the retired path.

All ten public ROMs are byte-identical to their pre-move builds:
* centered above: 9150b5aed532d62c587158276f5937253fbc119a38f0e90cd93c445cd0100934
* centered below: 2a40822a3e06c73a08a9c33aaae83258407f7e787ed6fa4b23946de2e89b9b49
* left above:     9cc932c9c990d306ab5069dd8c00974ad4d12a19de865d7b1ebcc0cb8131bf34
* left below:     9346f7597536214bf212fdaace3997c01c8c9980770b9abeb9ca9f0d37c8d9cb
* right above:    3f9a85f56ccc911595a62cdbd12ffbf0e3439a539ae26a0e68da3f13e708e27f
* right below:    1e3a2a30b55c840165c5ec4d7bc17823cc8a4ee341c01382f20ba6013ea05907
* 2+2 above:      61003bb3e805c4ef14476ec83400d2a21c6f28ea50d0ce618f102e2045064166
* 2+2 below:      84d8b071741a1c41e781174427d101c4d8e84f301228b79e5442ea5657ad2d90
* poison above:   03b0b51a044445c9bc591f31617a36c3398f1f6dd082d8e8903f9668745155ea
* poison below:   85b5efbc456887889d3fdda5c8f91549306af7554c6836056203f5c4ee830ee8

Focused validation passed the reorg inventory, source-tree hygiene, assembly
allowlist, eight-example multicolor/opcode-policy test, public score-control
matrix, unofficial renderer official/twin comparison, visible component handoff,
and the complete unofficial score-composition raster matrix including mixed
placement. ER14 is next.

2026-09-16 11:10:43 PDT, complete example reorg ER14-ER25
--------------------------------------------------------
ER14-ER18 completed the renderer-family consolidation. Official `all_five`
moved in two slices (7/7 then 7/7 byte-identical ROMs),
`all_five_unofficial` moved as a peer renderer (11/11 byte-identical with every
Makefile retaining explicit `-Wa,--illegals`), `all_five_player_color` moved
with its shared helper (3/3 byte-identical), and modern multisprite plus enhanced
asymmetric multisprite moved without touching the already-migrated faithful
legacy family (4/4 byte-identical). Their focused behavior/raster tests,
including pinned-Stella certifications for the combined and modern multisprite
renderers, pass.

ER19 transposed the twelve existing PAL/SECAM cartridges from standard-first to
demonstration-first layout under `05_video_standards`; all 12/12 ROMs remained
byte-identical and the layout/timing/VSYNC/Stella regressions pass. ER20-ER21
then added the six missing NTSC cells to form the complete 18-cell standards
matrix. Reusable NTSC renderer cells are byte-identical to their canonical
renderer examples. The unofficial all-five NTSC cell deliberately scene-locks
to its official peer while changing only renderer identity and preserving the
explicit illegal-opcode opt-in. The complete 18-cell Stella certification
passes under the pinned palette.

ER22 moved the standalone 6507 fingerprint and F4SC field diagnostic into
`07_diagnostics`. Their ROMs remained byte-identical; the fingerprint digest is
`5e16400a3da00f9c0e0d854223b0319178bda7c5b4f5835622d12fe121974891` and
the field diagnostic digest is
`9b9de4584325c227c0487b94dda967c5992c2f92393982ebf938aa59325ce172`.
Focused diagnostic regressions pass. The exhaustive paddle-threshold regression
also passes in the final unified e2e run.

ER23 moved the complete bankswitching family under
`07_diagnostics/bankswitching`, removed numeric mapper prefixes, fixed every
Makefile root depth and nine newly exposed relative `BANKSWITCHING.md` links,
and updated root discovery/install/test/documentation paths. All 29 mapper ROMs
intended as pure relocations matched their pre-move hashes. Focused mapper
regressions, the non-Stella F8/F6/F4 matrix, standard-renderer banked Stella
raster, and partitioned F8/F8SC/F6/F6SC/F4/F4SC/poison Stella certifications
pass.

ER24 removed the final dead `19_diagnostic` README shell and completed the
retired-path/documentation sweep. The examples top level is now exactly
`01_basics`, `02_components`, `03_controllers`, `04_renderers`,
`05_video_standards`, `06_games`, `07_diagnostics`, plus non-runnable `_common`;
there are no active retired-family references outside cold migration/history
records. Public example/root/platform/library/renderer documentation now uses
the purpose-oriented tree.

ER25 converted the final layout assumptions into executable inventory checks.
The recursive public-example verifier was corrected to follow local include
closure so wrapper examples inherit transitive illegal-opcode/define and
source-adjacent startup requirements without duplicating scene source. All 112
editable cartridge entry points pass its direct-driver compile/link/disassembly
round trip, and all 118 public Makefile leaves independently pass `make clean` +
`make all`. `make installcheck`, install-manifest/release-payload regressions,
source-tree hygiene, assembly allowlist, final layout inventory, and the focused
Stella suites pass. The ER1-ER25 migration is closed; future example-tree work
returns to the normal project roadmap.

2026-09-16 12:00:22 PDT, close ER25 final acceptance
----------------------------------------
Final post-reorganization acceptance exposed and fixed three stale contracts:

the generated-font subset test still synthesized the retired field-diagnostic
path, the interactive-sprite orientation inventory still assumed numbered
`01_interactive` shells, and the assembly-allowlist runner expectation still
reported 31 pinned sources after the final tree contained 32.  The first two
were corrected to discover the new purpose-oriented tree/include closure; the
allowlist contract now reports the actual 32-source inventory.

The corrected tree passes 537/537 compile-only cases.  The complete 386-case
e2e run passed 385 cases and identified only the stale 31-source allowlist
expectation; after correcting that expectation, the same runner-level
`example_assembly_allowlist.pl` case passes.  Thus every one of the 386 e2e
cases passes on the final tree.  The broad run also re-certified the exhaustive
paddle-threshold test, the complete bankswitching Stella matrix, all 12
NTSC/PAL/SECAM diagnostic rasters, the heart exact-RGB oracle, the 18-cell
video-standard matrix, all eight recursive public-example build shards, and the
hidden-path regressions fixed during ER25.

2026-09-16 12:33:13 PDT, simplify joystick tutorial
---------------------------------------------------
Replaced the old five-band left-port bit monitor with a direct two-player
joystick demonstration. The left joystick moves a red P0 square and the right
joystick moves a blue P1 square over a black background. Each player normally
uses four `..XXXX..` bitmap rows. Holding that port's fire button selects the
requested eight-row `.XXXXXX.` / `X......X` / `X.XXXX.X` outlined form while
preserving the 4x4 center.

The example now delegates frame timing and player positioning to the maintained
192-line `player_color` renderer so the controller tutorial itself contains only
direct SWCHA/INPT4/INPT5 input, movement bounds, and bitmap selection. The
README documents both SWCHA nibbles and both active-low fire inputs.

`test/vcs_joystick.pl` now runs the compiled cartridge through the shared 6502
frame harness, drives both joystick nibbles and both fire buttons, and verifies
independent red/blue position changes plus the 8-row fire-state graphics
pointers/heights. The joystick moved from direct VSYNC ownership to the shared
`vcs_ntsc_vsync()` helper, so the VSYNC-contract regression was updated
accordingly. Focused joystick/runtime, VSYNC-contract, hygiene, 32-source asm
allowlist, reorg-inventory, recursive public-example shard 2/8, and the full
537/537 compile-only gate pass.

2026-09-16 12:45:29 PDT, center normal joystick sprite in 8 rows
----------------------------------------------------------------
Adjusted the simplified joystick tutorial's normal player bitmap to the intended
8-row form: two blank rows, four `..XXXX..` rows, and two blank rows. The fire
state remains the requested 8-row outline. Both normal and fire states now keep
player height at 7; fire switches each graphics pointer by eight bytes instead
of changing sprite height. Updated the README and `vcs_joystick.pl` contract to
lock the exact normal bitmap and 8-byte fire-state offset. Focused joystick runtime,
VSYNC-contract, source-tree-hygiene, and direct example-build checks pass.

2026-09-16 13:18:01 PDT, replace paddle tutorial
-------------------------------------------------------
Replaced the old single-paddle raw timing/binary monitor with a direct visual
four-controller tutorial. The screen is black and uses P0, M0, M1, and P1 as
four equally spaced white markers at Atari logical X=20,60,100,140. Paddle
inputs 0..3 independently move those markers vertically. Each marker is exactly
2 logical pixels wide by 8 scanlines tall; it is solid with the corresponding
button released and alternating-scanline dotted while that button is held.

The cartridge now instantiates `four_paddles.c26`, preserves the normal
VBLANK.7 RC discharge/release lifecycle, and samples one analog input at a
fixed phase on each of the four scanlines in its repeating visible-kernel
schedule. P0/P1 use two adjacent player bits and M0/M1 use the TIA 2-pixel
missile width, avoiding multiplexing machinery in a controller tutorial.

`vcs_paddle_tutorial.pl` now locks the four-input/four-button mapping, marker
positions and widths, solid/dotted patterns, RC lifecycle, 192-line renderer,
and 4K build. The hand-timed visible kernel is classified in the example asm
allowlist, raising the pinned inventory from 32 to 33 sources. Focused paddle,
four-player paddle API, score-paddle sampling, source-tree hygiene,
example-reorg inventory, recursive public-example shard 3/8, and the complete
537/537 compile-only gate pass. Stella 7 with the pinned palette visually
confirms four solid 2x8 markers at X=20,60,100,140; the button-dotted state is
locked by source/test contracts rather than claimed as a Stella input-injection
capture.

2026-09-16 13:54:14 PDT, add driving-controller orbit display
--------------------------------------------------------------
Kept the two public driving-controller P0/P1 hexadecimal accumulated-rotation
displays and added M0/M1 as matching 4x4 orbit markers. Each missile advances
through a 16-position clockwise table or reverses through it for counterclockwise
motion, wrapping with the same low-nibble accumulator that selects the displayed
hex digit. The orbit is 12 logical pixels in horizontal radius and about 20
scanlines vertically so it appears approximately circular with VCS pixel aspect.
NUSIZ0/1 use the native four-pixel missile width and the 48-line visible scene
enables each missile for exactly four scanlines. Fire still changes that side from
white to red; the missile inherits its matching player's color automatically.

The old player positioning constants were also corrected from the raw RESP operand
to the calibrated values required for actual logical x=36 and x=116. Stella 7 with
the pinned palette confirms phase 0 as a centered 4x4 dot above each glyph, and a
forced phase 4/12 capture puts the left dot to the right of `4` and the right dot to
the left of `C`, confirming opposite sides of the orbit. `vcs_driving.pl` now locks
the 16-position tables, 4x4 missile contract, and runtime phase-derived orbit
coordinates while retaining Gray-code, direction, wrap, button, and stable-frame
checks. The driving example's beam-critical allowlist entry was deliberately updated
to 119 asm statements. Focused driving, VSYNC-contract, source-tree-hygiene,
33-source assembly-allowlist, recursive public-example shard 1/8, and the complete
537/537 compile-only gate pass.

The incoming paddle handoff accidentally contained `test/README.md` bytes at the
repository root despite the prior turn intending to restore the real project README.
Restored the root `README.md` from the immediately preceding known-good handoff;
source-tree hygiene now verifies the required `# VCSC Toolchain` heading again.

2026-09-16 14:21:44 PDT, fix left-side driving orbit clipping
--------------------------------------------------------------
The driving-controller orbit renderer no longer changes ENAM0/ENAM1 on the
scanline being drawn.  The horizontal M0/M1 positions are still established
completely during VBLANK, and each vertical on/off boundary is now prepared in
RAM one scanline before it is needed.  Immediately after the following WSYNC,
the prepared ENAM0/ENAM1 values are applied before the visible beam can reach
even the far-left M0 positions.  The leading blank gap was reduced from 72 to
71 lines because the 48-line scene now owns an initial WSYNC; a terminal WSYNC
still completes scene line 47, preserving the frame geometry.

This fixes the partial M0 square observed specifically at left-controller phases
B and C.  Stella 7 pinned-palette forced-phase captures verify a full 4x4 logical
missile at both phases: B occupies screenshot pixels x=54..61/y=115..118 and C
occupies x=52..59/y=107..110, i.e. 8 screenshot pixels wide (four VCS color
clocks at Stella's 2x horizontal presentation) by four scanlines.  The focused
runtime/frame test, VSYNC contract, 33-source assembly allowlist, hygiene,
example-reorg inventory, recursive public-example shard 1/8, and complete
537/537 compile-only gate pass.

2026-09-16 15:45:19 PDT, complete DRA1 parser surface
-------------------------------------------------------
Implemented DRA1 only. The compiler now gives the ten planned Direct Register
Access spellings dedicated lexer tokens and AST leaves, reserves `$P` and `$_`
with intentional diagnostics, and parses the deliberately narrow assignment and
flag-condition surface without treating the pseudo-objects as ordinary
identifiers. A pre-codegen DRA validator rejects declarations, address-taking,
general-expression use, unsupported register transfers, invalid status uses,
and non-direct assignment shapes. Valid DRA source is intentionally stopped
before optimization/code generation so this slice cannot emit functioning DRA
machine code. Existing lone `_` discard behavior is unchanged.

Added 13 focused DRA tests covering the accepted parser/AST surface, reserved
spellings, rejection cases, and the deliberate no-codegen boundary. Existing
discard-result/store and `(void)` regressions continue to pass. The exhaustive
`blanket.c26` and `coverage.c26` parser fixtures now exercise every new DRA
grammar production rather than exempting it. Focused DRA/discard/coverage tests
pass 18/18 and the complete compile-only suite passes 550/550. The full e2e
suite was not rerun for this syntax-only slice.


2026-09-16 16:28:42 PDT, fix diagnostic live-row certification
------------------------------------------------------
The user-reported DRA1 full-suite failure was traced to a stale Stella raster mask,
not DRA compiler output. The diagnostic certification still claimed to mask the two
live controller-detail rows at 109..126, but the current renderer places those live
bands at 125..133 and 138..146. Driving-button phase changes therefore leaked into
the supposedly stable digest as a four-pixel F0/P0 difference. Updated the mask and
its source-tree-hygiene contract to the actual two live bands; headings, collision
areas, first-lit-row checks, and exact pinned-palette RGB outside those bands remain
strict. A fresh ntsc_driving Stella certification and source-tree hygiene pass.

2026-09-16 16:28:42 PDT, complete DRA2 accumulator access
-------------------------------------------------
Implemented DRA2 exact accumulator access. `$A := byte/immediate` lowers directly to
LDA and `byte := $A` directly to STA for one-byte non-bitfield directly addressable
objects and legal absolute refs, with byte-range and directional-ref diagnostics.
Added DRA emission markers that are removed from final assembly but make explicit DRA
instructions non-rewritable/non-elidable while still feeding the peephole register
and N/Z state model. No hidden scratch, pointer setup, save/restore, or `_` changes
were introduced. Focused DRA2 exact-code/negative/simulator tests pass 10/10; the
simulator locks value transfer plus LDA N/Z behavior through an intervening STA. The
complete compile-only suite passes 558/558.

2026-09-16 16:30:44 PDT, complete DRA3 X/Y direct access
------------------------------------------------
Implemented exact LDX/LDY and STX/STY DRA lowering for the same direct-byte/immediate operand class as A, including directional absolute refs and byte/non-bitfield constraints. No implicit transfer through A is generated. Focused exact-code, negative, and simulator coverage passes 9/9; the simulator locks X/Y values and load-established N/Z through direct stores.

2026-09-16 16:31:33 PDT, complete DRA4 native transfers
--------------------------------------------
Implemented the six native DRA transfers TAX/TAY/TXA/TYA/TSX/TXS with no hidden intermediate register or stack traffic. Existing DRA1 diagnostics continue to reject X<->Y, unsupported S forms, memory<->S, and self-transfer. Focused exact-opcode/negative/simulator coverage passes 7/7; simulator checks transfer-established N/Z and TXS no-flag behavior.

2026-09-16 16:35:49 PDT, complete DRA5 direct flag conditions
------------------------------------------------
Implemented bare and unary-negated `$C/$Z/$N/$V` truth tests in if, while,
do/while, and for condition positions as direct native conditional branches. The
false-branch mappings are BCC/BCS, BNE/BEQ, BPL/BMI, and BVC/BVS respectively;
DRA emission barriers keep those explicit branches non-rewritable while leaving
them as normal bare/flex compiler branches for ordinary placement/relaxation.
No byte, stack, or hidden register operation is materialized. `$I/$D` reads,
compound flag conditions, and flag value uses remain rejected. Focused codegen,
negative, and simulator coverage passes 10/10, including taken and not-taken
execution for every readable flag and all four statement-condition positions.
The existing complete compile-only gate passed 565/565 after the compiler change;
newly added DRA5 fixtures passed separately.


2026-09-16 16:39:00 PDT, complete DRA6 direct writable flags
-------------------------------------------------------------
Implemented constant-only direct writes for `$C/$I/$D := 0/1` and `$V := 0`,
lowering exactly to CLC/SEC, CLI/SEI, CLD/SED, and CLV. `$V := 1`, `$Z/$N`
writes, runtime/nonconstant RHS values, out-of-range constants, and compound
assignments remain intentional errors. Focused exact-code, negative, and simulator
coverage verifies opcode selection and resulting processor flags without hidden
storage or register traffic.


2026-09-16 16:52:00 PDT, complete DRA7 control-flow/lifetime integration
---------------------------------------------------------------------
Extended readable `$C/$Z/$N/$V` tests through nested `!`, `&&`, and `||` using
the compiler's ordinary short-circuit branch lowering, while retaining intentional
errors for `$I/$D` reads and value-producing flag uses. Added optimizer/lifetime
regressions proving explicit DRA A/X writes invalidate affected ordinary register
and N/Z facts rather than allowing stale reuse, plus mixed ordinary/DRA condition
coverage and a simulator point-in-time test across intervening state changes.
Explicit DRA operations remain optimizer-visible barriers rather than opaque asm.
The focused DRA7 set passes 9/9 and the complete compile-only gate passes 580/580.
The user-reported diagnostic instability remains fixed separately by the corrected
Stella live-row mask; a fresh targeted `ntsc_driving` certification passes with
Stella 7 and the pinned palette.

2026-09-16 17:05:00 PDT, DRA7 handoff verification
--------------------------------------------------
Revalidated the completed DRA7 checkpoint after the chat transport interruptions.
All 49 DRA-focused compiler/simulator tests pass, and the complete compile-only
suite passes 580/580. The user-reported diagnostic failure is covered by the
corrected live-row mask; a fresh targeted `ntsc_driving` Stella 7 pinned-palette
certification passes. A later attempt to run the four-case NTSC diagnostic batch
hit the execution transport limit after `ntsc_joystick`; do not claim that batch
or the full 12-case diagnostic matrix as rerun for this handoff.
