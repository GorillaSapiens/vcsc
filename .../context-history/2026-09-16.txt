2026-09-16 08:53:00 PDT, complete example reorg ER13
ER13 moved the complete ten-cartridge unofficial player-color score matrix from
`07_player_color_181_unofficial` to the peer
`04_renderers/player_color_unofficial/score_{above,below}` tree. Centered,
left, right, two-plus-two, and poison leaves are now flattened directly under
the above/below composition directories. The retired numbered top-level family
and all single-child `01_interactive` layers are gone.

Every moved Makefile still enables `-Wa,--illegals` explicitly. Source-relative
`_common` paths and Makefile root depth were adjusted for the new tree. Public
renderer documentation, `examples/README.md`, matched score/raster tests, and
the reorg stale-path checklist now name the peer renderer path directly rather
than probing or preserving the retired path.

All ten public ROMs are byte-identical to their pre-move builds:
* centered above: 9150b5aed532d62c587158276f5937253fbc119a38f0e90cd93c445cd0100934
* centered below: 2a40822a3e06c73a08a9c33aaae83258407f7e787ed6fa4b23946de2e89b9b49
* left above:     9cc932c9c990d306ab5069dd8c00974ad4d12a19de865d7b1ebcc0cb8131bf34
* left below:     9346f7597536214bf212fdaace3997c01c8c9980770b9abeb9ca9f0d37c8d9cb
* right above:    3f9a85f56ccc911595a62cdbd12ffbf0e3439a539ae26a0e68da3f13e708e27f
* right below:    1e3a2a30b55c840165c5ec4d7bc17823cc8a4ee341c01382f20ba6013ea05907
* 2+2 above:      61003bb3e805c4ef14476ec83400d2a21c6f28ea50d0ce618f102e2045064166
* 2+2 below:      84d8b071741a1c41e781174427d101c4d8e84f301228b79e5442ea5657ad2d90
* poison above:   03b0b51a044445c9bc591f31617a36c3398f1f6dd082d8e8903f9668745155ea
* poison below:   85b5efbc456887889d3fdda5c8f91549306af7554c6836056203f5c4ee830ee8

Focused validation passed the reorg inventory, source-tree hygiene, assembly
allowlist, eight-example multicolor/opcode-policy test, public score-control
matrix, unofficial renderer official/twin comparison, visible component handoff,
and the complete unofficial score-composition raster matrix including mixed
placement. ER14 is next.

2026-09-16 11:10:43 PDT, complete example reorg ER14-ER25
--------------------------------------------------------
ER14-ER18 completed the renderer-family consolidation. Official `all_five`
moved in two slices (7/7 then 7/7 byte-identical ROMs),
`all_five_unofficial` moved as a peer renderer (11/11 byte-identical with every
Makefile retaining explicit `-Wa,--illegals`), `all_five_player_color` moved
with its shared helper (3/3 byte-identical), and modern multisprite plus enhanced
asymmetric multisprite moved without touching the already-migrated faithful
legacy family (4/4 byte-identical). Their focused behavior/raster tests,
including pinned-Stella certifications for the combined and modern multisprite
renderers, pass.

ER19 transposed the twelve existing PAL/SECAM cartridges from standard-first to
demonstration-first layout under `05_video_standards`; all 12/12 ROMs remained
byte-identical and the layout/timing/VSYNC/Stella regressions pass. ER20-ER21
then added the six missing NTSC cells to form the complete 18-cell standards
matrix. Reusable NTSC renderer cells are byte-identical to their canonical
renderer examples. The unofficial all-five NTSC cell deliberately scene-locks
to its official peer while changing only renderer identity and preserving the
explicit illegal-opcode opt-in. The complete 18-cell Stella certification
passes under the pinned palette.

ER22 moved the standalone 6507 fingerprint and F4SC field diagnostic into
`07_diagnostics`. Their ROMs remained byte-identical; the fingerprint digest is
`5e16400a3da00f9c0e0d854223b0319178bda7c5b4f5835622d12fe121974891` and
the field diagnostic digest is
`9b9de4584325c227c0487b94dda967c5992c2f92393982ebf938aa59325ce172`.
Focused diagnostic regressions pass. The exhaustive paddle-threshold regression
also passes in the final unified e2e run.

ER23 moved the complete bankswitching family under
`07_diagnostics/bankswitching`, removed numeric mapper prefixes, fixed every
Makefile root depth and nine newly exposed relative `BANKSWITCHING.md` links,
and updated root discovery/install/test/documentation paths. All 29 mapper ROMs
intended as pure relocations matched their pre-move hashes. Focused mapper
regressions, the non-Stella F8/F6/F4 matrix, standard-renderer banked Stella
raster, and partitioned F8/F8SC/F6/F6SC/F4/F4SC/poison Stella certifications
pass.

ER24 removed the final dead `19_diagnostic` README shell and completed the
retired-path/documentation sweep. The examples top level is now exactly
`01_basics`, `02_components`, `03_controllers`, `04_renderers`,
`05_video_standards`, `06_games`, `07_diagnostics`, plus non-runnable `_common`;
there are no active retired-family references outside cold migration/history
records. Public example/root/platform/library/renderer documentation now uses
the purpose-oriented tree.

ER25 converted the final layout assumptions into executable inventory checks.
The recursive public-example verifier was corrected to follow local include
closure so wrapper examples inherit transitive illegal-opcode/define and
source-adjacent startup requirements without duplicating scene source. All 112
editable cartridge entry points pass its direct-driver compile/link/disassembly
round trip, and all 118 public Makefile leaves independently pass `make clean` +
`make all`. `make installcheck`, install-manifest/release-payload regressions,
source-tree hygiene, assembly allowlist, final layout inventory, and the focused
Stella suites pass. The ER1-ER25 migration is closed; future example-tree work
returns to the normal project roadmap.

2026-09-16 12:00:22 PDT, close ER25 final acceptance
----------------------------------------
Final post-reorganization acceptance exposed and fixed three stale contracts:

the generated-font subset test still synthesized the retired field-diagnostic
path, the interactive-sprite orientation inventory still assumed numbered
`01_interactive` shells, and the assembly-allowlist runner expectation still
reported 31 pinned sources after the final tree contained 32.  The first two
were corrected to discover the new purpose-oriented tree/include closure; the
allowlist contract now reports the actual 32-source inventory.

The corrected tree passes 537/537 compile-only cases.  The complete 386-case
e2e run passed 385 cases and identified only the stale 31-source allowlist
expectation; after correcting that expectation, the same runner-level
`example_assembly_allowlist.pl` case passes.  Thus every one of the 386 e2e
cases passes on the final tree.  The broad run also re-certified the exhaustive
paddle-threshold test, the complete bankswitching Stella matrix, all 12
NTSC/PAL/SECAM diagnostic rasters, the heart exact-RGB oracle, the 18-cell
video-standard matrix, all eight recursive public-example build shards, and the
hidden-path regressions fixed during ER25.

2026-09-16 12:33:13 PDT, simplify joystick tutorial
---------------------------------------------------
Replaced the old five-band left-port bit monitor with a direct two-player
joystick demonstration. The left joystick moves a red P0 square and the right
joystick moves a blue P1 square over a black background. Each player normally
uses four `..XXXX..` bitmap rows. Holding that port's fire button selects the
requested eight-row `.XXXXXX.` / `X......X` / `X.XXXX.X` outlined form while
preserving the 4x4 center.

The example now delegates frame timing and player positioning to the maintained
192-line `player_color` renderer so the controller tutorial itself contains only
direct SWCHA/INPT4/INPT5 input, movement bounds, and bitmap selection. The
README documents both SWCHA nibbles and both active-low fire inputs.

`test/vcs_joystick.pl` now runs the compiled cartridge through the shared 6502
frame harness, drives both joystick nibbles and both fire buttons, and verifies
independent red/blue position changes plus the 8-row fire-state graphics
pointers/heights. The joystick moved from direct VSYNC ownership to the shared
`vcs_ntsc_vsync()` helper, so the VSYNC-contract regression was updated
accordingly. Focused joystick/runtime, VSYNC-contract, hygiene, 32-source asm
allowlist, reorg-inventory, recursive public-example shard 2/8, and the full
537/537 compile-only gate pass.

2026-09-16 12:45:29 PDT, center normal joystick sprite in 8 rows
----------------------------------------------------------------
Adjusted the simplified joystick tutorial's normal player bitmap to the intended
8-row form: two blank rows, four `..XXXX..` rows, and two blank rows. The fire
state remains the requested 8-row outline. Both normal and fire states now keep
player height at 7; fire switches each graphics pointer by eight bytes instead
of changing sprite height. Updated the README and `vcs_joystick.pl` contract to
lock the exact normal bitmap and 8-byte fire-state offset. Focused joystick runtime,
VSYNC-contract, source-tree-hygiene, and direct example-build checks pass.
