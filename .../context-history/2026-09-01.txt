2026-09-01 13:35:52 PDT, linker cfgectomy handoff
Began removing the parallel VCS mapper/linker cfg universe before FC. Deleted
all 31 checked-in `.cfg` profiles under `libraries/vcs`; opcode-table cfg files
and generic linker cfg fixtures intentionally remain. Added C26 `$stack` memory
metadata, allowed pure C26 topology without `-T`, taught the driver to recognize
C26 cartridge topology, emitted resolved topology in linker maps, and taught
`vcsc-sim` to consume/auto-discover those maps with a 256-bank table. Converted
many mapper, Superchip, replicated-ROM, bankswitch ABI, diagnostic, and e2e
consumers. Work remains in the F8 profile legacy byte-equality assertion, a few
renderer/FAITHFUL cfg consumers, hygiene/docs, and the final full-suite pass.

2026-09-01 13:40:45 PDT, cfgectomy validation and docs cleanup
Verified the F8 profile regression was already descriptor-aware and passes.
Updated maintained driver/linker/simulator/library/renderer/example/tool/test
documentation to remove deleted VCS linker-cfg assumptions while preserving
generic -T linker-script compatibility and assembler opcode cfgs. Confirmed
source_tree_hygiene recursively rejects every .cfg below libraries/vcs, repaired
the malformed September 1 history heading, and compacted context.txt below its
16 KiB ceiling. Full compile-only validation passed 503/503. E2E validation ran
green through the first 245/342 cases; additional tail checks passed the
diagnostic cartridge, DPC, driving, E0, enhanced multisprite, discard-strobe,
VSYNC, and example-build shards 1-6. The exhaustive/long remaining tail exceeded
the available execution window, so it is explicitly not recorded as rerun.

2026-09-01 14:05:44 PDT, cfgectomy regression fixes
Fixed the seven failures reported by the first complete post-cfgectomy e2e run.
Restored .../instruction.txt to the handoff tree and made .gitignore explicitly
unignore it so make tar from an unpacked/no-.git tree cannot silently omit it;
source_tree_hygiene now locks that packaging invariant. Shortened the malformed
history header. Removed dead cfg parameters from the player-color and banked
standard-renderer tests. Changed the fingerprint CRC selftest to flat .bin now
that its included 4K C26 cartridge topology correctly forbids Intel HEX output.
Made the return-memory test anchor its object-layout extraction in the OBJECTS
section so the new C26 CARTRIDGE TOPOLOGY source path cannot be mistaken for the
object header. All seven reported failures pass focused. Broad e2e reruns are
green through 245/342 and, separately, the tail through example-build shard 6;
the exhaustive diagnostic paddle test still exceeds this execution window.
2026-09-01 15:15:54 PDT, cfgectomy HEX output correction
Reversed the accidental fingerprint_selftest.hex -> .bin change and removed the
generic e2e runner's any-C26-topology -> .bin switch. Narrowed vcsc-ld's output
restriction: mapped C26 banks, including selector-controlled banks, may be Intel
HEX at their distinct linker addresses; only $data_only file-domain banks require
flat .bin because those bytes have no 6502 address. Added direct and selector
HEX regressions plus a data-only rejection regression. Verified an F8 diagnostic
links and runs as HEX under vcsc-sim with --map. vcsc-sim has no separate mapper
CLI argument: C26 mapper identity comes from --map topology/signature; legacy -T
uses mapper= from the cfg.
2026-09-01 16:46:11 PDT, WD two-logical-bank descriptor migration
Reframed WD's eight physical 1K chunks as two compiler-visible logical 4K
banks: state 1 (0,1,2,3) is bank0 and state 2 (4,5,6,7) is bank1. Added a
large mapper comment documenting why the other arrangements are not generally
safe for relocated 6502 code. Added WD descriptors 1/2, indexed read selectors
at $0038,Y, a replicated 72-byte inline bank-call block, automatic placement,
and compiler .banktarget lowering. The simulator map now stores two logical
banks while its WD hardware model still exposes all eight arrangements and the
always-live 64-byte split RAM. Reworked the WD diagnostic around automatic
nested bank calls and changed its visible result to the standard big/wide
PASS/FAIL renderer plus a small WD label. Focused WD/profile/template/hygiene
regressions pass.
2026-09-01 16:49:49 PDT, WD migration validation checkpoint
Completed WD descriptor-migration validation. Compile-only remains 503/503; all
eight editable-example shards pass, covering 103/103 compile/link/round-trip
examples. Focused WD, C26 profile, inline-template, source-hygiene, history, and
assembly-allowlist regressions pass. The WD simulator still exercises non-ABI
hardware state 3 independently, confirming the two compiler banks did not
remove support for the other six hardware arrangements.

2026-09-01 17:43:00 PDT, inline-template comments and WM font patch
Added an explicit assembly-only-origin warning immediately above `$6000` in all
15 `libraries/vcs/*/inline_bankcall.s26` templates: the synthetic non-zero-page
origin is never presented to the 6507 bus and exists to prevent accidental
zero-page relaxation before linker relocation. Applied the user-supplied
WM.patch, including the new seven-pixel default M/W glyphs and `*.swp` ignore.
Updated the font contract with tightly scoped exact M/W full-width shapes rather
than weakening side-margin checks globally. Ran top-level `make fonts`, which
regenerated the stale OMNI M subset and the UA/WD W subsets from
`default_ascii.c26`. Font contracts, generated subsets, ASCII alignment/catalog,
inline-bankcall source generation, WD diagnostic, source hygiene, and assembly
allowlist pass. A broad e2e checkpoint reached 224/342 with no failures before
the available execution window ended.
2026-09-01 20:11:42 PDT, mapper entry hooks and bankcall rename
Renamed the public cartridge opt-in from `$inline_bankcall` to `$bankcall`
and renamed all 15 maintained `inline_bankcall.s26` files to `bankcall.s26`.
Added mapper-owned three-byte `entry.s26` hooks for those same migrated
mappers. Entry sources use raw NMOS `op0C` absolute-NOP reads so reset-state
normalization preserves registers/flags and does not require assembler
`--illegal`; the linker now copies those maintained bytes ahead of each vector
handler instead of choosing BIT/STA reset selectors itself. Audited selector
accesses and changed FA2's descriptor transition from indexed STA to indexed
LDA because FA2 switches on access. Fixed the legacy generated JSR/JMP tables to
use side-effect-free `$0C` selector reads and updated their regressions.
Compile-only is 503/503; focused mapper/bankcall/entry/profile/hygiene tests are
green.
2026-09-01 20:35:31 PDT, bankcall selectors use raw op1C
Changed all 15 migrated descriptor bankcall templates to carry the destination
selector descriptor in X and use raw undocumented NMOS `op1C base,X` reads for
both switch and restore, preserving A/flags without requiring assembler
`--illegal`. Fixed the MOS6502 core so all six undocumented absolute-X NOPs
perform their real operand bus read; this repairs both vcsc-sim switching and
the disassembler's concrete discovery. Updated fixed renderer bridge tests for
the earlier `op0C` read selectors and taught their harness to count read-triggered
bank switches. The reported `vcs_standard_renderer_banked.pl` failure now passes.
The FA visible timing regression also passes unchanged after rebuilding vcsc-disas;
its 264-interval/262-line timing was not altered. Compile-only passed 503/503,
the focused mapper/linker set passed 18/18, and the final disassembler/version
tail passed 5/5.
