2026-09-20 09:08:37 PDT, Stella palette compiler option
Completed roadmap item 59.  `vcsc` and `vcsc-cc1` now accept
`--stella-palette FILE`, loading Stella's exact 792-byte combined user-palette
layout: 128 NTSC RGB triples, 128 PAL RGB triples, then 8 SECAM RGB triples.
The three compile-time RGB builtins select only their corresponding slice; no
option preserves the compiled-in tables exactly.

Added clear diagnostics for unreadable, short, long, and empty palette paths.
A focused e2e regression supplies deliberately different colors in all three
slices, verifies driver forwarding, checks short/long/missing-file failures, and
proves the checked-in test/fixtures/stella/stella.pal generates byte-identical
assembly to the compiled-in canonical tables for all three full palette fixtures.
Documented the option in compiler/README.md, driver/README.md, and the Stella
fixture README.  All 583 compile/unit tests plus focused palette/CLI tests pass;
full e2e was not rerun for this item.  Item 57 is now the main-roadmap next action.

2026-09-20 10:04:48 PDT, deterministic example Stella play
Updated every example `make play` Stella launch to print
`WARNING: ignoring user specific settings` immediately before Stella and pass
`-dev.tv.jitter 0 -basedir "$(CURDIR)"`.  The existing example-local `-userdir`
and absolute-ROM-path behavior remains unchanged where it already applied, and
the three max diagnostics retain their configurable 10x `-speed` option.  The
common example play-contract regression now checks all 115 play launches.

Regenerated `standard_4k_ntsc_timing_inventory.tsv` with its authoritative
`inventory.pl`, fixing the stale-inventory e2e failure.  All 101 semantic rows
are unchanged; every recorded source line moved by -8.  Focused play-contract,
timing-inventory, source-tree-hygiene, and bankswitching Makefile-dependency
checks pass.  Full e2e was not rerun after this correction.

2026-09-20 12:07:18 PDT, define renderer stripe roadmap
Roadmap item 57 now uses compile-time `stripes`; `stripes:=0` preserves the old
renderer contract, while positive counts use instance-qualified
`TEMPLATE_playfield_colors[N*2]` and `TEMPLATE_playfield_data[N*6]` arrays.
`.../stripes.txt` owns the staged one-renderer-at-a-time plan, starting with
`all_five` and including timing, Stella, examples, install, and public docs.

2026-09-20 13:06:32 PDT, consolidate renderer stripe roadmap
Expanded item 57's design checkpoint before implementation.  The roadmap now
creates `libraries/vcs/components/` for reusable lifecycle/score/display
components (including `poison_debug_score`), consolidates the duplicated modern
gameplay renderer profiles behind compile-time feature selection, retires the
unofficial variants after preserving useful coverage, and retires the two
legacy `standard_4k_ntsc*` monolithic profiles after migrating any unique
behavioral coverage.  `multisprite` and `enhanced_multisprite_asymmetric` remain
separate renderer architectures and receive the same stripe ABI later.

The stripe ABI itself is unchanged: `stripes:=0` is the zero-cost compatibility
path; positive counts use instance-qualified playfield color/data arrays and
fixed screen-space stripe boundaries.  The roadmap is deliberately flat so each
migration/refactor/certification step is a bounded checkpoint.  S2, moving
reusable components without behavior changes, is now the next action.

2026-09-20 18:50:21 PDT, move reusable VCS components
Completed renderer-stripes S2. Created `libraries/vcs/components/` and moved the
reusable driving, keypad, paddle, PAL50 frame-core, heart-score, six-glyph,
three-plus-three, and two-plus-two component/support sources there. Moved
`poison_debug_score` out of `renderers/` into the component tree. Updated all
external callers, examples, Makefile dependencies, public documentation, tests,
and `packaging/install.manifest` to the new paths while preserving moved C26
source bytes where existing source-digest regressions depend on them.

`vcsc_branding.pl` now treats `.../stripes.txt` as a canonical required hot
developer record. `source_tree_hygiene.pl` now requires the migrated component
layout and rejects reappearance at the old root/renderer locations. Path-sensitive
score-glyph and visible-handoff regressions were updated for the new relative
paths. Focused migration testing passed during the work; an interrupted broad e2e
run reached 240/389 with no failures before handoff. S3 profile inventory is next.

2026-09-20 19:23:56 PDT, repair poison component test path
A post-S2 full-suite run reached 388/389 and exposed one stale test-only path:
`test/vcs_poison_debug_score.pl` still opened the retired
`libraries/vcs/renderers/poison_debug_score/...` location after the component
moved to `libraries/vcs/components/poison_debug_score/`.

Updated the regression to the component path and corrected the stale instantiate
example/comment in the component source plus the renderer conversion document.
The reported `vcs_poison_debug_score.pl` regression now passes. Branding,
source-tree hygiene, and visible-component handoff focused checks also pass. A
broader score-composition raster rerun exceeded the container execution timeout,
so the complete 389-test suite was not rerun after this one-line regression fix.
S3 remains the next stripes action.

2026-09-20 20:16:26 PDT, inventory gameplay renderer profiles
User reports the complete 389-test suite passes after the post-S2 poison-debug
path correction. Completed renderer-stripes S3 by inventorying the six modern
gameplay profiles and two legacy monolithic profiles in `.../stripes.txt`.
The inventory records ABI families, 228/192 versus 181/170 schedule classes,
module RAM/playfield resources, public example groups, CPU/TIA raster oracles,
pinned-palette Stella equivalence coverage, and principal regressions that must
survive consolidation.

The inventory exposed and corrected stale public documentation: `all_five` now
lists the unofficial twin's 228-line mode; `all_five_player_color_181` now
matches its tested 46-byte mask/21-byte cache and 88-byte module-RAM contract
and current fixture/example footprints; the 192 combined-color example footprint
is current; and both legacy `standard_4k_ntsc*` READMEs now acknowledge planned
retirement in S10 instead of claiming no retirement roadmap. S4 is next.

2026-09-20 20:52:26 PDT, compact S3 handoff records
Completed the post-S3 hot-record cleanup without weakening the 64 KiB developer
record gate. Removed stale duplicated context that still described S3 inventory as
the next action; hot records now total 65,229 bytes and correctly point to S4.
Source-tree hygiene and branding pass. After a minimal tools build using the stored
Debian dependencies, vcs_poison_debug_score and vcs_visible_component_handoff also
pass. The user's reported full 389-test pass remains the broad baseline.

2026-09-20 22:46:41 PDT, add gameplay feature selector
Completed renderer-stripes S4. `renderers/all_five/all_five.c26` is now the
compile-time gameplay selector with compatibility defaults `missiles:=1` and
`player_colors:=0`; existing all-five instantiations therefore keep their prior
ABI and generated cartridge bytes. Added the `missiles:=0, player_colors:=1`
specialization by folding in the maintained player-color implementation while
leaving the old `player_color` source/callers intact for the bounded S5 migration.

Added `vcs_gameplay_selector.pl`, which checks the selector parameters and proves
cartridge-byte equivalence for both all-five and player-color specializations at
170, 181, 192, and 228 lines. Manual pre/post S4 baseline comparisons also matched
all eight cartridges exactly. Focused all-five/player-color mode regressions pass.
S5 is next: migrate callers/tests/docs to the selector and remove the duplicate
`player_color` implementation.
