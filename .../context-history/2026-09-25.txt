2026-09-25 00:52:00 PDT, add exact two-stripe PF boundary
S13 now has an executable `stripes=2` intermediate profile for the 192-line solid
all-five kernel. The first 96 lines bias the packed-mask X schedule by 24 bytes and
start X at $E8. Six mask rows advance X by exactly 24, so it wraps to zero at the
midpoint without adding a per-row stripe test. The taken midpoint BEQ plus
`LDX #$E8` has the same seven-cycle cost as the ordinary not-taken BEQ plus JMP.

A midpoint-only old-row transition keeps stripe-0 PF data through visible line 95;
the stripe-1 cache begins on line 96 exactly. Sharing the remainder of the second
half costs one midpoint JMP, giving the second 96 lines a fixed +3-cycle PF phase
offset rather than cumulative drift. The frame remains 262 lines and standard
P0/P1/M0/M1/Ball heights remain 7/7/6/8/4. An asymmetric second stripe exposed
and fixed the terminal-line expectation: the final three PF writes are the right
half bytes.

The intermediate two-stripe ABI intentionally keeps a single shared COLUPF/COLUBK
pair while using twelve playfield-data bytes. Exact per-stripe color switching is
still the next blocker; do not describe this checkpoint as the final N*2 color ABI.
Added `vcs_all_five_stripes2_192.pl` and a dedicated fixture. Focused two-stripe,
one-stripe, branch-contract, visible-handoff, source-hygiene, and context-format
checks pass.

2026-09-25 06:50:00 PDT, adopt stripe double buffering
The stripe roadmap now uses two six-byte PF buffers plus a two-byte next-color
latch. The visible scheduler counts two-scanline pairs; generated equal-height
tables come first, and a later public height table must use even heights, sum to
the visible height, and meet the measured inactive-buffer refill minimum.

S13a landed without changing raster timing. For `stripes=2`, the existing
`stripe_cache[6]` is buffer A, `stripe_buffer_b[6]` is buffer B, and
`stripe_next_color[2]` holds the next COLUPF/COLUBK pair. The public color payload
is now four bytes; VBLANK loads stripe 0 colors into TIA and preloads stripe 1
colors into `stripe_next_color`. The existing exact 96/96 PF-data fixture remains
green. Focused stripe, branch, handoff, hygiene, and context-format checks pass.

2026-09-25 08:20:00 PDT, complete exact two-stripe color handoff
S13b now switches both PF data and colors exactly at the 96-line boundary.
`stripes=2` keeps A/B six-byte PF buffers and preloads the second COLUPF/COLUBK
pair in `stripe_next_color[2]`. A boundary P1 byte is prepared during VBLANK.
At the midpoint, A is held across `DEC player1_y` and the final indexed mask
shift; those instructions preserve A, so GRP1 lands at the start of line 96
rather than the visible tail of line 95. COLUPF and COLUBK then write at line
96 cycles 8 and 14, respectively, inside horizontal blank.

The second-half PF schedule is uniformly one cycle later than the earlier
data-only checkpoint (+4 versus the stripe-0 phase family), but remains stable.
Moving steady second-half GRP1 commits ahead of their mask shifts keeps object
writes in hblank without changing pair length. The asymmetric regression now
uses distinct second-stripe colors and verifies exact data and color boundary,
262-line frame timing, and standard P0/P1/M0/M1/Ball raster heights.

2026-09-25 08:45:00 PDT, land generated pair-height state
S13c1 adds the internal height-table model without changing visible timing.
For the current 192-line/two-stripe profile the renderer owns a two-byte
`stripe_pair_heights` table containing 48,48 two-scanline pairs. VBLANK loads
`stripe_pairs_remaining` from entry zero and initializes `stripe_height_index`
to one. The fixed midpoint still uses the proven X-wrap trigger; S13c2 will make
the visible kernel consume the pair countdown and remove that special trigger.

The two-stripe map regression now requires the generated two-byte RODATA table
and both one-byte scheduler-state objects. Exact asymmetric PF/color boundary,
frame timing, object raster, branch, handoff, hygiene, and history contracts
remain unchanged.

2026-09-25 09:58:00 PDT, correct stripe scheduler direction
S13c2 rejected a RAM countdown on every two-line pair because it perturbs
PF/object phases. The kernel already advances X four times per eight-pair mask
row; generated advance slots can make X wrap on an even stripe boundary while
compensated operands keep BL/M1/M0 on the same row. Removed the temporary
countdown/index RAM; the generated pair-height table stays authoritative.
2026-09-25 11:58:00 PDT, prove intra-row stripe boundary
S13c2b moves the two-stripe proof boundary from 96/96 to 98/94 visible lines,
so the A-to-B handoff occurs two lines inside a packed-mask row. X still wraps
at the coarse 96-line row boundary; A owns line 96 and one final two-line pair.
The VBLANK-precomputed GRP1 byte is loaded with forced absolute addressing,
adding exactly one cycle so its commit wraps to cycle 0 of line 98. COLUPF and
COLUBK then switch at cycles 6 and 12, both in hblank.

The boundary line has its own +2-cycle PF phase family and later B lines settle
to a stable +5-cycle family. PF values/colors switch exactly at line 98, the
frame remains 262 lines, and P0/P1/M0/M1/Ball object commits remain hblank-safe.
Focused one-stripe, two-stripe, branch, handoff, hygiene, and history tests pass.

For S13c2c, generalize heights as coarse eight-pair rows plus a 0..7-pair tail.
X can remain the coarse-row counter; inactive service can prepare the next X
seed and an indirect-jump vector selecting one of eight fixed tail handlers.
This avoids a per-pair countdown and also fits the later caller-provided height
table design. The 98/94 split is a certification checkpoint, not final default
geometry.


2026-09-25 12:05:00 PDT, stripe coarse/tail descriptors
S13c2c1 materializes and certifies the ROM coarse/tail scheduler descriptor for
the current 98/94 proof split: 49/47 pairs -> {6,1,5,7}. The visible raster is
unchanged. S13c2c2 will consume the descriptor at the X-wrap hook.
2026-09-25 12:28:00 PDT, consume first stripe schedule descriptor
S13c2c2 now derives the first visible X seed from the ROM coarse-row descriptor
in VBLANK and draw loads the generated zero-page seed instead of immediate $e8.
One entry-padding cycle was reclaimed, preserving the certified 98/94 raster.

2026-09-25 14:03:13 PDT, stripe tail dispatch experiment
Tried a VBLANK-prepared zero-page indirect JMP to a visible tail landing. The
cross-inline-body label address caused generated caller-loop reachability to
break (unresolved @while_start_0), so that dispatch shape was rejected rather
than worked around. Kept descriptor work off the raster: VBLANK now prepares
both current and next coarse X seeds plus the next 0..7 tail selector. The next
implementation uses compiler-visible direct tail dispatch at the X-wrap seam.

2026-09-25 15:42:00 PDT, certify second stripe tail
S13c2c now certifies two fixed tails. The 49/47-pair schedule ({6,1},{5,7})
keeps the 98/94 build. `VCS_STRIPES2_CERT_TAIL2` uses 50/46 pairs
({6,2},{5,6}) and switches at line 100. A two-cycle GRP1 wrap plus entering B
one pair later preserves 192 visible lines. Frame timing is 262 lines, object
commits stay in hblank, colors switch at cycles 6/12, line 100 has a +4 PF
phase, and later B lines use +5. Descriptor and raster regressions cover both.
Next: runtime selection among compiler-visible fixed tail landings at X-wrap.
