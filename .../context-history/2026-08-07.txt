
2026-08-07 00:56:36 PDT, High-level animated-frame installation and example assembly policy

- Replaced the animated gallery frame installer and adjacent animation transition lookup with ordinary VCSC while preserving all frame hashes, controls, pointer installation, and exact 262-line behavior.
- Added reusable compact compiler lowering for direct table/pointer operations and proven small counted loops, with explicit fallbacks for unsafe indirect stores and ref-array formals.
- Audited and hash-pinned remaining example assembly by beam-critical, hardware-idiom, and documented compiler/language-limitation categories.
- Final gallery accounting is 110/128 RAM bytes and 3969/4090 ordinary ROM bytes, leaving 18 RAM bytes and 121 ordinary ROM bytes free; the high-level frame installer uses zero activation bytes.
- Rejected a shorter ordinary comparison branch after exact renderer tests exposed a one-scanline VBLANK shift; page-aligned standard-renderer placement remains timing-correct even when its absolute linked page moves.
- Verified 442 compile-only tests and 255 end-to-end tests.

2026-08-07 07:36:43 PDT, Measured animated frame-installer code-size pipeline and queued optimizer cleanup

- Verified the hand-written frame installer linked at 236 bytes and the high-level replacement links at 540 bytes.
- Measured `-fpeephole` versus `-fno-peephole`: the installer assembly and 540-byte symbol span are identical, while the existing peephole pass saves eight bytes elsewhere in the gallery.
- Demonstrated with analysis-only assembly copies that restoring existing zero-page relaxation opportunities reduces the installer to 462 bytes, ordinary branch/JMP inversion reduces it to 432 bytes, and page-aware pointer lowering reduces it to 326 bytes.
- Accounted for the remaining net 90-byte gap: 94 bytes of named control/data-flow waste are offset by four bytes the relaxed high-level color loops already save versus the hand-written `.ax` stores, projecting 232 bytes if all named patterns are removed.
- Added the measured optimizer follow-up to the RAM optimization roadmap ahead of phase-lifetime overlay and updated the compact handoff with the concrete measurements.
- Repaired four timestamp-only August 6 history headers, documented the standalone-description rule in the compact handoff, and made source-tree hygiene reject future timestamp-only headers.

2026-08-07 08:24:59 PDT, Delegated compiler addressing modes to assembler relaxation

Audited compiler-lowered ordinary 6502 memory operations and removed all forced
addressing-mode suffixes from compiler mnemonic emitters. The compiler now emits
semantic operands and lets vcsc-as select/relax modes from address-size contracts;
explicit source asm suffixes remain available for deliberate encoding and timing.
The audit removed 87 compiler-source suffix occurrences. Split-memory execution
continues to stay absolute through segment metadata.

The animated gallery now uses 3854 ordinary ROM bytes and 110 RIOT RAM bytes,
leaving 236 ROM bytes and 18 RAM bytes free. install_frames links at 462 bytes,
down from 540, exactly realizing the previously measured 78-byte relaxation loss.
Peephole-disabled gallery output is 3862 bytes and install_frames remains 462 bytes,
confirming the existing peephole still saves eight bytes elsewhere and none there.

Shorter compiler-owned accesses exposed timing dependencies in calibrated VCS code.
Those were kept out of compiler lowering: frame helper phases use explicit delay
instructions, cycle-counted six-glyph row setup uses handwritten absolute stores,
and synthetic generic-wait fixtures explicitly normalize their phase. Exact
scheduler, score, renderer, split-memory, and banked raster/runtime regressions pass.

Final verification for this change is 444 compile-only tests and all 255 end-to-end
cases in bounded batches. Clean source-tree hygiene passes.

2026-08-07 09:24:15 PDT, Optimized high-level frame installation below the handwritten ROM size

- Added timing-scoped generated branch/JMP inversion, compact hard-page selector and bounded pointer-low lowering, conditional pointer-update suffix merging, proven cross-loop doubled-offset reuse, and post-tested nonempty register-counted loops.
- Reduced linked `install_frames()` from the 462-byte relaxed high-level baseline to 232 bytes, four bytes smaller than the old 236-byte handwritten implementation, with zero activation RAM.
- Reduced the animated gallery from 3854 to 3624 ordinary ROM bytes while holding RIOT RAM at 110/128 bytes with 18 free; peephole-disabled final output is 3635 gallery bytes and a 235-byte installer.
- Added focused peephole on/off, hard-page pointer, doubled-offset reuse, and zero-iteration counted-loop regressions; fixed constant X-indexed array stores so they cannot reference an unmaterialized loop local.
- Preserved exact animation, scheduler, score, fingerprint, renderer, and banked raster contracts. Banked game-logic branch inversion legitimately reduces F8/F6/F4 bank1 from 82 to 76 bytes and F8SC bank1 from 121 to 115 bytes.
- Verified 447 compile-only tests and all 256 end-to-end cases in bounded batches.

2026-08-07 12:23:26 PDT, phase-aware RAM overlay, big ASCII font, and centered bank diagnostics completed

* Added conservative frame-phase lifetime metadata for VSYNC, VBLANK, visible draw, and overscan. File-scope storage now requires explicit `__phaseworkspace$V1$symbol` eligibility in addition to inferred phase-use metadata; fully phase-scoped compiler scratch receives eligibility automatically. The linker closes observed uses to a contiguous frame interval, overlays only compatible multi-object groups, and preserves ordinary allocation order for solitary phase-tagged objects.
* The animated gallery now aliases the one-byte VSYNC compiler scratch with the 48-byte VBLANK+visible `game_object_masks` workspace. RIOT RAM falls from 110/128 to 109/128 bytes, leaving 19 bytes free; ROM remains 3624/4090 bytes with 466 free and `install_frames()` remains 232 linked bytes.
* Added `libraries/vcs/fonts/big_ascii.c26` as a usable 95-glyph 8x16 printable-ASCII table, repaired its 16-row reversal alias, removed impossible page containment, documented it, and added a linked-ROM/distinct-glyph regression.
* Reworked the bankswitching diagnostic display to render complete centered white PASS or FAIL words with exact letters copied from `default_ascii.c26` through the six-glyph-wide component while retaining the complete mapper-transition simulator matrix and exact frame scheduler.
* Final verification on the completed tree: 447/447 compile-only tests and 258/258 end-to-end tests pass in bounded batches, including exact scheduler/score/renderer/banked-renderer/animation regressions and source-tree hygiene.

2026-08-07 13:34:04 PDT, Added explicit object alignment and removed animated-gallery ROM padding

- Added user-facing `align(N)` for file-scope data-object definitions, restricted to compile-time positive powers of two from 1 through 32768, while retaining `page` as the independent whole-object 256-byte containment contract.
- Reused the existing assembler/linker `.segmentalign` metadata path and fixed writable DATA/BSS/zero-page runtime placement to honor object alignment as well as ROM/load placement.
- Audited page-aware compiler proofs and fixed two real low-byte-zero conflations: a small `page` object no longer enables page-base pointer lowering unless `align(256)` or stronger proves the base, while an exactly 256-byte page-contained object remains sufficient by construction.
- Converted all 760-byte conventional ASCII fonts and the 1520-byte big ASCII font to contiguous `align(256)` objects with linked tests proving no 8-byte or 16-byte glyph crosses a hardware page; small decimal/hex fonts remain `page` contained.
- Removed the animated gallery's 64 literal zero padding bytes after sprite 120. The final bitmap object is 192 real bytes, gallery ROM falls from 3624 to 3560 bytes with 530 free, RAM remains 109/128 with 19 free, and `install_frames()` remains 232 bytes.
- Audited example/library padding references after the cleanup: no other example application-data padding remains; remaining renderer padding references are cycle/layout contracts rather than filler data.
- Verified 455/455 compile-only tests and all 260/260 end-to-end cases in bounded batches, including exact renderer, score, animation, bankswitching, and alignment-placement regressions.

2026-08-07 14:29:01 PDT, Packed animated-gallery persistent state without changing behavior

- Packed Select-ready, Fire-ready, and paused state into one control byte, and packed the normal modulo-4 phase, independent source-set-03 modulo-3 phase, and eight-frame hold clock into one animation-state byte.
- Persistent gallery bookkeeping falls from seven bytes to four. Total RIOT RAM falls from 109/128 to 106/128 bytes, free RAM rises from 19 to 22 bytes, and object bytes fall from 101 to 98; the eight-byte hardware-stack reservation and one-byte main activation are unchanged.
- Measured rather than assumed the sprite-selector trade: keeping sprite1 gives 3545 ROM bytes and a 232-byte install_frames(), while deriving P1 from the adjacent-set relation gives 3595 ROM bytes, 105 RAM bytes, and a 303-byte installer. The roadmap's smaller-ROM exception therefore keeps the stored sprite1 byte.
- Packed-state lowering also reduces ordinary gallery ROM from 3560 to 3545 bytes, leaving 545 bytes free, while preserving all 1,875 automatic frames, source pixels/colors, pair wrapping, controls, independent modulo-3 playback, and exact 262-line timing.
- Final verification is 455/455 compile-only tests and 260/260 end-to-end tests in bounded batches with no timing or raster oracle changes.

2026-08-07 15:07:12 PDT, Reduced player-color 192 hardware-stack reservation by flattening single-use VBLANK helpers
- Measured the animated gallery's 8-byte hardware-stack reserve as four source-call bytes plus four supplementary assembly-helper bytes.
- Flattened the single-use prepare-object-masks and prepare-one wrappers in player_color_192 while retaining set_range as the only assembly subroutine; explicit .callstackextra 0 records the audited zero supplementary requirement.
- Gallery hardware-stack reservation falls from 8 to 4 bytes, total RIOT RAM from 106/128 to 102/128, and free RAM rises from 22 to 26 bytes with ROM unchanged at 3545/4090 and install_frames unchanged at 232 bytes.
- The authoritative RAM fixture is schema 8. Verification passed 455/455 compile-only tests and 260/260 end-to-end cases.

2026-08-07 17:02:22 PDT, animated-gallery post-optimization remeasurement confirms useful RAM margin

Marked the RAM roadmap remeasurement complete after rebuilding the existing general
P0/P1/Ball player_color_192 gallery at 3545/4090 ROM bytes and 102/128 RIOT-RAM
bytes, leaving 26 RAM bytes free with a one-byte main activation and four-byte
hardware-stack reserve. Added schema-9 machine-readable checkpoint accounting for
the baseline through items 1-7, retained Ball capability, and recorded the decision
that the current renderer is sufficient for this gallery; the direct-countdown
object-schedule redesign remains the next general RAM optimization. Updated hygiene
expectations and test documentation. Verification: 455/455 compile-only and 260/260
end-to-end cases pass in bounded batches.


2026-08-07 17:51:46 PDT, Replaced player-color 192 object masks with official direct countdown

- Replaced the 48-byte `game_object_masks` schedule and VBLANK range builder in `player_color_192` with official-opcode direct P0/P1/Ball countdown state; no unofficial `DCP` and no hidden assembly JSR remain.
- Reduced renderer private RAM from 56 to 10 bytes and total module RAM from 69 to 23. The animated gallery falls from 3545 to 3290 ROM bytes and from 102/128 to 56/128 RIOT-RAM bytes, leaving 72 bytes free; stack remains four bytes and `main` activation remains one.
- Reduced the measured VBLANK marker span from 920 to 468 CPU cycles while retaining 152-cycle visible pairs, exact 192-visible/262-total-line frames, and the general P0/P1/Ball public capability.
- Added direct-countdown timing and Ball edge regressions. The conversion explicitly fixes the old mask builder's first-row Ball defect, which could shift/clip Ball early or stretch a four-pair Ball to five pairs; top clipping and first-row-boundary crossing are now pinned.
- Updated the interactive full-height raster oracle to use the direct-countdown transition profile: steady PF writes remain 10/17/40/47, while the first P1 transition half uses 9/16/40/47 with pixel-identical visible output.
- Advanced the accounting fixture to schema 10, completed RAM-roadmap item 9, and closed item 10's compact-mask fallback as unnecessary because the official direct design succeeded. The next RAM-roadmap decision is whether a separate two-sprite-only profile is generally useful.
- Verification: 455/455 compile-only tests and all 260/260 end-to-end cases pass in bounded batches.
