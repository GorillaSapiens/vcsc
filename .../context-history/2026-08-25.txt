2026-08-25 00:03:05 PDT, fonts and enhanced 228 timing
Added the supplied compact printable-ASCII 4x6 font as
`libraries/vcs/fonts/half_ascii.c26` and the supplied two-character pairing
helper as executable `make_pair_font.pl`. Documented the pair-font workflow
and added `vcs_half_ascii_font.pl` to verify the 95-glyph/570-byte source,
blank high source bit, pairing behavior, and compilability of both source and
generated tables.

Completed native 228-line support for the sole enhanced asymmetric multisprite
renderer and PAL/SECAM examples. Diagnosed the apparent 229-line renderer as an
F8 call-placement bug: the post-VBLANK bank trampoline delayed `game_draw`
long enough for its first WSYNC to miss the current line. The examples now enter
the renderer bank while VBLANK is still asserted and run end-VBLANK, draw,
component handoff, and begin-overscan resident there. Restored the renderer's
normal final-WSYNC contract for both 192 and 228 instead of special-casing the
bottom path.

The 192 randomized asymmetric stress, 50 Hz CPU timing, native-228 matrix/layout,
renderer branch contracts, focused font test, changed recursive example shards,
and Stella 7 PAL/SECAM example matrix pass. The public 228 F8 examples use
123/128 RIOT RAM. The stale video-standard layout test was generalized to keep
`00_blank`, `01_all_five`, and contiguous numbering as the durable contract.

2026-08-25 04:46:33 PDT, isolate asymmetric PF corruption
The 192/PAL/SECAM asymmetric playfield investigation found two concrete renderer
bugs: relocatable PF0 assembler '=' aliases lost relocation and linked indexed
loads against zero-page addresses, while two special setup paths retained the
192-only +97 PF1/PF2 plane stride. The WIP tree converts PF0 aliases to textual
.def names and derives those offsets from TEMPLATE_PLAYFIELD_HALF. Correcting
PF0 placement increases the 228 F8 hard-component pressure; VBLANK/top-preload
work is therefore being separated from the visible hard component, with a tiny
228-only coarse-slot table. Emergency handoff preserves this unfinished state;
revalidation and a linked-operand regression remain mandatory.

2026-08-25 05:45:52 PDT, finish asymmetric PF and static cleanup
Revalidated the emergency enhanced-asymmetric WIP and completed the playfield
repair. PF0 aliases remain textual `.def` aliases so final indexed loads retain
the ROM-table relocation; special PF1/PF2 accesses derive their second-plane
offset from `TEMPLATE_PLAYFIELD_HALF` instead of the old 192-only +97. The
focused 192 torture test now also inspects the final linked ROM and requires the
PF0 LDA abs,X operands to target `game_playfield_left_pf0` and
`game_playfield_right_pf0`, with no `$0000,X` regression. The same test forbids
hard-coded +97 plane accesses in the renderer.

Revalidated native 228 PAL/SECAM after the WIP VBLANK/preload split. CPU 50 Hz
timing, native-228 matrix, renderer branch contracts, and Stella 7 PAL/SECAM
example tests pass. Fresh Stella snapshots show the intended orderly asymmetric
playfield for 192, PAL 228, and SECAM 228; the remaining narrow bars at the
extreme left of the 192 diagnostic are the renderer's intentional HMOVE blank,
not stale PF data. Current measured footprints are 3996/4090 ROM and 115/128
RAM for the 192 4K diagnostic, and PAL 228 bank1=3616, bank0=1039, RAM=123/128.

Removed the redundant public static score-above/score-below examples from
`examples/16_all_five_player_color_181`. Their interactive counterparts are now
`01_interactive`. Exact static raster sources were retained privately under
`test/fixtures/all_five_player_color_181/` for renderer regressions, and the
focused Perl/Stella tests and top-level installcheck paths were updated. All
eight recursive example build/link/disassembly shards pass with 95 public
examples. A broad E2E run reached 78/323 with no failures before the container
execution window ended. The focused non-Stella all-five-player-color-181 regression passes.
The explicit Stella equivalence test still exposes its pre-existing eight-pixel
terminal-row difference between the combined 181 renderer and its all-five
golden; neither renderer nor fixture source changed from the prior checkpoint,
so that unrelated issue was not folded into this cleanup.

2026-08-25 06:22:50 PDT, fix blank-screen 263-line frame
The hand-written `examples/01_basic/01_blank_screen` frame loop still emitted
30 explicit overscan WSYNCs after the exact-VSYNC repair. Because the next
frame now begins with a leading alignment WSYNC before asserting VSYNC, that
alignment is itself the 30th overscan line; retaining all 30 explicit waits
made the assertion-to-assertion frame period 263 scanlines in Stella.

Changed the explicit overscan loop to 29 lines so the next frame's alignment
WSYNC completes the intended 30-line overscan interval. Strengthened
`vcs_example_vsync_contract.pl` to require an actual 262-line raw
assertion-to-assertion period instead of the stale 263-line harness calibration,
and refreshed the reviewed assembly fingerprint. The exact 228-cycle VSYNC
pulse remains unchanged. Focused VSYNC, blank-noasm, Ode-to-Joy, cartridge
profile, and recursive example shard 1/8 tests pass.


2026-08-25 08:42:06 PDT, refresh font subset contracts
Audited the font-related failures after the ASCII/subset refresh. The committed
subsets regenerate byte-for-byte from their ASCII sources and all recursive
public example build shards still pass, so the font data/build changes are
sound. Updated stale tests for `(subset)` provenance headers and `page`
placement, added Wonk as the ninth conventional 8x8 family, and added lower-hex
coverage across the conventional, Big, and compact 4x6 families. The new
`make_font_subsets.pl` also needed the library tree's explicit CC0 notice; that
was a real source-tree hygiene failure rather than a stale expectation. Updated
the font README to describe four-module families and generated lower-hex
subsets. Focused font/contracts/hygiene/example-build tests pass.

2026-08-25 09:16:02 PDT, profile test-suite wall time
The apparent stall on e2e_default_xform_string_escape_verify.c26 was a runner
presentation artifact: that test itself takes about 0.08 seconds, but parallel
results are reported in deterministic source order, so the terminal displays
the last completed case while waiting for the next one.

Removed nine pathological successful E2Es that could consume the full simulator
timeout: direct/archive init, named-memory, three zeropage-init checks, and four
stale cfg-mismatch memory-region cases. The first five printed success and then
spun forever; the four cfg-mismatch cases predated authoritative C26 mem metadata,
so their intentionally stale cfg now links successfully and then spun forever.
They now exit explicitly; the cfg cases also assert the authoritative C26 map.
This removes up to 405 seconds of aggregate worker timeout from a successful run.

The existing --timings/--timings-append TSV instrumentation is now flushed a row
as each deterministic result is reported, preserving partial profiling data if a
long run is interrupted. Added make slow-tests (default top 20,
TEST_SLOWEST=N override) and documented it. Profiling identified the score
composition raster shards and recursive example build+roundtrip shards as the
largest remaining individual E2Es (roughly 13-20 seconds here).

gen_version_h.pl now leaves an identical version.h untouched. The FORCE rule
still checks Git state every invocation, but unchanged branch/tag/SHA/dirty
metadata no longer bumps the header mtime and needlessly rebuilds every tool
before make test. No-Git archive builds now likewise reuse the existing header
stamp until clean removes it. Git-backed make and no-Git generator smokes both
confirm unchanged repeated invocations are stable.

Validation: 502/502 compile-side tests pass. All 323 E2E cases were exercised
across bounded profiling runs and pass; the timing/slow-test, version-header,
hygiene, nine repaired E2Es, all recursive example shards, renderer suites, and
disassembler suites were included.

2026-08-25 11:18:15 PDT, canonicalize observed RAM hardware operands
The concrete RIOT-RAM execution listing now routes operands through the same
access-aware TIA/RIOT symbol lookup used by ordinary ROM disassembly. Direct
and indexed observed operands therefore print canonical base names where the
access direction makes the name honest (for example VBLANK, AUDV0, WSYNC,
HMOVE, and GRP0,X). TIA reads whose address only has a write-side mnemonic and
ordinary RIOT RAM addresses intentionally remain numeric. The same observed-
instruction formatter is shared by Starpath reconstructed RAM payload comments.

Added a synthetic concrete-RAM regression requiring both STA AUDV0 and indexed
STA GRP0,X while preserving exact-byte round trip. The focused vcsc-disas suite
passes 131/131. The supplied c33caa7d6ac7251bb804e0473198901d 1K removable-
cartridge ROM now shows canonical TIA names throughout its concrete RAM tail;
BIT read-side addresses and RIOT RAM remain raw by design.

2026-08-25 13:14:14 PDT, E0 output profile and public diagnostic
Implemented first-class E0 build support as eight physical 1K chunks: three
independently selected CPU windows plus fixed physical bank 7. The linker now
validates the segmented C26 topology without pretending E0 has one selector per
bank. vcsc-sim models the three selectors independently and powers up as physical
banks 4/5/6/7. Added installed vcs_8k_e0.c26/vcs_8k_e0.cfg profiles.

Added examples/09_bankswitching/11_e0 with probes in all eight physical chunks.
The diagnostic verifies all three selectable windows and the fixed window, has a
fixed-bank green-PASS/red-FAIL display, forces Stella -bs E0, and carries the
E0\0\0 signature. Simulator self-test, live Stella snapshot grading, high-
confidence vcsc-disas detection, and exact byte round trip all pass. Installed
staging also built the E0 example as exactly 8192 bytes and disassembled it as E0.

Focused topology, memory-authority, cartridge-profile, example-allowlist, and
recursive example-build tests pass. Bankswitching Item 39 is retired; Item 40,
the 3F public diagnostic/profile, is the next open bankswitching item.

2026-08-25 17:19:32 PDT, review E0 direct VSYNC writer
The new E0 public diagnostic legitimately owns VSYNC directly in fixed bank 7,
but vcs_example_vsync_contract.pl still pinned only the three older low-level
examples. Added the E0 source to the explicit reviewed writer inventory and
made the contract assert its exact same-phase 3-scanline VSYNC store sequence,
rather than merely allowlisting the filename. Updated the test README wording.
Focused VSYNC/E0/hygiene checks pass, followed by the full suite: 324/324.

2026-08-25 18:14:19 PDT, add E0 PASS/FAIL text display
The E0 diagnostic now matches the other bankswitch examples: large `pass`/`FAIL`
with small `E0` underneath. The completed mapper self-test explicitly freezes
E0's three selectable windows to physical banks 0/1/2 before display entry;
bank 0 holds renderer code, bank 1 holds the glyph tables, bank 2 holds component
lifecycle code, and fixed bank 7 retains startup/self-test control. This explicit
0/1/2 handoff is required because E0 calls do not implicitly select a physical
bank. The old direct-VSYNC E0 exception was removed; the display now uses the
shared NTSC frame helpers.

The E0 regression now pins the display topology and the Stella snapshot grader
checks real `pass` + `E0` glyphs instead of accepting a solid green background.
The Stella test also isolates XDG_CONFIG_HOME so a developer's saved debugger
state cannot hijack certification. A forced `stella -bs E0` snapshot graded as
320x228 with green PASS, large `pass`, and small `E0`.


2026-08-25 18:55:00 PDT, complete classic 3F and 3E output support
Implemented classic Tigervision-family build support around a selectable lower
2K window plus fixed final 2K. Public 8K and 16K C26 profiles/cfgs now exist
for both 3F and 3E; the linker validates their segmented physical-bank topology
without fake whole-window trampolines. vcsc-sim models 3F value-written lower
ROM selection and 3E exact $3F ROM selection plus 32 1K RAM banks selected by
$3E, with read alias $1000-$13FF and write alias $1400-$17FF.

Added public 3F and 3E diagnostics with large PASS/FAIL and small mapper text.
Both simulator self-tests pass, both are detected by vcsc-disas at high
confidence and round-trip byte-exactly, and forced Stella snapshots grade PASS.
Classic 3F required an important hardware-facing profile rule: because Stella's
3F mapper owns low-page writes for bank selection, normal TIA accesses use the
$40-$7F TIA mirror through the new tia_mirror_40.c26 binding. 3E deliberately
keeps normal low TIA addresses because its mapper forwards those accesses.
Bankswitching Items 40 and 41 are retired; Item 42 FE/SCABS is next.

2026-08-25 20:01:58 PDT, fix 3E TIA mirror frame timing
The public 3E profiles had intentionally retained ordinary low-page TIA
addresses based on Stella 7 forwarding non-hotspot writes. Current Stella
behavior can leave that page cartridge-owned, swallowing writes such as VSYNC
and WSYNC; the resulting picture can still look plausible while frame cadence
is catastrophically wrong (the reported case was 675 scanlines per frame).

Both 8K and 16K classic 3E profiles now select VCS_TIA_USE_40_MIRROR, matching
3F for ordinary TIA I/O while keeping literal $3E/$3F mapper-control writes.
The focused 3F/3E regression compiles vcs_frame_timing.cpp with strict warnings
and requires 47 consecutive 262-line frames from both visible diagnostics. The
timing harness now models classic 3F/3E low-page cartridge ownership, 3F ROM
selection, and 3E ROM/RAM selection plus split RAM aliases. Under that model the
old 3E image fails before producing enough frames; the corrected image passes
at exactly 262 lines. Screenshot grading remains as a separate glyph/display
check rather than a substitute for timing certification.
