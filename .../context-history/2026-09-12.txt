2026-09-12 07:43:04 PDT, disassembler roadmap A8 complete
A8 now feeds execution from the A7-selected mapper back into trusted code/data
presentation instead of discarding the hypothesis-local result after mapper
comparison. The selected mapper is replayed from every legal RESET/startup
mapping in isolated analyses; only A5-viable startup executions contribute code
and data roles, conservative abstract-state summaries, generated RIOT-RAM
instruction provenance, and display/audio source provenance to the final graph.
This feedback is presentation-only and does not cast another mapper-selection
vote. Multicart slice analysis uses the same feedback path.

ROM bytes proven to construct executable RIOT-RAM instructions are now explicit
data-source bytes even when they are also operands of established ROM code. The
emitter therefore preserves both roles and annotates code-as-data rather than
choosing one interpretation. Proven ROM provenance consumed by any valid TIA
write is data-source evidence; GRP0/GRP1 retains the stronger graphics role. A
new AUDC0-through-RIOT-RAM fixture verifies non-graphics consumer provenance.

Detached speculative islands still run only after established roles/evidence are
frozen and remain presentation-only gap fillers. An attempted A8 cleanup that
removed the legacy all-bank RESET presentation seeding exposed its current use
for cross-startup Superchip read/write evidence, so that removal was deliberately
backed out; the older static/concrete pass remains supplemental plumbing rather
than being prematurely deleted in A8.

Regression coverage now explicitly locks Congo-style generated F8 selectors,
unknown-input branch forking as established (not speculative) code, prior A7
trust-gate hard failures, duplicate images, 4094/4098-byte preservation forms,
all-bank accounting, detached islands, and mapper-specific read/write hotspot
direction. `vcsc_disassembler.pl` passes. The compile-only suite passes 537/537,
and an uninterrupted full e2e run passes 365/365, including disassembler fuzz,
GL, hardening, and opcode coverage. The mapper-hypothesis execution roadmap
A1-A8 is complete; no post-A8 mapper work has begun.

2026-09-12 09:16:10 PDT, post-A8 mapper mismatch corpus hardening
Recovered and completed the interrupted 17-ROM Stella differential cleanup without
starting the next mapper roadmap item. FC commit-only accesses no longer invent FC;
CV/WD hypothetical RAM traffic requires independent family corroboration; JANE only
gets mapper-specific credit for its exclusive selectors; established RESET execution
no longer treats undefined TIA reads/writes as hard mapper contradictions. FC's
multi-access staged protocol may break an execution tie, and FC is included in the
explicit VCSC tail-signature recognizer.

Added two conservative A7 boundaries found by the remaining corpus cases. CDF/CDFJ
images matching the established CDF-family fingerprint are recognized as unsupported
and preserved exact unknown/raw instead of falling through to F4/F0; this is quarantine,
not CDF support, so roadmap item 26 remains open. Separately, when A5 hard-contradicts
a detector-signed family but the sole surviving hypothesis has no independent selector,
RAM, explicit/detector signature, switch-save, or complete-bank evidence, the result is
unknown/raw rather than a weak last-survivor size/topology claim. Hard execution
contradictions remain authoritative and the rejected detector is reported only as a
conflicting static signal. Synthetic regressions pin both cases.

The original 17-ROM mismatch corpus now round-trips 17/17 byte-exactly. Against the
user-reported Stella mapper results it reports 7 matches, 0 mismatches, 10 unresolved.
The former final mismatch was a 16K preservation image made from two identical 8K
halves: VCSC intentionally reduces it to F8 while Stella reports physical-size F6.
roundtrip.pl now normalizes exact repeated-half wrappers only for Stella's ordinary
size-default F8/F6/F4/F0 families before comparison; exotic mapper names are untouched,
and unknown/raw remains UNRESOLVED. The hardening suite pins duplicated 16K F6/F8
normalization with a fake Stella.

Validation: vcsc_disassembler.pl passes; vcsc_disassembler_hardening.pl passes; the
full e2e gate passes 365/365 including fuzz, GL, hardening, and opcode suites; the
compile-only gate passes 537/537. source_tree_hygiene.pl and context_history_format.pl
passed before this entry and must be rerun after it before packaging.

2026-09-12 11:28:09 PDT, second post-A8 Stella mismatch corpus hardening
Worked the user's second 11-ROM mismatch set without starting GL closeout or any
later mapper roadmap item. The failures came from several independent forms of
model-relative evidence rather than one detector threshold.

A7 now treats the established 3E `STA $3E` plus repeated `STA $3F` fingerprint as
a multi-access protocol signal alongside FC, so generic F4 union coverage cannot
outvote a viable 3E family signal. Direction-correct RAM traffic for E7 and GL,
like the earlier CV/WD fix, requires independent family corroboration before it
can vote. Complete bank accounting from broad 0840/UA/UASW/0FA0 decoders is also
non-dispositive until detector/explicit metadata, a switch-save, or generated
selector provenance establishes the family. A single FC staging access remains
execution activity but no longer counts as family-specific evidence by itself.

Fixed asymmetric E0/E7 overlap evidence: on 8K carts both schemes decode
$1FE4-$1FEB, so neither side can label accesses in that range family-specific.
FE's delayed-JSR model likewise no longer treats an ordinary cross-bank JSR target
high byte as independent FE identity evidence unless FE has raw-image
corroboration; switch-saves remain stronger execution evidence. Added the omitted
`FE\0\0` VCSC explicit-tail recognizer.

F6SC exposed a separate A5 modeling bug. Structural duplicated-window or explicit
SC evidence is now applied to F8/F6/F4 hypotheses before RESET viability is
judged, so legal SC traffic is not rejected under a temporary plain-ROM model.
Writes through the SC read alias are treated as harmless bus writes rather than
RAM stores or hard contradictions. Synthetic regressions cover 3E-vs-F4 coverage,
E7/GL RAM self-proof, broad UASW coverage, FE JSR self-proof, and structural F6SC
execution containing such a read-alias write.

The 11-ROM corpus now round-trips 11/11 byte-exactly. With the bundled Stella it
reports 8 mapper matches, 0 mismatches, 3 unresolved/raw. The unresolved cases are
intentional conservatism where UASW-vs-F8, duplicated-F8-vs-0FA0, or FE-vs-0FA0
remain underdetermined by evidence VCSC can currently trust. Validation is complete: the compile-only gate passes 537/537 and the full e2e
gate passes 365/365, including every disassembler-specific suite. Source-tree
hygiene and context-history formatting pass after final documentation compaction.
