2026-08-10 09:41:35 PDT, Reduced multisprite application RAM outside the renderer: stock startup workspace 8->4 bytes, multisprite main activation 4->0, and exact-raster mutable six-glyph score 18->15; 181 score examples now use 99/128 RAM.

2026-08-10 10:44:44 PDT, propagated compact six-glyph score storage to left/right/wide variants
Default left score storage now uses five full pointers plus a digit-6 page offset; right uses four full pointers plus two page offsets; wide removes its row counter and uses five full pointers plus one biased digit-2 offset whose deliberate page-cross preserves the old five-cycle load. Left/right keep optional compact_font:=0 full-pointer compatibility for fingerprint logo redirection. Focused score composition, wide exact-raster/Stella, and fingerprint regressions passed.
2026-08-10 11:54:13 PDT, fixed garbage leading glyph in bankswitching PASS/FAIL diagnostics
The compact wide-score layout had only five full pointers, but the diagnostic still wrote six arbitrary ASCII pointers, so the sixth pointer overwrote the compact offset/delayed state. Wide now supports `compact_font:=0` full-pointer compatibility and the diagnostic requests it; ordinary wide scores remain compact. Map tests lock the full 12-byte pointer array, and the Stella bank grader now requires exact PASS/FAIL geometry, pixel count, and glyph shapes; the pre-fix extra-glyph snapshot is rejected while the ordinary compact-wide Stella digest remains unchanged.
2026-08-10 12:31:00 PDT, added relocatable statically linked Linux packaging
Top-level `make linux` now builds the six native host tools with static linkage, stages the installed runtime/support tree and editable examples, verifies the ELF executables have no dynamic interpreter or shared-library dependencies, smoke-builds the packaged blank-screen example, and creates `vcsc.linux.YYYYMMDD_HHMMSS.tar.gz`; README and LINUX.md document usage and overrides.

2026-08-10 12:50:37 PDT, installed examples added to native install image and package
`make install` now copies editable examples to `$(PREFIX)/examples` with installed tool/support paths, `make package` includes the same tree, uninstall removes it, and installcheck verifies the staged blank-screen example layout/build.
2026-08-10 13:10:23 PDT, added GitHub Actions binary-package publishing
The workflow builds Windows then Linux packages in the requested clean-separated order, uploads both on every run, and publishes them as GitHub Release assets for tagged builds.

2026-08-10 13:42:13 PDT, centralized version-header generation and tagged-build identity
Top-level `gen_version_h.pl` now owns VERSION generation for driver, compiler, assembler, linker, archiver, simulator, and stego; exact HEAD tags take precedence over branch/detached labels, and the GitHub package checkout fetches full history/tags. A focused regression covers branch, annotated tag, dirty, detached, no-Git, Makefile delegation, and workflow depth.
2026-08-10 13:51:49 PDT, made unsupported-option diagnostics point to full tool help
All six public command-line tools now reject unsupported options with a nonzero diagnostic that names the option and tells the user to run that exact tool with `--help` for the complete supported-option list. `vcsc-cc1` no longer turns arbitrary unknown options into successful help output, `vcsc-sim` no longer dumps full usage for an unknown option, assembler getopt diagnostics are normalized, and `vcsc-ar` now has explicit `-h`/`--help`. A focused e2e regression locks the behavior for all six tools.
2026-08-10 15:24:12 PDT Big 8x16 decimal/hex fonts and wide six-glyph score component
Derived big_decimal.c26 and big_hex.c26 exactly from big_ascii.c26, added the 19-line six_glyph_big_wide_component.c26 and 2K examples/01_basic/07_big_wide_score demo, integrated install/docs/tests, and locked exact 88x16 score writes plus 262-line frame timing.
