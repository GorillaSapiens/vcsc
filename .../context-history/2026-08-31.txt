2026-08-31 00:18:30 PDT, migrate 0840 inline bankcall
Migrated 0840 to the inline-target bankcall ABI with a mapper-specific fixed
block. Logical bank identity comes from the linked PC high byte; the trampoline
maps it to Y=$00/$40 and performs LDA $0800,Y so bank selection uses the
mapper's required read hotspot. The payload is 75 bytes with an 80-byte
reservation (generic-jsr=$050) and emits zero legacy per-target JSR entries.

Reworked examples/09_bankswitching/08_0840 into a complete 2x2 ordered call
matrix: source banks 0/1 each call destination banks 0/1, checking return values
and hardware-stack balance, with an additional nested cross-bank call. Compiler,
linker template generation, disassembler signature handling, install packaging,
example smoke builds, docs, and regressions were updated. The dedicated 0840
regression, exhaustive bank-call matrix, inline-template test, assembly
allowlist, source hygiene, and example-build shard 3/8 pass. Next migration is
UA/UASW, then 0FA0.

2026-08-31 00:39:04 PDT, migrate UA and UASW inline calls
Migrated UA and UASW to mapper-local inline-target bankcall blocks. Both use
indexed read selection from base $0220. UA maps the logical-PC bank bit through
EOR #$20 / AND #$20 for $0220/$0240; UASW's reversed selector association uses
AND #$20 directly. UA payload is 73 bytes and UASW 69 bytes; both reserve 80
bytes (generic-jsr=$050) and emit zero legacy per-target JSR entries.

Reworked the shared public UA/UASW diagnostic to execute all four ordered bank
pairs. Same-bank 0->0 and 1->1 remain ordinary JSRs; cross-bank 0->1 and 1->0
use the five-byte inline-target bundle. Every pair checks 16-bit return value and
hardware-stack balance, plus one nested cross-bank composition case. The focused
UA/UASW regression, independent exhaustive matrix, inline-template regeneration,
assembly allowlist, source hygiene, and example-build shards 6/8 and 7/8 pass.
Next existing migration is 0FA0.

2026-08-31 08:17:57 PDT, define descriptor bankcall ABI
Made root BANKSWITCHING.md the public authority for selector-controlled
cross-bank calls. The target ABI is now JSR plus a three-byte inline field:
16-bit target CPU address followed by one opaque mapper-defined destination
descriptor. Each trampoline instance has its source descriptor baked in, pushes
that byte below the synthetic return, and the return half consumes it directly
to restore the caller before the final RTS. Same-bank calls remain ordinary
JSRs; neither source nor destination bank identity may be inferred from PCs.

This was intentionally a documentation/developer-record-only checkpoint. The
compiler, linker, tests, mapper profiles, and inline_bankcall.s26 sources still
implement the superseded five-byte PC-derived ABI and must be migrated next,
before FC/F0. Updated public component docs to point at BANKSWITCHING.md and
corrected context.txt's stale pre-0FA0 next-step summary.

2026-08-31 09:17:33 PDT, migrate F8/F6/F4 descriptor bankcall ABI
Implemented the public six-byte descriptor bankcall ABI in the toolchain and
converted F8/F8SC/F6/F6SC/F4/F4SC end to end. `.banktarget` now occupies three
bytes (16-bit target plus linker-supplied destination descriptor), bank topology
metadata is `__bankmeta$V3` with an optional one-byte bankcall descriptor, and
the linker retains V1/V2 compatibility. Descriptor-aware stack accounting adds
one hardware-stack byte per active cross-bank call.

For the F family the descriptor is the selector hotspot low byte. Each replicated
fixed block has its source descriptor patched into one immediate operand, pushes
that byte before the synthetic JSR, switches the destination with indexed access
through `$1F00`, and on return pops the source descriptor and restores that bank
before the final RTS. No source/destination bank identity is decoded from a PC.
The maintained block is 69 bytes with 72 bytes reserved (`generic-jsr=$048`),
down from the old 79/80 implementation. Linked F8 bank copies differ in exactly
one fixed-block byte (`$F8` versus `$F9`). Same-bank calls remain ordinary JSRs.

The other existing inline mappers remain intentionally transitional this
checkpoint: FA/DPC, FA2, JANE, 0840, UA/UASW, and 0FA0 accept the new three-byte
payload but their old mapper-local blocks skip the descriptor and retain their
PC-derived selector logic. The independent ordered-pair matrix proves those
compatibility paths still work. Public docs and hot state now identify their
conversion as the next bankswitching work before FC/F0.

Regression coverage now locks the destination descriptor byte, the one-byte
source-descriptor difference between replicated F8 blocks, +3 return-PC carry
across pages, seven-byte call-function bank-edge indivisibility, full F8/F6/F4
ordered call matrices, A:X preservation, nested calls, stack balance, and zero
legacy JSR entries. Focused e2e sets pass 8/8 for the new ABI/F family and 7/7
for transitional mapper compatibility; the F864 public example builds all six
normal profiles plus the poisoned F8SC diagnostic successfully. The branding
failure introduced with BANKSWITCHING.md was fixed by adding the required VCSC
FIGlet banner.

2026-08-31 09:34:45 PDT, migrate FA and DPC descriptor ABI
FA and DPC moved from the transitional PC-derived bankcall block to the public
three-byte destination-descriptor ABI. Their program-bank descriptors are the
selector low bytes: FA bank0/1/2 = FA/F9/F8 and DPC bank0/1 = F9/F8. Both now
use the 69-byte / 72-reserved common descriptor trampoline with baked source
descriptors and zero legacy per-target entries. FA's public diagnostic now runs
the complete 3x3 ordered call matrix; DPC runs the complete 2x2 matrix plus a
nested cross-bank return. Same-bank matrix legs remain ordinary JSRs.
The pre-descriptor compatibility source moved to linker/legacy_inline_bankcall.s26
so lower-level legacy inputs remain supported without making FA the legacy
source authority.

2026-08-31 10:01:47 PDT, convert FA2 descriptor bankcalls
FA2-24/28 now use the public three-byte destination-descriptor ABI end to end.
Descriptors are selector low bytes $F5-$FA/$FB; the mapper-local fixed block
uses STA $1F00,Y for forward selection and restore, with a baked source
descriptor carried on the hardware stack. The fixed block shrank from 83/84 to
69/72 bytes. The public 28K diagnostic now executes all 49 ordered bank pairs;
the 24K regression executes all 36. Removed the unused generic legacy-inline
template and descriptor-less __bankmeta$V2 compatibility added during the
unreleased ABI transition; old bank-call forms are not compatibility contracts.

2026-08-31 10:42:00 PDT, make migrated descriptor ABI unconditional
The unreleased bank-call compatibility scaffolding was removed instead of
maintained. F8/F8SC/F6/F6SC/F4/F4SC, FA, DPC, and FA2-24/28 now declare
$inline_bankcall unconditionally in their mapper profiles; their public examples
and regressions no longer define VCSC_INLINE_BANKCALL. Omitting a build flag can
therefore no longer fall back to the per-target bridge path for a migrated
mapper. The unused generic legacy-inline template and descriptor-less
__bankmeta$V2 parser support are gone. Pending JANE/0840/UA/UASW/0FA0 retain
their temporary opt-in only until each is migrated.

FA2's 28K public diagnostic executes the complete 7x7 ordered matrix (49 calls)
and the 24K regression executes the complete 6x6 matrix (36 calls); diagonal
legs are ordinary JSRs and off-diagonal legs use the current three-byte
destination-descriptor payload. All eight recursively discovered example-build
shards pass with the migrated profiles using the descriptor ABI by default.

2026-08-31 10:43:00 PDT, tighten FA2 descriptor regression
FA2 validation now inspects the linked listings as well as executing the full
matrices. Each 28K destination has seven JSR sites and exactly six .banktarget
payloads; each 24K destination has six JSR sites and exactly five .banktarget
payloads. This locks the diagonal same-bank legs to ordinary JSRs while every
off-diagonal leg uses the descriptor ABI. Source-tree hygiene also rejects any
return of the removed linker/legacy_inline_bankcall.s26 compatibility source.
All eight recursively discovered example-build shards pass.

2026-08-31 10:32:51 PDT, normalize DPC diagnostic display
The DPC public diagnostic now uses the same visible PASS/FAIL presentation as
01_f864: the 8x16 Big-font lowercase pass / uppercase FAIL renderer, compact
mapper-name renderer immediately below it, and the identical centered
81+19+11+81 visible-field layout. DPC now checks in deterministic
status_font.c26 and cart_type_font.c26 subsets regenerated from big_ascii.c26
and default_ascii.c26; the old one-off diagnostic_font.c26 was removed.
Focused DPC validation, generated-font regeneration, example-build shard 6/8,
assembly allowlist, branding, and source-tree hygiene pass. vcs_dpc.pl locks the
common presentation contract so it cannot silently drift again.

2026-08-31 12:22:56 PDT, split diagnostic by controller
User chose four controller-specific frame/render paths after F4SC descriptor-bankcall timing exposed the field diagnostic. Joystick alone retains TIA/switch/collision testing/display; paddle, keypad, and driving omit it and may use larger fonts. Preserve the existing paddle-sampling contract; fix placement/control flow instead of retiming the shared sampler. No implementation was attempted before the chat-limit handoff.

2026-08-31 13:21:43 PDT, finish controller-specific diagnostic kernels
Completed the F4SC field-diagnostic timing repair by splitting frame ownership
into four bank-local controller paths: joystick bank2, paddle bank1, keypad
bank3, and driving bank4, with bank0 left for control/TIA support. Each path has
a same-bank deadline poller; paddle/keypad/driving omit joystick-only TIA,
switch, and collision rendering. Controller dispatch is mutually exclusive and
processes mode changes before stale controller-specific formatting.

Paddle midpoint acquisition now aligns to WSYNC, snapshots INPT0-INPT3 together,
and commits all four channels with one timestamp. This removes the threshold-228
left/right split without changing the generic score sampler. Paddle dump work is
kept between the final scheduler WSYNC boundaries so bank return phase is fixed.
Per-standard visible-tail constants were recalibrated for the split paths.

Validation on the final source: real SELECT/RESET controller/TV cycles are stable;
NTSC, PAL, and SECAM moving-driving sweeps each pass 127 checked frames; all 256
NTSC equal-paddle thresholds pass, including 228, with representative
0/16/64/128/255 PAL and SECAM thresholds passing; both 7800 reset-signature
probes pass. Structural regression coverage now locks the four renderer banks,
same-bank deadline pollers, and absence of joystick-only work from the other
three paths. Resume descriptor migration at JANE/0840/UA/UASW/0FA0.

2026-08-31 14:03:57 PDT, stabilize high paddles and mute audio
Leaving joystick now explicitly clears AUDV0 and AUDV1 on both normal SELECT
mode changes and the synthetic sweep transition, preventing a diagnostic tone
from remaining latched when the joystick renderer stops running.

The prior 0..255 paddle oracle measured whole scanlines and therefore missed a
real high-resistance paddle displaying 95, which completes after about 380
scanlines and can span frames. Tracing that case found paddle-1 completion moved
the first visible GRP0 write by 10 CPU cycles. Channel 1 now commits its shared-
timestamp snapshot on the blank row-alignment line; its score-renderer hook is a
fixed nine-cycle placeholder. Channel 0 retains its safe post-visible-write slot
and channels 2/3 commit in blank preparation.

The timing harness gained --require-stable-tia-write-phase so regressions can
lock visible TIA write phase as well as frame length. All 256 NTSC equal-threshold
cases pass, including 228; each paddle independently reaches displayed 95 with
stable GRP0 phase; representative PAL/SECAM thresholds and all three moving-input
TV sweeps pass. The cartridge regression also leaves joystick during an audible
burst and requires both audio volumes to be zero.

2026-08-31 14:30:32 PDT, finish paddle regression runtime
Finished the diagnostic paddle-test runtime cleanup without reducing threshold
coverage. `vcs_frame_timing` now accepts an explicit
`--minimum-checked-frames N`; its default remains 1000 frames with audio and 40
without audio. The exhaustive 0..255 diagnostic value oracle requests 12 VSYNC
assertions / 9 checked frames, while the 227/228/229 completion boundary and all
four displayed-95 visible-write-phase cases retain their original 45-frame
soaks. The 256 ordinary probes are serialized because the shortened sweep takes
about 2.3 seconds and nested worker pools are counterproductive under the outer
`test.pl --jobs N` runner. The focused paddle regression passes through the
normal project runner inside its 90-second budget. The final broad checkpoint,
`make test TEST_JOBS=8`, passes all 342 tests; under that contention the
diagnostic cartridge took 89.14 seconds and the paddle regression 74.53 seconds,
inside their 120- and 90-second budgets respectively.

2026-08-31 14:57:01 PDT, fix multi-frame paddle raster jitter
The earlier displayed-95 GRP0 fix was incomplete. Real hardware testing showed
jitter beginning around displayed paddle value 57 even though the diagnostic
still held the correct total frame length. Timing-harness tracing reproduced it
as state-dependent WSYNC phase once an RC measurement remained active across a
frame boundary.

The four-paddle lifecycle is now cycle-balanced where it can precede a WSYNC.
Each latched snapshot commit takes a fixed 20 cycles whether the channel
completes or remains active. The VBLANK reset-versus-continue paths both take 52
cycles, so the first blank sampler starts at one phase on fresh and carried
measurements. The four-line overscan sampler always runs; completed channels make
its latches harmless rather than shortening the raster schedule. The diagnostic
also removed two unused paddle lifecycle wrappers, recovering bank-1 space.

Regression coverage now drives each paddle independently to threshold 270,
which reports 61 (the first representable value above the observed ~57
boundary), and requires stable WSYNC phases on representative VBLANK, visible,
and overscan lines. The exhaustive 0..255 value oracle still runs every NTSC
threshold, 227/228/229 retain 45-frame soaks, and the displayed-95 GRP0 phase
checks remain. The shortened ordinary sweep uses the timing harness's explicit
minimum-checked-frame override while its default 40-frame no-audio floor is
unchanged. Focused score/four-player paddle tests and real diagnostic audio,
controller, and TV transitions pass.

2026-08-31 15:25:11 PDT, finish diagnostic validation
The post-jitter diagnostic state is fully validated. The cartridge regression no
longer recompiles three synthetic DIAGNOSTIC_TEST_SWEEP images just to exercise
NTSC/PAL/SECAM moving-driving timing. It reuses the ordinary input-driven F4SC
image, enters PAL/SECAM through real RESET+SELECT edges, and cycles all four
controllers through real SELECT edges while applying the moving SWCHA sequence.
This strengthens transition coverage and reduces that regression to about 11.5
seconds in the development container.

The full compile-only suite passed. The 342-test e2e suite was completed across
the broad run plus exact runner-selected tail batches after the outer execution
cap interrupted display at test 236; every e2e case passed.

2026-08-31 15:50:48 PDT, migrate JANE descriptor bankcalls
JANE now consumes the public three-byte linked bankcall descriptor ABI end to
end. Its cartridge profile declares unconditional `$inline_bankcall` plus
per-bank selector descriptors `$F0/$F1/$F8/$F9`; the replicated trampoline
uses the payload descriptor directly with `LDA $1F00,Y` for destination and
return selection instead of recovering logical bank identity from program
counters. The fixed JANE bankcall block shrinks from the legacy 96-byte class to
69 bytes with 72 reserved (`$048`).

Regression coverage checks the emitted descriptor bytes, the ordered 4x4
source/destination call matrix including nested calls and A:X/stack preservation,
and that the four replicated trampoline copies differ only at the baked source
descriptor byte. JANE no longer needs `-DVCSC_INLINE_BANKCALL=1` in its example,
install check, or example-build harness. Compile-only passes 503/503 and the full
e2e file set passes. The next descriptor migration target is 0840, followed by
UA/UASW and 0FA0 before FC/F0.

2026-08-31 16:12:31 PDT, 0840 descriptor ABI migration
- Migrated 0840/EconoBanking automatic cross-bank calls to the public three-byte .banktarget descriptor ABI.
- Bank descriptors are $00/$40 and are consumed directly as Y offsets from $0800 for read-only selection/restoration; source descriptors are baked into each replicated trampoline and carried on the hardware stack.
- Removed PC-derived bank decoding and the VCSC_INLINE_BANKCALL pilot define. The fixed block is now 69 bytes with 72 reserved ($048), recovering 8 bytes per bank / 16 bytes per 8K image.
- 0840 tests now lock destination descriptor payloads, the single baked source-descriptor difference between copies, ordered 2x2 calls, nesting, A:X/stack behavior, simulator/disassembler round trip, and zero per-target JSR entries.
- Validation: 503/503 compile-only passed; e2e 1-236 passed in the broad run and exact remaining 106/106 passed in bounded reruns. UA/UASW is next.

2026-08-31 16:34:43 PDT, migrate UA/UASW descriptor bankcalls
UA and UASW now consume the public three-byte linked bankcall descriptor ABI end
to end. Their profiles declare `$inline_bankcall` unconditionally and use selector
offsets from `$0200` as descriptors: UA bank0/bank1 are `$20/$40`; UASW reverses
them to `$40/$20`. Destination and return selection use indexed `LDA $0200,Y`;
each replicated trampoline copy has its source descriptor baked in and carries
that byte on the hardware stack. The old logical-PC bit decoding is removed.

Both fixed bankcall blocks are 69 bytes with 72 reserved (`$048`), down from the
80-byte reservation class and recovering 8 bytes per bank / 16 bytes per 8K
image. The example and broad call-matrix harness no longer need the
`VCSC_INLINE_BANKCALL` pilot define. Regression coverage locks destination
payload descriptors, the single baked source-descriptor difference between bank
copies, read-only selector access, same-bank ordinary JSRs, nested/A:X/stack
behavior, alias semantics, disassembly round trip, and zero per-target JSR
entries.

Validation completed with 503/503 compile-only tests and all 342 e2e cases green.
The monolithic e2e run reached test 245 before the outer execution cap; the
remaining exact runner list was completed in bounded batches, with the slow
diagnostic paddle regression run independently. 0FA0 is the next descriptor
migration target, followed by FC and F0.

2026-08-31 17:01:00 PDT, migrate 0FA0 descriptor bank calls
0FA0 now consumes the public three-byte bank-call payload end to end. Logical
bank0 uses descriptor $C0 for canonical selector $0FC0; logical bank1 uses $A0
for $0FA0. The replicated 69-byte/72-reserved trampoline selects and restores
with indexed reads from base $0F00 and no longer derives bank identity from the
logical PC. The profile and example no longer need the inline-bankcall pilot
define. Focused tests lock descriptor bytes, baked source descriptors, ordered
cross-bank calls, nesting, A:X and stack preservation, simulator behavior,
disassembly, and exact round trip. The full 503-case compile-only suite and all
342 e2e cases pass; FC is the next descriptor migration, followed by F0.
