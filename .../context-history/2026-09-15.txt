2026-09-15 00:10:24 PDT, use C26 discard-store for heart demo gap
The line-marker interactive demos used a raw `asm sta WSYNC` only to consume
the one blank composition scanline. Replaced both occurrences with the ordinary
C26 discard-store `WSYNC := _`, which is the maintained source form for this
hardware idiom. `example_assembly_allowlist.pl` now passes without adding a new
allowlist exception. Rebuilt both 4K interactive ROMs and compared them against
the pre-fix `070153` sources compiled with the same tools: above and below are
byte-identical, so the beam timing and Stella raster are unchanged.
A full `make test TEST_JOBS=8` checkpoint completed compile-only 537/537 and
e2e 229/367 with no failures before the execution window terminated the run.
The user's previously failing allowlist case is among the passing e2e prefix.

2026-09-15 00:35:43 PDT, plan Direct Register Access
Documented the proposed C26 Direct Register Access facility in `.../DRA.txt`
without changing compiler behavior. The design uses `$A/$X/$Y` for direct
physical-register access, narrowly exposes `$S` through TSX/TXS, uses direct
condition tests for branchable `$C/$Z/$N/$V`, and permits only native
single-instruction writes for C/I/D/V. `$P` is deliberately not an ordinary
pseudo-register because whole-status access requires visible stack/A effects.
There will be no `$_`: explicit discard uses `(void) expression`. The old
lone `_` remains unchanged for now and is planned for hard migration to `$A`
or `(void)` only after DRA codegen exists. The implementation roadmap is split
into twelve focused slices sized for one short execution window each; DRA1 is
lexer/parser/AST scaffolding and rejection tests only.

2026-09-15 08:35:00 PDT, audit multi-ROM example play targets
Audited every example Makefile and independently expanded the GNU make target
database to identify directories that build more than one cartridge image. There
are three: `01_basic/14_heart_score`, `09_bankswitching/01_f864`, and
`09_bankswitching/09_ua`. F864 already launched each `.bin` sequentially from a
shell `for` loop. Heart-score now loops over its three `TARGETS` instead of
passing `*.bin` to one Stella process. UA/UASW now loops over two bin/mapper
specifications while preserving the explicit `-bs UA` and `-bs UASW` choices;
the individual `play-ua` and `play-uasw` convenience targets remain.
Added `example_play_makefiles.pl`, which walks all example Makefiles and requires
any declaratively multi-bin example to put Stella inside a shell `for` loop.
Also corrected the developer-record branding inventory to recognize the
previously added `.../DRA.txt`; no DRA/compiler behavior changed here.

2026-09-15 10:35:22 PDT, plan coherent example directory reorg
--------------------------------------------------------------
* Add `.../example_reorg.txt` as a design-only contract.  It reorganizes public
  examples by purpose: basics, components, controllers, renderer identity,
  video standard, games, and diagnostics, with shared support under `_common`.
* Record that faithful legacy player-color, faithful legacy multisprite,
  player_color, player_color_unofficial, all_five, all_five_unofficial,
  all_five_player_color, multisprite, and enhanced_multisprite are distinct
  renderer identities.  Heart score above/below move with player_color while the
  standalone heart component remains under components.
* Preserve every current runnable example and explicitly retain the existing
  all-five-unofficial 170 dual-score composition that an earlier sketch omitted.
  Plan eight new examples: joystick and paddle controller tutorials plus six
  NTSC cells completing the PAL/SECAM standards matrix.
* Split implementation into ER1-ER25 bounded migration slices.  Every move slice
  owns relative includes/Makefiles plus affected tests, fixtures, packaging,
  licensing paths, documentation, and byte/raster validation; no code or public
  path is changed in this planning turn.
