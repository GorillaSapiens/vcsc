2026-09-15 00:10:24 PDT, use C26 discard-store for heart demo gap
The line-marker interactive demos used a raw `asm sta WSYNC` only to consume
the one blank composition scanline. Replaced both occurrences with the ordinary
C26 discard-store `WSYNC := _`, which is the maintained source form for this
hardware idiom. `example_assembly_allowlist.pl` now passes without adding a new
allowlist exception. Rebuilt both 4K interactive ROMs and compared them against
the pre-fix `070153` sources compiled with the same tools: above and below are
byte-identical, so the beam timing and Stella raster are unchanged.
A full `make test TEST_JOBS=8` checkpoint completed compile-only 537/537 and
e2e 229/367 with no failures before the execution window terminated the run.
The user's previously failing allowlist case is among the passing e2e prefix.

2026-09-15 00:35:43 PDT, plan Direct Register Access
Documented the proposed C26 Direct Register Access facility in `.../DRA.txt`
without changing compiler behavior. The design uses `$A/$X/$Y` for direct
physical-register access, narrowly exposes `$S` through TSX/TXS, uses direct
condition tests for branchable `$C/$Z/$N/$V`, and permits only native
single-instruction writes for C/I/D/V. `$P` is deliberately not an ordinary
pseudo-register because whole-status access requires visible stack/A effects.
There will be no `$_`: explicit discard uses `(void) expression`. The old
lone `_` remains unchanged for now and is planned for hard migration to `$A`
or `(void)` only after DRA codegen exists. The implementation roadmap is split
into twelve focused slices sized for one short execution window each; DRA1 is
lexer/parser/AST scaffolding and rejection tests only.

2026-09-15 08:35:00 PDT, audit multi-ROM example play targets
Audited every example Makefile and independently expanded the GNU make target
database to identify directories that build more than one cartridge image. There
are three: `01_basic/14_heart_score`, `09_bankswitching/01_f864`, and
`09_bankswitching/09_ua`. F864 already launched each `.bin` sequentially from a
shell `for` loop. Heart-score now loops over its three `TARGETS` instead of
passing `*.bin` to one Stella process. UA/UASW now loops over two bin/mapper
specifications while preserving the explicit `-bs UA` and `-bs UASW` choices;
the individual `play-ua` and `play-uasw` convenience targets remain.
Added `example_play_makefiles.pl`, which walks all example Makefiles and requires
any declaratively multi-bin example to put Stella inside a shell `for` loop.
Also corrected the developer-record branding inventory to recognize the
previously added `.../DRA.txt`; no DRA/compiler behavior changed here.

2026-09-15 10:35:22 PDT, plan coherent example directory reorg
--------------------------------------------------------------
* Add `.../example_reorg.txt` as a design-only contract.  It reorganizes public
  examples by purpose: basics, components, controllers, renderer identity,
  video standard, games, and diagnostics, with shared support under `_common`.
* Record that faithful legacy player-color, faithful legacy multisprite,
  player_color, player_color_unofficial, all_five, all_five_unofficial,
  all_five_player_color, multisprite, and enhanced_multisprite are distinct
  renderer identities.  Heart score above/below move with player_color while the
  standalone heart component remains under components.
* Preserve every current runnable example and explicitly retain the existing
  all-five-unofficial 170 dual-score composition that an earlier sketch omitted.
  Plan eight new examples: joystick and paddle controller tutorials plus six
  NTSC cells completing the PAL/SECAM standards matrix.
* Split implementation into ER1-ER25 bounded migration slices.  Every move slice
  owns relative includes/Makefiles plus affected tests, fixtures, packaging,
  licensing paths, documentation, and byte/raster validation; no code or public
  path is changed in this planning turn.

2026-09-15 11:24:43 PDT, execute example reorg through ER11
----------------------------------------------------------
Implemented the example-tree migration through ER11. ER1 added an executable
old-to-new runnable-leaf inventory plus an exact stale-retired-path checklist;
ER2 established `examples/_common`. ER3-ER5 moved strict basics, standalone
components, heart compositions, keypad, and driving. ER6/ER7 added focused
joystick and paddle tutorials, with the paddle example using the maintained
`two_paddles` lifecycle and explicit cross-frame VSYNC gap accounting. ER8 moved
Paddleball, four-player Paddleball, and Tanks to `06_games`; all three ROMs were
byte-identical before/after. ER9 flattened the faithful legacy player-color and
multisprite renderers into peer `04_renderers` leaves; both ROMs stayed exact,
the oracle tests passed, and the multisprite Stella raster passed. ER10 moved
player-color no-score, animated-sprites, and dual-score leaves while preserving
the animated local-license exception and RAM golden; all three ROMs stayed
exact and focused non-Stella regressions passed. ER11 moved the eight official
centered/left/right/two-plus-two 181-line score leaves into the new
`player_color/score_{above,below}` matrix; all eight ROMs stayed exact and the
full public score-control matrix passed. ER12 (wide + poison) is next.

The reorg exposed and fixed two independent test-infrastructure defects. The
faithful multisprite Stella test still passed obsolete `-T $cfg` state after VCS
linker cfg profiles were removed, so that dead argument was deleted. Source-tree
hygiene now ignores generated `.cfg` sidecars inside the animated-sprites local
license subtree just as it already did for ordinary examples. The player-color
192 Stella PNG certification currently produces stable digest
`4093a6bcb808a710638b1fc01891b6cd15e8a44d7891566fd75904be80c137ab`
while its checked-in reference digest is
`8046bf5c73b3e5a8f519d24f47e81c26e5d2e79077312d6b2e8a2e501aa0c250`.
The moved ROM is byte-identical to its pre-ER10 baseline, so this is a preexisting
certification mismatch, not a relocation behavior change; do not update that
golden as part of a path-only move without separately diagnosing it.

2026-09-15 11:40:01 PDT, repair stale player-color Stella reference
The ER10 investigation traced the stale player-color-192 Stella PNG to the
September 4 repo-wide interactive sprite-animation change, not the example move.
The Aug-07 reference still showed frame-zero static alien/radish artwork while
the public example now selects frame ((X XOR Y) & 3); its initial P0/P1 state
selects frame 2.  An A/B build of the current source with both animation offsets
removed reproduced the old reference RGB digest exactly
8046bf5c73b3e5a8f519d24f47e81c26e5d2e79077312d6b2e8a2e501aa0c250,
proving the provenance.  The Sep-04 normal orientation/raster tests were updated
for the new animation, but vcs_player_color_192_stella.pl is deliberately an
opt-in Stella target and source_tree_hygiene only pinned the unchanged PNG file;
there was no default-suite edge joining current selected sprite frames to the
reviewed PNG, so the stale golden escaped.

Regenerated the reviewed Stella 7.0 reference from the current public example;
its RGB digest is now
4093a6bcb808a710638b1fc01891b6cd15e8a44d7891566fd75904be80c137ab and raw PNG
SHA-256 is 938ad57703701d195424c4d6490bedcfacb3ecfd6546209c0a1ea9865722f2c8.
Extended vcs_interactive_sprite_orientation.pl to decode that PNG during the
normal e2e suite and compare both doubled 8x8 sprite masks directly with the
source animation frames selected by the initial X/Y; the old PNG demonstrably
fails this new guard.  Also fixed the reorg regressions reported by the user:
vcs_example_vsync_contract.pl now inventories and timing-checks the new joystick
tutorial as a direct C26 VSYNC owner, and vcs_score_composition_raster.pl resolves
the ER11-flattened official player-color centered/left/right/two-plus-two leaves
while keeping poison explicitly on its legacy path until ER12.

Focused validation after the repair passes the two user-reported cases through
the normal runner, the new default PNG/source guard, source-tree hygiene, example
reorg inventory, context-history format, and the independent Stella 7.0 snapshot.
A broad 371-case e2e run reached 231/371 clean through vcs_0fa0.pl before the
container execution window expired; no broad-suite failure was observed.

2026-09-15 15:41:12 PDT, require Stella in normal e2e
-----------------------------------------------------
Changed the project test policy so Stella and Xvfb are required dependencies for
`make test` and `make e2e`; `make unit` remains compile-only. The test Makefile
now hard-fails the required dependency preflight, maintained standalone Stella
certifications are discoverable normal-e2e cases, and mapper diagnostics run
their Stella mode in the normal suite. Focused `stella-*` targets remain only as
convenient reruns. The unified runner gained a serialized-resource annotation
and self-test so Stella/Xvfb cases cannot race one another under `--jobs`.

Replacing the old opt-in tier exposed several stale or nondeterministic tests.
Stella launchers now explicitly disable unintended CPU/TIA/RAM/bank/hotspot
power-on randomization; tests that deliberately exercise randomized banks
explicitly re-enable it. `stella_snapshot_keys.pl` and the new
`stella_png_sequence.pl` support complete-frame sequence capture and stable-tail
or reviewed-phase grading instead of asynchronous one-shot F12 timing. This made
player-color-192 and 4K/F8/F8SC standard-renderer certification deterministic.
The F8SC experiment also exposed unspecified RESMP power-on state in the public
standard-renderer diagnostic, so that example now initializes RESMP0/RESMP1.

The faithful legacy multisprite certification is intentionally stateful across
frames, so it now requires a full completed-frame phase cycle containing the
reviewed reference color population while the existing CPU/TIA oracle remains
the exact write/position authority. Heart continuous-motion certification waits
for its completed-frame settled state rather than sampling an arbitrary motion
phase. Field-diagnostic Stella coverage now captures stable completed frames for
all 12 NTSC/PAL/SECAM controller cases. The four PAL references were regenerated
only after reducing old/new captures to foreground/background masks and proving
zero geometry changes; the remaining diagnostic references were likewise
updated for the maintained font/presentation state.

Promoting the old tests also found genuine stale assumptions: the FE bank-label
grader still embedded the pre-widening ASCII font, DPC expected an obsolete
compact-uppercase result presentation, and the wide-score and three-plus-three
PNG/bitmap guards still described the pre-September glyphs. Those graders and
reviewed references were synchronized with maintained source. The previously
known-failing all-five-player-color-181 Stella equivalence exposed a real
one-cycle terminal reflected-playfield seam error; replacing the terminal
padding with four 3-cycle BITs preserves the eight-byte footprint and now
matches `all_five(lines:=181)` pixel-for-pixel.

Final broad validation with the new required policy is 384/384 e2e passing,
including every promoted Stella/mapper certification. Example reorganization is
unchanged by this policy work: ER12 remains the next migration slice.

2026-09-15 16:10:21 PDT, fix Stella PATH and PAL portability
------------------------------------------------------------
A real-user `make test` exposed two portability holes in the newly mandatory
Stella tier. The Makefiles intentionally export command names such as
`VCSC_STELLA=stella` and `VCSC_XVFB=Xvfb`, but seven mapper tests treated any
nonempty environment override as an already-resolved filesystem path and then
applied `-x` to the bare name. They therefore reported "requires Stella" even
when `/usr/bin/stella` was in PATH. All 19 maintained Stella launchers now pass
environment-provided names through their executable finder, so both absolute
paths and PATH-resolved command names work. `source_tree_hygiene.pl` enforces
that rule for future Stella/Xvfb launchers.

The user's PAL joystick diagnostic also showed that the reviewed completed-frame
PNG still over-specified Stella's very-dark framebuffer luminance: otherwise
identical PAL frames may distribute RGB gray 6 versus gray 7 differently. The
diagnostic digest helper now has `--normalize-dark`, mapping only pixels whose
three channels are <=16 to black before hashing. The diagnostic certification
uses that normalization in addition to its existing live-row mask; foreground
pixels, dimensions, first-lit-row geometry, and the joystick trailing-dark
rectangle remain exact. All four PAL controller cases pass this normalized
oracle with packaged Stella 7.0.

Runner-level focused validation with `VCSC_STELLA=stella` passes 3F/3E, DPC, E0,
FE, FA2, WD, a focused F8 bankswitching certification, and the exact PAL joystick
case that failed on the user's machine. Source-tree hygiene and syntax checks for
all maintained Stella launchers also pass. ER12 remains the next example-reorg
slice.

2026-09-15 16:45:28 PDT, PAL diagnostic luminance portability
------------------------------------------------------------------
A second real-user broad run reached 383/384 and showed that the first PAL
portability repair was still too narrow. `pal_joystick` had identical raster
geometry but Stella rendered the reviewed foreground at gray 227 instead of the
reference gray 223. Replacing only the reference foreground with gray 227
reproduces the user's reported normalized digest exactly, proving the failure
was palette luminance rather than geometry.

`stella_png_rgb_digest.pl` now supports `--normalize-mono`, thresholding decoded
pixels at the diagnostic's existing >16 lit threshold. The field-diagnostic
Stella test uses that geometry hash only for PAL; NTSC and SECAM continue to
certify their reviewed RGB values. Existing first-lit-row, live-detail-row mask,
and joystick trailing-dark checks remain independent and strict. The hygiene
contract now requires the PAL-only monochrome normalization.

Focused validation passes the exact `pal_joystick` test through the unified e2e
runner with `VCSC_STELLA=stella`, plus all four PAL controller cases individually
and source-tree hygiene. The user's immediately preceding full run was 383/384
with only this PAL joystick case failing; a new full 384-case run was not needed
for this PAL-only digest-oracle change. ER12 remains the next example-reorg
slice.

2026-09-15 18:59:19 PDT, pin Stella palette and restore exact RGB
-----------------------------------------------------------------
The PAL `--normalize-dark` and `--normalize-mono` experiments recorded earlier
in this file are superseded and are not the maintained policy. They were useful
diagnostics for proving the user's Stella 8.x failures were palette-luminance
changes rather than geometry changes, but normalization would weaken raster
certification.

The maintained solution pins Stella to `test/fixtures/stella/stella.pal`, a
792-byte combined user palette containing the canonical VCSC RGB tables from
`compiler/builtin_rgb.c` in Stella order: 128 NTSC triples, 128 PAL triples, and
8 SECAM triples. `test/stella_palette_contract.pl` locks the exact file size,
each standard slice, the combined digest, and the required launch arguments.
Every maintained Stella launcher uses `test/stella_test_lib.pl`, a private
Stella basedir, `-palette user`, and explicit neutral palette/display settings,
so saved user configuration and Stella-version built-in palette changes cannot
alter certification. Reviewed NTSC, PAL, and SECAM raster references now use
exact RGB; the PAL monochrome/dark-background normalization paths are gone.

Focused final validation with packaged Stella 7.0 and Xvfb passed the palette
contract, source-tree hygiene, all 537 compile-only tests, the maintained mapper
and renderer Stella certifications, the heart 54-state exact-RGB oracle, and
all 12 NTSC/PAL/SECAM diagnostic controller rasters. The first complete 385-case
e2e run had no Stella or raster failure; its only failure was `vcsc_branding.pl`
because the newly added `test/fixtures/stella/README.md` omitted the required
VCSC FIGlet banner. The README was fixed and the branding test passes. The final clean
`make test` rerun passed all 537 compile-only tests and all 385 e2e tests, including every
mandatory Stella certification under the pinned palette.

Main-roadmap item 59 remains planning-only: a future compiler/driver option may
accept this same combined Stella palette format for RGB builtins, but no compiler
palette-file option was implemented in this work. `.../example_reorg.txt` is
unchanged and ER12 remains the next example-reorganization slice.

2026-09-15 19:40:14 PDT, complete example reorg ER12
---------------------------------------------------
ER12 finished the official `player_color` score-family relocation.  The poison
and wide score-above/score-below cartridges moved from the last four numbered
`04_player_color_181` leaves to
`04_renderers/player_color/score_{above,below}/{poison,wide}`.  Their Makefile
root depth and `_common` includes were adjusted for the new depth, the family
README moved to `04_renderers/player_color/README.md`, and the now-empty retired
`04_player_color_181` top-level directory was removed.  Directly affected tests
and public renderer documentation now use the new paths; the reorg stale-path
fixture was updated to match.

All four moved ROMs are byte-identical to their pre-move baselines:
* poison above: 03b0b51a044445c9bc591f31617a36c3398f1f6dd082d8e8903f9668745155ea
* poison below: 85b5efbc456887889d3fdda5c8f91549306af7554c6836056203f5c4ee830ee8
* wide above:   6127892c74964a0aaf74caa34d5a005e40d020036d1a980b0280efcc1027e147
* wide below:   53c29c62b3a2641d7f9eaa01c31f2d4788a60ae49b3e04fead115e7cba566ba2

The complete 17-Makefile official `04_renderers/player_color` example family
built successfully.  Focused score-composition, wide-raster, multicolor, public
score-control, 192-line, animation, 170-line, poison-handoff, sprite-orientation,
example-reorg inventory, and source-tree hygiene tests pass.  ER13 is next.
