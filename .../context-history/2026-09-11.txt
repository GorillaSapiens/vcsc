2026-09-11 00:19:52 PDT, fix F0 vs 8IN1 inference
Investigated supplied corpus image b65d4a38d6047735824ee99684f3515e.bin,
which VCSC classified as 8IN1 while Stella classified it as F0. The 64K image
contains one direct STA $FFF0 switch stub in every one of its 16 physical 4K
banks. Forced F0 analysis observes 17 decoded hotspot accesses and exact
round-trip reconstruction succeeds.

The root cause was that VCSC's raw F0 detector accepted only the private VCSC
"F0" tail signature. Without that marker the whole 64K image could remain RAW,
after which the outer-container detector found eight independently plausible
8K slices and promoted the file to 8IN1. Extended the F0 raw detector to accept
repeated direct accesses to $1FF0/$FFF0 using STA, LDA, BIT, or absolute NOP;
two such occurrences are required. This provides positive CPU-visible F0
evidence without copying Stella's blanket otherwise-unknown-64K => F0 fallback,
which would hide unsupported 64K mapper families behind a false F0 result.

Added paired synthetic regressions. f0_vs_8in1.bin deliberately has eight
independently executable 8K slices but replicated direct F0 switch stubs; the
untouched prior tree calls it 8IN1 and the corrected tree calls it F0.
multicart_8in1.bin is a genuine eight-game 64K container with no F0 hotspot
evidence and remains 8IN1, guarding against an over-broad 64K/F0 heuristic.
Both fixtures exact-roundtrip in the focused disassembler regression.

Validation: source_tree_hygiene.pl passes; compile-only is 537/537; the full
parallel E2E run produced 364 passes plus one unrelated vcs_3e_max_diagnostic.pl
timeout while eight heavyweight mapper diagnostics competed for CPU. Rerunning
that timed-out test alone passes. vcsc_disassembler.pl and the F0 mapper tests
pass in the full run. Rechecking the supplied mismatch corpus gives F0 for
b65d4a38d6047735824ee99684f3515e.bin while preserving the previously corrected
E0/E7/F8 results. The remaining corpus mismatches are the separate unfinished
CDF, DPC+, and CM cases.

2026-09-11 01:53:41 PDT, improve island viability analysis
Reworked detached speculative-island discovery from a JAM-anywhere rejection
heuristic into edge/path viability. JAM/KIL/HLT kills only that speculative
continuation; conditional branches survive when at least one feasible successor
is live, while all-dead successors reject backward. Abstract flag facts prune
known-impossible branch arms, and JSR requires both the callee and return
continuation to remain viable.

Added speculative execution-target and hardware-access plausibility. Direct
transfers into non-executable TIA/RIOT hardware space are dead, while RIOT RAM
remains a possible copied-code target. Candidate opcode evidence now
separates official, stable unofficial, unstable unofficial, and JAM classes.
TIA/RIOT accesses use hardware read/write decode; canonical spellings score
more strongly than legal mirrors, and physically invalid accesses are dead.
Known mapper hotspots override generic hardware scoring only in the directions
the mapper actually uses. WD regressions pin the read-hotspot exception and
reject the corresponding write.

Negative-evidence barrier seeding now includes directly provable bad transfers
and hardware cycles instead of JAM alone. Added regressions for one-live/one-JAM
branches, all-JAM branches, known flag state used as an unconditional branch,
impossible JMP/JSR targets, RIOT TIM64T read mirroring, canonical-vs-mirror
promotion confidence, stable/unstable unofficial opcodes, and direction-aware
WD speculative selector handling. Updated disassembler/README.md and the hot
disassembler contract accordingly.

Validation from a clean rebuild: compile-only 537/537 passed. The focused
vcsc_disassembler.pl, vcsc_disassembler_fuzz.pl, vcsc_disassembler_gl.pl,
vcsc_disassembler_hardening.pl, vcsc_disassembler_opcodes.pl, and
source_tree_hygiene.pl set passed 6/6. A full parallel E2E run reached 229/365
with no failures before the container execution ceiling; the heavyweight
vcs_3e_max_diagnostic.pl passes alone. vcs_3ex_max_diagnostic.pl itself exceeds
the available per-command execution window in this environment, so no false
full-suite claim is recorded.

2026-09-11 02:12:54 PDT, reject speculative writes without sink
Detached speculative-island viability now rejects stores and read-modify-write
instructions whose resolved destination has no write sink in the established
hardware model. Valid sinks include RIOT RAM, direction-valid TIA/RIOT device
registers, active cartridge-RAM write ports, DPC write-side registers, and
mapper-triggered accesses. Plain cartridge-ROM stores and writes into otherwise
unclaimed bus space are dead speculative edges. Configuration-dependent GL,
E7, and 3E RAM is checked against carried mapper state; the structural
rejected-start prefilter stays conservative when that state is unavailable.

Expanded the direct mapper-exception prefilter so legitimate F0/FC/E0/E7,
ordinary address-triggered mapper selectors, GL read controls, WD read selectors,
and 3E/3F write selectors are recognized before generic no-sink rejection.
Added regressions proving plain-ROM STA and RMW candidates die, RIOT-RAM stores
remain promotable, forced F8SC writes through $1000 remain valid, the old
plain-F8 speculative SC self-promotion case is rejected, and overlapping
no-sink stores can seed a JAM-free negative-evidence barrier.
Validation after the change: focused vcsc_disassembler.pl plus fuzz, GL,
hardening, and all-opcode round-trip suites pass; source_tree_hygiene.pl passes;
compile-only is 537/537. The broad E2E run reached 229/365 with zero failures
before the environment execution ceiling, consistent with the prior handoff.

2026-09-11 09:28:28 PDT, disassembler hypothesis roadmap
Documented the next disassembler architecture: analysis-only duplicate halving with
physical provenance, isolated simulation of every structurally possible mapper,
known-state branch selection plus unknown-state forking, ROM/RAM/TIA provenance,
path viability rejection, strict unique-bank access coverage, hypothesis comparison,
and eventual integration back into presentation.  Added weak nonsense-operation
scoring based on dead definitions/clobbered effects, with hardware side effects and
beam/timing code explicitly protected from hard rejection.

2026-09-11 09:41:06 PDT, implement duplicate analysis view
Completed disassembler roadmap A1. Automatic analysis now recursively halves an
image while its halves are byte-identical, so repeated preservation shapes such
as 2K x4 are analyzed as one 2K payload rather than stopping after one doubling.
The physical image remains authoritative for SHA/output and exact round trip.
Analysis state retains the unique-byte size, physical copy count, contiguous copy
base offsets (the many-to-one provenance map), and current-bank-width explanation
flags for exact duplicate banks and wholly $00/$FF fill banks. Explicit mapper or
layout overrides retain their physical mapper-bank topology while still recording
duplicate provenance.

Generalized duplicate preservation emits every excluded physical copy raw after
the analyzed unique image. Historical doubled-2K/doubled-4K presentation remains
compatible, including the CV doubled-2K annotation. Added recursive_duplicate_8k
(2K x4) to the exact round-trip corpus, an explicit-F8 duplicate test proving a
forced mapper is not collapsed, and an F6 fixture proving an all-$FF bank is
recorded as explained fill. Updated disassembler/README.md accordingly.

Also amended the destination mapper-hypothesis design: 2IN1/4IN1/8IN1/32IN1 and
future N-in-1 layouts are first-class mapper families, preferably one parameterized
internal family. They will compete directly with ordinary mapper hypotheses in A2;
current container/sidecar handling is legacy compatibility plumbing until then.

Validation: vcsc_disassembler.pl passes including exact reconstruction of the new
recursive duplicate fixture; deterministic fuzz is 30/30; GL, hardening, all-opcode
round-trip, and source-tree hygiene all pass. A2 mapper-hypothesis enumeration is
next.

2026-09-11 10:22:59 PDT, enumerate mapper hypotheses
Completed disassembler roadmap A2. Automatic analysis now constructs the complete
structurally compatible mapper-hypothesis set before init_analysis(), legacy mapper
selection, or control-flow refinement. Ordinary cartridge hypotheses are derived
from the A1 unique analysis image; additional structurally compatible mapper models
remain in the set even when the legacy evaluator does not yet score them. Detector
and tail signatures annotate hypotheses rather than creating topology.

N-in-1 layouts are now first-class parameterized mapper hypotheses in that same
preselection set. Their structure is derived from the physical image, so exact A1
duplicate copies do not manufacture a multicart hypothesis. The current 2IN1/
4IN1/8IN1/32IN1 slice evaluator is retained only as migration compatibility and may
consume only hypotheses that were enumerated before whole-image selection; it can
no longer discover a topology after the fact. --container remains legacy CLI
spelling for forcing one of these mapper hypotheses.

Generated disassembly headers report the preselection hypothesis set and physical/
analysis sizes. Regressions cover 24K structural alternatives [FA2, FC, 3F, 3E],
F0 competing with 8IN1/4IN1 before selection, first-class 4IN1/8IN1, ambiguous
2IN1, and A1 recursive duplicate reduction without false N-in-1 discovery. Existing
presentation/tie-break behavior remains intentionally unchanged until A3 replaces
the legacy static and slice evaluators with hypothesis-local exhaustive execution.
Focused disassembler, fuzz, GL, hardening, opcode round-trip, and source-tree
hygiene tests pass. A3 exhaustive per-hypothesis state-space execution is next.

2026-09-11 10:48:45 PDT, exhaustive mapper-hypothesis state space
Completed disassembler roadmap A3. Every A2 mapper hypothesis is now executed in
isolation before legacy/default mapper selection. The canonical A3 engine reuses
the established abstract CPU/mapper tracer: execution state is keyed by physical
instruction byte + runtime PC + mapper configuration, equivalent arrivals are met
in a finite decreasing-knowledge lattice, changed joins are requeued to a fixed
point, and convergence has no instruction-count cap. Known flag state selects one
conditional edge; unknown flag state forks every feasible edge. Hardware input
reads remain abstract uncertainty rather than sampled game-mode scenarios.

Hardware-defined mapper reset arrangements seed their documented state. F8/F6/F4,
whose latch state is not a safe presentation assumption, execute every physical
bank as a legal startup latch state. First-class N-in-1 hypotheses execute every
external selector constituent and every structurally possible inner cart model,
without cross-hypothesis state leakage. Generated headers report per-hypothesis
startup/live-state, context, unknown-branch-fork, instruction, and halt counts.

Removed mapper trust from the A3 hypothesis executor. Automatic and explicit
--mapper now use the same fixed-point execution strength; --mapper only constrains
the hypothesis set. The older bounded sampled concrete/H2 runner remains a
secondary selected-hypothesis RAM/dynamic-code/presentation helper pending A4.
Its presentation-safety predicate is deliberately independent of mapper_overridden,
so forcing the same weak F4 bytes cannot secretly unlock sampled reachability or
reopen the historical OMNI-as-F4 false-code failure. Regressions prove unknown
RIOT-RAM-dependent branches fork, known CLC/BCC follows only the feasible edge
past a dead JAM arm, all structurally retained 24K candidates execute before
selection, 4IN1/8IN1 explore every external selector state, and automatic/forced
F4 produce identical A3 state-space summaries while neither weak case enables the
sampled helper. Congo Bongo was checked manually: automatic and forced F8 now have
the same A3 state-space result; following its generated RIOT-RAM selector with
source provenance remains the explicit A4 regression. Legacy mapper rejection and
ranking remain intentionally unchanged until A5/A7 consume A3 results. A4
value/source provenance is next.

2026-09-11 10:56:47 PDT, A3 validation checkpoint
Final post-safety-gate validation: vcsc_disassembler.pl, deterministic fuzz (30/30),
GL, hardening, all-opcode exact round-trip, and source-tree hygiene pass. Full
compile-only suite passes 537/537. Broad E2E reached 229/365 with zero failures
before the execution environment's command-time ceiling in the slow mapper tail;
no 365/365 claim is made from that partial run.

2026-09-11 12:09:37 PDT, A4 first-class provenance
Completed disassembler roadmap A4. The A3 abstract state now carries physical
ROM-byte provenance through A/X/Y, supported arithmetic/transfers, RIOT RAM, and
path-local sparse cartridge-RAM provenance. Read/write aliases collapse to one
physical RAM-byte identity for Superchip, FA/FA2, CV, WD, E7, 3E, and GameLine;
state joins retain a source only when all incoming paths agree. Provenance is
positive evidence only, so sparse-slot exhaustion conservatively drops facts.

Known ROM-sourced bytes copied into RIOT RAM can now be fetched and executed by a
hypothesis-local fixed-point RAM executor. Distinct generated byte patterns at the
same RAM PC are retained separately rather than merged away, and generated RAM
instructions can perform mapper transitions and return to ROM. This was required
by Congo Bongo: separate feasible paths construct `AD F8 FF 4C E0 F0` and
`AD F9 FF 4C 89 D8` at $00F0/$00F3. Automatic F8 analysis retains all 12 physical
source bytes and observes four decoded F8 hotspot accesses without a Congo-specific
signature. ROM provenance reaching GRP0/GRP1 marks the physical source byte as
read data/graphics evidence.

Added synthetic exact-roundtrip regressions for a Congo-style generated F8
selector, ROM -> RIOT RAM -> reload -> GRP1, and ROM -> FA split cartridge RAM
write/read aliases -> GRP0. Also fixed A3 provenance attachment for 4094/4098-byte
preservation forms: their padded/truncated normalized analysis view no longer
fails merely because it is not an exact divisor of the physical file. The main
disassembler regression, deterministic fuzz 30/30, GL, hardening, all-opcode
round-trip, and source-tree hygiene pass. A5 unified path viability and hardware-
bus rejection is next.

2026-09-11 12:14:02 PDT, A4 validation checkpoint
Post-A4 validation: full compile-only suite passes 537/537. Focused disassembler
validation remains green: main exact-roundtrip regression, deterministic fuzz
30/30, GL, hardening, all-opcode exact round-trip, context-history format, and
source-tree hygiene. The real Congo Bongo corpus ROM was also checked directly:
automatic F8 reports four decoded hotspot accesses and both generated RIOT-RAM
selector/control-transfer variants with physical ROM-byte source offsets.

2026-09-11 12:41:19 PDT, A5 unified path viability
Completed disassembler roadmap A5. Every A2/A3 mapper hypothesis now gets an
isolated RESET-path viability result using the mapper-aware speculative CFG rules
rather than treating raw instruction count as survivability. Hypothesis execution
uses a greatest fixed point so coherent closed game loops remain viable; detached
island promotion keeps the stricter evidence-rooted least fixed point.

JAM/KIL/HLT, transfers into non-cartridge/non-executable-RAM space, reads with no
physical source, and writes/RMW with no physical sink kill the affected feasible
path. Known branch state prunes impossible arms; unknown conditionals retain every
feasible arm and survive when any arm remains live. JSR is conjunctive: both the
callee and return continuation must survive. An unresolved executable-RAM leg is
weak, not dead, and cannot excuse a dead return continuation.

Added a symmetric hardware read-source decoder alongside the existing write-sink
logic. RIOT RAM and valid TIA/RIOT read aliases are sources; split cartridge RAM
obeys read/write direction for Superchip, FA/FA2/CV/WD, DPC, E7, and 3E; GameLine
retains its Stella-compatible direction-bit behavior. Mapper/peripheral accesses
are judged before generic TIA/RIOT legality. The structural negative-evidence
prefilter also recognizes directly provable no-source reads while remaining
conservative when mapper-selected cartridge RAM state is unresolved.

WD viability now mirrors the established tracer's hardware-delay model: after a
$30-$3F selector read, both old and new arrangement continuations are retained, so
one live timing arm keeps the path viable. Generated output reports per-hypothesis
live/dead/weak startup counts plus invalid-target, invalid-bus, and halt-path
evidence. A5 records these results but deliberately does not change the legacy
mapper winner; A7 owns final hypothesis comparison.

Regression coverage includes one-JAM-arm vs all-JAM branches, known-state pruning,
JMP into hardware space, no-source TIA read, no-sink ROM store, coherent closed
loops, conjunctive JSR-to-RAM behavior, FA split-RAM direction, and WD delayed
selector viability. Main disassembler exact-roundtrip, deterministic fuzz 30/30,
GL, hardening, all-opcode round-trip, and source-tree hygiene pass. A6 bank-access
completeness is next.

2026-09-11 12:42:47 PDT, A5 validation checkpoint
Post-A5 focused validation is green: vcsc_disassembler.pl, deterministic fuzz
30/30, GL, hardening, all-opcode exact round-trip, context-history format, and
source-tree hygiene pass. Full compile-only suite passes 537/537. The real Congo
Bongo ROM remains automatically F8 with two live A5 startups, four generated
RIOT-RAM instructions, all 12 generated-code ROM source bytes, and four decoded
F8 hotspot accesses. A broad parallel E2E run reached 229/365 with zero failures
before the execution environment's command-time ceiling in the slow mapper tail;
no 365/365 claim is made from that partial run.
