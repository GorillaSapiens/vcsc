2026-09-07 08:37:29 PDT, BCD RAM/cartram mutation matrix
-------------------------------
Added `test/vcs_bcd_ram_cartram_operators.pl` with static fixture
`test/fixtures/vcs_bcd_ram_cartram_operators.c26`. The F4SC simulator test
explicitly covers bcd8_t/bcd16_t/bcd24_t/bcd32_t in both ordinary Atari RAM
and split-address Superchip `cartram`, with runtime value checks for `+=`,
`-=`, postfix `++`, prefix `++`, postfix `--`, and prefix `--` (48 operator
cases total). Prefix/postfix checks validate both the expression result and the
stored lvalue. The wrapper also structurally verifies all 48 forms are present
and checks the linker map proves the four cart operands are split `cartram` and
the four RAM operands are ordinary RAM. One storage/type pair is pinned per
F4SC bank to keep the fully explicit matrix comfortably within ROM capacity.
Validation: 525/525 compile-only, 13/13 BCD-focused e2e, and
`source_tree_hygiene.pl` pass.

2026-09-07 08:55:31 PDT, add scalar operator Cartesian coverage
Fixed the malformed first entry in today's history file by restoring the required
timestamped entry header; `context_history_format.pl` and `source_tree_hygiene.pl`
now accept the file.

Added `test/vcs_scalar_operator_cartesian.pl` plus
`test/scalar_operator_cartesian.cfg`. The generated e2e matrix covers every
scalar value/mutation operator that is uniformly meaningful for the 12 stock
scalar types (`int8_t`/`uint8_t` through `int32_t`/`uint32_t`, plus `bcd8_t`
through `bcd32_t`) in both ordinary RAM and split `cartram`. The legal side has
896 runtime-checked type/storage/operator cells: assignment expression, same-type
cast, `sizeof`, unary operators, arithmetic, ordinary-integer bitwise/shift,
comparisons, logical operators, all applicable compound assignments,
prefix/postfix increment/decrement, conditional, and comma. BCD multiply,
divide, and remainder cells use their supported constant forms, including both
multiplication operand orders.

The same test adds 144 explicit compile-fail cells for every BCD width in both
RAM and `cartram`: unary minus/complement, bitwise/shift operators and compounds,
and unsupported variable `*`, `/`, `%`, `*=`, `/=`, `%=`. Total Cartesian
coverage is 1040 cells. The generic simulator topology uses the real Superchip
split aliases (`$F080` read / `$F000` write); map assertions prove all cart
operands are split and all RAM operands are ordinary. Pointer/member/index/call
operators remain in their dedicated tests because they are not operators over a
scalar value domain. Documented unary `+` as a supported packed-BCD operation.

Validation: the new Cartesian test and the existing F4SC BCD RAM/cartram mutation
matrix both pass; compile-only is 525/525; history-format and source-tree hygiene
pass. A broad e2e run was clean through 227/360 before the known VCS-heavy tail
exceeded the local execution window. No compiler/linker production code changed
in this task.

2026-09-07 09:50:00 PDT, install/release manifest cleanup
-------------------------------
Removed the historical forced-clean behavior from the ordinary `tools` target;
`make tools` is incremental again, and an explicit `rebuild: clean tools` target
provides the old pristine-rebuild behavior when wanted. Release targets retain
an explicit clean before rebuilding with platform-specific compiler/link flags,
so native and cross/static objects cannot be mixed accidentally.

Added `packaging/install.manifest` and `packaging/install_manifest.pl` as the
single ownership description for the public VCS data tree, installed examples,
and Linux/Windows release extras. The data scope exactly reproduces the prior
198-file public VCS payload. The helper supports install, uninstall, and verify;
example staging also removes build products and rewrites source-tree Makefiles
to use the installed `bin/vcsc[.exe]` and `share/vcs` paths. Windows `vcsc.cmd`
is now a manifest-owned source file rather than an independently fabricated
release artifact.

`make install`/`uninstall` now consume the manifest for data/examples instead of
hundreds of handwritten file operations. `make windows` and `make linux` both
call the shared `stage-release-payload` target, which stages examples and the
platform/common package scopes and verifies data, examples, and package extras
against the same manifest before archive creation. Platform targets retain only
the genuinely platform-specific compilation, stripping/static-link checks, and
archive format logic.

Removed the old top-level `sieve` target and added `test/sieve.pl`, which compiles
and simulates the existing sieve program and checks the first ten primes. Added
`test/install_manifest.pl` to exercise data/example/package install, verification,
uninstall, Windows `.exe` Makefile rewriting, package scope separation, and the
CRLF Windows launcher. Reduced `installcheck` from a second large hand-maintained
test suite to exact manifest verification, installed-tool smoke checks, and
representative installed 4K/F8 example builds.

Validation: `test/install_manifest.pl`, `test/sieve.pl`, history format, source
hygiene, and `make installcheck` pass; compile-only is 525/525. An actual
`make linux` completes through the shared manifest staging/verification path,
produces a statically linked release archive, and passes its installed blank-screen
smoke (17.80 s in this container). The Windows manifest staging path is exercised
with `RELEASE_PLATFORM=windows` and `.exe` rewriting and passes; a full Windows
cross build cannot be run here because the MinGW cross compiler is not installed.
Earlier timing in this tree measured an immediate second `make tools` at about
0.17 s instead of a complete rebuild, staged install about 0.59 s, uninstall
about 0.10 s, and the reduced installcheck about 1.2 s once tools are built.

2026-09-07 10:06:14 PDT, fix stale manifest-refactor packaging tests
-------------------------------
Fixed three e2e regressions left by the install/release manifest cleanup. The
release/install payload itself was already correct; the tests were still
asserting the deleted handwritten Makefile copy/install lines.

`test/package_addons.pl` now verifies that `addons` is owned by the
`package-common` manifest scope, that shared `stage-release-payload` consumes
that scope, and that both Linux and Windows package targets use the shared
staging path. `test/vcs_bankcall_source.pl` and
`test/vcs_mapper_entry_source.pl` now verify maintained mapper `bankcall.s26`
and `entry.s26` sources against `packaging/install.manifest` instead of looking
for their old literal install lines in the top-level Makefile.

Validation: all three formerly failing tests pass individually;
`test/install_manifest.pl`, `make installcheck`, context-history format, and
source-tree hygiene pass. A full `make linux` release build also passes static
link checks, shared manifest data/example/package verification, and the installed
blank-screen smoke, confirming the tests were stale rather than the payload.

2026-09-07 10:45:23 PDT, mirror source libraries in install layout
-------------------------------
Reworked installed and binary-release trees to mirror the source hierarchy
instead of scattering VCSC-owned files across conventional Unix `share`,
`include`, and `lib` buckets. The package root is now `bin/`, `cfg/`,
`libraries/`, `examples/`, and `addons/`; `libraries/vcs` and
`libraries/runtime` retain those exact source-tree-relative paths. Runtime
`libvcsc.l26`, `vcsc-runtime.inc`, startup/zero-page S26 sources, runtime docs,
and the VCS public library are all manifest-owned under `libraries/`.

Updated the driver to discover installed runtime/profile material through the
same `libraries/runtime` and `libraries/vcs` paths it uses in a source tree.
Assembler installed cfg discovery now uses top-level `cfg`. Installed example
Makefiles retain `$(ROOT)/libraries/vcs`; staging only rewrites the driver path
to `$(ROOT)/bin/vcsc[.exe]`. Top-level install/uninstall, generic package, Linux
release, and Windows release staging all consume the shared manifest/root layout;
tests reject resurrection of the old `share`, `include`, or `lib` destinations.
Safe `rmdir` cleanup removes an otherwise-empty installed prefix on uninstall.

A broad e2e run exposed that generic `make package` left its `pkgroot/` staging
tree behind, which then polluted source-tree hygiene. The package recipe now
removes that staging directory after archive creation, and the package regression
locks the cleanup. Validation after the layout change: compile-only 525/525;
focused manifest, mapper bankcall/entry, package, history, hygiene, and
`make installcheck` pass. An actual static `make linux` release passes shared
manifest verification and its relocatable installed smoke using
`libraries/vcs`. MinGW is unavailable in this container, so Windows is verified
through the common staging/manifest path rather than a complete cross build.
The broad e2e run reached 229/362 before the execution window; its sole observed
failure was the now-fixed stale `pkgroot` hygiene residue.

2026-09-07 11:09:22 PDT, fix release example compiler-path rewrite
-------------------------------
Fixed a Linux/Windows release packaging regression in example Makefiles. The
shared manifest helper only rewrote the source-tree compiler spelling
`$(ROOT)/driver/vcsc`; five examples used direct relative spellings such as
`../../../driver/vcsc` or `../../../../driver/vcsc`, so those paths survived
inside binary release archives. The manifest verifier repeated the same narrow
assumption and therefore certified the broken staged examples.

`packaging/install_manifest.pl` now rewrites the canonical `driver/vcsc`
component regardless of whether the Makefile reaches it through `$(ROOT)` or
explicit `../` components, preserving the relative prefix and platform
`vcsc[.exe]` name. Verification now rejects any surviving source-tree
`/driver/vcsc[.exe]` path. `test/install_manifest.pl` explicitly checks the
direct-relative Tanks/keypad/drive Makefiles in addition to the `$(ROOT)`
form. `make installcheck` and `make linux` now build Tanks through its staged
Makefile so release/install testing exercises this path rather than only
invoking the packaged compiler directly.

Validation: `test/install_manifest.pl` and `make installcheck` pass. A fresh
static `make linux` archive was generated, unpacked into a new temporary tree,
and plain `make` from `vcsc/examples/01_basic/13_tanks` invoked
`../../../bin/vcsc` and produced a 4096-byte `tanks.bin`.

2026-09-07 14:48:31 PDT, remove bankswitch glyph-pointer asm
-------------------------------
Promoted the five non-beam example-asm cleanup tasks to the head of the main
roadmap and completed the first one. Across 19 bankswitch diagnostic C26
sources, replaced 352 low/high-byte glyph-pointer constructions (1,408 inline
asm statements) with typed C26 `uint16_t` pointer assignments. Intentional
stack-state probes, selector writes, detector idioms, and simulator stop jumps
remain assembly.

Added `test/vcs_bankswitch_glyph_pointer_c26.pl` to reject any future inline asm
that touches the diagnostic glyph-pointer tables and updated the reviewed
example-assembly allowlist to describe only the remaining intentional asm.
Focused ordinary mapper diagnostics pass across F8/F6/F4(+SC), FA, 4KSC, OMNI,
CV, JANE, 0840, UA/UASW, 0FA0, 3F/3E, FE, WD, DPC, FA2, FC, and F0. The 3F max
diagnostic also passes its 512K / 65,535-call torture run. Compiler compile-only
validation remains 525/525. The next main-roadmap item is enhanced-asymmetric
initialization and joystick-motion conversion to C26.

2026-09-07 15:28:14 PDT, convert asymmetric controls to C26
-------------------------------
Completed main-roadmap Item 53. The public 192-line enhanced-asymmetric example
now implements scene initialization and joystick motion in ordinary C26; its
C26 source contains no inline asm. The visible renderer and custom startup
assembly are unchanged.

Added a narrow general byte-index lifetime optimization: an initialized unsigned
byte local may remain register-only in X when every subsequent use is proven to
be an array index and the statement tail cannot clobber X. Direct byte
initializers can load X without an A/TAX detour, and direct truth tests of
`byte & 0x80` use the 6502 N flag instead of emitting `AND #$80`. Ordinary
out-of-line void `return;` lowers directly to RTS because VCSC functions have
no dynamic epilogue; inline returns retain their convergence label. The
register-only local analysis explicitly rejects bare-value uses after broad
e2e testing exposed unresolved suppressed-local symbols in memory-authority and
inline-IR tests.

The converted example remains an unbanked 4K cartridge at 3996/4090 ROM and
115/128 RAM. `update_asymmetric_motion()` remains exactly the former 61-byte
size; the readable ascending initialization loop is 59 bytes versus the former
57-byte descending asm. The full asymmetric renderer/timing harness passes its
stable-raster, dense-X, phase-edge, continuous-X, and combined X/Y stress.
`test/register_local_x_tail_codegen_test.c26` locks the generic codegen, the
example assembly allowlist dropped this source, and the asymmetric regression
rejects any inline asm returning to the public C26 source. Compile-only is
526/526; the two unrelated e2e regressions found during development pass after
the index-only safety restriction. The main roadmap now starts with Item 54.

2026-09-07 15:45:57 PDT, lock direct bit-7 truth lowering
-------------------------------
The full 363-test user run after the enhanced-asymmetric C26 conversion exposed
five stale code-shape/footprint guards: all-five player color 192, animated
gallery RAM accounting, blank_noasm, multisprite profiles, and wide six-glyph
181. Assembly comparison against the pre-conversion tarball showed the same
single generic improvement in every case: direct truth tests of `byte & 0x80`
now use the load's N flag (`BMI`/`BPL`) instead of emitting a redundant
`AND #$80` before the branch. Runtime semantics and raster/timing contracts are
unchanged.

Updated the exact ROM baselines by two bytes where that lowering occurs:
all-five player color 192 is 3568 bytes; animated gallery is 3236 bytes with
854 free; multisprite 192 is 2621 bytes and both 181 score profiles are 3603
bytes; wide six-glyph 181 is 2891 bytes. Updated the animated-gallery JSON
accounting delta accordingly. `vcs_blank_noasm.pl` now positively requires the
direct `TIMINT` N-flag polling and rejects reintroduction of `AND #$80`.

All five originally reported tests pass individually, including the full
multisprite and wide-score emulator-backed raster checks. Compile-only remains
526/526. A broad e2e rerun is clean through 229/363 before the maximal VCS-heavy
tail exceeds this container window; a separate VCS-tail run is clean through
its first 33 ordinary cases, including the three reported failures encountered
in that range.

2026-09-07 16:08:33 PDT, replace compact score asm with shared C26
----------------------------------------------------------
Removed `examples/14_multisprite/fixed_six_digit_controls_compact.c26` and its
102 ordinary inline-asm statements. Both public 181-line multisprite score
examples now use the same readable `examples/common/fixed_six_digit_controls.c26`
as the other six-digit examples; their Makefiles depend on that shared source.
The public-score regression explicitly rejects resurrection of the obsolete
compact duplicate, and the example asm allowlist drops it.

To preserve the old 101-byte RAM footprint, the direct packed-BCD array compound
lowering now computes a three-byte element offset as `2*index + index` directly
from an addressable byte index instead of parking the index in runtime `arg0`.
`bcd_indexed_array_compound_codegen_test.c26` requires the direct load/ASL/ADC
shape and forbids `arg0`. The multisprite score profiles are 3650/4090 ROM and
101/128 RAM, leaving 440 ROM bytes free; runtime score-control and raster tests
pass. Wide six-glyph examples also legitimately shrink 2891->2887 ROM and
53->52 RAM from the same improvement. Compile-only is 526/526, all 13 BCD e2e
tests pass, affected public-score/profile tests pass, and a broad e2e run is
clean through 229/363 before the usual VCS-heavy tail exceeds the execution
window. Main-roadmap Item 55 is next.

2026-09-07 17:04:46 PDT, convert all-five interactive controls to C26
---------------------------------------------------------------------
Completed main-roadmap Item 55. The combined all-five/player-color 181 public
interactive examples now implement RESET/SELECT, object movement, score-digit
selection, score color, and packed-BCD score editing in ordinary C26. The
shared control source contains zero inline asm; visible renderer code is
unchanged.

The two input latches now use zero-at-rest polarity (`select_switch_latched`
and `right_joystick_latched`), so normal BSS clearing leaves SELECT and the
right joystick armed without startup DATA-copy records. The runtime harness has
dedicated `all5_color181_above` / `all5_color181_below` profiles for this
polarity so older all-five examples retain their existing nonzero-ready latch
contract.

Added a general direct-u8 immediate-comparison lowering: safe unsigned byte
expressions already evaluable directly in A (for example `(SWCHA & 0x0f)`) can
feed an immediate comparison without generic scratch. The compile regression
`direct_u8_expr_compare_codegen_test.c26` requires the direct LDA/AND/CMP/branch
shape and forbids `__vcsc_scratch_`.

Both score-above and score-below cartridges remain unbanked 4K at 4070/4090 ROM
and 124/128 RAM, leaving 20 ROM bytes and 4 RAM bytes free. The all-five/score
focused family passes 17/17, compile-only passes 527/527, and a broad checkpoint
was clean through 708/890 total tests before the execution window expired. The
example asm allowlist now contains 31 classified sources. Main-roadmap Item 56,
ordinary diagnostic UI/CRC/audio cleanup, is next.


2026-09-07 16:50:00 PDT, fix direct-u8 pointer-subscript regression
------------------------------------------------------------------
The full 363-test user run after Item 55 exposed five BCD e2e failures.  The
new direct-u8 immediate-comparison shortcut was incorrectly accepting pointer
subscripts such as `p[0]` and emitting `lda (main$p),y` when the pointer local
itself lived in ordinary BSS.  6502 `(zp),Y` requires the pointer storage to be
zero-page; the established generic lowering correctly copies BSS pointers
through guaranteed-zero-page `ptr0` first.

Narrowed the complex direct-u8 comparison shortcut to exclude all single
subscript lvalues, leaving them on the existing dedicated/generic lvalue paths.
`direct_u8_pointer_subscript_compare_codegen_test.c26` locks the BSS-pointer
case by requiring the `ptr0` copy and forbidding `lda (main$p),y`; the original
masked-byte comparison regression still requires the optimized direct
LDA/AND/CMP path.  All 12 runnable BCD e2e tests pass, the five originally
reported failures pass, all-five focused tests pass, and compile-only is
528/528.

2026-09-07 18:31:30 PDT, diagnostic non-beam C26 cleanup
-----------------------------------------------------------
Completed main-roadmap Item 56. Removed all 151 inline-asm statements identified
by the example audit as ordinary non-beam diagnostic work: host-prefix row
initialization/update, paddle/driving numeric formatting, CRC-24 feed, and the
dual-channel audio cadence. The CPU ARR/status probes, hardware-state tests,
WSYNC/deadline paths, collision/TIA probes, and raster preparation remain asm.

The CRC helper now uses the same readable uint24_t CRC-24/OPENPGP algorithm as
the standalone fingerprint example and reuses the existing Superchip
`diagnostic_crc_input` byte as its loop counter after consuming each input.
Numeric detail rows use ordinary C26 table lookups and the existing number-row
formatter. `diagnostic_audio_tick()` directly indexes the two 64-frame volume
tables and advances the phase in C26.

The maintained diagnostic source drops from 744 to 593 inline-asm statements.
`test/vcs_diagnostic_nonbeam_c26.pl` locks all six converted routines against
inline-asm regression while retaining explicit CRC/audio source-shape checks;
the assembly allowlist digest/count is updated accordingly. The main diagnostic
emulator/timing regression passes, including controller transitions, UI,
fingerprint, collision, and audio checks. The long paddle-sampling regression
also passes. Compile-only passes 528/528. The C26 conversion increases ordinary
RIOT RAM accounting from 108 to 110 bytes (18 bytes remain free); bank7 remains
comfortably within F4SC at 2029/3584 bytes used and bank0 at 2447/3584.
Full e2e validation passes 364/364 after the cleanup. Main-roadmap Item 57,
playfield-color renderer support, is next.
