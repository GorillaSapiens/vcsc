2026-09-06 18:40:00 PDT, Tanks C26 cleanup checkpoint
Continued the measurement-driven asm-to-C26 conversion. `tanks_process_knockback()` was translated only after general compiler fixes reduced the clean C26 version from 296 bytes to a fitting form; the real Tanks gameplay oracle passes with the C function. General fixes include X-backed counted-loop lvalue/array handling, runtime-indexed compound updates, compact logical/immediate comparisons and outside-range tests, and keeping updated bytes live in A across immediate range tests/corrections instead of store/reload/store sequences.

Converted `tanks_prepare_graphics()` to readable C26. A literal duplicated P0/P1 translation cost 18 ROM bytes and one RAM byte; rewriting the C as a natural two-player counted loop let the compiler keep the player index in X and use Y for the descriptor lookup. The final C form is 11 ROM bytes smaller than the old asm. `tanks_misc` was also corrected from 10 bytes to the 8 actually used, recovering RAM.

Used `tanks_compute_player_position()` as another compiler microscope. Added general direct codegen for byte `constant - scalar`, direct scalar compound ALU, adjacent direct-byte statement chains with live A, and `while (u8 >= constant) { u8 -= constant; ... }` reuse of A/carry. The real helper is now readable C26 at 51 bytes versus 44 asm; Tanks still fits at 4031/4090 ROM and 120/128 RAM.

A broad e2e run exposed an unsafe N/Z reuse regression for valued postfix/prefix ++/--: an expression such as `if (!value++)` could be mistaken for a side-effect-free value test and omit the increment. The optimization now explicitly rejects valued ++/-- expressions; the focused valued-storage regression passes. Compile-only suite is 524/524.

The broad e2e run also exposed `optimizer_inline_legality_e2e.pl` as a size-sensitive fixture: repeated `status += 8` statements had existed only to inflate a page, and improved scalar-chain codegen shrank them. Increased the fixture's explicit `.res` padding instead; the isolated e2e test passes. Broad validation reached 225/356 before the slow VCS-heavy tail exceeded the execution window.

Explicit Tanks asm is now 167 statements, all in beam/cycle-sensitive missile positioning, post-score player positioning, and visible arena drawing. The old allowlist rationale was repinned to this actual boundary.

Unresolved observations: the linker can fragment near-full 4K ROM by prioritizing `.flex` page-crossing placement over ROM compactness (observed 86-byte and 23-byte holes during the C-knockback experiment, preventing placement of a contiguous 96-byte `__zero_table` despite nominal aggregate free bytes). Treat as a general linker-policy question, not a Tanks workaround. `forbidcountasm:` remains parsed-but-unenforced test infrastructure and reveals stale expectations if enabled.

2026-09-06 19:56:23 PDT, Fix broad-suite regressions
The user completed the 356-test e2e suite and exposed three remaining regressions. `declaration_contract_metadata.pl` was a real compiler provenance bug in the adjacent direct-byte statement-chain optimizer: later compound statements emitted semantic read/write metadata using the first statement's resolved lvalue, so `write_object += 1` inherited the previous line/column. The chain now resolves each statement's own matching lvalue before emitting use metadata; generated machine code is unchanged and the metadata test passes.

The other two failures were stale exact-ROM guards after legitimate codegen improvements. `vcs_all_five_player_color_192.pl` now locks the already-real 3570-byte public example (520 free) instead of 3590; its timing/composition oracle passes. `vcs_animated_gallery_ram_accounting.pl` now locks 3238 bytes (852 free) instead of 3242. The four-byte gallery reduction is exactly the scalar-chain fold of `control_flags ^= CONTROL_PAUSED; control_flags &= 0xfb;`, eliminating one `STA control_flags`/`LDA control_flags` pair in `main`; install_frames remains 232 bytes and the full accounting test passes. The gallery report now derives its current ROM/current-free fields from the measured map, and the schema-16 golden was refreshed so its fields named `current`/`totals` no longer retain stale 3242/3244 values.

Post-fix validation: compile-only 524/524 passes. Focused declaration metadata, all-five-player-color-192, and animated-gallery accounting tests pass. A local broad e2e rerun passed 1-225/356 before the known slow VCS-heavy tail exceeded the execution window; the user's immediately preceding full run had 353/356 with only these three failures.

2026-09-06 20:37:51 PDT, Close Tanks register-lifetime gap
Promoted the three Tanks-adjacent general cleanup items ahead of the previous main roadmap: direct-byte register lifetime/allocation, compact near-full-ROM `.flex` placement, then `forbidcountasm:` enforcement. Completed the first item and removed it from the hot roadmap; Item 50 (linker compactness) is now the default next action.

Added a structural direct-byte register-lifetime lowering in `compiler/compile_stmt.c`. For the generic normalization shape `counter := immediate; while (state >= k) { state -= k; counter++; }`, with an optional same-state threshold adjustment and following immediate state arithmetic, the compiler keeps `state` live in A and `counter` live in Y across the control-flow region. The repeated-subtraction backedge reuses the carry proven by CMP/SBC, accepted counter updates use INY/DEY, and both source-language objects are spilled once when generic lowering resumes. The matcher is based on resolved direct-byte lvalues and AST shape, not source names, example paths, or Tanks constants. Semantic-use records remain attributed to each exact source statement/lvalue.

Added `test/u8_register_lifetime_codegen_test.c26` to lock the general lowering and explicitly forbid the old RAM churn. In the real Tanks cartridge, `tanks_compute_player_position()` shrank from 51 bytes to 44 bytes, exactly matching the former hand-crafted asm. Tanks as a whole shrank from 4031/4090 to 4024/4090 ROM while RAM remains 120/128, giving 66 ROM bytes and 8 RAM bytes free. `test/vcs_tanks.pl` passes.

Validation: focused register-lifetime/scalar codegen tests pass; `declaration_contract_metadata.pl` passes; full compile-only is 525/525. The broad e2e prefix remains clean through 225/356. A resumed post-`vcs_0fa0` run passed another 29 non-maximal VCS e2e cases through `vcs_cv.pl`, and `vcs_diagnostic_cartridge.pl` passes individually; longer VCS tests exceed the current execution window, so do not claim an uninterrupted 356/356 local run.

2026-09-06 20:57:23 PDT, Make flex placement compact
Completed main-roadmap Item 50. Branch-aware code placement now treats ROM/image growth as the primary soft score, with flexible-branch page crossings only a tie-breaker among equally compact candidates. `.same` and `.cross` remain hard legality constraints. The production allocator and the automatic bank-placement dry-run share the same score helper so they cannot silently diverge.

Added `test/linker_flex_compactness.pl`, a reduced exact-fit 241-byte ROM case. Under the old policy a 128-byte `.flex` layout moved from `$2094` to `$20FE` solely to avoid one taken-page crossing, fragmenting the region enough that the final startup table could not be placed. The fixed linker keeps the layout at `$2094`, accepts the permitted crossing, packs the following 101-byte layout at `$2114`, and uses all 241 bytes exactly. Updated `linker_branch_metadata.pl` because its old expectation explicitly required the now-rejected one-byte growth for a flexible timing win. Public linker documentation now states the compactness-first score order.

Focused validation passes for the new regression, branch metadata/annotations, soft page preference, hard page containment/indexed ranges, function page placement, and both optimized/simple bank-placement paths. Main-roadmap Item 51 (`forbidcountasm:` enforcement) is now next.

2026-09-06 21:32:32 PDT, repair compact-linker stale guards
-----------------------------------------------------------
The compact `.flex` placement policy exposed two exact-address expectations that
were artifacts of the former padding-first layout rather than semantic
contracts. `vcs_omni_32k.pl` now requires explicitly placed `main` to remain in
bank0 without requiring offset $000; its bank-placement record still proves the
pin. `vcs_six_digit_score.pl` now expects the legacy pre-template reset vector at
its compact $F000 placement instead of the old padded $F056. Both tests retain
their functional simulator/timing checks and pass with `linker_flex_compactness`.

2026-09-06 21:32:33 PDT, enforce forbidcountasm directives
----------------------------------------------------------
`test/test.pl` now parses `forbidcountasm: PATTERN|MAX`, rejects malformed
spellings, strips semantic-use metadata before counting just like other assembly
expectations, counts literal non-overlapping occurrences, and fails when the
count exceeds MAX. Added `runner_forbidcountasm_smoke.test` plus pass/fail
fixtures so the directive cannot silently become decorative again. The stale
`page_pointer_offset_reuse_codegen_test` ceiling is now the real accepted three
`lda sprite` loads; the frame and shift/XOR ceilings remain unchanged. Documented
the directive in `test/README.md` and removed completed Item 51 from the hot
roadmap. Compile-only validation is 525/525; focused compactness, OMNI,
six-digit-score, and harness tests pass. The broad e2e rerun reached 227/358
clean before the environment execution window hit the slow VCS-heavy tail.
