2026-08-27 05:45:03 PDT, TIA collision animation
Reworked the field diagnostic's TIA area from an opaque PASS/FAIL self-test into
three visible collision demonstrations. The display now shows raw CXM0P, CXM1P,
and CXBLPF values while M0 approaches P0, M1 approaches P1, and Ball approaches
a center playfield wall. CXCLR is strobed only when the 64-frame animation cycle
restarts, so the viewer can watch the collision registers latch and remain set.
The moving objects stop at contact and the final row gains OK only after the
expected 40/40/80 collision bits have all latched.

Changed each horizontal object reposition to a deterministic two-scanline
transaction: the first line times RESPx and the second computes/stores fine
motion before the shared HMOVE. This removed animation-X-dependent 262/263-line
frame jitter seen when the old variable-time positioning crossed a scanline.

Added a DIAGNOSTIC_TEST_TIA_FREEZE build mode used only by the Stella raster
regression. It starts normally so CXCLR is exercised, then holds the final
contact geometry for deterministic reference screenshots. Updated all 12
NTSC/PAL/SECAM x joystick/paddle/keypad/driving Stella reference images and the
diagnostic README to describe the collision lanes, raw register values, sticky
latches, and expected bits.

Extended the page-safe five-row pair font with collision labels/values. It now
contains 127 glyphs in 637 bytes (635 payload plus the two existing page pads).
Current diagnostic footprint is bank0 3461/3584 bytes, RIOT RAM 122/128, and
F4SC RAM 100/128.

Validation completed here: focused diagnostic regression passes; the reviewed
example-assembly allowlist was updated for the intentionally expanded diagnostic
and passes; compile suite passes 502/502. A full 327-test e2e rerun was started,
but the container execution window expired before completion. No remaining
failure attributable to this change is known.

2026-08-27 06:02:00 PDT, Keep release reminder
The user's full diagnostic handoff e2e run passed 326/327. The sole failure was
vcsc_branding.pl treating intentional .../release.txt as unexpected residue.
Kept release.txt by design, added it to the developer-record allowlist and
required-file checks, and documented it in .../README.md. The focused branding
test passes with release.txt present.

2026-08-27 10:32:28 PDT, TIA collision icon panel
Replaced the diagnostic's three textual collision-register rows with a compact
15-bit collision display covering every pair among M0/M1/P0/P1/BL/PF.  Two
rows of eight six-pixel icons fit the existing 48-pixel display width; each
icon concatenates two editable three-pixel object microglyphs and fills its
blank middle row when the corresponding hardware collision bit is set.  The
sixteenth slot is a check mark that appears only for the exact intended bitmap
(M0-P0, M1-P1, and BL-PF, with no extra collisions).

Added diagnostic_collision_objects.font as the human-editable glyph source,
make_collision_font.pl, and generated diagnostic_collision_font.c26.  The
glyph source uses the user's proposed six 3x6 shapes essentially verbatim so
future visual tweaks do not require renderer changes.  Added regeneration and
layout checks to vcs_diagnostic_cartridge.pl and documented the display.

Because the 48-pixel UI itself uses P0/P1 and can set collision latches, CXCLR
now fires every frame after the text/icon raster and immediately before the
three controlled collision lanes.  All eight TIA collision registers are read
after those lanes, packed into the 15 visible pair bits, and checked for the
exact expected bitmap.  The animation still approaches and holds the three
intended contacts.

The prior working session reported the focused diagnostic regression green
after recalibrating fixed visible tails and measured bank0 at 2200/3584 bytes,
RIOT RAM at 112/128, and F4SC RAM at 81/128.  Stella fixture refresh was still
incomplete: eight post-change captures had completed, while the four SECAM
cases had not finished before the execution window ended.

This immediate handoff was reconstructed onto vcsc.20260827_131646 source after
the user requested the current state before final certification.  The local
container currently lacks the built driver/vcsc prerequisite, so this handoff
was packaged without rerunning the focused build.  Exact next step: build the
toolchain, run vcs_diagnostic_cartridge.pl, finish all 12 Stella reference
captures, then run the normal checkpoint tests.

2026-08-27 12:03:17 PDT, Font utility scripts
Added user-supplied bdf2c26.pl and make_message_glyphs.pl under
libraries/vcs/fonts, preserving executable mode and naming the latter without
the upload-only "(1)" suffix. Added the explicit library CC0 notice to
bdf2c26.pl so it satisfies source-tree policy. Documented both helpers in the
font README: BDF import constraints/options and the fixed-message six-glyph
48-column packing behavior. Font contracts, branding, and source-tree hygiene
pass with the additions.

2026-08-27 12:03:18 PDT, Collision panel certification
Completed certification of the diagnostic TIA 15-bit collision icon panel on
the reconstructed handoff lineage. Rebuilt the toolchain from the supplied
Bison/Flex packages and reran vcs_diagnostic_cartridge.pl successfully.
Verified all 12 Stella 7.0 NTSC/PAL/SECAM x joystick/paddle/keypad/driving
reference cases against the final per-frame CXCLR semantics. One SECAM driving
attempt produced no F12 snapshot under Xvfb; an immediate isolated rerun passed
with an exact digest, so this was treated as launch/snapshot flake rather than a
raster mismatch.

The Stella digest intentionally masks only live controller-detail rows 109-126;
the standard/controller heading, switches, both collision-icon rows, and all
three controlled collision lanes remain pixel-certified. The full jobs=8 e2e
checkpoint reached 325/327 with only vcs_diagnostic_cartridge.pl and
vcs_examples_build_3of8.test timing out under contention; both pass when rerun
alone. Compile-only validation passes 502/502. Thus every one of the 327 e2e
cases is individually green on this source state, while the two heavy cases can
exceed their wall-clock limits when run concurrently on this container.
