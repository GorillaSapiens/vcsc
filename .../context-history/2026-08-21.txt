2026-08-21 06:22:00 PDT, dual RESP proof and hybrid scheduler
Continued from vcsc.20260821_065301.  The enhanced asymmetric same-Y/same-coarse
classifier now reaches a real PF-aware pair path that can issue RESP0 and RESP1
on the same physical scanline while retaining 262-line frame timing.  A fixed
X=31/32,Y=74 diagnostic proves the dual path is exercised; four additional
10,000-frame combined randomized X/Y runs remained stable while requiring dual
RESP occurrence.

An exact randomized failure exposed a top-edge marker interaction: Y=89 pair
records carry bits 7+6, while the special top dispatcher only understands the
ordinary lane bit.  Y=89 same-slot pairs now deliberately fall back to the two
proven one-RESP paths, and the former failing state is pinned by the asymmetric
regression.

Stella 7.0 pixel checks then showed that horizontal calibration is older than the
current asymmetric RESP lattice.  Ordinary requested X=31/100 rendered around
10/73 and a true pair requested at X=31/32 rendered around 13/23.  The source
comment claiming complete Stella calibration was corrected.  The next plan is
to calibrate the ordinary eleven-phase table first, prefer adjacent-scanline
split one-RESP positioning when possible, retain literal dual RESP only where it
helps, use VDELP0/VDELP1 only if graphics-latch scheduling needs another line,
and allow fair rotating flicker only as a final unschedulable fallback.
2026-08-21 07:09:00 PDT, make tar fallback preserves sources
The top-level `make tar` target now hard-codes the VCSC archive basename and
works both in a real Git checkout and in an unpacked source release.  Without a
Git worktree it creates a temporary external Git index, stages the source tree
under the shipped ignore rules, emits `git ls-files`, and removes the temporary
repository without touching the source tree.

`examples/.gitignore` now explicitly unignores the faithful legacy multisprite
diagnostic data source.  That `.s26` is intentionally revision-controlled even
though generated `.s26` files are ignored generally.  This makes release-tar
regeneration preserve the tracked-ignore exception instead of depending on Git
history that is absent from the release archive.
2026-08-21 08:10:00 PDT, asymmetric X calibration checkpoint
Ordinary asymmetric P0/P1 positioning now uses a passing twelve-phase RESP
lattice: 14,19,24,28,33,37,42,47,50,53,58,63.  Combined randomized X/Y timing
remains stable at 262 displayed lines while still exercising the true dual-RESP
proof path.

Stella 7.0 pixel checks verify exact ordinary placement at representative
middle/left positions including P0 X=31/X=55 and P1 X=20/X=100.  The simple
phase/HMP arithmetic does not survive HBLANK wrap: provisional far-right entries
around X=140/X=159 fold toward the left edge.  Far-right positioning is now an
explicit scheduling class rather than a modulo-table extrapolation.

A first expanded late-RESP-family experiment was rejected after it disturbed
shared dispatch routing, leaving only RESP0 phases 28/61 and no RESP1 in the
static scene.  The known-good twelve-phase renderer was restored.  Next work is
a safe far-right ordinary-X path, complete Stella X=0..159 certification, then
the `(X0,X1,Y)` split/dual/VDEL/flicker feasibility scheduler.

2026-08-21 09:41:21 PDT, 152-cycle phase-73 split proof
Replaced the experimental same-line dual-RESP pair machinery with the first
adjacent-line 152-cycle feasibility class.  Exact same-Y X=159/X=159 pairs now
write RESP0 at phase 73 on physical line A, RESP1 at phase 73 on line B
(absolute cycle 149), and HMOVE at absolute cycle 152 without an internal
WSYNC.  Stella 7.0 shows the expected wrapped glyph pixels at X=158..159 and
0..5.  Other pair geometries fall back to the proven independent one-RESP
paths.  Added a timing-harness adjacent-RESP assertion and pinned the phase-73
case.  The hardware-derived final target is eleven phases 23..73 in five-cycle
steps per line and a direct 160-byte packed X->{slot,HMP} table; the full target
table and 152-cycle schedule are recorded in enhanced_asymmetric.txt.
2026-08-21 12:49:00 PDT, asymmetric edge motion regression
The enhanced asymmetric visual-fix checkpoint repurposes provisional RESP slots
0/1/2 as ordinary edge classes: phase 23 for public X=1..15, phase 68 for
X=136..150, and phase 73 for X=0/X=151..159.  Middle positions still use the
provisional schedules, so the active measured lattice is
23,28,33,37,42,47,50,53,58,63,68,73 rather than the final eleven 23+5n phases.

Added `vcs_frame_timing --sweep-zp ADDR MIN MAX`; the asymmetric regression now
moves sprite 0 continuously 0..159..0 across synchronized frames and remains at
262 displayed lines.  Updated the regression to assert the actual twelve-phase
ordinary lattice and phase 73 on both lanes.  Removed obsolete assertions for
14/19/24 and for the retired experimental adjacent-line pair raster.

Visible work remains: phase-53/phase-63 PF schedules have suspicious register
write windows, and the renderer still reuses rank-0 color for later events.
Ordinary PF/X/color correctness remains ahead of reintroducing pair scheduling.
