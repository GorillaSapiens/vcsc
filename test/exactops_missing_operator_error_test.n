// n65c -I .
// expectfail
// expecterr: type 's4x' uses '$exactops' and requires visible overload 'operator+' for same-type operands

include "machine_6502.n"

type s4x { $size:2 $integer:signed $endian:little $exactops };

s4x add(s4x a, s4x b) {
   return a + b;
}
