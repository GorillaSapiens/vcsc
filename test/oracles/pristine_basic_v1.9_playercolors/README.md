```text
 __   __ ___  ___   ___
 \ \ / // __|/ __| / __|
  \ V /| (__ \__ \| (__
   \_/  \___||___/ \___|
```

# Pristine upstream Atari 2600 BASIC 1.9 player-color oracle

This directory contains the external gold-standard cartridge for the retained
legacy player-color profile.  It was built from an untouched checkout of
upstream Atari 2600 BASIC tag `v1.9`, commit
`1cefae05151da014467a0ecfcbacee6847d61404`, using the stock native compiler
and include files plus DASM 2.20.14.1.

Files:

* `faithful_legacy_playercolors.bas` is the complete upstream BASIC source.
* `faithful_legacy_playercolors.bin` is the exact 4096-byte cartridge image.

The source defines `playercolors` and `player1colors` directly instead of using
`set kernel_options playercolors`.  Upstream BASIC 1.9 rejects that setter
combination even though its shipped standard kernel contains and assembles the
corresponding conditional branches.  No compiler, include, or assembler source
was patched.

Clean-room reproduction command sequence:

```sh
make LEX=flex
export bB="$PWD"
export PATH="$PWD:$PATH"
ln -s /path/to/dasm dasm
./2600basic.native.sh faithful_legacy_playercolors.bas
```

Expected SHA-256 values:

```text
bdc04204ea6f5faa23e89f95398e22bd6763cb2ccc522fbed6eaf57132ff628f  pristine upstream source archive
8daaeb4eb35131a5beb2c93d2e2e4732f09c86c97d83763b627d6be7c4c130d3  faithful_legacy_playercolors.bas
573cc86c7ba12c0626fb73678480721e043e3468795151635beaae8f7f3e0b6a  faithful_legacy_playercolors.bin
```

A second extraction and build from the supplied pristine archive reproduced the
binary byte for byte.  This ROM is deliberately not generated by VCSC and does
not depend on the retained in-tree kernel copy.

The oracle currently proves two gaps in the existing VCSC "faithful" audit
baseline: stock upstream BASIC produces a stable 264-raw-line frame rather than
265, and the first visible TIA mismatch is the HMM0 write caused by the upstream
compiler's documented `player0colorstore = missile0x` RAM alias.  The known-gap
regression must be converted to a positive equivalence test when the baseline is
repaired.
