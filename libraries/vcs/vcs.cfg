# Generic VCS linker operations retained during C26 profile migration.
# C26 mem declarations own allocator ranges and ordinary segment routing.
# This file only requests whole-program hardware-stack reservation in RIOT RAM.
MEMORY {
    ram: start = $0080, size = $0080, type = rw, define = yes, callstack = callgraph;
}
