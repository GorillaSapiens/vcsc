# n.cfg

MEMORY {
    ZEROPAGE:     start = $0000, size = $0100, type = rw, define = yes;
    HS:     start = $0100, size = $0100, type = rw, define = yes;
    RAM:    start = $0200, size = $7E00, type = rw, define = yes;
    ROM:    start = $8000, size = $7FFA, file = %O, fill = yes, fillval = $FF, define = yes;
    VECTORS: start = $FFFA, size = $0006, file = %O, define = yes;
}

SEGMENTS {
    ZEROPAGE: load = ZEROPAGE, type = zp;
    DATA:     load = ROM, run = RAM, type = rw, define = yes;
    BSS:      load = RAM, type = bss, define = yes;
    ARGSTACK: load = RAM, type = bss, define = yes;
    STARTUP:  load = ROM, type = ro, define=yes; # must be first in ROM !!!
    CODE:     load = ROM, type = ro;
    RODATA:   load = ROM, type = ro;
    VECTORS:  load = VECTORS, type = ro, start = $FFFA;
}

