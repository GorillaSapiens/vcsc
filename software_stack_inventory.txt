 __   __ ___  ___   ___
 \ \ / // __|/ __| / __|
  \ V /| (__ \__ \| (__
   \_/  \___||___/ \___|

VCSC SOFTWARE-STACK INVENTORY
=============================

Purpose
-------
This checked inventory records target-runtime `_pushN` and `_popN` calls emitted
directly by compiler source. A regression test scans every compiler `.c` file and
fails if either helper reappears.

Snapshot
--------
Input archive:

    vcsc.20260719_120555.tar.gz

Compiler emission totals:

    0 `jsr _pushN` allocation sites
    0 `jsr _popN` emissions

Current compiler model
----------------------
Named parameters, automatic locals, return objects, call arguments, expression
temporaries, lvalue calculations, assignments, initializers, control-flow tests,
and switch expressions all use fixed-address symbols or function/depth-pooled BSS
scratch. The compiler selects each pooled scratch symbol at compile time and
addresses it directly. There is no target software-stack pointer or frame
pointer.

Removed final emitters
----------------------
The final five `_pushN` sites and thirteen `_popN` emissions belonged only to:

* overloaded compound assignment;
* overloaded pre/post increment and decrement;
* weak built-in operator dispatch.

Operator overloading, `$exactops`, weak operator dispatch, their runtime generator,
and their positive tests have now been removed. Ordinary named-function overloading
has also been removed; each function name has one direct signature.

Runtime/library status
----------------------
The target software stack has been removed completely:

* `_vcsc_sp` is no longer allocated or exported;
* `_pushN` and `_popN` are absent from the runtime archive;
* all `sp2ptr*` helpers and their source file are gone;
* startup initializes no software-stack or frame-pointer state, and compiled
  functions have no stack-frame prologue;
* retained division/remainder copies its dividend into a private two-byte BSS
  object inside the `_divNle` archive member. That object is linked only when
  division or remainder selects the member.

Pooled scratch is linker-resolved static storage; there is no runtime scratch-base pointer.
