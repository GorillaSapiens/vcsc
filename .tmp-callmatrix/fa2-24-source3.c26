// Generated exhaustive ordered JSR/RTS bank-pair regression.
include "vcs_24k_fa2.c26"

uint8_t matrix_failure;
uint8_t matrix_count;
uint8_t matrix_sp_before;
uint8_t matrix_sp_after;

bank0 uint8_t matrix_probe0(void) { matrix_count := matrix_count + 1; return 0x80; }
bank1 uint8_t matrix_probe1(void) { matrix_count := matrix_count + 1; return 0x81; }
bank2 uint8_t matrix_probe2(void) { matrix_count := matrix_count + 1; return 0x82; }
bank3 uint8_t matrix_probe3(void) { matrix_count := matrix_count + 1; return 0x83; }
bank4 uint8_t matrix_probe4(void) { matrix_count := matrix_count + 1; return 0x84; }
bank5 uint8_t matrix_probe5(void) { matrix_count := matrix_count + 1; return 0x85; }

bank3 void matrix_source(void) {
   matrix_count := 0;
   asm tsx; asm stx matrix_sp_before;
   if (matrix_probe0() != 0x80) { matrix_failure := 0x01; }
   asm tsx; asm stx matrix_sp_after;
   if (matrix_sp_after != matrix_sp_before) { matrix_failure := 0x40; }
   asm tsx; asm stx matrix_sp_before;
   if (matrix_probe1() != 0x81) { matrix_failure := 0x02; }
   asm tsx; asm stx matrix_sp_after;
   if (matrix_sp_after != matrix_sp_before) { matrix_failure := 0x41; }
   asm tsx; asm stx matrix_sp_before;
   if (matrix_probe2() != 0x82) { matrix_failure := 0x03; }
   asm tsx; asm stx matrix_sp_after;
   if (matrix_sp_after != matrix_sp_before) { matrix_failure := 0x42; }
   asm tsx; asm stx matrix_sp_before;
   if (matrix_probe3() != 0x83) { matrix_failure := 0x04; }
   asm tsx; asm stx matrix_sp_after;
   if (matrix_sp_after != matrix_sp_before) { matrix_failure := 0x43; }
   asm tsx; asm stx matrix_sp_before;
   if (matrix_probe4() != 0x84) { matrix_failure := 0x05; }
   asm tsx; asm stx matrix_sp_after;
   if (matrix_sp_after != matrix_sp_before) { matrix_failure := 0x44; }
   asm tsx; asm stx matrix_sp_before;
   if (matrix_probe5() != 0x85) { matrix_failure := 0x06; }
   asm tsx; asm stx matrix_sp_after;
   if (matrix_sp_after != matrix_sp_before) { matrix_failure := 0x45; }
   if (matrix_count != 6) { matrix_failure := 0x7E; }
}

bank0 void matrix_done(void) { while (1) { } }

bank0 void main(void) {
   matrix_failure := 0;
   matrix_source();
   asm jmp matrix_done;
}
